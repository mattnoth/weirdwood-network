Run the end-of-session checklist for the Weirwood Network project. Do each step in order:

1. **Update worklog.md** — Add a session log entry (dated today) with:
   - What happened (key actions taken)
   - Decisions made
   - Ideas that surfaced
   - What's next
   - Update the Current State checkboxes if any items were completed

2. **Update working/progress.md** — If any work is mid-flight or needs handoff, log it under Active Handoffs. If scratch notes accumulated, make sure they're tagged with source.

3. **Update working/todos.md** — If new TODOs surfaced during the session, add them to the appropriate section.

4. **Verify .gitignore** — Confirm sources/raw/ and sources/chapters/ are still in .gitignore. Run `git status` and flag if any copyrighted content is showing as untracked.

5. **Show session summary** — Print a brief summary of what was accomplished and what the next session should pick up.
