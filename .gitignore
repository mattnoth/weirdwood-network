# OS
.DS_Store

# Python
__pycache__/
*.pyc

sources/raw/
full-txt-files/

# Matt's top-level scratch file — private notes, not for agents (only /endsession triages it)
/scratch
/scratch.md
/scratch.txt

# Matt's top-level personal roadmap — same rule as scratch (gitignored, agents don't read)
/next.md

# Session 63 (2026-05-21): enriched candidate files are derived data — regenerate via
# `python3 scripts/wiki-pass2-enrich-candidates.py --apply` in ~13s.
# Excluded to avoid bloating git with ~280MB of computable output.
working/wiki/pass2-buckets/*/prose-edge-candidates-enriched/

# Session 66 (2026-05-23): Pass-1-derived book edges are derived data — regenerate via
# `python3 scripts/stage4-pass1-edge-candidates.py --apply` then
# `python3 scripts/stage4-pass1-evidence-locator.py --apply` in seconds.
# Audit reports + the firstname-alias map (working/wiki/data/pass1-derived-*) ARE tracked.
working/wiki/pass2-buckets/pass1-derived/

# subagent isolation worktrees (runtime, never commit)
.claude/worktrees/

# Matt's private scratch notes — never commit
scratch*
