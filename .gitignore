# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Copyrighted source texts — NEVER commit
sources/raw/
sources/chapters/
full-txt-files/
epubs/

# Reference layer (TWOIAF OCR, etc.) — large, regenerable
sources/reference/

# Wiki scraper output — large, regenerable from MediaWiki API
# Graph nodes (graph/nodes/) are the canonical artifact and ARE committed
sources/wiki/
