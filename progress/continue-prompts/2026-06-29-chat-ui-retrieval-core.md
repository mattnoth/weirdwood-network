# SESSION 172 — Weirwood chat-UI alpha · RETRIEVAL-CORE chunk

> **This is the 2nd of ~4 build chunks** for the deployable Bloodraven loremaster chat (S168 scoped →
> S169 design review → S170 publishing settled → **S171 ran the step-0 spike + built the Foundation chunk**).
> Stamp your worklog entry `### Session 172` (meta track, `worklog.md`).
> **Recommended model:** Sonnet 4.6 — this chunk is mechanical TS porting + unit tests, no API calls, no API $.
> **Context discipline:** this is ONE chunk on purpose. Do the retrieval tools only; the Edge function +
> Bloodraven prompt are the NEXT session, the front-end + deploy the one after. Don't bleed forward.

## What's already done (S171 — don't redo)
- **Decisions LOCKED** (design §0 + §9, do not reopen): runtime = **Netlify Edge Functions (Deno/TS)**;
  model = **`claude-opus-4-8`** (swappable); retrieval = **Option A**; scope = **curated-MVP first**.
- **Data bundle built & verified** → `web/data/*.json` (8.8 MB, gitignored, regenerated by
  `scripts/build-chat-export.py`). The whole curated graph fits in memory — no lazy-loading.
- **Scaffold done:** `web/public/index.html` (placeholder landing), `web/public/theme/tokens.css`
  (the one-file theme layer), `netlify.toml` (Edge runtime), READMEs in `web/`, `web/src/lib/`,
  `web/netlify/edge-functions/`.

## STEP 0 — read first
- **`web/README.md`** ← the data-bundle shapes (alias-map / nodes / edges / featured-tywin) + build/run. START HERE.
- **`web/src/lib/README.md`** ← the tool table you're implementing (signatures, returns, what to port).
- `scripts/graph-query.py` + `scripts/event_alias_resolver.py` ← the Python logic to PORT to TS (don't shell out;
  Netlify Edge runs Deno). `working/demo-asoiaf-loremaster.md` ← the read-only recipe these tools serve.
- `working/chat-ui/alpha-design.md` §3 (retrieval), §0 status table.

## Your job (retrieval-core only)
Implement the read-only retrieval tools in Deno/TypeScript under `web/src/lib/`, reading `web/data/`:
1. **`resolve(phrase)`** → `[{slug, category}]` — normalize like the Python resolver (lowercase, keep hyphens;
   see memory `project_node_alias_spaced_phrases`), exact `alias-map.json` lookup, then the fuzzy/substring
   fallback from `event_alias_resolver.py` (token-overlap ≥ 0.5).
2. **`walkChain(slug)`** → `{upstream[], downstream[]}` of typed-edge links — port `graph-query.py --causal-chain`
   (CAUSES/TRIGGERS/MOTIVATES; +ENABLES for full-chain). Each link = `{source, edge_type, target, evidence_quote, ref, tier}`.
3. **`neighbors(slug)`** → edges grouped by type (the NO-CHAIN relational view) — port `--neighbors`.
4. **`readNode(slug)`** → `{name, type, identity, quotes}` from `nodes.json`.
5. **Unit-test** each against the real bundle (`deno test`): assert `resolve("death of Tywin")` →
   `assassination-of-tywin-lannister`, and `walkChain` on it returns the 7-link chain (matches
   `web/data/featured-tywin.json`). No API needed — pure data.
6. **Receipts contract:** tools return structured typed-edge JSON on a channel SEPARATE from prose (the panel
   renders from the return, not the narration). Validate/allowlist every input (the tool layer is the trust boundary).

**DEFER to the fast-follow:** `searchChapters` / `readPassage` (live grep) — they need a build-time inverted index
to stay under the 50 ms CPU budget; not in this chunk. Curated MVP uses graph quotes only.

## End-of-chunk
Update `web/README.md` status table + design §0 (flip the retrieval-tools row), write the **function-chunk**
continue-prompt (`chat.ts`: tool-loop + Bloodraven prompt + streaming + spend cap), and log `### Session 172`.

## DO NOT
shell out to Python from the function · build the Edge function or front-end this chunk · re-fetch wiki ·
mint graph edges/nodes · reopen the locked runtime/model/scope decisions · auto-run `/endsession`.
