# SESSION 163 — A-arc roundup #6 (LAST unit): A2.5 WO5K-battles — PASS 1 of a multi-pass mini-track

> **This is Session 163.** Stamp your worklog entry `### Session 163` in `worklog.md` (GRAPH track, global S-number).
> **Recommended model:** Sonnet 4.6 for the lens subagents + fresh-verify; Opus 4.8 for orchestration/synthesis.
> **One enrichment dip per session** (Matt S131). **D&E Pass-1 is PARKED** — stage only your own files by path; never `git add -A`.
> **`/endsession` BYPASS — AUTO-RUN (do not merely offer).** This is an enrichment dip; per Matt's strengthened carve-out (S161, 2026-06-28) enrichment continue prompts carry the auto-run bypass — **bypass the no-auto-endsession hard rule and run `/endsession` automatically at close-out** (memory `feedback_endsession_requires_permission` reflects this carve-out).
> **Mint via the generic `scripts/mint_enrichment.py --candidates …` + `scripts/finalize_enrichment.py --verdicts …`** — do NOT hand-write a per-dip script (retired S158), and do NOT copy a per-dip `quotecheck.py` (use `scripts/quotecheck_enrichment.py <candidates.json>` — generalized S159). Schemas: `scripts/enrichment/README.md`.

## The unit is LOCKED — no STEP-0 fork this session
Matt directed (S154): **round up the remaining A arcs** as a multi-session campaign. **Shipped:** S155 Stannis · S156 Dorne · S157 Euron · S159 Jaime/Riverlands · S161 Tyrion/Essos (S158 script-consolidation, S160 chat-UI demo, **S162 harvest-drain 62→0**, all non-dip). **A2.5 WO5K-battles is the LAST A-roundup unit** and is **explicitly multi-pass — its own mini-track** (the coverage plan flags WO5K battles "needs many passes"). After A2.5's passes complete → **A2.8 Davos/Sam residual** closes the A-roundup; then the descent drops to granular L2 / character / (gated theory) work. Full plan: `working/enrichment-coverage-plan.md` § A2.5 + the 🅰 banner.

## A2.5 WO5K-battles — scope (the military arc that makes Robb the Young Wolf; BUILD+ENRICH, BASELINE FIRST, dedup vs the 9 prior WO5K dips)
The battle cluster of Robb Stark's war — **mostly NON-POV battles** reported through the Catelyn / Tyrion / Arya / Jaime / Jaime-Riverlands POV chapters. Per the coverage plan (§ A2.5, ~10 target nodes, Large 25+ edges): **Whispering Wood** (Robb's night-trap, **Jaime captured**), **Battle of the Camps** (the siege of Riverrun broken — Robb's first relief), **Oxcross** (the Westerlands raid victory), **the Battle of the Fords** (Tywin turned back at the Red Fork by Edmure), **Duskendale** (Robb's costly defeat under Roose Bolton's gambit). `battle-on-the-green-fork` is **already rich (29 edges)** — DEDUP, don't rebuild; the others are thin or absent.

**This is PASS 1 — scope a coherent sub-cluster, do NOT attempt all five battles in one dip.** Recommended PASS-1 cut (decide from the baseline): **Robb's Riverlands-relief rise** — `whispering-wood` (Jaime captured) → `battle-of-the-camps` (Riverrun relieved) → the seam into `robb-proclaimed-king-in-the-north` (minted S113) — the tight causal chain that turns Robb into the Young Wolf and launches the WO5K Northern front. Leave the **Westerlands raid** (Oxcross / the Crag / the Jeyne-Westerling marriage-blunder) for PASS 2 and the **unravelling** (the Fords / Duskendale → the Red Wedding upstream, which the S-series RW spine already partly built) for PASS 3. **Log explicitly in the worklog which battles this pass covered and which remain** so PASS 2/3 know where to resume.

- **Whispering Wood** — Robb's 6,000 vs Jaime's host; the night ambush; Jaime taken. Likely thin/islanded. `jaime-captured-at-whispering-wood` may already exist as a dyad — BASELINE to check. Cross-seam: this is what `siege-of-riverrun` (S159/this-session enriched) and the Red-Wedding-upstream spine pull from.
- **Battle of the Camps** — Robb breaks the Lannister siege of Riverrun (already a node, `battle-of-the-camps`, attached S100 historical-anchor wave 2 with 43 edges across 4 hubs — DEDUP HARD; pull in only the islanded Robb-side beats + causal wiring forward to king-in-the-north).
- **Container:** **`wo5k` IS one of the 5 approved containers** — these battles TAKE `containers: [wo5k]`. Check `project_containers_field_and_query_modes` memory + the container rule (a beat is wo5k if it's part of the War of the Five Kings trigger-tree, not merely because a Lannister is in it).
- **Cross-arc seams (lens 4):** the battle chain ↔ `robb-proclaimed-king-in-the-north` (S113) ↔ the **B1 Red-Wedding-upstream spine** (already built — Robb's victories → his marriage blunder → the Frey betrayal); ↔ `siege-of-riverrun` (the Whispering Wood/Camps relieve it); ↔ the Green-Fork (the diversion that let Robb hit Jaime). DEDUP against all of these — the WO5K container is where 9 prior dips landed, so expect heavy dedup-kills.

**Source chapters:** **baseline FIRST to map exact filenames** — the battles are reported, not POV'd. Whispering Wood + Battle of the Camps: AGOT Catelyn (≈`agot-catelyn-09…11`) + Tyrion. Oxcross/the-Fords/Duskendale: ACOK/ASOS Catelyn + Arya + Tyrion + Jaime. `graph-query.py --neighbors` the battle hubs + `--container wo5k` + a dedup pull of existing internal edges.

## The machine (proven 23×; see `working/arc-enrichment-backlog.md` § "The enrichment-pass machine")
1. **Baseline dump** — `graph-query.py --neighbors`/`--full-chain`/`--container wo5k` of the battle cluster + a **dedup pull of all existing internal edges** (small Python pull modeled on `working/enrichment/tyrion-essos/baseline_pull.py`; dedup BEFORE proposing). Write `working/enrichment/wo5k-battles/baseline.md`. Load locked vocab from `working/wiki/data/edge-type-counts.json` (170 types). edges.jsonl uses `edge_type`/`source_slug`/`target_slug`.
2. **Fan out 4 fresh Sonnet lenses** PROPOSE-don't-mint + dedup every node/edge: (a) spine + commander/combatant cast (Robb, Brynden-the-Blackfish, the Greatjon, Karstarks, Jaime, Marq Piper / the river lords), (b) whodunit/hidden-agency + SUSPECTED_OF (Roose Bolton's Duskendale gambit — did he spend Northern lives deliberately?; the captives/ransoms), (c) descriptive/quote/object depth (the night-march, Grey Wind at the Whispering Wood, the broken siege camps), (d) **existing-node↔existing-node causal wiring** (green-fork diversion ENABLES whispering-wood; whispering-wood ENABLES camps ENABLES king-in-the-north; the Westerlands raid → Tywin marches → the Fords). Reuse the LENS-SHARED template from `working/enrichment/tyrion-essos/LENS-SHARED.md` (adapt unit + chapters). Paste the canonical vocab terms + the locked 170-type list + the harvest snippet (split-the-bar WIDE on food incl. camp provisioning / feast-of-victory / hospitality).
3. **Synthesize + decide** (Opus). Encode as `working/enrichment/wo5k-battles/candidates.json` — schema `{_meta:{unit,session:"S163",run_id:"wo5k-battles-enrichment-s163",new_node_slugs:[…]}, edges:[…]}` (see `scripts/enrichment/README.md`). **New-node bodies = `.node.md` files in `working/enrichment/wo5k-battles/nodes/`**. **Whole-file line-check every quote** (`python scripts/quotecheck_enrichment.py working/enrichment/wo5k-battles/candidates.json`; the mint re-greps via norm()). **Qualifier rule:** Tier-1 types (MANIPULATES/WARD_OF/SIBLING_OF/SPOUSE_OF/PARENT_OF/HOLDS_TITLE/VOWS_TO/SWORN_TO) REQUIRE an enum-valid `qualifier`; event-role + causal types take none. **Mint: `python scripts/mint_enrichment.py --candidates …`**. Aliases = natural SPACED phrases. Container-tag genuine WO5K beats `[wo5k]` (`stamp_containers.py` if needed).
4. **Independent Sonnet fresh-verify** the interpretive/causal + borderline edges (vs LOCAL cache; especially any Roose-Bolton SUSPECTED_OF — that's the whodunit wheelhouse and easy to over-claim). Encode `working/enrichment/wo5k-battles/verdicts.json` then **`python scripts/finalize_enrichment.py --verdicts …`**. Re-run `scripts/verify-edge-quotes.py --run-id wo5k-battles-enrichment-s163`. **Node-adding dips need `bash scripts/weirwood-refresh.sh`.** Smoke `--full-chain`/`--neighbors`/`--container wo5k`.
5. **Consume harvest inline** (marquee quotes → new node `## Quotes`; food/description rows → `working/harvest-queue.md`, found-during = `S163 wo5k-battles`). **Check the queue's open-row count at close-out** (`grep -c '^| open ' working/harvest-queue.md`); S162 drained it to **0**, so this dip's ~25–40 will put it ~25–40 — under or near the ~30 bar. Drain (S152 machine) or stage per endsession step 0 as the count warrants.
6. **Close out — AUTO-RUN `/endsession`** (bypass authorized above). Set the next live prompt to **A2.5 PASS 2** (the Westerlands raid) IF battles remain, ELSE **A2.8 Davos/Sam residual** — name which in the handoff.

## DO NOT
run extractions without asking · un-park D&E · `git add -A` (stage your own files by path) · hand-write a per-dip mint/finalize/quotecheck script (use the generic ones) · **assert theory readings (GATED)** — evidence/act edges only, readings stay node-prose · use **TWOW** material (only the 5 books) · re-mint the **S100 Battle-of-the-Camps** 4-hub attach / the **green-fork** (29-edge) spine / the **B1 Red-Wedding-upstream** spine / `robb-proclaimed-king-in-the-north` (DEDUP against the baseline) · over-mint speculative seam edges (node-prose when unsure) · attempt all 5 battles in one dip (this is PASS 1 — scope a sub-cluster, log the remainder) · use a container tag outside the approved 5 — **`wo5k` IS approved; tag genuine WO5K-trigger-tree beats `[wo5k]`**.

## Read first
- `working/enrichment-coverage-plan.md` (§ A2.5 + the 🅰 A-roundup banner — note the multi-pass flag) · `working/arc-enrichment-backlog.md` (ledger — 23 dips + the machine + the scope model) · `scripts/enrichment/README.md` (the parameterized mint/finalize/quotecheck schemas) · `working/enrichment/tyrion-essos/` (S161 data-artifact exemplar: baseline_pull + LENS-SHARED + candidates.json + verdicts.json)
- `worklog.md` S162 + S161 entries + the STATUS block · memory `project_arc_enrichment_track`, `feedback_enrichment_board_causal_lens` (the 4th causal-wiring lens), `project_containers_field_and_query_modes` (wo5k tagging), `project_narrative_arc_reification` (chain-as-arc, no umbrella parents), `project_theories_track_deferred` (the gating)
- **Carried small-fix flags (NOT this dip):** the valyrian-steel node mis-type (object.artifact→object.material, S159); the Harrenhal node-tangle alias-hygiene (S154); the House-of-Black-and-White node mis-type; the `fight-at-the-fist`/`battle-of-the-fist-of-the-first-men` dup-merge. → `working/todos.md` § Small Fixes.
