# SESSION 150 — Next enrichment dip (the descent reaches the cheap L2 round, OR a remaining A2 arc)

> **This is Session 150.** Stamp your worklog entry `### Session 150` in `worklog.md`.
> **Recommended model:** Sonnet 4.6 for the lens subagents + fresh-verify; Opus 4.8 for orchestration/synthesis.
> **One enrichment dip per session** (Matt S131). **D&E Pass-1 is PARKED** — stage only your own files by path; never `git add -A`.
> **`/endsession` is PRE-AUTHORIZED for an enrichment dip** (Matt S142/S144; step 5 of the machine in `working/arc-enrichment-backlog.md`). Run it yourself at the end once harvests are accounted for.

## Where we are
The reopened L1 round is **COMPLETE** (Dany S144 · Jon S145 · Bran S146 · AEGON S147), and **both big build+enrich A2 arcs are now done**: **A2.2 Sansa/Vale (S148)** and **A2.1 Theon/Reek (S149)** — the last big cross-identity arc (wired the dead-end escape into Jon's stabbing; documented the three-bearer Reek tangle via `ramsay-snow IMPERSONATES reek` + disambiguation notes). Full ranked plan + scope cards: `working/enrichment-coverage-plan.md`; ledger `working/arc-enrichment-backlog.md`.

## STEP 0 — surface the fork to Matt (don't auto-pick)
Prior dips (S140–S149) surfaced the genuine fork via AskUserQuestion at STEP 0. Do the same. With both A2 build+enrich arcs done, the fork now tilts toward the **cheap L2 interleave round** (the plan's step 3) — but a remaining A2 arc is also live:
- **The cheap L2 round (lighter; recommended).** A granular sub-plot inside one of the enriched arcs. Top picks (all in `enrichment-coverage-plan.md` Class B):
  - **B1 Frey-pies / Grand Northern Conspiracy** (Red Wedding) — **cheapest fully-ready high-value pick; recommended default.** All constituent nodes already built (Wyman/Wylis + the 4 S93 deception events) — only the `grand-northern-conspiracy` event.conspiracy hub + the `ENABLES stannis-march-on-winterfell` seam + the Manderly-bakes-the-pies beat are missing. wo5k→north seam; fixes the `frey-pies-theories` 0-evidence dead-end. **The Manderly Rat-Cook / wedding-pie foreshadowing was just harvested S149** (`working/harvest-queue.md`, `adwd-the-prince-of-winterfell-01:130/141`) — the seed is ready.
  - **B2 Robert Strong** (Oberyn POISONS Gregor + Qyburn; closes 2 dead-ends; AFFC names the manticore venom — the deferred S139 venom).
  - **B3 Sandor/gravedigger** (Brienne→Stoneheart; salience 10/10; gravedigger=Sandor stays GATED — evidence edges only).
  - **B4 Ned black-cells** (the `varys-visits-ned ENABLES → confession` gap). · **B7 Kingslayer-quote** (cheapest; the slaying hub has no `## Quotes`).
- **The remaining A2 arcs (heavier; build+enrich).** **A2.3 Arya/Braavos** (House of B&W; identity-dissolution; architecturally isolated — low cross-arc payoff), **A2.4 Tyrion/ADWD-Essos journey** (the events are the value; Tyrion already saturated), **A2.5 WO5K battles** (Whispering Wood/Camps/Oxcross/Fords — Robb's military arc).
- **Recommendation:** **B1 Frey-pies/GNC** — cheapest, fully-ready, the seed is harvested, and it deepens an already-enriched cluster. But surface the fork; confirm the unit, then run the machine.

## The machine (proven 15×; see `working/arc-enrichment-backlog.md` § "The enrichment-pass machine")
1. **Baseline dump** — `graph-query.py --neighbors`/`--full-chain` of the unit's hub + satellites + **a dedup pull of all existing edges within the core node-set** (S149 lesson: the graph is denser than the baseline implies — many lens proposals already exist; pull `(source,type,target)` for the core nodes first to avoid re-minting). Write `working/enrichment/<unit>/baseline.md`. Load the locked vocab from `working/wiki/data/edge-type-counts.md` (170 canonical types; **verify membership against the JSON `type_counts` list — the per-type counts there are infobox-only and STALE for the curator layer, so DON'T claim "first-use" from them; grep edges.jsonl to confirm**). edges.jsonl uses `edge_type`/`source_slug`/`target_slug`, NOT `type`.
2. **Fan out 4 fresh Sonnet lenses** PROPOSE-don't-mint + dedup-check every node/edge: (a) spine + secondary-character sub-arcs, (b) the whodunit/revelation thread + SUSPECTED_OF layer, (c) descriptive/quote/object depth, (d) **the existing-node↔existing-node causal-wiring lens** (cross-container seams). Paste the canonical vocab terms (Pass/Track/Tier/lowercase-step; Tier=confidence 1–5 only) + the harvest snippet (split-the-bar WIDE-OPEN on food incl. the grim register) — subagents don't load CLAUDE.md.
3. **Synthesize + decide** (Opus). Encode as `candidates.json` (schema = `scripts/mint_theon_reek_enrichment_s149.py` / `mint_sansa_vale_enrichment_s148.py` — `{id, source, type, target, tier, book, chapter, line, quote, verify, note}` + optional `qualifier` for Tier-1 relationship types). **Whole-file line-check every quote** (copy `working/enrichment/theon-reek/verify_lines.py`, point it at your unit's candidates.json — it caught 0 slips this run because the quotes were grepped exact; do the same). **Qualifier rule (validator-enforced):** Tier-1 relationship types (MANIPULATES, WARD_OF, SIBLING_OF, SPOUSE_OF, PARENT_OF, HOLDS_TITLE, VOWS_TO, SWORN_TO) REQUIRE an enum-valid `qualifier`; Tier-3 must NOT; Tier-2 optional. (Event-role + causal types don't take one.) Mint via a `scripts/mint_<unit>_enrichment_s150.py` (reads candidates.json, re-greps the authoritative line, backup + re-run guard + slug pre-check via `mint_arc_lib.precheck_slugs`). Node aliases = natural SPACED phrases. **Check live edge-direction conventions before emitting.**
4. **Independent Sonnet fresh-verify** the interpretive/causal edges + any borderline mints (vs LOCAL cache). Apply drops/adjusts/retirements via a `finalize_<unit>_s150.py` (+ stamp `verified_by`). Re-run the 0-drift check (`scripts/verify-edge-quotes.py --run-id <run>`). **Node-adding dips need `bash scripts/weirwood-refresh.sh`.** Smoke `--full-chain`/`--container`. **A flagged-borderline edge that fresh-verify rejects → drop it in finalize** (the propose-then-gate flow; S147 dropped a node, S148 confirmed an upgrade-and-retire, S149 retired a factually-false existing edge + downgraded a coy whodunit AGENT_IN→SUSPECTED_OF).
5. **Consume harvest** (attach load-bearing quotes inline incl. book-cite overlays onto wiki nodes; park food/description in `working/harvest-queue.md` — ~170 open + parked as of S149).
6. **Close out — run `/endsession`** (pre-authorized).

## DO NOT
run extractions without asking · un-park D&E · `git add -A` (stage your own files by path) · assert theory readings (frey-pies-beyond-the-named-cooks, Robert-Strong-is-Gregor-as-CERTAIN, gravedigger-is-Sandor-as-CERTAIN, fAegon/Blackfyre, R+L, Azor Ahai — these stay GATED, evidence edges only) · claim "first-use" for an edge type without grepping edges.jsonl first (the vocab JSON counts are infobox-only) · perpetuate the stray `containers: [jon]` tag (use approved containers only: essos/wo5k/north/aegon/bran; L2 sub-plots inherit their parent arc's container tag, A2 arcs get none).

## Read first
- `working/enrichment-coverage-plan.md` (the S143 ranking + Class A/B/C scope cards — B1–B8 + A2.3–A2.8)
- `working/arc-enrichment-backlog.md` (ledger — 15 dips logged + the enrichment-pass machine + the scope model)
- `worklog.md` S149 entry + the STATUS block · memory `project_arc_enrichment_track`
- For B1 (the recommended pick): the scope card is in `enrichment-coverage-plan.md` (§ B1, "cheapest high-value L2"); the Manderly Rat-Cook/wedding-pie seed is in `working/harvest-queue.md` (S149 rows). Source: ADWD Davos chapters (Manderly's plot) + the-prince-of-winterfell-01 (the pies) + the existing S93 Wyman/Davos deception nodes.
