# SESSION 159 — A-arc roundup #4: A2.6 Jaime / Riverlands enrichment

> **This is Session 159.** Stamp your worklog entry `### Session 159` in `worklog.md` (GRAPH track, global S-number).
> **Recommended model:** Sonnet 4.6 for the lens subagents + fresh-verify; Opus 4.8 for orchestration/synthesis.
> **One enrichment dip per session** (Matt S131). **D&E Pass-1 is PARKED** — stage only your own files by path; never `git add -A`.
> **`/endsession` is PRE-AUTHORIZED for an enrichment dip** (Matt S142/S144; step 6 of the machine). OFFER it, don't auto-run.
> **NEW TOOLING (S158):** mint via the parameterized `scripts/mint_enrichment.py` / `finalize_enrichment.py` — **do NOT hand-write a per-dip `mint_*`/`finalize_*` script** (that copy-paste debt was retired S158; the 74 historical ones are frozen in `scripts/enrichment/archive/`). See `scripts/enrichment/README.md` for the JSON schemas.

## The unit is LOCKED — no STEP-0 fork this session
Matt directed (S154): **round up the remaining A arcs** as a multi-session campaign. **S155 Stannis + S156 Dorne + S157 Euron shipped.**
Per the order, **S159 = A2.6 Jaime / Riverlands.** So **do NOT surface a fork** — go straight to baselining the Jaime/Riverlands core and running the machine. (The A-roundup order after this: A2.4 Tyrion-Essos → A2.5 WO5K-battles [LAST, multi-pass] → A2.8 Davos/Sam residual. Full plan: `working/enrichment-coverage-plan.md` § A2.6 + the 🅰 banner.)

## Where we are
**21 major-arc enrichment dips done** (through S157 Euron). Edges **23,169**, nodes **8,689**. Ledger + the machine:
`working/arc-enrichment-backlog.md`. The data-artifact exemplar to copy: **`working/enrichment/euron/`** (S157 — baseline + baseline_pull.py + LENS-SHARED + 4 lens files + candidates.json + quotecheck.py + fresh-verify.md). **NOTE the mint/finalize step is now the generic tool, not a per-dip script** — write `candidates.json` (+ any new-node `.node.md` bodies in `working/enrichment/jaime-riverlands/nodes/`) and a `verdicts.json`, then run the two parameterized scripts.

## A2.6 Jaime / Riverlands — scope (Jaime is a POV; WIRE+ENRICH likely, BASELINE FIRST)
Jaime is a saturated POV character with a dense dyad web but (per the A2 pattern) likely a thin/islanded **event-hub** layer for his AFFC Riverlands command. **BASELINE FIRST** to see exactly what exists. The dip's marquee value (per Matt's handoff):
- **The Riverrun siege resolution** (AFFC) — Jaime arrives to end the siege he inherited from the Freys; the negotiation, the threat to trebuchet Edmure, **Edmure's surrender** (Jaime's "I will trebuchet your child into the keep" coercion), the bloodless taking of Riverrun. **Overlaps RW pass-2 Riverrun-siege + the S100 `siege-of-riverrun` historical-anchor wave** — DEDUP against those.
- **The Blackfish's escape** (Brynden Tully slips the castle rather than yield) — a clean event to reify if unbuilt.
- **Harrenhal** — Jaime's command tenure / the restoration-of-order; overlaps the S154 D5 Harrenhal cluster (DEDUP; carried small-fix = the Harrenhal node-tangle alias-hygiene, NOT a merge).
- **The burning-of-Cersei's-letter rupture** — the marquee Jaime↔Cersei break (Jaime burns her plea-for-help letter unread, refuses to ride to her aid). A character/relationship beat — model honestly (an act + a MOTIVATES/refusal, NOT an over-asserted edge).
- **The ASOS backstory layer** (the hand-loss, the bath confession of the wildfire-plot/Aerys, Vargo Hoat / the Brave Companions) — much **overlaps S141 Brienne→Stoneheart + S142 Sack-of-KL** (the wildfire thread). DEDUP; pull in only what those dips left islanded.
- **Cross-arc seams (lens 4):** Jaime ↔ the WO5K Riverlands board (Riverrun/Harrenhal as the last Tully holdouts the Iron Throne mops up); Jaime ↔ Brienne (the oath, the sword Oathkeeper, the Stoneheart hand-off → S141); Jaime ↔ Cersei (the letter; the Kingsguard-vow arc).

**Source chapters** (map exact filenames in the baseline): the **AFFC Jaime chapters** (`affc-jaime-01` … `affc-jaime-07` — the Riverlands command + Riverrun siege are here) + the **ASOS Jaime chapters** (`asos-jaime-*` — hand-loss / bath confession / Brave Companions, mostly DEDUP vs S141/S142). **Baseline FIRST** — `graph-query.py --neighbors jaime-lannister` + the Riverrun/Harrenhal/Blackfish/Edmure hubs + a dedup pull of existing internal edges in the core node-set (the S100 historical-anchor + S141 + S142 work means a chunk is wired — expect the dedup to kill several lens proposals).

## The machine (proven 21×; see `working/arc-enrichment-backlog.md` § "The enrichment-pass machine")
1. **Baseline dump** — `graph-query.py --neighbors`/`--full-chain` of the Jaime/Riverlands core + a **dedup pull of all existing internal edges** (small Python pull modeled on `working/enrichment/euron/baseline_pull.py`; dedup BEFORE proposing). Write `working/enrichment/jaime-riverlands/baseline.md`. Load locked vocab from `working/wiki/data/edge-type-counts.json` (170 types). edges.jsonl uses `edge_type`/`source_slug`/`target_slug`.
2. **Fan out 4 fresh Sonnet lenses** PROPOSE-don't-mint + dedup every node/edge: (a) spine + secondary-character sub-arcs (Edmure, the Blackfish, the Freys at Riverrun, Ser Ilyn/Jaime's training), (b) whodunit/hidden-agency + SUSPECTED_OF (the coercion of Edmure; the Tully holdouts; any unproven agency), (c) descriptive/quote/object depth (Oathkeeper, the golden hand, Riverrun/the Tully water-gate, the weirwood-and-river register), (d) **existing-node↔existing-node causal wiring** (the Riverrun-siege resolution → WO5K board; the Cersei-letter → Jaime's refusal). Reuse the LENS-SHARED template from `working/enrichment/euron/LENS-SHARED.md` (adapt the unit + chapters). Paste the canonical vocab terms + the locked 170-type list + the harvest snippet (split-the-bar WIDE on food incl. the Riverrun siege fare / Jaime's camp meals).
3. **Synthesize + decide** (Opus). Encode as `working/enrichment/jaime-riverlands/candidates.json` — schema `{_meta:{unit,session:"S159",run_id:"jaime-riverlands-enrichment-s159",new_node_slugs:[…]}, edges:[{id,type,source,target,book,chapter,quote,tier,note,qualifier?,verify?}]}` (see `scripts/enrichment/README.md`). **New-node bodies = `.node.md` files in `working/enrichment/jaime-riverlands/nodes/`** (the `type:` frontmatter routes them), NOT embedded Python. **Whole-file line-check every quote** (`quotecheck.py`; the mint re-greps via norm()). **Qualifier rule (validator-enforced):** Tier-1 types (MANIPULATES/WARD_OF/SIBLING_OF/SPOUSE_OF/PARENT_OF/HOLDS_TITLE/VOWS_TO/SWORN_TO) REQUIRE an enum-valid `qualifier`; Tier-3 must NOT; event-role + causal types take none. **Mint: `python scripts/mint_enrichment.py --candidates working/enrichment/jaime-riverlands/candidates.json`** (does slug pre-check + re-run guard + quote re-grep + backup + node-create + edge-append). Aliases = natural SPACED phrases.
4. **Independent Sonnet fresh-verify** the interpretive/causal + borderline edges (vs LOCAL cache). Encode drops/adjusts/bug-fixes as `working/enrichment/jaime-riverlands/verdicts.json` (`{run_id, verified_by:"fresh-verify-s159", drop:[…], note_patch:{…}, bug_drop:[…], bug_repoint:[…]}`) then **`python scripts/finalize_enrichment.py --verdicts working/enrichment/jaime-riverlands/verdicts.json`**. Re-run `scripts/verify-edge-quotes.py --run-id jaime-riverlands-enrichment-s159`. **Node-adding dips need `bash scripts/weirwood-refresh.sh`.** Smoke `--full-chain`/`--neighbors`.
5. **Consume harvest inline** (marquee quotes → new node `## Quotes`; food/description rows → `working/harvest-queue.md`, table `| status | kind | book | chapter:line | note | source-dip |`, source-dip = `S159 jaime-riverlands`). **Check the queue's open-row count at close-out** (`grep -c '^| open ' working/harvest-queue.md`); if ≥~30, drain or stage a drain (endsession step 0).
6. **Close out — OFFER `/endsession`** (pre-authorized; don't auto-run without Matt's nod). Set the next live prompt to the next A-roundup unit (**A2.4 Tyrion-Essos**).

## DO NOT
run extractions without asking · un-park D&E · `git add -A` (stage your own files by path) · **hand-write a per-dip `mint_*`/`finalize_*` script** (use the generic `mint_enrichment.py`/`finalize_enrichment.py` — S158) · **assert theory readings (GATED):** any valonqar / Jaime-as-valonqar / prophecy reading — evidence/act edges only, the readings stay node-prose · use **TWOW** material (only the 5 books) · claim "first-use" without grepping edges.jsonl · over-mint speculative seam edges (node-prose when unsure) · re-mint the S100 `siege-of-riverrun` historical-anchor edges / the S141 Brienne→Jaime arc / the S142 wildfire-confession thread (DEDUP against the baseline) · re-cut the Harrenhal node-tangle (alias-hygiene small-fix, separate from this dip) · use a container tag outside the approved 5 (essos/wo5k/north/aegon/bran) — **the Riverlands is NOT an approved container; default NO container tag** (a Riverrun/WO5K-board beat may take `[wo5k]` only if it is genuinely a WO5K-campaign event, per the container rule).

## Read first
- `working/enrichment-coverage-plan.md` (§ A2.6 + the 🅰 A-roundup banner) · `working/arc-enrichment-backlog.md` (ledger — 21 dips + the machine [step 3 now uses the generic tool] + the scope model) · `scripts/enrichment/README.md` (the parameterized mint/finalize schemas — NEW S158) · `working/enrichment/euron/` (S157 data-artifact exemplar: baseline_pull + LENS-SHARED + candidates.json + quotecheck)
- `worklog.md` S157 + S158 entries + the STATUS block · memory `project_arc_enrichment_track`, `feedback_enrichment_board_causal_lens` (the 4th causal-wiring lens), `project_theories_track_deferred` (the gating)
- **Carried small-fix flags (NOT this dip):** the Harrenhal node-tangle alias-hygiene (S154); the House-of-Black-and-White node mis-type; the `fight-at-the-fist`/`battle-of-the-fist-of-the-first-men` dup-merge. → `working/todos.md` § Small Fixes.
