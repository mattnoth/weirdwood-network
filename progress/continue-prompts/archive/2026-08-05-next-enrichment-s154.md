# SESSION 154 — Next enrichment dip (the A2/Class-D fork: another Class-D event cluster OR a remaining A2 arc)

> **This is Session 154.** Stamp your worklog entry `### Session 154` in `worklog.md` (GRAPH track, global S-number).
> **Recommended model:** Sonnet 4.6 for the lens subagents + fresh-verify; Opus 4.8 for orchestration/synthesis.
> **One enrichment dip per session** (Matt S131; the S151 cheap-L2 batch was a one-time relaxation). **D&E Pass-1 is PARKED** — stage only your own files by path; never `git add -A`.
> **`/endsession` is PRE-AUTHORIZED for an enrichment dip** (Matt S142/S144; step 6 of the machine). OFFER it, don't auto-run.

## Where we are
**17 major-arc enrichment dips done.** The reopened L1 round is complete (Dany S144 · Jon S145 · Bran S146 · AEGON S147); the three big A2 build+enrich cross-identity arcs are done (Sansa/Vale S148 · Theon/Reek S149 · Arya/Braavos S150); the cheap L2 round shipped S151; the dedicated harvest pass drained the queue S152; and **the FIRST Class-D event-cluster dip — D1 Battle of Castle Black — shipped S153** (+6 nodes/+34 net edges → edges **23,022**, nodes **8,639**). Full ranked plan + scope cards: `working/enrichment-coverage-plan.md`; ledger + the machine: `working/arc-enrichment-backlog.md`.

## STEP 0 — surface the fork to Matt (don't auto-pick)
Every dip S140–S153 surfaced the fork via AskUserQuestion at STEP 0. Do the same. With D1 done, the fork is the **remaining Class-D clusters** vs the **remaining A2 arcs**:
- **Class D — big current-narrative event clusters** (`enrichment-coverage-plan.md` § Class D; baseline each before picking — these aren't graph-verified):
  - **D5 Arya's AGOT→ACOK flight & Harrenhal** — Yoren's recruits, Needle's first kill, the capture, Harrenhal (the Tickler, weasel soup, Jaqen's three deaths, **the iron-coin's origin**), the Bloody Mummers. **Natural follow-on to S150/S153** — it's the origin of the iron-coin seam the Arya/Braavos arc hangs off, and the Harrenhal cluster connects to the Tywin/wartime-Riverlands material.
  - **D2 Hand's Tourney** (AGOT; Gregor beheads his horse / Hound saves Loras — Gregor/Sandor/Loras set-piece) · **D3 Greyjoy's Rebellion** (289 AC backstory hub linking Iron Islands + Starks + Baratheons + Lannisters) · **D4 Riot of King's Landing** (ACOK mob event). (D6 Rebellion prelude is theory-adjacent → gated, lower priority.)
- **Remaining A2 arcs (heavier build+enrich):** A2.4 Tyrion/ADWD-Essos journey · A2.5 WO5K battles (Whispering Wood/Camps/Oxcross/Fords — flagged "needs many passes") · A2.6 Jaime/Riverlands-command (AFFC) · A2.7 Stannis.
- **Recommendation:** surface **D5 Arya/Harrenhal** (the S150/S153 follow-on, highest continuity payoff) as the strongest default, plus D2/D3 and the A2 remainder; confirm the unit with Matt, then run the machine. **Baseline the picked unit's existing density FIRST** (Class-D clusters aren't graph-verified — S153 found the Castle Black hub already had 5 sub-beats + the Noye↔Mag dyad, so always check before proposing).

## The machine (proven 18×; see `working/arc-enrichment-backlog.md` § "The enrichment-pass machine")
1. **Baseline dump** — `graph-query.py --neighbors`/`--full-chain`/`--container` of the unit's hub + satellites + **a dedup pull of all existing edges within the core node-set** (write a small Python pull; dedup BEFORE proposing). Write `working/enrichment/<unit>/baseline.md`. Load locked vocab from `working/wiki/data/edge-type-counts.json` (170 types; the histogram in the .md is infobox-only — DON'T claim "first-use" from it, grep edges.jsonl to confirm). edges.jsonl uses `edge_type`/`source_slug`/`target_slug`.
2. **Fan out 4 fresh Sonnet lenses** PROPOSE-don't-mint + dedup every node/edge: (a) spine + secondary-character sub-arcs, (b) whodunit/revelation + SUSPECTED_OF, (c) descriptive/quote/object depth, (d) **existing-node↔existing-node causal-wiring** (cross-container seams — the standing 4th lens). Paste the canonical vocab terms (Pass/Track/Tier/lowercase-step; Tier=confidence 1–5 only) + the locked 170-type list + the harvest snippet (split-the-bar WIDE-OPEN on food incl. the grim register) — subagents don't load CLAUDE.md. (S153 lesson: lenses independently caught two text-traps — they're worth trusting, and they catch show-vs-book contamination.)
3. **Synthesize + decide** (Opus). Encode as `candidates.json` (schema = `scripts/mint_battle_castle_black_enrichment_s153.py` — edges array; node bodies live in the mint script). **Whole-file line-check every quote** (the mint re-greps via norm() which handles curly quotes; a quote must be a single contiguous substring, never spliced across a `," said X, "` attribution). **Qualifier rule (validator-enforced):** Tier-1 relationship types (MANIPULATES/WARD_OF/SIBLING_OF/SPOUSE_OF/PARENT_OF/HOLDS_TITLE/VOWS_TO/SWORN_TO) REQUIRE an enum-valid `qualifier` (see `reference/edge-qualifier-vocab.md` — e.g. MANIPULATES ∈ {via_bribe,via_flattery,via_false_information,via_threat,via_seduction,unknown}); Tier-3 must NOT; event-role + causal types take none. Mint via `scripts/mint_<unit>_enrichment_s154.py` (re-greps, backup + re-run guard + `mint_arc_lib.precheck_slugs`). Aliases = natural SPACED phrases.
4. **Independent Sonnet fresh-verify** the interpretive/causal edges + borderline mints (vs LOCAL cache). Apply drops/adjusts/retirements via `finalize_<unit>_s154.py` (+ stamp `verified_by`). Re-run `scripts/verify-edge-quotes.py --run-id <run>`. **Node-adding dips need `bash scripts/weirwood-refresh.sh`.** Smoke `--full-chain`/`--container`/`--event-participants`. A flagged-borderline edge that fresh-verify rejects → drop it (propose-then-gate). Retire any junk cross-theater PRECEDES you find on the hub (S153 dropped 2).
5. **Consume harvest inline** (attach load-bearing quotes into the new node bodies' `## Quotes`; push food/description breadcrumbs to `working/harvest-queue.md` — table format `| status | kind | book | chapter:line | note | source-dip |`; currently ~20 open after S153 + 53+ parked).
6. **Close out — OFFER `/endsession`** (pre-authorized; don't auto-run without Matt's nod).

## DO NOT
run extractions without asking · un-park D&E · `git add -A` (stage your own files by path) · assert theory readings (Robert-Strong=Gregor, gravedigger=Sandor, fAegon/Blackfyre, R+L, Azor Ahai, Faceless-Men cosmology, **Horn-of-Winter actually-wakes-giants** — GATED, evidence/possession edges only) · claim "first-use" without grepping edges.jsonl · over-mint speculative foreshadowing/seam edges (node-prose when unsure) · mint show-canon beats not in the books (S153 caught "Grenn holds the gate" as HBO-only) · use a container tag outside the approved 5 (essos/wo5k/north/aegon/bran; Class-D/A2 arcs that aren't in a container get none — the Castle Black battle nodes got `[north]` because it IS a North-container battle).

## Read first
- `working/enrichment-coverage-plan.md` (§ Class D candidates D2–D6 + § A2.4–A2.7; D1 ✅ done) · `working/arc-enrichment-backlog.md` (ledger — 18 dips + the machine + the scope model)
- `worklog.md` S153 entry + the STATUS block · memory `project_arc_enrichment_track`, `feedback_enrichment_board_causal_lens` (the 4th causal-wiring lens)
- **Carried small-fix flags (NOT this dip):** the House of Black and White node mis-typed `organization.house` (should be `place.location` — a TEMPLE); the `fight-at-the-fist` / `battle-of-the-fist-of-the-first-men` possible dup-merge (S153 flag). → `working/todos.md` § Small Fixes.
