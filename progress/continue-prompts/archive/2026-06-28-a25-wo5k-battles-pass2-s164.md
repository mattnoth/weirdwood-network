# SESSION 164 — A2.5 WO5K-battles PASS 2: the Westerlands raid (Oxcross / the Crag / the Jeyne-Westerling blunder)

> **This is Session 164.** Stamp your worklog entry `### Session 164` in `worklog.md` (GRAPH track, global S-number).
> **Recommended model:** Sonnet 4.6 for the lens subagents + fresh-verify; Opus 4.8 for orchestration/synthesis.
> **One enrichment dip per session** (Matt S131). **D&E Pass-1 is PARKED** — stage only your own files by path; never `git add -A`.
> **`/endsession` BYPASS — AUTO-RUN (do not merely offer).** This is an enrichment dip; per Matt's strengthened carve-out (S161, 2026-06-28) enrichment continue prompts carry the auto-run bypass — **bypass the no-auto-endsession hard rule and run `/endsession` automatically at close-out** (memory `feedback_endsession_requires_permission` reflects this carve-out).
> **Mint via the generic `scripts/mint_enrichment.py --candidates …` + `scripts/finalize_enrichment.py --verdicts …`** — do NOT hand-write a per-dip script (retired S158), and use `scripts/quotecheck_enrichment.py <candidates.json>` (generalized S159). Schemas: `scripts/enrichment/README.md`.

## The unit is LOCKED — no STEP-0 fork this session
A2.5 WO5K-battles is the LAST 🅰 A-roundup unit and an explicitly **multi-pass mini-track**. **PASS 1 (S163) shipped Robb's Riverlands-relief rise** (the Whispering Wood → the Battle of the Camps → the King-in-the-North seam; the wiki-shell `battle-in-the-whispering-wood` went 0→22 edges + the green-fork→whispering-wood→camps→king-in-the-north ENABLES spine + siege→camps; 3 bug-drops; 4 hubs stamped `[wo5k]`; net +19 edges). **THIS IS PASS 2 — the Westerlands raid.** After PASS 2 → **PASS 3** (the unravelling: the Fords / Duskendale → RW-upstream) → **A2.8 Davos/Sam residual** closes the A-roundup. Full plan: `working/enrichment-coverage-plan.md` § A2.5.

## A2.5 PASS 2 — scope (Robb carries the war into the Westerlands; the victory that breeds the marriage blunder)
After relieving Riverrun, Robb takes the war west past the Golden Tooth into the Lannister heartland. The cluster:
- **Oxcross** — `battle-of-oxcross` EXISTS (cIn=7/cOut=3 but **causally ISLANDED** — only role/PRECEDES edges, no ENABLES). Robb's night victory over Stafford Lannister's green host raised at Casterly Rock (Grey Wind opens the night attack; Stafford slain). Baseline shows existing: `robb-stark COMMANDS_IN`, `grey-wind AGENT_IN`, `rickard-karstark AGENT_IN`, `stafford-lannister VICTIM_IN`. **DEDUP** — add the missing roles + the causal wiring.
- **The Westerlands raid / the mills / the Crag** — `storming-of-the-crag` EXISTS. The raiding campaign (burning the Lannister coast, the ore mines, Ashemark) — mostly reported through ACOK/ASOS Catelyn + Robb's letters.
- **The Jeyne-Westerling marriage blunder** — ⚠️ **HEAVY DEDUP: the B1 Red-Wedding-upstream spine is ALREADY BUILT.** `--full-chain robb-proclaimed-king-in-the-north` shows it: `storming-of-the-crag ENABLES robb-weds-jeyne-westerling`, `robb-receives-false-news-of-brans-death TRIGGERS robb-weds-jeyne-westerling`, `robb-weds-jeyne-westerling TRIGGERS red-wedding-conspiracy`. **Do NOT rebuild the marriage→RW chain.** PASS 2's value is the BATTLE/raid half feeding INTO it.
- **The PASS-1 seam already planted:** `gawen-westerling VICTIM_IN battle-in-the-whispering-wood` (S163) — Gawen's capture at the Whispering Wood is the thread that later puts Robb at the Crag (Gawen's seat) and in Jeyne's path. Wire Oxcross/the raid → the Crag, and let the existing marriage spine take it from there.

**PASS-2 cut (decide from the baseline):** the Westerlands campaign as a causal segment — **the relief-rise (PASS 1) ENABLES the westward raid → Oxcross → the raid breeds the Crag → (existing) the marriage blunder.** Wire Oxcross's causal in/out (it's islanded), enrich its roster, build the raid beats if load-bearing, and connect the segment to the already-built marriage spine. **Leave the Fords / Duskendale for PASS 3** (log the remainder).

**Source chapters:** **baseline FIRST to map exact filenames** (the battles are reported, not POV'd). Oxcross + the raid: **ACOK Catelyn** (Robb's victories reported at Riverrun) + ASOS Catelyn I–II (Robb returns married). `graph-query.py --neighbors battle-of-oxcross` + `--neighbors storming-of-the-crag` + `--neighbors robb-weds-jeyne-westerling` + `--container wo5k` + a dedup pull of existing internal edges (model on `working/enrichment/wo5k-battles/baseline_pull.py` — adapt the CORE set to the Westerlands cluster).

## The machine (proven 24×; see `working/arc-enrichment-backlog.md` § "The enrichment-pass machine")
1. **Baseline dump** — `graph-query.py --neighbors`/`--full-chain`/`--container wo5k` of the Westerlands cluster + a dedup pull of all existing internal edges (adapt `working/enrichment/wo5k-battles/baseline_pull.py`; dedup BEFORE proposing). Write `working/enrichment/wo5k-battles-pass2/baseline.md`. Load locked vocab from `working/wiki/data/edge-type-counts.json` (170 types). edges.jsonl uses `edge_type`/`source_slug`/`target_slug`.
2. **Fan out 4 fresh Sonnet lenses** PROPOSE-don't-mint + dedup every node/edge: (a) spine + commander/combatant cast (Robb, Grey Wind, the Greatjon, Karstarks, Stafford Lannister, the Westerling household), (b) whodunit/hidden-agency + SUSPECTED_OF (HONEST — is the Jeyne marriage a Westerling/Spicer trap? the Sybell-Spicer scheming is ASOS-revealed/AFFC — gate carefully, much may be PASS-3 or theory-tier; the storming-of-the-crag "wound" is the in-text hook), (c) descriptive/quote/object depth (the night attack at Oxcross, the burning Westerlands, the Crag, Jeyne), (d) **existing-node↔existing-node causal wiring** (battle-of-the-camps/relief ENABLES the westward raid → oxcross → the Crag → the EXISTING marriage spine). Reuse the LENS-SHARED template from `working/enrichment/wo5k-battles/LENS-SHARED.md` (adapt unit + chapters). Paste the canonical vocab terms + the locked 170-type list + the harvest snippet (split-the-bar WIDE on food incl. camp provisioning / the wedding feast / Westerling hospitality).
3. **Synthesize + decide** (Opus). Encode `working/enrichment/wo5k-battles-pass2/candidates.json` — schema `{_meta:{unit,session:"S164",run_id:"wo5k-battles-pass2-enrichment-s164",new_node_slugs:[…]}, edges:[…]}`. New-node bodies (if any) = `.node.md` in `…/nodes/`. **Whole-file line-check every quote** (`python3 scripts/quotecheck_enrichment.py …`). **Qualifier rule:** Tier-1 types (MANIPULATES/WARD_OF/SIBLING_OF/SPOUSE_OF/PARENT_OF/HOLDS_TITLE/VOWS_TO/SWORN_TO) REQUIRE an enum-valid `qualifier`; event-role + causal types take none. **Mint: `python3 scripts/mint_enrichment.py --candidates …`**. Container-tag genuine WO5K beats `[wo5k]` (`stamp_containers.py` if needed — Oxcross/the Crag are WO5K-trigger-tree beats).
4. **Independent Sonnet fresh-verify** the interpretive/causal + borderline edges (vs LOCAL cache; especially any Westerling/Spicer SUSPECTED_OF — the marriage-trap reading is theory-adjacent, easy to over-claim, and much of the proof is ASOS-late/AFFC). Encode `…/verdicts.json` then **`python3 scripts/finalize_enrichment.py --verdicts …`**. Re-run `scripts/verify-edge-quotes.py --run-id wo5k-battles-pass2-enrichment-s164`. **Node-adding dips need `bash scripts/weirwood-refresh.sh`** (PASS 1 added 0 nodes and skipped it; if PASS 2 adds nodes, run it). Smoke `--full-chain`/`--neighbors`/`--container wo5k`.
5. **Consume harvest inline** (marquee quotes → node `## Quotes`; food/description rows → `working/harvest-queue.md`, found-during = `S164 wo5k-battles-pass2`). **Check the queue's open-row count at close-out** (`grep -c '^| open ' working/harvest-queue.md`); PASS 1 left it at **20**, so this dip's ~20–40 will likely cross the ~30 bar — DRAIN (the S152 parallel-attacher machine) or stage a drain per endsession step 0 as the count warrants.
6. **Close out — AUTO-RUN `/endsession`** (bypass authorized above). Set the next live prompt to **A2.5 PASS 3** (the Fords / Duskendale → RW-upstream) — name it in the handoff.

## DO NOT
run extractions without asking · un-park D&E · `git add -A` (stage your own files by path) · hand-write a per-dip mint/finalize/quotecheck script (use the generic ones) · **assert theory readings (GATED)** — evidence/act edges only, readings stay node-prose · use **TWOW** material (only the 5 books) · **re-build the B1 Red-Wedding-upstream marriage spine** (`storming-of-the-crag ENABLES robb-weds-jeyne-westerling TRIGGERS red-wedding-conspiracy` etc. — ALREADY BUILT; DEDUP against the `--full-chain` walk) · re-mint the S163 PASS-1 Whispering-Wood/Camps/king-in-the-north spine · over-mint speculative Westerling-trap SUSPECTED_OF (much is ASOS-late / AFFC / theory — node-prose when unsure) · reach into the Fords / Duskendale (that's PASS 3 — log the remainder) · use a container tag outside the approved 5 (`wo5k` IS approved).

## Read first
- `working/enrichment-coverage-plan.md` (§ A2.5 — PASS 1 done, PASS 2/3 remain) · `working/arc-enrichment-backlog.md` (the WO5K-battles ledger row + the machine + the scope model) · `scripts/enrichment/README.md` (the parameterized mint/finalize/quotecheck schemas) · **`working/enrichment/wo5k-battles/` (the S163 PASS-1 exemplar: baseline_pull.py + baseline.md + LENS-SHARED.md + the 4 lens-*.md + candidates.json + verdicts.json + fresh-verify.md)**
- `worklog.md` S163 entry + the STATUS block · memory `project_arc_enrichment_track`, `feedback_enrichment_board_causal_lens` (the 4th causal-wiring lens), `project_containers_field_and_query_modes` (wo5k tagging), `project_narrative_arc_reification` (chain-as-arc, no umbrella parents), `project_theories_track_deferred` (the gating)
- **Carried small-fix flags (NOT this dip):** the valyrian-steel node mis-type (object.artifact→object.material, S159); the Harrenhal node-tangle alias-hygiene (S154); the House-of-Black-and-White node mis-type; the `fight-at-the-fist`/`battle-of-the-fist-of-the-first-men` dup-merge. → `working/todos.md` § Small Fixes.
