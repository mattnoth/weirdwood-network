# SESSION 149 — Next enrichment dip (the descent continues below L1: A2.1 Theon/Reek OR the cheap L2 round)

> **This is Session 149.** Stamp your worklog entry `### Session 149` in `worklog.md`.
> **Recommended model:** Sonnet 4.6 for the lens subagents + fresh-verify; Opus 4.8 for orchestration/synthesis.
> **One enrichment dip per session** (Matt S131). **D&E Pass-1 is PARKED** — stage only your own files by path; never `git add -A`.
> **`/endsession` is PRE-AUTHORIZED for an enrichment dip** (Matt S142/S144; step 5 of the machine in `working/arc-enrichment-backlog.md`). Run it yourself at the end once harvests are accounted for.

## Where we are
The reopened L1 round is **COMPLETE** (all 4 spine-only heavyweights: Dany S144, Jon S145, Bran S146, AEGON S147), and the descent has now dropped below it: **A2.2 Sansa/Vale was DONE S148** — the FIRST build+enrich dip (built the dark Vale spine: smuggle→{Alayne-identity, wedding}→death-of-lysa→{Marillion-hearing, Lords-Declarant}; fixed the longest-orphaned node; lit the war's root `murder-of-jon-arryn`; upgraded petyr SUSPECTED_OF→COMMANDS_IN on Jon-Arryn's-murder). Full ranked plan + scope cards: `working/enrichment-coverage-plan.md`.

## STEP 0 — surface the fork to Matt (don't auto-pick)
Prior dips (S140–S148) surfaced the genuine fork via AskUserQuestion at STEP 0. Do the same. The fork now:
- **A2.1 Theon / Reek (build+enrich; the other A2)** — the flagged **cross-identity Reek=Theon** case (a key graph use case). ACOK capture + sack of Winterfell (thin nodes exist) + ADWD Reek/Ramsay captivity + Jeyne-as-"Arya" rescue (mostly unbuilt). High weight, high build cost. No spine yet — build the spine first (like S119/S128/S130/S148), THEN enrich. Source: ACOK Theon chapters + ADWD Reek/"The Prince of Winterfell"/"A Ghost in Winterfell" chapters.
- **The cheap L2 interleave round (lighter; the plan's step 3)** — a granular sub-plot inside one of the 14 enriched arcs. Top picks (all in `enrichment-coverage-plan.md` Class B): **B1 Frey-pies / Grand Northern Conspiracy** (Red Wedding; all constituent nodes already built — only the GNC hub + the `ENABLES stannis-march-on-winterfell` seam are missing; wo5k→north seam; **cheapest fully-ready high-value pick — recommended L2 default**) · **B2 Robert Strong** (Oberyn POISONS Gregor + Qyburn; closes 2 dead-ends; AFFC names the manticore venom) · **B3 Sandor/gravedigger** (Brienne→Stoneheart; salience 10/10) · **B4 Ned black-cells** (the varys-visits-ned ENABLES→confession gap) · **B7 Kingslayer-quote** (cheapest; the slaying hub has no `## Quotes`).
- **Recommendation:** no strong default — Matt has alternated heavy (A2) and could now want a cheap L2 round, OR continue the build+enrich track with Theon/Reek (the last big cross-identity arc). Surface both; confirm the unit, then run the machine.

## The machine (proven 14×; see `working/arc-enrichment-backlog.md` § "The enrichment-pass machine")
1. **Baseline dump** — `graph-query.py --neighbors`/`--container`/`--full-chain` of the unit's hub + satellites + dedup checks; write `working/enrichment/<unit>/baseline.md`. Load the locked vocab from `working/wiki/data/edge-type-counts.md` (170 canonical types; **verify membership against the JSON schema — edges.jsonl uses `edge_type`/`source_slug`/`target_slug`, NOT `type`**). For build+enrich (Theon/Reek), there is NO spine yet — read the chapter files directly + define the spine skeleton, like S148.
2. **Fan out 4 fresh Sonnet lenses** PROPOSE-don't-mint + dedup-check every node/edge: (a) spine + secondary-character sub-arcs, (b) the whodunit/revelation thread + SUSPECTED_OF layer, (c) descriptive/quote/object depth, (d) **the existing-node↔existing-node causal-wiring lens** (cross-container seams — the highest-value structural fixes). Paste the canonical vocab terms (Pass/Track/Tier/lowercase-step; Tier=confidence 1–5 only) + the harvest snippet (split-the-bar WIDE-OPEN on food incl. the grim register) — subagents don't load CLAUDE.md.
3. **Synthesize + decide** (Opus). Encode as `candidates.json` (match the schema in `scripts/mint_sansa_vale_enrichment_s148.py` — `{id, source, type, target, tier, book, chapter, line, quote, verify, note}` + optional `qualifier` for Tier-1 types). **Whole-file line-check every quote** (reuse `working/enrichment/sansa-vale/verify_lines.py`, point it at your unit's candidates.json). **Qualifier rule (validator-enforced):** Tier-1 types (MANIPULATES, WARD_OF, SIBLING_OF, SPOUSE_OF, PARENT_OF, HOLDS_TITLE, VOWS_TO, SWORN_TO) REQUIRE an enum-valid `qualifier`; Tier-3 types must NOT carry one; Tier-2 optional. Mint via a `scripts/mint_<unit>_enrichment.py` (reads candidates.json, re-greps the authoritative line, backup + re-run guard + slug pre-check via `mint_arc_lib.precheck_slugs`). Node aliases = natural SPACED phrases. **Check live edge-direction conventions before emitting** (GIFTED_TO = giver→recipient; MOTIVATES target = the person).
4. **Independent Sonnet fresh-verify** the interpretive/causal edges + any borderline mints (vs LOCAL cache). Apply via a `finalize_<unit>.py` (drops/adjusts/retirements/re-points + stamp `verified_by`). Re-run the 0-drift check (`verify-edge-quotes.py --run-id <run>`). (Node-adding dips need `weirwood-refresh`.) Smoke `--full-chain`/`--container`. **A flagged-borderline edge/node that fresh-verify rejects → drop it in finalize (the propose-then-gate flow; S147 dropped a node, S148 confirmed an upgrade-and-retire).**
5. **Consume harvest** (attach load-bearing quotes inline incl. book-cite overlays onto wiki nodes; park food/description in `working/harvest-queue.md` — ~148 open + parked as of S148).
6. **Close out — run `/endsession`** (pre-authorized).

## DO NOT
run extractions without asking · un-park D&E · `git add -A` (stage your own files by path) · assert theory readings (fAegon/Blackfyre, R+L, Azor Ahai, frey-pies-confirmed-beyond-the-named-cooks, Robert-Strong-is-Gregor-as-CERTAIN, gravedigger-is-Sandor-as-CERTAIN, sweetsleep-is-murder, Reek-will-X — these stay GATED, evidence edges only) · perpetuate the stray `containers: [jon]` tag (use approved containers only: essos/wo5k/north/aegon/bran; A2 arcs like Theon/Sansa get NO container tag).

## Read first
- `working/enrichment-coverage-plan.md` (the S143 ranking + Class A/B/C scope cards — B1–B8 + A2.1/A2.2)
- `working/arc-enrichment-backlog.md` (ledger — 14 dips logged + the enrichment-pass machine + the scope model)
- `worklog.md` S148 entry + the STATUS block · memory `project_arc_enrichment_track`
- For Theon/Reek (build+enrich): there is NO spine yet — read the chapter files directly + build the spine first (like S148 Sansa/Vale), THEN enrich. Cross-identity Reek=Theon: model via SAME_AS / DISGUISED_AS / IMPERSONATES per `cross-identity-detector` semantics; the flagged use case.
