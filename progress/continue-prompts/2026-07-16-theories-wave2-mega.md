# SESSION 219 — Theories wave-2 mega-session: harvest drain → redo pool → Jon Snow clusters (+ riders)

> **This is Session 219.** Stamp your worklog entry `### Session 219` at endsession.
> **CHECK FIRST (rule 9 / the S218 collision):** grep `^### Session` in `worklog.md` — if S219 is already taken by a parallel window, take the next free number and note it.
> **Recommended model: Fable orchestrator** + Sonnet proposers + Haiku fresh-verifiers/redo-grounders (cheapest-viable per step; Opus only where reasoning depth demands).
> **Matt's framing (S218 endsession): max-throughput weekend session — lots of usage, 5-hour session cap is the only limit. Work the list top-down; each item lands independently, so stopping anywhere is safe.**
> **PARALLEL windows running:** theory-toggle build (web/-only) and D&E DE-3 (own files). **This is the ONLY graph-mutating window** — never spawn a second one. Stage-by-path at commit; do not endsession while another window is mid-endsession.

## State (end of S218)

Theories wave 1 is MINTED: 118 SUPPORTS/CONTRADICTS + 2 parallel-FORESHADOWS live, edges.jsonl **26,860**, 15 theory nodes carrying edges. Conventions RATIFIED — canonical block in `reference/architecture.md` § Evidentiary (Theory Support): tier floor 3–5 · layer rule · subject-link · whodunit question-form names · novel source classes · **fab-* citable in wave 2** · parallel-FORESHADOWS. All gates green post-mint (node quotes 3083/3083 · pytest 1458 · deno 102).

## The work (priority order — each lands independently)

1. **Harvest drain 37→0 FIRST** (endsession-step-0 debt; the standalone prompt was archived into this). The S152/S139 machine: route rows → disjoint node-dirs → parallel Sonnet attachers (node files only, orchestrator flips the queue centrally) → Haiku fresh-verify a sample → flip to done. Attach-existing-only; new-node needs park. Watch for already-present dedup (S215: ~13/44 were).
2. **Byte-fail redo pool** (~36 wave-1 rows + 41 jonsnow rows in `working/theories/regrounding-agent/*.jsonl`): one consolidated Haiku redo fleet under the anti-giveup contract (≥2 logged grep attempts per not-groundable row, famous-line calibration, event-vs-interpretation distinction). **First fold that contract into `working/theories/regrounding-agent-spec.md`** (small edit, queued since S216). The deterministic byte-check outranks every agent verdict.
3. **Wave-2 opener: Jon Snow cluster builds** from the qSy2uaJ7ecU substrate (205 beats / 8 headers on disk, ~57 byte-verified cites + whatever step 2 rescues). Full wave-1 machine per cluster: Sonnet proposer → deterministic quotecheck → 2 Haiku adversarial fresh-verifiers → Fable adjudication → scratch dry-run (`--edges` + `--nodes-root` overrides) → per-cluster ADJUDICATION record. Known shape: the Night's King parallel cluster + resurrection/AA material. **Dedup carefully vs the MINTED azor cluster** (Jon-as-AA beats: azor Z8 exists — no double-mint). **Character-enrichment beats are NOT theory material** — route to harvest/a future enrichment dip. fab-* cites now allowed (tier + `in_universe_source` carry chronicler unreliability).
4. **MINT GATE for wave-2 clusters: Matt's explicit go still required** (the S218 go covered wave 1 only). Builds land on staging; ping Matt with a tally when clusters are staged — OR he may grant the session-scoped autonomous mint protocol (dry-run + fresh pressure-test subagent with BLOCK authority stands in; memory `project_autonomous_mint_protocol`) — his call, ask once, don't assume.
5. **Riders (cheap, anytime):** (a) retype the 4 legacy tier-1 interpersonal SUPPORTS edges — luwin→osha, osha→luwin, renly-baratheon→jon-arryn, haldon→ysilla — read each edge's evidence, retype to interpersonal vocab (ALLIES_WITH/SERVES/ADVISES class; verify against the locked 163-type vocabulary); (b) coldhands substrate beats B30/B35 (S214 spend-wall gap) if a quick re-extract closes it.

## Vocabulary (paste into subagent prompts — they don't load CLAUDE.md)

Pass = big numbered corpus sweep; Track = named chunk of work; step (lowercase) = ordered piece inside a Track; Tier = confidence rating 1–5 ONLY.
Harvest rule: while in chapter text for ANY reason, drop `chapter:line / kind / note` pointers to notable off-task finds into the agent's own harvest file; POINT, don't extract.

## DO NOT

- Do NOT mint wave-2 theory nodes/edges without Matt's go OR his explicit session grant of the autonomous protocol (ask once).
- Do NOT let theory claims into tier-1/2 or into node prose stated as fact (floor is absolute — no exceptions).
- Do NOT touch the chat SHARED_RULES no-theories guardrail (the toggle build owns that, in its own window).
- Do NOT deploy (Matt-gated; note the theory layer ships to dossiers on next deploy regardless of this session).
- Do NOT re-pull ASX transcripts (16 clean on disk).
- Do NOT spawn a second graph-mutating window; do NOT run /endsession without permission.
