# Worklog Archive 040

> Archived worklog Session Log entries (oldest-first within this file). Rule #8: each archive holds 5 entries; this file opened when archive039 filled. Frozen records — not active state.

### Session 192 — CLASS-5 DUP-SLUGS APPLIED + hardened + REDEPLOYED; next 2 sessions sequenced — [Track: meta/graph] (2026-07-04)
**Detail:** `history/session-details/session-192.md`. **Model:** Fable 5 SOLO (no subagents — S191 spend-ceiling caution). **API $:** none (verification via keyless `/api/node` curls). **Graph mutation + prod deploy: Matt-approved in-session** (AskUserQuestion checkbox read = the go). Harvest queue 0 open.
**Changes made:**
- **Class-5 dup-slugs APPLIED (all 8, zero remain):** 5a sweetsleep → medical/ canonical (`concept.medical`, tier-1, foods folded, Identity bug fixed); 5b sourleaf → species/ canonical (Yoren quote/cite/aliases folded); 5c food peach → `renlys-peach` (inn keeps `peach`); 5d character → `porridge-gaoler` (name `Porridge (gaoler)`, 4 edges rewritten; bare "porridge" → the food, VERIFIED); 5e/5f mistyped events/ ASOS prologue+epilogue deleted; 5g stallion → prophecies/ canonical, type `concept.prophecy` (Amendment 1), concepts prose folded. **+1 dup neither scan caught:** wiki `artifacts/renlys-peach` (same entity) merged into foods/. Reconciliation + checkboxes annotated APPLIED; worktree `vigilant-chebyshev` landed (scan record → `curation/slug-collision-resolutions-2026-07-04.md`) + removed with branch.
- **Hardening found a live bug:** `load_nodes()` rglob was walking `graph/nodes/_conflicts/` — **7 phantom slugs (e.g. `aerys-targaryen`, `battle-of-the-green-fork`) were shipping in the live bundle**; now excluded + both builders fail loudly on dup slugs; `find_node_file`/`build_node_index` sorted walk + collision warning (deterministic first-wins).
- **5h close-out:** `weirwood refresh` clean; 8,119 timestamp-only index restamps reverted (S165 rule); 8 orphaned index files of deleted nodes removed; bundle rebuilt; **prod DEPLOYED + live-verified** (porridge=food / porridge-gaoler=character / renlys-peach merged / phantoms 404 / aerys-ii intact).
- **Test cleanup:** `cwd_is_tmp` fixed (stale `/tmp` literal; S83 moved cwd to `~/source/claude-cwd`) + mini-fixture's 5 doubled-article alias keys normalized + a Salt Debt hit case → **pytest 1447/0 FULLY GREEN** (was 1445+1 env-fail); deno 99/0; run_cases 37/0.
**Decisions:** Matt approved all reconciliation recommendations (medical/, species/, `concept.prophecy`, porridge-gaoler). Planning: Matt picked **quote minting + dossier substance** as the next Fable session (over granular dips / front-end-on-Fable) — S188 census: quotes ~73% characters vs ~3% descriptive, and theme/list now route users at empty dossiers. Sequence: **S193 quotes (Fable) → S194 receipts/node-UX (Opus)** — fill the dossiers, then make them reachable. Matt UI steer folded into S194: neighbors-card `▶` pattern unified to click-opens-dossier, styling boost ("side panel still looks weird"), fuller dossier markdown, tier-1 prose entity-linking, About-page provenance story. 8d SERVED_AT stays gated (evidence ~1 day old).
**What's next:**
- **S193 (live):** `progress/continue-prompts/2026-07-05-quote-minting-dossier-substance.md` (Fable)
- **S194 (queued behind it):** `progress/continue-prompts/2026-07-05-chat-ui-receipts-and-node-ux.md` (Opus 4.8)

### Session 193 — QUOTE MINTING census-first: 71 verbatim book quotes minted onto 61 chat-exposed nodes — [Track: graph] (2026-07-05)
**Model:** Fable 5 SOLO (no subagents — census/mint/verify all deterministic Python; quote selection stayed in-context per the handoff). **API $:** none. **Graph mutation: additive `## Quotes` only** (the track's Matt-launched purpose) + 1 in-passing cite fix; **NO new nodes, NO new edges** (quote work surfaced nothing edge-worthy under existing vocab; SERVED_AT untouched/gated); **NO deploy** (Matt-gated — the new quotes ship with the next manual deploy per DEPLOY.md). Harvest queue untouched (1 open row, below trigger).
**Changes made:**
- **Step 0 census (NEW `scripts/quote_census.py`, re-runnable):** whole-graph quote-coverage census + ranked worklist (`min(degree,150) + 60·theme + 150·POV + 75·marquee-hub` scoring) → `working/quote-census/quote-worklist.md` + `quote-census-full.jsonl`. Baseline: only **1,598/8,467 nodes (18.9%)** carried rendered quotes; **317 theme-index members quote-less** (the exact nodes the chat's `theme` tool routes users at); texts 1.2% / materials 0% / customs 5.4% coverage.
- **71 quotes minted onto 61 nodes** (NEW reusable `scripts/mint_node_quotes.py` — extracts each span **verbatim from `sources/chapters/` by start/end marker**, so transcription drift is impossible; plan = `working/quote-census/s193-mint-plan.json`): the meals&feasts slice (lemon-cake, moon-tea, stallion-heart, shade-of-the-evening, poisoned-locusts, wine-of-courage, fermented-milk, honeyfingers, bedding, cupbearer, harvest-feast + the ACOK harvest-feast catalogue lighting aurochs/goose/herring/cod/salmon/lobster/whitefish/beet in one verified line), medical (greyscale, bloody-flux, tears-of-lys, manticore-venom), materials (dragonglass, dragonbone, tokar), **16 songs/texts** (bear-and-the-maiden-fair, rat-cook, dornishman's-wife, last-of-the-giants, hands-of-gold, white-book, jade-compendium, lives-of-four-kings, seven-pointed-star, jenny's-song, seasons-of-my-love + the Dareon-repertoire line lighting 8 song stubs at once), marquee/POV (war-of-the-five-kings, pate-novice, sunspear, water-gardens, daznaks-pit, naggas-hill, quiet-isle, inn-at-the-crossroads, windblown, mormonts-raven, the-night-that-ended, brave-danny-flint, beef/deer/hardbread/mutton). Wiki-quoted-only nodes (greyscale, bedding, lemon-cake) got the Tier-1 navigable book-cite upgrade beside their un-rendered wiki text.
- **Cite verification (NEW `scripts/verify_node_quotes.py` — parses with the engine's own `parse_quotes`): 71/71 minted quotes verify at their cited line.** The first-ever graph-wide node-quote check also found REAL pre-existing drift: **185 of 803 book-cited `## Quotes` entries fail** (2 pure line-drift, 183 not-found-verbatim — the splice/paraphrase class; a further 90 wrap-quote-only rows are fine and the verifier now tolerates them) → `working/quote-census/node-quote-drift-report.md`; queued as a follow-up beside the 58-edge-quote re-grounding backlog item. Fixed in passing: greyscale body cite `adwd-tyrion-05:127`→`:125`.
- **Close-out:** `weirwood refresh` (the SEARCH INDEX now carries the new quotes — engine `search "hands of gold are always cold"` returns the minted asos-tyrion-04:95 cite at #1); 8,110 timestamp-only index restamps reverted per the S165 rule (18 real `_summary.json` deltas kept — S192 porridge/renlys-peach fallout surfacing); chat bundle rebuilt (8,467 nodes / **1,659 with quotes**, was 1,598); golden case `list-customs-has-quotes` updated 2→5 (bedding/cupbearer/harvest-feast — data legitimately changed). **Suites: pytest 1446/0 · run_cases 31/0 · deno 99/0** (deno via npm `deno-bin` 2.2.7 + `--no-lock` in this container — the repo lockfile is v5, fine at Netlify).
**Decisions:** none needing Matt in-session. `duck` deliberately left unminted (wiki stub; bare "duck" collides with Duck/Rolly Duckfield — noted for a future cross-identity look).
**Post-session (Matt's go at close): deploy APPROVED but BLOCKED from the remote container** — no Netlify credentials there (`netlify status` not logged in; no `NETLIFY_AUTH_TOKEN`; `.netlify/state.json` is local-only). The quotes ship with S194's deploy or a manual `npx netlify deploy --prod --build` from Matt's linked machine (DEPLOY.md). /endsession run WITH Matt's permission; harvest queue 0 open.
**What's next:**
- **S194 (LIVE next):** `progress/continue-prompts/2026-07-05-chat-ui-receipts-and-node-ux.md` (Opus 4.8) — receipts/node-UX; **its deploy ships the S193 quotes** (see the prompt's S193 addendum).
- **S195 (Matt-sequenced at S193 close, after S194):** `progress/continue-prompts/2026-07-05-quote-regrounding-cleanup.md` (Fable solo, Python-first) — re-ground the 185 drifted node quotes + the 58 legacy edge-quote mismatches; designed cheap (Python proposes, Fable bulk-dispositions, no per-row agents).
- Future quote slices: re-run `scripts/quote_census.py`, pick from the top of `working/quote-census/quote-worklist.md`.

### Session 194 — Receipts rail for ALL 8 tools + node-UX boost + fuller dossiers + About in Matt's voice — DEPLOYED (ships the S193 quotes) — [Track: meta] (2026-07-05)
**Detail:** `history/session-details/session-194.md`. **Model:** Fable 5 (handoff recommended Opus 4.8; capability step up, flagged at start). **API $:** ~pennies (3 Sonnet dev-turn smoke tests). **Graph mutation: none** (web/-only track + build scripts). **Deploy: Matt's go** — LIVE, prod-smoked. Harvest queue 0 open.
**Session start:** the S193 handoff's claims weren't on main — **S193 ran remotely on branch `claude/quote-minting-census-k5lonc`** (cloud container, no Netlify creds = its deploy block); fast-forwarded main onto it before starting (rule-#9 verify paid off).
**Changes made:**
- **`web/public/app.js`**: `ingestReceipt` cases for `search_quotes`/`list_nodes`/`theme` (the S191 known gap CLOSED) → passages-consulted card (quote hits as bookQuotes with cites; identity blurbs plain), theme/list chip cards with counts + paging; **neighbors card unified** onto chip + always-visible evidence (the `▶ <details>` pattern retired); ONE shared `nodeChip` atom — every node name in every card opens the dossier; **prose entity-linking tier 1** (client-only post-stream pass over the per-turn `entities` map; first occurrence per entity; quote blocks untouched; single-word names case-sensitive); `bestName`/`prettyTitle` for name-less chips; rail header "How this was answered".
- **`web/public/app.css`**: dotted-accent-underline clickable affordance everywhere (chips, spine names, prose links); rail scrolls within viewport on desktop / static on mobile (cascade-order bug caught live: the mobile block sits before the base rule — override moved after); neighbors rows + card bound (62vh); dead CSS of retired patterns removed; About font unified to one size.
- **Dossier widened:** `graph/query/build/build_node_assets.py` now ships reader-facing body `sections` (Origins/Appearances & Description/Culture/Aftermath/…; Notes + overlay-ledger headings excluded) → assets 4,395→**6,693**; `node.ts` passes `sections` through (fail-soft); app.js renders them between quotes and arc. **`netlify.toml` build now regenerates the assets** (clean-checkout deploys stay complete).
- **`lib/agent.ts` grounding fix (live-caught):** list/theme `items` rows now count as grounding — a survey turn no longer mislabels "no scene here" (+ regression test; deno 99→**100**).
- **About page rewritten in Matt's dictated voice** (2 rounds): wiki credit, theories-NOT-integrated / no post-ADWD-inference disclaimer, "Provenance is the point" AI-ism killed, citation-gate paragraph corrected to actual behavior (marks answer unverified, does NOT identify the quote), trace-to-line claim dropped (markdown line numbers unverified vs printed books), small **mattnoth.com** contact line; `<title>` = "The Weirwood Network — ASOIAF Knowledge Graph Interface" + header descriptor.
**Verification:** deno **100/0** + 3 typechecks + fmt clean on touched files; pytest **116/0**; 3 live dev turns exercised every card type + dossier-from-prose-link ("Bowl of brown": quotes + Origins + 4-book arc) + mobile/desktop. **Prod deploy** (`npx netlify deploy --prod --build`, 4,951 files): title live, lemon-cake serves its 2 S193 quotes + Culture + arc — **the S193 quotes are LIVE**.
**Decisions:** tier-2 prose-linking (model-emitted `[[n|…]]` markers) presented, NOT built — judge tier 1 live first. Matt's "python scripts" → "graph tools" wording swap flagged (live tools are TS).
**What's next:**
- **S195 (LIVE next, Matt-sequenced):** `progress/continue-prompts/2026-07-05-quote-regrounding-cleanup.md` (Fable solo, Python-first, cheap) — re-ground 185 drifted node quotes + 58 legacy edge-quote mismatches → 0 FAIL. Residue for its vein (noted in the prompt addendum): internal-provenance lines inside arc/section prose; double-source quote attributions (cosmetic).
- Natural follow-up after: alpha-tester notes round (live logs accumulating); 8d SERVED_AT trigger check.
