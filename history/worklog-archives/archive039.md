# Worklog Archive 039

> Holds up to 5 sessions, starting at Session 187; archived from `worklog.md` per CLAUDE.md rule #8 (5-entry Session Log cap).
> Newest first within the live worklog; oldest-archived-first here.

### Session 187 — Chat-UI: cite-verification false-positive root-caused to build parser + fixed + deployed — [Track: meta] (2026-07-03)
**Detail:** `history/session-details/session-187.md`. **Model:** Opus 4.8. **API $:** ~$0.03 (3 fan-out subagents; no live chat turns). Harvest queue 0.
**Changes made (all committed + pushed + DEPLOYED to prod):**
- **Root-cause fix — `scripts/build-chat-export.py` `parse_quotes()`:** two defects made ~158 quotes ship with `cite: null` and the real `chapter:line` buried in `.text` (a book-cite-overlay byproduct). (1) An attribution line sitting INSIDE the `>` block (`> — sources/…`) was swallowed into `.text`; the following-line branch never fired. (2) `CITE_RE` required backticks, so bare cites never matched. Rewrote to detect the `—` attribution line whether inside or after the block, and made `CITE_RE` backtick-optional. Rebuilt bundle → structured md-cites **572→731**; buried-in-text **158→4**.
- **Gate backstop — `web/netlify/edge-functions/lib/agent.ts` `harvestResult()`:** also scan a quote's `.text` for `CITE_RE` and admit matches to `validCites`. Covers the 4 malformed-source residuals (cite embedded mid-quote-body) + any future overlay additions before a rebuild. Scoped to quote text only (not attributions/whole payload) per the adversarial subagent's safest-variant recommendation.
- **`agent_test.ts`:** new regression test (buried cite in `cite:null` quote enters allowlist). Full suite **41 passed / 0 failed**; `deno check` + `check:fn` clean.
- **`web/.env`** (gitignored, NOT committed): added `NETLIFY_SITE_ID` + `NETLIFY_AUTH_TOKEN` (from `.netlify/state.json` + Netlify CLI login) so `read-logs.ts` works locally.
**Decisions:** Diagnosis via 3 parallel fan-out subagents (gate-logic / data-hygiene / adversarial). My FIRST hypothesis (path-normalization) was WRONG — production logs disproved it (all 3 flagged tokens were full, correct, graph-present paths). Data-hygiene + adversarial independently converged on "fix the build parser (root cause), keep the gate change narrow." Implemented both: parser is the real fix, the `.text` scan is a scoped backstop. Banner was ~25% of turns (3/12 logged), clustered on Red Wedding/Catelyn/Stoneheart (Matt's test topics) so it felt universal.
**What's next:** No live continue prompt — the fix is deployed and healthy. Backlog (not mid-flight): (1) optionally hand-fix the 4 malformed-source quotes (mance-rayder, nymeria-direwolf, victarion-admits-euron ×2) so their cite is a clean attribution line; (2) `cost-report.ts` aggregate rollup (S186 carry-over, Matt-gated); (3) About-panel copy still says "voice of an old record-keeper" (now the non-default voice) — neutralize (S186 carry-over); (4) Step C deterministic wiki-date backfill of undated causal events (S184/S185 carry-over, gated).

### Session 188 — Graph traversal & query-surface analysis: two new top-level docs — [Track: meta/graph] (2026-07-04)
**Detail:** `history/session-details/session-188.md` (full narrative: the 7 findings + the two-apertures framing). **Model:** Opus 4.8. **API $:** 1 live loremaster turn on the local `weirwood-live` server (~cents) + read-only usage-log read; **NO graph writes; harvest queue 0.**
**Changes made (docs only — no graph/code mutation):**
- **NEW `GRAPH-QUERY-ROADMAP.md`** (top level, living) — forward-looking traversal/query design: §0 LIVE-EVIDENCE exhibit + "two apertures / essential-vs-incidental shrink" framing; §1 census; §1b resolve explainer; §1c bundle-ships-slim; §1d traversal grammar + the Python-vs-chat **query-mode divergence** (chat exposes ~5 of ~11 modes; `--container`/`--expand-beats`/`--path`/`--event-participants` never ported); gaps **G1–G10**; directions **D1–D8**; §4 script-org/interview framing; §8 prior-art.
- **NEW `GRAPH-STATE.md`** (top level, living, companion) — §1 census; §2 enrichment story; **§2a descriptive-layer census** (food 96% islanded, materials 91%, customs 96%; quotes 73% on characters / 3% on descriptive; containers 100% events); **§2b harvest-mechanism root cause** (descriptive material captured to harvest queue but never wired); §3 chat-UI live status; §4 parked tracks; **§4b backlog salvage**.
- Read live usage logs (17 turns, ~$1.30 total spend, one real cite-fabrication self-correction) via `web/scripts/read-logs.ts` using the netlify-CLI token.
- Reproduced the failure live: "describe meals" → ~13 fuzzy/no-match resolves → `loop-bound-hit`, no answer (every gap at once).
**Decisions:** The event-bias is a **retrieval-surface** problem, not a model preference — two drivers (G3 walk_chain MANDATORY framing + G10 fuzzy-fallback favoring long-slug events), rooted in G2 alias holes. The descriptive layer is **orphaned, not just under-queried** (96% of food nodes edge-less) because harvest never had a wiring step. Most of the roadmap is **un-defer, not greenfield** (content-search D1 was a KNOWN S172 fast-follow; fuzzy-resolve is a parked S177 track). Framing: keep the **essential** shrink (edge runtime / spend cap / latency / grounding), fix the **incidental** shrink (content search, ported modes, projection, aliases). No graph or code changed — deliverable is the analysis + two docs.
**What's next (UPGRADED per Matt, S188 endsession):** → `progress/continue-prompts/2026-07-04-graph-query-docs-and-script-org.md` (`/continue graph-query-docs-and-script-org`, **Opus 4.8 at MAX reasoning** — "a super high reasoning model for designing"). A **master design session**: analyze the plans + produce a thorough, mechanical-agent-executable design document for the whole query/traversal layer — NOT restricted to the two MDs (create whatever doc structure serves best, e.g. `reference/design/`). Must cover: the vision/where-we're-going, content-search (D1), the traversal **script library** organization + Python/TS unification (§4), the descriptive-layer wiring (orphan ring), resolver/discoverability (G2/G10), slim-projection (G9), chronology/event-ordering (Step C remaining — GRAPH-STATE §5), the charter'd-but-unbuilt + missing pieces (braid D7, trigger table D8, TWOIAF, prophecy, MCP), **options/trade-offs explicitly**, and novel ideas we haven't considered. Design ONLY (no graph mutation, no build). Deliverable = documentation/plan with each item spec'd + sequenced so mechanical agents know exactly what to do.
### Session 189 — Query-layer MASTER DESIGN: contract-first plan + Fable-orchestrated execution — [Track: meta/graph] (2026-07-04)
**Detail:** `history/session-details/session-189.md`. **Model:** Fable 5 (handoff asked Opus 4.8 max; ran on Fable as orchestrator). **API $:** 6 read-only Sonnet survey subagents; no live chat turns; **NO graph writes; harvest queue 0.**
**Changes made (docs only):**
- **NEW `working/query-layer/design.md`** — master design for the **query-layer track** (plan of record; `GRAPH-QUERY-ROADMAP.md` demoted to diagnosis/history). **Contract-first reframe:** the repo has FOUR accidental query surfaces (graph-query.py / event_alias_resolver.py / the 48 MB never-read `graph/index/` / web lib) with no shared contract — so build the contract first (`graph/query/` package + `spec/operations.md` + golden parity cases run by pytest AND deno; CLI + chat as thin adapters), then land capabilities inside it. New gaps **G11–G19**; G1–G10 verdicts; decisions D-A…D-I with options; step cards 0–9 + gated hygiene side-step H.
- **Verified live:** victim-phrase aliases ABSENT from `web/data/alias-map.json` ("robb stark's death" → MISS — G19, the S96 fix never reached the product); `graph/index/foods/*` EMPTY for harvest-minted nodes (`acorn-paste` 0 appearances — index unrouted AND stale, G13); census: 27.8% orphan nodes, artifacts edge-sinks-only, 5 YAML-broken nodes, 91 dangling edges, 28 dup rows; G10 mechanism = no candidate-length penalty in fuzzy scoring.
- Pointer banners + appendix entries in `GRAPH-QUERY-ROADMAP.md` / `GRAPH-STATE.md`; `working/todos.md` Track 7 repointed.
**Decisions (Matt, S189b/S189c reviews in-session):** home = **`graph/query/`** (not top-level; the mutation gate covers DATA dirs only); **pytest traversal suite DEFERRED TO LAST** (no half-finished states — golden spec cases carry drift protection from session A); **project-first, not interview-first**; execution = **three Fable-ORCHESTRATED sessions** (A engine / B retrieval / C reach+close-out) with Sonnet builders + Haiku verifiers; open forks (read_passage-to-chat / SERVED_AT timing / MCP / shim retirement) → session-C **advisory-board fan-out**, Matt overrules on read; **S189c:** loremaster persona reframed in step 5c around a researcher/thought-experimenter user (voice only, SHARED_RULES safety block untouched, A/B eval row; Matt reviews before deploy).
**What's next:** → `progress/continue-prompts/2026-07-04-query-layer-orchestrated-build.md` (`/continue query-layer-orchestrated-build`, **Fable as ORCHESTRATOR; Sonnet builds / Haiku verifies**) — execute design.md bundles **A→B→C in order, as far as context allows**, stop ONLY at a clean bundle boundary, mint the remainder prompt there. Gated outside the bundles: 8d SERVED_AT Haiku pass, hygiene-H apply, any prod deploy, chronology Step C.

_(Session 188 archived to `history/worklog-archives/archive039.md` at S193; Session 187 archived there at S192; Session 186 archived to `history/worklog-archives/archive038.md` at S191; Session 185 archived there at S190; Session 184 at S189.)_
### Session 190 — QUERY-LAYER ORCHESTRATED BUILD: sessions A+B+C executed in one sitting — [Track: graph/meta] (2026-07-04)
**Detail:** `history/session-details/session-190.md`. **Model:** Fable as ORCHESTRATOR + Sonnet builder/verifier subagents (the design's Haiku-verify slots went to deterministic diff scripts — cheaper than any model). **API $:** none (live-model eval columns stay GATED/empty). **Commits:** `adae223e00`/`0693e4dbd1` (A), `8fc7f01152`/`5545421b84`/`cce3645c64` (B), `f5b90a57d0`/`f8c30f2886` (C) + the docs close-out commit. **No graph/nodes|edges|index writes anywhere.**
- **Session A — the engine (steps 0+1+7) ✅:** `graph/query/` shipped — ONE package `weirwood_query/` (model/load/normalize/resolve/traverse/report/cli; 3 frontmatter parsers→1; engine reads edges.jsonl only, G16), builders (`build_alias_table.py` identical-mod-timestamp, kept the regex parser for the 5 G18 files — documented; `build_chat_bundle.py` byte-identical), familyTree TS→Python (independent fresh-verify caught a missing `isValidSlug` trust boundary + a cli nodes-map wiring bug — both fixed, 6/6 golden cases), compat shims at all 3 old `scripts/` paths (every test-consumed symbol re-exported; deprecation on stderr only), `weirwood query` subcommand front door, refresh-script swap, `spec/operations.md` v1 + golden cases + the deno drift alarm. **Exit gate:** 36/36 CLI cases byte-identical via BOTH paths; pytest 1322/3-pre-existing; deno green. **Braid rode along:** fork-hubs/join-hubs/braid (showcase: the hairnet = sole shared ancestor of tywin-assassination × death-of-joffrey); convergence-map writes stay Matt-gated.
- **Session B — retrieval (steps 3+4+5) ✅:** eval harness Q1–Q21 + frozen baseline (10/20 exact-resolve; Q11 meals = the loop-bound row); telemetry miner over the live Blobs logs (found TurnLog never persisted resolve OUTCOMES → fixed; real feast/wedding alias gaps at n=20 turns); resolver hardening (G19 victim-phrases→bundle; 15,235 deterministic variants, 217 collisions LOGGED → `working/query-layer/variant-collisions-s190.md`; G10 length de-bias both runtimes — flipped "House Targaryen" + bare "Tywin") → **16/20 exact, zero regressions**; **content search** (BM25, 13,714 docs, 2.4 MB compact bundle index, byte-identical scores Python↔TS) + `list` + `corpus-search` (CLI-only per board); chat wiring: `search_quotes` tool, routing DECISION TABLE (walk_chain = only hard-mandatory; 2-failed-resolves→search rule), **researcher persona DRAFT** (board-authored per Matt's live steer — in repo, NOT deployed), SHARED_RULES safety text now test-pinned.
- **Session C — reach + close-out (6+8a–c+H+9+boards) ✅:** exit-gate evals SETTLED the 5d fork (search ≠ browse) → `list_nodes`+`theme` chat tools → **Q11 ∞→2 tool calls** (Q16 4→2; zero regressions; known gap: 3/5 target dishes beyond the 25-row theme page cap). Theme index (5 seed themes) + `theme` op both runtimes; **mention-index repair PREVIEW-ONLY** (181/344 chapters would change; `acorn-paste` blocked by a Pass-1 format gap [2,464 un-bulleted lines], NOT aliasing; apply = Matt-gated → `working/query-layer/mention-index-repair-report.md`); food-grep seeder (4,865 pointers, POINT-don't-extract); **step 6 parity table CLOSED** (containers→bundle +3.3KB; path/participants/expand-beats TS ports; NO new chat tools for them — evals-driven, overrule point open; **G9 by measurement** = 4,401 per-node narrative-arc static assets, 9.2 MB gitignored, `/api/node` fail-soft fetch + dossier render); side-step H **hygiene PROPOSAL** (2 census claims did NOT reproduce [28-dup-edges=0; dangling=67 not 91]; **7** cross-category slug collisions incl. true different-entity collisions `peach`/`porridge`); step 9 doc-truth sweep (G17) + `graph/query/README.md` + `--explain`.
- **BOARDS (all 5 in `working/query-layer/boards/`; Matt overrules on read):** persona = researcher/wiki-reader frame (drafted + wired per Matt's steer); read_passage = **CLI-only** (flip needs a verbatim-text quota); SERVED_AT = **wait for 8a–c evidence** (settle metric = Q21 "served at the Purple Wedding", now 2 calls); MCP = **parked** (unanimous; trigger = a real external client); shims = **keep until the final pytest session** migrates the 89 shim-loaded tests.
- **MATT RESOLVED IN-SESSION ("approve all your recs"):** hygiene classes 1–4 APPLIED-as-recommended (class 1 = the 5 YAML alias repairs, all parse; classes 2–4 = deliberate no-action); **class 5 (7 cross-category dup slugs) = Matt's separately-spawned dup-slug session, own worktree `vigilant-chebyshev` — do not touch from this tree**; **mention-index repair APPLIED** (`graph/index/chapters/` ← preview; 821 entity rollups gained mention data, `crow-cage` 0→2; 7,657 timestamp-only restamps reverted per S165 rule); **persona RULINGS:** flat register = deliberate BASE persona ("more later"), connections tool-grounded only, **NO A/B eval — deploy and judge by feel** (memory `chat-persona-decisions`; design.md §8). Post-apply audit agent: **CLEAN** (`working/query-layer/s190-audit.md`). Commit `0408819e20`.
- **STILL FOR MATT:** (1) pre-deploy read of persona+routing in `web/netlify/edge-functions/lib/agent.ts` → manual deploy per DEPLOY.md (the live site runs the OLD prompt/tools until then); (2) the theme-page-cap gap (3/5 Q11 dishes beyond 25 rows — someday). **NEXT = the FINAL Track session: Fable-ORCHESTRATED (Matt's steer at endsession: "go hard on the next section... It can use cheaper agents as needed while orchestration, and advisory boards")** — step 2 traversal suite + shim Tier B + Track close-out, boards for forks, Matt overrules on read (his two D-G sign-offs = board-proposals-he-reads) → `progress/continue-prompts/2026-07-04-query-layer-final-pytest-suite.md`. Gated riders (NOT scheduled): 8d SERVED_AT pass (trigger = Q21 evidence), prod deploy. Harvest queue 0 open at endsession.

### Session 191 — QUERY-LAYER TRACK COMPLETE + the new chat build DEPLOYED LIVE — [Track: graph/meta] (2026-07-04)
**Detail:** `history/session-details/session-191.md`. **Model:** Fable as ORCHESTRATOR + Sonnet subagents (3-lens fixture board, Tier-A doc sweep, fixture builder, suite builder) — the monthly spend ceiling killed the suite-builder at its REPORT step (files had landed; agent later resumed from transcript and delivered the report: zero fixture-data edits, zero pinned engine bugs); orchestrator finished Tier B + close-out solo. **API $:** ~$0.03 (one live post-deploy verification turn, Opus). **NO graph/nodes|edges|index writes.** Harvest queue 0 open at endsession.
- **Step 2 (D-G) LANDED — the Track's final step.** 3-lens board (coverage/story/adversarial) → the **"Salt Debt" mini-fixture** (`graph/query/tests/fixtures/mini/`, 35 nodes/39 edges, invented House Quorwyn saga; Matt checkpoint #1 approved as proposed) — causal diamond, 2-hop ENABLES break, beat-hub straddling the two role-edge sets (participants ≠ expand-beats, PINNED), 5-gen deep-spine family (remarriage/marry-in/single-parent), 2 disjoint container bags, "the Eel King" ambiguous alias, cycle/self-loop/dangling traps, 3 quote-bearing orphan foods. **Traversal suite: 116 tests** at `graph/query/tests/` (fixture smoke/resolve/traverse/braid/spec-cases-as-pytest/corpus-smoke; `corpus` marker auto-deselects without the graph). Op-semantics table frozen (checkpoint #2) → `working/query-layer/boards/op-semantics-s191.md`.
- **Housekeeping:** repo `pytest.ini` (testpaths `tests graph/query/tests` + corpus marker); orphaned `scripts/stage4-formalize-edges-test.py` → `tests/test_stage4_formalize_edges.py` (`_assert` now raises — was silently un-pytest-able); vocab assertions **167→170** (trail: +SUSPECTED_OF S116, +WITNESS_IN S117, +HONORED_AT S134). **`find_node_file` path-escape guard** (narrow `/`+`..` block — 247 legacy timestamp-suffixed slugs keep resolving; full-regex guard would have broken them).
- **Shim retirement Tier B (Matt-confirmed):** the 89 shim-loaded tests migrated to package imports (test-count parity proven vs HEAD: 39+9+41), `backfill-epithet-aliases.py` repointed, `weirwood graph`/`resolve` zsh aliases now forward to the engine (permanent short aliases), Tier-A doc sweep (12 live-usage swaps, 4 reference docs), scripts/README + web/README shim language dropped → **the 3 shims DELETED** (`graph-query.py`, `event_alias_resolver.py`, `build-chat-export.py`).
- **Drift-alarm re-proof — and it caught a real hole:** broke `resolve-red-wedding-exact` → deno FAILED but both Python runners PASSED (run_cases' resolve handler never compared `candidates[0].slug`) → fixed → **all three runners fail on the break**, all green restored.
- **Matt priority-steer folded in (logging + deploy):** (1) per-call tool **outcome logging extended to EVERY chat tool** (`agent.ts outcomeFor()` — matchType/topSlug/resultCount + first-10 `slugs` for search/list/theme; TurnLog reviews now show what ran AND what came back); (2) **netlify.toml build command now regenerates ALL FIVE bundle files** (was core-bundle-only — search-index/theme-index shipped whatever sat on local disk, deploy-by-accident) + repointed off the deleted shim; DEPLOY.md updated. **Known gap, deferred by Matt:** the receipts rail (`app.js ingestReceipt`) has no case for search_quotes/list_nodes/theme — those receipts silently drop; side-window design work queued behind the deploy.
- **Suites at close:** pytest **1445 passed** / 1 skip / 1 env-fail (`cwd_is_tmp`, pre-existing); run_cases 37/0/1; deno **99/0/1**; `deno check` + `check:fn` clean.
- **→ THE QUERY-LAYER TRACK IS COMPLETE** (design.md status header updated; roadmap/state banners updated). **AND THE DEPLOY SHIPPED (Matt's go, in-session):** pushed + `npx netlify deploy --prod --build` → **LIVE** (researcher persona, search_quotes/list_nodes/theme, routing table, full outcome logging). Verified with a live turn on the old killer question — "What meals are served in the books?" → **3 tool calls (`theme → read_node → read_node`), $0.0324, 0 unverified cites**; raw blob confirms `claude-opus-4-8` + the new per-call outcome slices (10-slug theme inventory). Post-report amendment: braid(node, own-descendant) is NOT all-empty (shared_ancestors = the common upstream cone) — op-semantics table precision-fixed (`cfebd60969`). Remaining riders, Matt-gated: 8d SERVED_AT (trigger = Q21 evidence, now accumulating in live logs), hygiene class-5 dup-slugs (his `vigilant-chebyshev` worktree session, NOT landed — reconciliation file still untracked; live bundle serves the alphabetically-later copy for colliding slugs), the search/list/theme receipts-rail rendering (Matt-deferred). **What's next:** → `progress/continue-prompts/2026-07-05-cleanup-and-planning.md` (S192: short Fable solo cleanup + next-steps planning — Matt-proposed).
