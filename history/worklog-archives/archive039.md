# Worklog Archive 039

> Holds up to 5 sessions, starting at Session 187; archived from `worklog.md` per CLAUDE.md rule #8 (5-entry Session Log cap).
> Newest first within the live worklog; oldest-archived-first here.

### Session 187 — Chat-UI: cite-verification false-positive root-caused to build parser + fixed + deployed — [Track: meta] (2026-07-03)
**Detail:** `history/session-details/session-187.md`. **Model:** Opus 4.8. **API $:** ~$0.03 (3 fan-out subagents; no live chat turns). Harvest queue 0.
**Changes made (all committed + pushed + DEPLOYED to prod):**
- **Root-cause fix — `scripts/build-chat-export.py` `parse_quotes()`:** two defects made ~158 quotes ship with `cite: null` and the real `chapter:line` buried in `.text` (a book-cite-overlay byproduct). (1) An attribution line sitting INSIDE the `>` block (`> — sources/…`) was swallowed into `.text`; the following-line branch never fired. (2) `CITE_RE` required backticks, so bare cites never matched. Rewrote to detect the `—` attribution line whether inside or after the block, and made `CITE_RE` backtick-optional. Rebuilt bundle → structured md-cites **572→731**; buried-in-text **158→4**.
- **Gate backstop — `web/netlify/edge-functions/lib/agent.ts` `harvestResult()`:** also scan a quote's `.text` for `CITE_RE` and admit matches to `validCites`. Covers the 4 malformed-source residuals (cite embedded mid-quote-body) + any future overlay additions before a rebuild. Scoped to quote text only (not attributions/whole payload) per the adversarial subagent's safest-variant recommendation.
- **`agent_test.ts`:** new regression test (buried cite in `cite:null` quote enters allowlist). Full suite **41 passed / 0 failed**; `deno check` + `check:fn` clean.
- **`web/.env`** (gitignored, NOT committed): added `NETLIFY_SITE_ID` + `NETLIFY_AUTH_TOKEN` (from `.netlify/state.json` + Netlify CLI login) so `read-logs.ts` works locally.
**Decisions:** Diagnosis via 3 parallel fan-out subagents (gate-logic / data-hygiene / adversarial). My FIRST hypothesis (path-normalization) was WRONG — production logs disproved it (all 3 flagged tokens were full, correct, graph-present paths). Data-hygiene + adversarial independently converged on "fix the build parser (root cause), keep the gate change narrow." Implemented both: parser is the real fix, the `.text` scan is a scoped backstop. Banner was ~25% of turns (3/12 logged), clustered on Red Wedding/Catelyn/Stoneheart (Matt's test topics) so it felt universal.
**What's next:** No live continue prompt — the fix is deployed and healthy. Backlog (not mid-flight): (1) optionally hand-fix the 4 malformed-source quotes (mance-rayder, nymeria-direwolf, victarion-admits-euron ×2) so their cite is a clean attribution line; (2) `cost-report.ts` aggregate rollup (S186 carry-over, Matt-gated); (3) About-panel copy still says "voice of an old record-keeper" (now the non-default voice) — neutralize (S186 carry-over); (4) Step C deterministic wiki-date backfill of undated causal events (S184/S185 carry-over, gated).

### Session 188 — Graph traversal & query-surface analysis: two new top-level docs — [Track: meta/graph] (2026-07-04)
**Detail:** `history/session-details/session-188.md` (full narrative: the 7 findings + the two-apertures framing). **Model:** Opus 4.8. **API $:** 1 live loremaster turn on the local `weirwood-live` server (~cents) + read-only usage-log read; **NO graph writes; harvest queue 0.**
**Changes made (docs only — no graph/code mutation):**
- **NEW `GRAPH-QUERY-ROADMAP.md`** (top level, living) — forward-looking traversal/query design: §0 LIVE-EVIDENCE exhibit + "two apertures / essential-vs-incidental shrink" framing; §1 census; §1b resolve explainer; §1c bundle-ships-slim; §1d traversal grammar + the Python-vs-chat **query-mode divergence** (chat exposes ~5 of ~11 modes; `--container`/`--expand-beats`/`--path`/`--event-participants` never ported); gaps **G1–G10**; directions **D1–D8**; §4 script-org/interview framing; §8 prior-art.
- **NEW `GRAPH-STATE.md`** (top level, living, companion) — §1 census; §2 enrichment story; **§2a descriptive-layer census** (food 96% islanded, materials 91%, customs 96%; quotes 73% on characters / 3% on descriptive; containers 100% events); **§2b harvest-mechanism root cause** (descriptive material captured to harvest queue but never wired); §3 chat-UI live status; §4 parked tracks; **§4b backlog salvage**.
- Read live usage logs (17 turns, ~$1.30 total spend, one real cite-fabrication self-correction) via `web/scripts/read-logs.ts` using the netlify-CLI token.
- Reproduced the failure live: "describe meals" → ~13 fuzzy/no-match resolves → `loop-bound-hit`, no answer (every gap at once).
**Decisions:** The event-bias is a **retrieval-surface** problem, not a model preference — two drivers (G3 walk_chain MANDATORY framing + G10 fuzzy-fallback favoring long-slug events), rooted in G2 alias holes. The descriptive layer is **orphaned, not just under-queried** (96% of food nodes edge-less) because harvest never had a wiring step. Most of the roadmap is **un-defer, not greenfield** (content-search D1 was a KNOWN S172 fast-follow; fuzzy-resolve is a parked S177 track). Framing: keep the **essential** shrink (edge runtime / spend cap / latency / grounding), fix the **incidental** shrink (content search, ported modes, projection, aliases). No graph or code changed — deliverable is the analysis + two docs.
**What's next (UPGRADED per Matt, S188 endsession):** → `progress/continue-prompts/2026-07-04-graph-query-docs-and-script-org.md` (`/continue graph-query-docs-and-script-org`, **Opus 4.8 at MAX reasoning** — "a super high reasoning model for designing"). A **master design session**: analyze the plans + produce a thorough, mechanical-agent-executable design document for the whole query/traversal layer — NOT restricted to the two MDs (create whatever doc structure serves best, e.g. `reference/design/`). Must cover: the vision/where-we're-going, content-search (D1), the traversal **script library** organization + Python/TS unification (§4), the descriptive-layer wiring (orphan ring), resolver/discoverability (G2/G10), slim-projection (G9), chronology/event-ordering (Step C remaining — GRAPH-STATE §5), the charter'd-but-unbuilt + missing pieces (braid D7, trigger table D8, TWOIAF, prophecy, MCP), **options/trade-offs explicitly**, and novel ideas we haven't considered. Design ONLY (no graph mutation, no build). Deliverable = documentation/plan with each item spec'd + sequenced so mechanical agents know exactly what to do.
### Session 189 — Query-layer MASTER DESIGN: contract-first plan + Fable-orchestrated execution — [Track: meta/graph] (2026-07-04)
**Detail:** `history/session-details/session-189.md`. **Model:** Fable 5 (handoff asked Opus 4.8 max; ran on Fable as orchestrator). **API $:** 6 read-only Sonnet survey subagents; no live chat turns; **NO graph writes; harvest queue 0.**
**Changes made (docs only):**
- **NEW `working/query-layer/design.md`** — master design for the **query-layer track** (plan of record; `GRAPH-QUERY-ROADMAP.md` demoted to diagnosis/history). **Contract-first reframe:** the repo has FOUR accidental query surfaces (graph-query.py / event_alias_resolver.py / the 48 MB never-read `graph/index/` / web lib) with no shared contract — so build the contract first (`graph/query/` package + `spec/operations.md` + golden parity cases run by pytest AND deno; CLI + chat as thin adapters), then land capabilities inside it. New gaps **G11–G19**; G1–G10 verdicts; decisions D-A…D-I with options; step cards 0–9 + gated hygiene side-step H.
- **Verified live:** victim-phrase aliases ABSENT from `web/data/alias-map.json` ("robb stark's death" → MISS — G19, the S96 fix never reached the product); `graph/index/foods/*` EMPTY for harvest-minted nodes (`acorn-paste` 0 appearances — index unrouted AND stale, G13); census: 27.8% orphan nodes, artifacts edge-sinks-only, 5 YAML-broken nodes, 91 dangling edges, 28 dup rows; G10 mechanism = no candidate-length penalty in fuzzy scoring.
- Pointer banners + appendix entries in `GRAPH-QUERY-ROADMAP.md` / `GRAPH-STATE.md`; `working/todos.md` Track 7 repointed.
**Decisions (Matt, S189b/S189c reviews in-session):** home = **`graph/query/`** (not top-level; the mutation gate covers DATA dirs only); **pytest traversal suite DEFERRED TO LAST** (no half-finished states — golden spec cases carry drift protection from session A); **project-first, not interview-first**; execution = **three Fable-ORCHESTRATED sessions** (A engine / B retrieval / C reach+close-out) with Sonnet builders + Haiku verifiers; open forks (read_passage-to-chat / SERVED_AT timing / MCP / shim retirement) → session-C **advisory-board fan-out**, Matt overrules on read; **S189c:** loremaster persona reframed in step 5c around a researcher/thought-experimenter user (voice only, SHARED_RULES safety block untouched, A/B eval row; Matt reviews before deploy).
**What's next:** → `progress/continue-prompts/2026-07-04-query-layer-orchestrated-build.md` (`/continue query-layer-orchestrated-build`, **Fable as ORCHESTRATOR; Sonnet builds / Haiku verifies**) — execute design.md bundles **A→B→C in order, as far as context allows**, stop ONLY at a clean bundle boundary, mint the remainder prompt there. Gated outside the bundles: 8d SERVED_AT Haiku pass, hygiene-H apply, any prod deploy, chronology Step C.

_(Session 188 archived to `history/worklog-archives/archive039.md` at S193; Session 187 archived there at S192; Session 186 archived to `history/worklog-archives/archive038.md` at S191; Session 185 archived there at S190; Session 184 at S189.)_
