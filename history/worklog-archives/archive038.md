# Worklog Archive 038

> Archived worklog Session Log entries (oldest-first within this file). Each archive file holds up to 5 entries per CLAUDE.md rule #8. This file opened at S187 endsession when S182 rolled off the live worklog; S183 added at S188 endsession.

### Session 182 — Chat-UI: Targaryen family-tree design finish + deep main-line spine — [Track: meta] (2026-07-01)
**Detail:** `history/session-details/session-182.md` (the deep-spine algorithm design + collision-review gate). **Model:** Opus 4.8 (handoff recommended Sonnet 4.6). **API $:** a few live Sonnet 4.6 family-tree verification turns (cents); no graph-data writes; harvest queue 0.
**Changes made (all `web/`, UNCOMMITTED before this endsession):**
- **Collision-review gate passed** (the S181 handoff's first step): fresh `audit-parent-conflation.py` → 1 node >2 parents (`joffrey-baratheon`, intentional); Dany/Rhaella/Aegon I show clean 2-parents; `web/data/` matches committed `edges.jsonl` (23099==23099). Graph verified before any UI build.
- **Prominence-tier highlighting wired into the live render** (`app.js buildTreeSVG` + `app.css`): ported the prototyped `web/dev/family-tree-fixture.html` tiers — `FT_P_MAJOR=40`/`FT_P_NOTABLE=12` + `ftTierOf`, `tier-major`/`tier-notable`/`tier-minor` classes, accent prominence-dot on majors, richer `<title>`. Tier rules placed before `.hub` so the queried root always wins. Expand modal inherits it. `index.html` `?v=` bumped 178→182a.
- **NEW: deep main-line spine in `familyTree` (`graph.ts`)** — the substantive work. `familyTree(aegon-i)` never reached the book era (TWO limits: `GENEALOGY_DOWN_DEFAULT=4` AND the 64-breadth-cap spent on the wide early dynasty; connectivity was always there — a 12-hop chain to Dany). Added an ADDITIVE prominence-anchored path-threading phase: full-depth `PARENT_OF` BFS (`DEEP_SPINE_MAX_DEPTH=14`) → rank descendants beyond the breadth horizon by `degree+4·quoteCount` → thread the top `DEEP_SPINE_ANCHORS=24` anchors' paths back to the tree. Cap raised 64→96 (breadth added first, so nothing displayed is dropped). Gated on `generationsDown >= GENEALOGY_DOWN_DEFAULT` (an explicit shallow window opts out).
- **`graph_test.ts`**: fixed the stale "size cap holds" test (was RED at session start — S181 cleanup shrank Aegon below the old 64-cap, unrelated to my edits; re-pointed at the 96-cap) + NEW deep-spine regression test (reaches Dany/Rhaegar/Egg/Blackfyre; deep gen stays narrow; shallow-window opt-out). **36/36 deno tests pass.**
**Verified live:** Aegon I tree now reaches gen 13 — Dany, Rhaegar, Aerys II, Egg, Maester Aemon, and the full Blackfyre split (Daemon I/II Blackfyre, Bloodraven, Bittersteel), plus the Robert's-Rebellion Baratheon branch. Shape is wide-near-root / narrow-through-middle / wide-at-book-generation — Matt's exact ask. All previously-displayed nodes preserved (additive). No console errors.
**Decisions:** deep-spine uses prominence as the "main line" signal (rejected pure best-first — gets stuck breadth-first among prominent Dance siblings); additive-only per Matt's "don't remove displayed nodes"; cap 96. Fuzzy-resolve (item 3) TESTED not fixed — `"Aegon the Conqueror"` resolves exact (common path fine); `"...Conqueror Targaryen"` scatters + `"targaryen dynasty"` 3-way-ties, but both are resolver/alias-layer fixes needing out-of-sample validation or a graph-data alias mutation (out of scope for a frontend session) — deferred to their own track.
**What's next:** **PUBLISH the chat-UI alpha live** (Matt's directive). Netlify already wired (`netlify.toml`). → `progress/continue-prompts/2026-07-01-chat-ui-deploy-netlify.md` (`/continue chat-ui-deploy-netlify`, **Sonnet 4.6**). Also queued: update root `README.md` (still graph-only, no chat-UI/deploy) + the deferred fuzzy-resolve resolver track.

### Session 183 — Chat-UI alpha DEPLOYED LIVE on Netlify — [Track: meta] (2026-07-01)
**Detail:** `history/session-details/session-183.md` (the edge-runtime debugging narrative: 5 deploy-breakers, filesystem-free bundle). **Model:** Sonnet 4.6 (as handoff recommended) — but actually ran on Opus 4.8. **API $:** 2 live Sonnet 4.6 prod smoke queries (~$0.10 total, under the $5/day cap); no graph writes; harvest queue 0.
**Changes made (all committed `9ba0d204b4`):**
- **DEPLOYED: https://weirwood-network.netlify.app** — site `weirwood-network` created under team `mattnoth`, linked, `netlify deploy --build --prod` from the working tree. Prod env vars: `ANTHROPIC_API_KEY` (secret, production context, read from gitignored `web/.env` — never echoed/committed) + `WEIRWOOD_MODEL=claude-sonnet-4-6` (else `chat.ts:32` defaults to Opus).
- **`netlify.toml`**: dropped the stale `&& cp web/data/featured-tywin.json …` tail (S175 removed that file; nothing produces/consumes it — would have failed the build). Command is now just the python bundle build.
- **`web/netlify/edge-functions/chat.ts`**: `npm:@anthropic-ai/sdk` + `npm:@netlify/blobs` → **esm.sh URLs** (Netlify's edge bundler can't resolve `npm:` specifiers — experimental/broken); import path `./agent.ts` → `./lib/agent.ts`.
- **Moved `agent.ts` + `agent_test.ts` → `web/netlify/edge-functions/lib/`** (git mv; fixed relative import depth) so only `chat` + `node` package as functions (Netlify treats only top-level dir files as routes).
- **`web/src/lib/data.ts`**: rewrote `loadGraphData` to **inline the 8.7 MB bundle via JSON module imports** (`with { type: "json" }`) instead of `Deno.readTextFile` — Netlify Edge has NO filesystem ("Reading or writing files with Edge Functions is not supported yet" → every request 502'd). Dropped unused `dataDir` param + `DEFAULT_DATA_DIR` export (`mod.ts`).
- **`web/deno.json`**: `nodeModulesDir: "auto"` so `node:assert` type-check resolves `@types/node` (deno 2.9 byonm mode; **36/36 tests green** with full type-check).
- **READMEs**: root `README.md` → new "Live Chat Demo" section (URL + stack + local-run); `web/README.md` → deploy row LIVE + filesystem-free data-bundle note.
**Decisions:** deploy path = **Netlify CLI** (cold-subagent-recommended over UI: draft-first surfaces build/edge errors locally before prod; auto-deploy on git-push deferred — `netlify link` makes it a trivial add later). Verified each fix on a keyless **draft deploy** (`/api/node` needs no key) before spending on prod. Custom GoDaddy domain deferred (Matt will point one later; default `*.netlify.app` for now). **CLI-deploy runs from the working tree, so the changes MUST be committed before any future git-based auto-deploy** — committed this endsession.
**Verified on prod:** landing 200 · `/api/node` real dossiers · `/api/chat` streaming w/ receipts + cite-check + `end_turn` · cost math confirms Sonnet 4.6 ($0.0531, not Opus's ~$0.088) · family tree reaches book era (Aegon→…→Aerys→Rhaegar→Daenerys) · **no key leak** (grepped served HTML/app.js/SSE for `sk-ant` + the key value — 0 hits).
**What's next:** Matt has **alpha-tester notes** to feed in next session → `progress/continue-prompts/2026-07-01-chat-ui-alpha-tester-notes.md` (`/continue chat-ui-alpha-tester-notes`, **Sonnet 4.6**). Also still open: the deferred **fuzzy-resolve** track (`"Aegon the Conqueror Targaryen"` scatters, `"targaryen dynasty"` 3-way-ties — resolver/alias-layer fix needing out-of-sample validation).
