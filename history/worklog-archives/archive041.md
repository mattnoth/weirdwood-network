# Worklog Archive 041

> Sessions archived from worklog.md per CLAUDE.md rule #8 (5-entry window). Newest first.

### Session 197 — WIKI-PROSE DISAMBIGUATION COMPOSER built + APPLIED (475 nodes); redirect-trap class found; F&B overlap de-risked — [Track: graph] (2026-07-06)
**Detail:** worklog entry sufficient (execution session). **Model:** Sonnet orchestrator + subagents (script-builder ×3, general-purpose audit ×1, Haiku thin-polish ×7). **API $:** modest (7 Haiku batches ~cheap + 2 `claude -p` smoke calls). **Graph mutation: Matt-approved in-session** ("Apply all now"). **No deploy** (rides the next manual `netlify deploy --prod --build`; new Identity/frontmatter is data-side). Harvest queue 0 open.
**Built — `scripts/wiki-prose-identity-composer.py`** (deterministic-first, one script, subcommands `--build-pack` / `--compose --dry-run` / `--enrich-thin` / `--apply`):
- **`--build-pack`** → the machine-readable products F&B's reconciler consumes: `working/wiki/data/same-name-clusters.json` (159 clusters, per-member discriminators from `edges.jsonl` kinship + `page-categories.jsonl` life-years — NOT edges, per S196 C4), `disambig-node-blocklist.json` (**236 traps**, `trap_kind`-tagged), `redirect-node-map.json`.
- **NEW trap class found via the thin-rate:** wiki **`Redirect`**-page nodes (bare `aenys-targaryen`→`aenys-i-targaryen`, `robert-baratheon`→`robert-i-baratheon`, `baelish`→`house-baelish`, …). 197 graph-wide; the design/S196 only knew the 39 `Disambiguation pages` traps. Both classes must run — redirects aren't in the disambiguation category. Target resolved deterministically from cached `redirectMsg` HTML (3 false-positives w/ real content, e.g. `timett-father`, flagged not corrupted).
- **`--enrich-thin`** bakes `claude -p --model haiku` (cwd=/tmp, cached, gated) INTO the script per `reference_llm_pass_via_claude_p` (Matt's ask). Cache `thin-sentences.jsonl` = 85 rows / **82 enriched** (3 legit contentless); seeded from a one-off 7-agent Haiku fan-out so the apply cost nothing new; live path smoke-proven.
**Applied — 477 nodes total, 0 errors:** cluster set 475 (336 Identity swaps + 104 inserts + 26 disambiguation hubs `disambiguation_hub: true` + 9 redirect stamps `redirect_to:` + 82 thin sentences w/ `(wiki:…)` cites; 15 skipped = real non-boilerplate Identity preserved). **+ 37 F&B marquee SINGLETONS** (`--compose-singletons`: Rhaenyra/Daemon/Alicent/Otto/Criston/Aemond/Helaena/Bloodraven=`brynden-rivers`/Cregan/Mysaria/the Velaryon kids/…): **Matt caught the cluster-only scope skipped the unique-named principals** — "isn't she the main character of F&B?" (Rhaenyra was a singleton → boilerplate). New subcommand filters to singleton + boilerplate/absent, composes the same deterministic line minus the cross-pointer; 28 swap + 9 insert, 14 skipped (11 already-composed-via-cluster + 3 slug-mismatch redundant). All 4 shapes spot-verified — rich nodes (`aegon-i-targaryen`, `rhaenyra-targaryen` ~90 lines) lost zero prose; `weirwood refresh` clean (twice).
**Targaryen-overlap audit (`working/node-enrichment-wiki-prose/targaryen-overlap-audit.md`):** verdict **SAFE for UPDATE-not-CREATE, 0 true duplicates** — the wiki already covers essentially the whole F&B cast (Matt's "probably most" confirmed; only 2 trivial coverage gaps). Thin-rate was mostly a mirage: only 2 Targaryen thin members (both redirects); the other 83 are already slug-disambiguated non-F&B legends/dragons/smallfolk.
**HAND-OFF NOTE for F&B (verified live):** `weirwood query resolve` STILL returns a confident `HIT` on trap/redirect nodes — the de-risk lives in the DATA (blocklist + redirect-map + `disambiguation_hub`/`redirect_to` frontmatter), which the **F&B reconciler MUST consult**; the raw resolver doesn't honor them yet (wiring that in = F&B step 1). This IS the S196-R1 mitigation substrate.
**Two PARALLEL-SAFE tracks teed up (Matt wants both prompted; they touch different code):**
- **S198 — F&B build + smoke (graph track, sequential within itself):** `progress/continue-prompts/2026-07-06-fire-and-blood-build-smoke.md` — splitter → QA-gate freeze → reconciler (consumes this session's pack/blocklist/redirect-map) → `fab_merge_node.py` → two-stage smoke. Spec: `working/fire-and-blood/fire-and-blood-enrichment-design.md` v2. **F&B READINESS CHECK (S197): the epub landed on disk TODAY** (`sources/raw/fire-and-blood.epub`, ~264K words — backlog #306 "not on disk" now STALE), **but the pipeline is entirely unbuilt** (`sources/chapters/fab/` doesn't exist; no splitter/reconciler/merge-writer/prompt/harness). So **F&B extraction can NOT start "in parallel" — it is the OUTPUT of the S198 build**; the first extraction is the smoke, Matt-fired after the build (R6: the `claude -p`/`longrun.sh` harness has never completed a unit — smoke is its first proof).
- **Boilerplate-strip cleanup (independent, deterministic):** `progress/continue-prompts/2026-07-06-strip-wiki-boilerplate-identity.md` — remove "… from the AWOIAF wiki" from all node Identity prose (6,739 nodes, backlog HIGH). ~~Safe to run alongside S198.~~ **[CORRECTED 2026-07-07: NOT parallel-safe — a fresh-eyes review + reconcile analysis proved strip-before-F&B silently defeats F&B's Identity swap (stripped lines stop matching `fab_merge_node.py`'s `BOILERPLATE_RE`; ~1,108-node real overlap). Matt PARKED the strip behind F&B's FULL apply cycle (not just its first apply — F&B is incremental, ~23 packs). Strip-after-F&B is provably safe. Analysis of record: `working/node-enrichment-wiki-prose/reconcile-strip-vs-fab-RECOMMENDATION.md` (recommends the optional B-patch to F&B shape-detection if decoupling is ever wanted). Prompt archived; before un-parking, also fix the strip's 46-node thin-misclassification nit (`RICH_SECTIONS_RE` misses `## Heraldry & Sigil`/`## Culture`/`## Aftermath`).]**

### Session 198 — FIRE & BLOOD ingestion pipeline BUILT end-to-end + first-ever smoke run (PASS-WITH-CONCERNS) — [Track: graph] (2026-07-06)
**Detail:** `history/session-details/session-198.md`
**Model:** Opus 4.8 orchestrator + 5× script-builder subagents + 1 fresh general-purpose evaluator. **No graph/edges.jsonl mutation all session** (pipeline is dry-run only; node writes gated).
**Changes made:**
- **`working/fire-and-blood/unit-map.json`** — authoritative section→file→unit map (the epub `toc.ncx` is unreliable: broken `#page_N` anchors, no navPoint for files 011/014). 23 sections; NN = HTML file number, 2-digit (matches the design's worked examples; NCX playOrder ≠ file number).
- **`scripts/fire-and-blood-splitter.py`** → `sources/chapters/fab/` — 23 sections → **39 unit files**; QA gate **0 warnings** (0 PDF-path leaks / 0 word-miss / 0 boundary dupes); `working/fire-and-blood/ocr-scan.md` (OCR-hot: 025 Lineages 275, 012 111, 014 108 — matches design prediction). **NOT frozen** (Matt gate).
- **`scripts/fab-build-candidate-packs.py`** → 23 packs, 1,526 nodes mapped from `Rfab` anchors; 015-pack acceptance PASS; 4 typo-noise unmapped sections.
- **`working/fire-and-blood/prompts/fab-enrichment-v1.md`** — node-first schema; descends from D&E v4; NEW: node prose, mandatory Disambiguator column, OCR dual-rule, Gyldayn `in_universe_source`/`disputed` dispute model.
- **`scripts/fab-reconcile-candidates.py`** — the R1 crux (resolver + blocklist + clusters + redirect-map + discriminator scoring; quote pre-validation w/ row-level quarantine; `disputed⇒tier-2`; emits candidates.json + merge-plan.json + contradictions + run-summary + 3 review files; CREATE stamps era/occurred). 6/6 unit assertions pass.
- **`scripts/fab_merge_node.py`** — NEW UPDATE writer (mint is skip-if-exists). 3 node shapes + both (c) sub-cases; rhaenyra wiki prose byte-identical; idempotency + hard-error-on-not-found (R3). Smoke-passed on copies.
- **`working/fire-and-blood/fire-and-blood-extraction.py`** — long-run worker (mirrors D&E); 39-unit queue + roster hints + safety guard; registered `PLANNED` in `weirwood-run.sh`.
- **Staged (Matt-gated, lands WITH first apply):** `working/fire-and-blood/architecture-batch-s198.md` (`fab` code / `book-fab` evidence_kind / `in_universe_source`+`disputed` edge fields / `disputed⇒tier≤2` invariant / 3-line mint passthrough patch).
**Stage-1 smoke (Matt-fired from iTerm — FIRST-ever completed run of the `claude -p`+`longrun.sh` extraction pattern):** unit `fab-aegons-conquest-03`, 40KB/350-line output, all 5 headers. Reconciler on real output: 148 entities → 39 matched / 108 review / 36 created; 58 edges; **89.6% quotes located** (14 quarantined); disputed_rate 3.4%; **0 trap edges / 0 trap merges** (R1 verified live — `weirwood query resolve "Aegon Targaryen"` STILL confidently HITs the trap, reconciler routes it to review).
**Fresh-eval verdict = PASS-WITH-CONCERNS** (`working/fire-and-blood/smoke/v1/EVAL-stage1.md`): extraction half STRONG (ship the prompt as-is; disputed-tagging correctly calibrated; 14 quarantines = 100% line-wrap false-negatives, 0% fabrication). Reconciler has a CREATE-routing bug class that would corrupt the graph IF applied (nothing minted): (1) **CREATE guard too weak** — 9 house dupes (`blackwoods`→`house-blackwood`) + fuzzy-`candidates` hits minted instead of routed to review (design §5.1 rule 3); (2) **composite/collective cells minted as nodes** (`;`-joined Event cells, `the-targaryen-fleet`); (3) **quote locator misses paragraph-spanning quotes** (fix must mirror `mint_enrichment.py:authoritative_line` lockstep); (4) contradictions report polluted by the dupe bug; (5) new-node type defaults to `character.human` not the extractor's Type-guess. All deterministic reconciler fixes.
**Decisions:** NN = 2-digit HTML file number (cite_refs bake it in); packs bridge by slug not padded-NN. Splitter delegates cutting to `unit-map.json`, not the NCX. Worker `PLANNED` not `READY` (D&E precedent — a working/ scaffold shouldn't be READY). Extraction runs on Matt's `claude -p` subscription (not a metered API key); 401s from inside Claude Code so Matt fires from iTerm.
**What's next:** **S199 (LIVE):** `progress/continue-prompts/2026-07-07-fire-and-blood-reconciler-fix-stage2.md` (**Sonnet**, Python-first) — fix the 5 reconciler issues → re-run reconcile on `fab-aegons-conquest-03` (CREATEs 36→~14, quarantine→~0) → then Stage-2 smoke `fab-heirs-of-the-dragon-15-p01` (Matt-fired, the ambiguity/dispute quality test) → tune §5.1 auto-accept on ≥2 fresh units → Matt's apply-go (lands the architecture batch). Worklog archive: Session 193 → `archive040.md`.

### Session 199 — F&B reconciler fixed + tuned + ALL §7 pre-bulk gates GREEN (dispute-gate FAIL→quarantine→PASS) — [Track: graph] (2026-07-07)
**Detail:** `history/session-details/session-199.md`. **Model:** Fable 5 (orchestrator) + Opus `claude -p` extraction (Matt-fired, 4 units) + 5 fresh verifier subagents. **Graph mutation: NONE** (all `--smoke`; apply gated). Harvest queue 3 open (no drain needed).
**Changes made:**
- `scripts/fab-reconcile-candidates.py` — (earlier window) Stage-1 fixes 1–6; (this window) quote repair (trailing-punct + enclosing-quote-pair canonicalization → 98.9%/94.1%/95.0%/97.4% located across the 4 units), `norm()` OCR `''`-collapse, type-agreement gate (caught `Lorath`→jaqen AND `Sea Snake`→corlys live wrong-matches), `run_id`/cites keep `-pMM` (R3 silent-drop fix), §5.1 tuned (parent/spouse base-name + punct-tolerant scorer; subset-margin rule; exact-1.0 + positive-type-agree rule), review-candidate probes, CREATE type normalization + year-aware `era:`, merge-plan hygiene + `matched.jsonl`, edge-type vocab guard (0 drift found), `identity_line` derivation (58/58 entries), **dispute-proximity quarantine** (`dispute-review.jsonl`; strong-terms lexicon ±1 paragraph-line; romance-class hold).
- `scripts/mint_enrichment.py` — lockstep mirrors only (norm `''`-collapse, blank-start window skip).
- `scripts/test-fab-reconcile.py` — 23 → **63 tests, all green**.
- `reference/architecture.md` — 4 new event subtypes (appointment/exile/birth/investiture) + `birth-of-<canonical-char-slug>` convention (Matt-adopted).
- Evals under `working/fire-and-blood/smoke/v1/`: EVAL-stage2 (PASS-WITH-CONCERNS), EVAL-stage2-reconciler-tuning (record of everything), EVAL-oos-accept-validation (**0 wrong / 66 rows**), EVAL-oos-create-verify + fresh-verify-creates (semantic dupe caught in **3 of 4 units** — step now permanent), EVAL-dispute-axis-audit v1 (**FAIL 26.9% inflation**) + v2 (**PASS 0.0%/0.0%, quarantine recall 100%**).
- (Earlier window, inherited & committed here): strip-boilerplate track PARKED behind F&B full apply (`working/node-enrichment-wiki-prose/reconcile-strip-vs-fab-RECOMMENDATION.md`), its continue prompts archived; strip scripts staged for later.
**Decisions:** Matt adopted the 4 novel event subtypes into architecture.md (rule 6 sync done) and chose **WIRE** for `identity_line` (reconciler derives book-grounded Identity lines; merge writer still swaps only boilerplate — strip-track parking premise preserved). Dispute-gate philosophy: repair/quarantine deterministically in the reconciler, never touch the passing extraction prompt; weak hedges deliberately excluded from the lexicon (measured: 100% of false holds, 0 catches). Extraction prompt v1 untouched all session.
**What's next:** Matt's **apply-go** → apply the 4 smoke units per §8 (checkpoint→mint→merge→finalize→refresh; dispositions: 5 folds, 2 renames, CAUSES edge, jaqen alias strip; mint patch + architecture batch land with it; adjudicate 19 dispute-held rows) → then bulk extraction of the remaining 35 units (Matt-fired, ~5h). Continue: `progress/continue-prompts/2026-07-07-fire-and-blood-apply-go.md`.

### Session 200 — F&B FIRST REAL APPLY: 4 smoke units minted into the graph (+264 book-fab edges, +18 nodes, 76 node merges); bulk block handed to Matt — [Track: graph] (2026-07-07)
**Detail:** `history/session-details/session-200.md` (adjudication-override reasoning; disposition judgment calls; 3 small incidents). **Model:** Fable 5 (orchestrator) + 1 Sonnet adjudication subagent. **Graph mutation: Matt's explicit in-session apply-go** (AskUserQuestion; craghas = mint-with-PART_OF, Matt's call). Per-unit git checkpoints throughout (`5d5c9960` pre-apply → one commit per unit). Harvest queue **8 open** (adjudicator added ~5; under the 30 bar, no drain).
**Changes made:**
- **Step 0 re-reconcile (non-smoke, fresh `working/fire-and-blood/apply/` dirs):** routing diffed vs the smoke runs — 0 mismatches / 0 regressions; auto-accepts matched the S199-validated sets EXACTLY (23 discriminator + 188 exact-1.0; zero unvalidated accepts). Dispute-held 19→21 (2 benign new p01 holds: House Velaryon/Corlys artifacts that smoke had review-held upstream).
- **Schema batch LANDED** (Active Decision above): `fab` book code, `Rfab*` cite-anchor note, `evidence_kind: book-fab`, edge fields `in_universe_source`/`disputed`, invariant `disputed ⇒ tier ≤ 2`, 3-line mint passthrough.
- **Disposition surgery (deterministic, pre-mint):** 4 folds (`aegons-second-coronation`→`aegons-coronations` [edge retyped VICTIM_IN→HONORED_AT, lyanna precedent] · `baelon-avenges-aemon-on-tarth`→`myrish-bloodbath` · `accession-melee-at-maidenpool`→`tourney-for-king-viserys-is-accession` [AGENT_IN→PARTICIPATES_IN] · `death-of-alyn-stokeworth`→`harren-the-reds-rebellion` [recast as `harren-the-red KILLS alyn-stokeworth` + VICTIM_IN]); 2 renames (`birth-of-aegon-ii-targaryen`, `birth-of-maegor-i-targaryen`); 5 wired ADD edges (vaegon CAUSES great-council; craghas PART_OF war-for-the-stepstones; field-of-fire CAUSES loren-capture + torrhen-kneeling; **parley PART_OF `aegons-conquest` — CORRECTED from the S199 note's `conquest-of-dorne`, which is Daeron I's 161 AC war**); jaqen-hghar junk `"Lorath"` alias stripped.
- **Dispute adjudication (21 held rows, fresh Sonnet + Fable primary-text verification):** 15 clear / 4 tag-as-disputed / 2 event-residue. **2 orchestrator OVERRIDES** of subagent clears after reading p02 113–125: the exile decree exists only in the Eustace/Mushroom tale zone (Runciter has a mere departure) → `viserys BANISHES daemon` + the Daemon scandal-prose bullet tagged tier-2/`gyldayn-synthesis`. Pattern confirmed: euphemistic "favorite" LOVER_OF rows = disputed/unattributed; flat "paramour" = tier-1. 12 edges fed back; 6 prose bullets re-inserted; 1 redundancy skip. Record: `working/fire-and-blood/apply/ADJUDICATION-s200.md`.
- **Apply per §8, per-unit:** checkpoint → mint → merge, ×4. **+264 book-fab edges** (edges.jsonl 23,099→**23,363**; tier-1 247 / tier-2 17 / disputed 14, all with `in_universe_source`; invariant 0 violations), **+18 CREATE nodes**, **76 merge-plan entries applied (0 skipped / 0 not-found ×4)**, **18 boilerplate Identity lines upgraded** (exactly the S199 prediction). Multi-part run_id idempotency proven live (Rhaenyra carries distinct p01/p02 markers; mint re-run correctly aborts). `weirwood refresh` ✅.
- **Verify:** spot resolves (rhaenyra / craghas→craghas-drahar / natural-phrase CREATE lookup) + daemon neighbors show the new edges w/ quotes; tests **63/63** after un-colliding one fixture (`Capture of Loren Lannister` is now a real node — test moved to Sharra Arryn; the S192 cwd-class lesson again: tests that assert live graph state).
- **Bulk prep:** 4 completed enrichments installed at canonical `extractions/fire-and-blood/` paths so `--resume` skips them; telemetry median 480s/$1.80/unit → bulk ≈ $63, ~10.5h at `LONGRUN_SLEEP_BETWEEN=600`.
**Known residue (non-blocking):** 161 reconcile-review rows across the 4 units (post-run triage per design §10.10); thin Events-table CREATE bodies (`nghai`, craghas "beheaded singlehanded") — prose depth comes later; the reconciler's patient-role→VICTIM_IN mapping is semantically loose on non-harm events (births, kneelings) — candidate deterministic re-type sweep post-bulk (filter `evidence_kind: book-fab` + event type); 2 residued events (Lonmouth-death beat-node candidate); `vaegon` vs `vaegon-targaryen` dupe pair + `great-council-of-101-ac` mistyped `event.battle`/empty body (S199 flags, untouched).
**What's next:** **Matt fires the 35-unit bulk run from iTerm** (block in the session handoff; observability = `run-summary.jsonl` per §7a: located% ≥90, `disputed_rate ≈ 0` on a Dance unit = prompt failure, `created` spike = resolver failure, `dispute_held` spike = OK on gossip units). Then → `progress/continue-prompts/2026-07-07-fab-bulk-reconcile-apply.md`.

### Session 201 — F&B bulk reconcile-apply: advisory-board pre-mint sweep (P1–P7) + batches 1–3 of 7 applied (all pre-Dance, sec 04–14) — [Track: graph] (2026-07-09)
**Detail:** `history/session-details/session-201.md`. **Model:** Opus 4.8 orchestrator + Sonnet subagents (3-seat advisory board, 3 fresh-verify, 4 script-builders). **Graph mutation: YES — Matt's explicit apply-go (AskUserQuestion).** Per-batch git checkpoints. Harvest: main queue **8 open** (no drain); F&B harvest jsonl **315 rows** staged for the batch-7 close-out (NOT drained now — apply incomplete).
**Changes made:**
- **Bulk extraction COMPLETE** (35 new units, v1, manifest `88670d51`). The `--resume` re-extracted the 4 S200 smoke units (manifest-keyed, not file-keyed) → restored to HEAD (applied versions preserved).
- **Advisory board** (Matt-requested) at the mint boundary: mint-safety=READY-WITH-CONDITIONS, vocab=all-3-LEAVE-HELD, process-skeptic=PROCEED-WITH-GUARDRAILS (found VICTIM_IN-on-non-harm + junk-node bugs the 4-unit smoke missed). Matt: run all 6 fixes + non-harm patient→PARTICIPATES_IN.
- **Pre-mint sweep (deterministic, all tested):** P1 `fab-reconcile` VICTIM_IN harm-gate + P2 junk char.human screen + P3 disputed⇒in_universe_source (153 tests); P4 `mint_enrichment` edge same-quote dedup (10 tests); NEW `scripts/fab-dispute-preclassify.py` (P5, 320→257 auto/63 human, 68 tests); NEW `scripts/fab-semantic-gate.py` (P6, 33 tests); NEW `scripts/fab-dispute-inject.py` (P7, 38 tests); NEW `scripts/fab-s200-residue-fix.py`. Re-reconciled all 35.
- **S200 residue cleaned** (my "0 residue" query was buggy — real schema is `source_slug`/`target_slug`; P6 agent's 3 verifications were right): **11 book-fab VICTIM_IN→PARTICIPATES_IN + 1 dup dropped** (edges.jsonl 23,363→…). Global auto-inject: 66 edges + 155 prose, 0 leftover.
- **Batches 1–3 applied** (15 units, sec 04–14): B1 `3348a66b26` (FOLD qhorin→volmark-conspiracy; 4 PART_OF, 2 dropped), B2 `344af9979d` (2 disambiguation RENAME; +cleaned 18 orphan junk .node.md files), B3 `57a825d1fe` (3 FOLD + 4 RENAME; 1 dispute cleared). Each: mint+merge 0-skipped/0-not-found → `weirwood refresh` → semantic gate **PASS** → 153 tests green.
- **Learnings:** mint quote-validates EVERY edge → PART_OF needs a locatable verbatim quote; re-reconcile leaves orphan node files (cleaned); "eustace"/"mushroom" false-positive class (chronicler name == character given name). Reusable `scratchpad/fab-apply-surgery.py` (FOLD+RENAME auto-locate) built for batches 4–7.
**Decisions:** Non-harm patient edges → PARTICIPATES_IN (Matt). Vocab holds DISINHERIT/REGENT/BOUNTY all LEAVE_HELD (REGENT/BOUNTY parked as future ADD_TYPE candidates only if main-corpus demand appears — no mid-bulk edge-type add). PART_OF ADD_PARENT only when a child has a locatable quote, else drop. Wrapped at the pre-Dance boundary (batches 4–7 hold all 62 dispute-adjudication rows → fresh context + primary-text verify).
**What's next:** **Batch 4 (Dance opens)** → `progress/continue-prompts/2026-07-07-fab-bulk-reconcile-apply.md` (updated; **resume at Batch 4**). Batches 4–7 = 20 Dance units + 62 needs-read/romance adjudications (per-unit fresh subagent verdicts + orchestrator primary-text verify; `fab-dispute-inject.py --verdicts`). Then close-out (Lineages diff, review-bucket triage, F&B harvest drain of 315 rows, deferred-events triage, un-park strip track). State: `working/fire-and-blood/apply/BATCH-PROGRESS-s201.md`.
