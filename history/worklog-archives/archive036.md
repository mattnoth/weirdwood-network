# Worklog Archive 036

> Archived Session Log entries from `worklog.md` (CLAUDE.md rule #8: worklog keeps the 5 most recent; older entries move here, 5 per archive file). Newest-first within the file.

---

### Session 174 — Chat-UI alpha BUILD: front-end chunk (the LAST chunk) — [Track: meta] (2026-06-29)
**Detail:** `history/session-details/session-174.md` (the design-review narrative: one-renderer architecture, the 3 Matt decisions, band/spine). **Model:** Opus 4.8 (handoff recommended Sonnet 4.6 — static HTML/CSS/JS; ran on the session's Opus). **No graph writes; NO API calls / no API $** — all validation was dry (static-server preview render + a SIMULATED SSE stream via `fetch` override). No harvest rows (web-tooling).
**Context:** 4th and final build chunk per the now-archived `progress/continue-prompts/2026-06-29-chat-ui-frontend.md`. Build the browser UI against the S173 SSE contract: chat page + SSE client, typed-edge receipts panel, the static Tywin exchange, failure-mode UX. Locked decisions (Edge/Deno · `claude-opus-4-8` · Option A · curated-MVP · private→Netlify) NOT reopened.
**Changes made:**
- **`web/public/` front-end (vanilla, token-driven, no framework):** `index.html` (header + out-of-character "How it works" framing per design §5, chat thread, sticky receipts panel, composer + example chips, footer, subtle weirwood backdrop), `app.css` (every colour/font/size reads `theme/tokens.css` — zero hard-coded values), `app.js` (ES module: SSE client + the shared renderers + failure UX).
- **ONE renderer drives BOTH featured + live** (the key architecture call, confirmed with Matt): shared atoms `linkRow` (typed-edge `source —[EDGE]→ target`, click → evidence quote + cite + tier, edge-type-coloured), `nodeCard`, `chainCard`, `neighborsCard`. `normLink` adapts the featured-json shape (`source_name`/`evidence_ref`) and the live tool shape (`source`/`ref`) to one contract. So the static showpiece IS the design fixture for every live answer — the look was designed and validated with zero API spend.
- **SSE client:** `fetch` POST `/api/chat` → manual `text/event-stream` frame parser (matches `chat.ts` `event:…\ndata:…\n\n`). `token`→append to the streaming bubble; `receipt`→render a card from `result` (never the prose); `status`→failure-mode UX; `error`→plain line; `cite-check`→console; `done`→close. Stateless multi-turn: client resends a bounded history (`MAX_HISTORY` 8).
- **Failure-mode UX (design §9):** `STATUS_UX` maps every state to distinct copy — `no-grounding` (positive reframe), `loop-bound-hit`, `unverified-cites`, `api-error`, `cost-cap-tripped` ("answered enough for today"), plus a plain `error` line. Empty-turn receipts panel reframes ("no graph grounding for this one").
- **DESIGN REVIEW (in-session, Matt watching the live preview):** Matt loves "the chain walked" — DON'T gut it. Two changes landed: (a) **deduped the chain nodes** — each node renders ONCE joined by its typed edge (was repeating the shared node target=next-source down the column); (b) built **TWO toggleable layouts** — **Band** (full-width horizontal flow, node-click → quotes panel, edge-click → evidence line; Matt's horizontal lean) and **Spine** (vertical rail in the side panel, click-to-expand inline). Toggle in the chain header, choice persists in `localStorage`. Refactored the receipts feed into a per-turn model so a layout flip just re-reads it. Both verified (featured + simulated live, 0 errors). **OPEN (next session): pick a default layout (or keep toggle), Matt's remaining review notes, live slug→name prettification.**
- **Data path:** `web/data/` (Edge-fn only, gitignored) isn't served — so the featured exchange is served at `/data/featured-tywin.json`; `netlify.toml` build command now `cp`s it into `web/public/data/` (gitignored) at deploy.
- **Dry validation (no API):** static `python3 -m http.server` + the Launch-preview browser. Confirmed: featured render (7-link chain + 8 beat cards, link-expand → quote/cite/tier), a SIMULATED live turn (token deltas accumulate, receipts rebuild resolve→read_node→walk_chain, streaming cursor clears on `done`), all 4 status states + `error` render. Zero console errors.
- **Docs flipped (anti-drift):** `web/README.md` (front-end row ✅ + layout) + design §0 (chat-page/receipts/failure rows ✅ S174; featured-render 🟡; deploy ⏳-gated).
**Decisions:** **No mocked AI prose (Matt, firm).** The featured chain/beats/quotes are REAL (graph-walked); the answer PROSE is a clearly-marked design-fixture placeholder (`FEATURED_PLACEHOLDER_ANSWER`) — it MUST be replaced with a CAPTURED REAL transcript (run the live fn over the seed question, save genuine model output) before the public deploy. Weirwood backdrop = subtle (Matt). Build UI first, art last (Matt).
**What's next:** **a FRESH-CONTEXT chat-UI DESIGN session** (Matt's call — keep context clean for design work) → `progress/continue-prompts/2026-06-30-chat-ui-design.md`: pick the default chain layout (Band vs Spine vs keep toggle), work through Matt's remaining review notes, polish (live slug→name, weirwood-backdrop opacity, etc.). THEN the two gated ship steps (real API $ — Matt's go-ahead): (1) capture a REAL featured transcript to replace the placeholder (Matt likes "pre-generate real & rotate"); (2) `netlify dev` proof → deploy private repo to Netlify + `ANTHROPIC_API_KEY` env. Front-end CODE is done + dry-validated. Live `search_chapters` stays the fast-follow. Parked: granular dips, D&E Pass-1, SIFT.

### Session 173 — Chat-UI alpha BUILD: function chunk (`chat.ts` + `agent.ts`) — [Track: meta] (2026-06-29)
**Detail:** none (execution chunk; `web/netlify/edge-functions/README.md` + design §0 carry specifics + the SSE contract). **Model:** Opus 4.8 (handoff recommended Sonnet 4.6 — mechanical TS; ran on the session's Opus). **No graph writes; no API calls / no API $** — all validation was offline (stubbed model). No harvest rows (web-tooling).
**Context:** 3rd of the 4 build chunks, per the now-archived `progress/continue-prompts/2026-06-29-chat-ui-function.md`. Build the Edge function: a Claude tool-use loop over the S172 retrieval tools + Bloodraven system prompt + SSE streaming + global spend cap + cite-verification gate. Locked decisions (Edge/Deno · `claude-opus-4-8` swappable · Option A · curated-MVP) NOT reopened.
**Changes made:**
- **Split the function into a pure core + a thin shell** so the loop is testable offline (no SDK, no API spend): **`web/netlify/edge-functions/agent.ts`** (SDK-FREE) holds the `SYSTEM_PROMPT` (Bloodraven persona notes baked in verbatim — golden lines, the tidbit-don't-volunteer rule, the flagged anti-patterns — plus the tool/grounding/cite rules), the four `TOOL_DEFS`, the streaming tool-loop `runAgent`, the cite gate (`harvestResult`/`verifyCites`), and `estimateCostUsd`. The **`RunTurn` seam** is what a stub swaps for the real model. **`chat.ts`** is the Netlify handler: Anthropic client (`npm:@anthropic-ai/sdk@^0.106.0` — newest before the dep-date cap that types adaptive thinking), the SSE `Response`, request parsing, and the durable spend cap.
- **Tool loop:** manual streaming loop (not `toolRunner`) for per-token control + the separate receipts channel. Adaptive thinking on; full assistant content (incl. thinking blocks) pushed back each turn so multi-tool turns stay valid. Bounded: `max_tokens` 4096 + `MAX_TOOL_ITERATIONS` 6. Tools self-validate (S172 trust boundary) so bad input yields data, never a throw.
- **Receipts channel (design §3):** emitted from the tool RETURNS as `receipt` SSE events ({tool, input, result}) — the panel renders from the structured result, NOT by parsing prose. SSE event vocabulary documented in `web/netlify/edge-functions/README.md`: `token` / `receipt` / `cite-check` / `status` (no-grounding · loop-bound-hit · unverified-cites · api-error · cost-cap-tripped) / `error` / `done`.
- **Cite-verification gate:** `harvestResult` collects every cite the tools return this turn into an allowlist; `verifyCites` flags any `chapter:line` the model writes in prose that isn't in it (fabricated). Emitted on `cite-check`. "Quotable book lines" (node-quote `cite`s / link `ref`s) kept separate from "context" in the prompt so the model doesn't quote grounding prose as a book line (the S171 smoke slip).
- **Cost guard:** GLOBAL daily ceiling in Netlify Blobs (`spend-YYYY-MM-DD`, $5/day) pre-checked before any token; trips → `cost-cap-tripped`. Blobs-absent (local dev) degrades gracefully (cap advisory). Per-request `max_tokens` + iteration bound are the always-on backstops.
- **Auth (design §4):** `chat.ts` passes `ANTHROPIC_API_KEY` when set (deployed), else constructs bare so the SDK's own resolution (AUTH_TOKEN / OAuth profile) runs for local `netlify dev`. Key-present is the only dev/prod diff.
- **Static validation (offline, no spend):** **27 Deno tests green** (`deno task test` = 21 lib + **6 new `agent_test.ts`** driving the loop with a STUBBED model against the REAL bundle — resolve→read→walk→answer with receipts + grounding, the cite gate catching a fabricated cite while a real one passes, no-grounding status, loop-bound-hit, cost math). **`deno task check:fn`** type-checks `chat.ts` against the SDK + Netlify edge types — clean. Lint clean (excluded only the stylistic `no-import-prefix` — inline `npm:`/`https:` is the proven Netlify-edge pattern).
- **Docs flipped (anti-drift):** `web/netlify/edge-functions/README.md` (TO BUILD → BUILT + status table + the SSE contract) + `web/README.md` row + design §0 (5 function-chunk rows → ✅ S173).
**Decisions:** none new (executed the locked plan). The one live `netlify dev` "who killed Tywin?" proof is **GATED on Matt** — it calls the real API and spends $ (no-extraction-without-asking spirit); everything validatable offline was validated.
**What's next:** **the FRONT-END chunk** (the last) — `progress/continue-prompts/2026-06-29-chat-ui-frontend.md` (Session 174, **Sonnet 4.6** — static HTML/CSS/JS): the chat page + SSE client, the typed-edge receipts panel (renders from `receipt` events), the static Tywin exchange from `featured-tywin.json`, the failure-mode UX, then deploy (private repo → Netlify, `ANTHROPIC_API_KEY` env). Live `netlify dev` + deploy gated on Matt. Parked: granular dips, D&E Pass-1, SIFT.

### Session 172 — Chat-UI alpha BUILD: retrieval-core chunk (`web/src/lib/`) — [Track: meta] (2026-06-29)
**Detail:** none (execution chunk; `web/src/lib/README.md` + design §0 carry specifics). **Model:** Sonnet 4.6 (per the S171 handoff — mechanical TS porting, **no API calls, no API $**). No graph writes; no harvest rows (this is web-tooling, not a corpus pass).
**Context:** 2nd of the 4 build chunks, per `progress/continue-prompts/2026-06-29-chat-ui-retrieval-core.md`. Port the read-only retrieval tools the demo agent runs by hand (`graph-query.py` + `event_alias_resolver.py`) to Deno/TS over the S171 `web/data/` bundle. Locked decisions (Edge/Deno · `claude-opus-4-8` swappable · Option A · curated-MVP-first) NOT reopened.
**Changes made:**
- **Built `web/src/lib/` (Deno/TypeScript, pure over the in-memory bundle, no Python shell-out, no network, no LLM):** `normalize.ts` (`normalize`/`tokenize` ported verbatim from the Python resolver — the alias-map keys were produced by that same normalize, so a query must normalize identically or exact lookups silently miss), `validate.ts` (the trust boundary — slug allowlist + phrase cap; invalid input → empty result, never throw/scan), `data.ts` (`loadGraphData` reads the 3 JSON files once at cold start), `resolve.ts` / `graph.ts` / `read-node.ts` (the four tools), `types.ts` (bundle + receipts-contract shapes), `mod.ts` (public surface + `createTools(data)` for the function chunk).
- **The four tools:** `resolve(phrase)` → ranked `[{slug, category, score, matchType}]` (exact alias-map lookup → fuzzy token-overlap fallback ≥ 0.5 with a slug-token bonus, top 5); `walkChain(slug, {full?})` → `{upstream[], downstream[]}` BFS over CAUSES/TRIGGERS/MOTIVATES (+ENABLES for full-chain), links carry the receipts fields `{source, edge_type, target, evidence_quote, ref, tier, depth}`; `neighbors(slug)` → edges split by direction, grouped by type; `readNode(slug)` → `{name, type, identity, quotes}` or null.
- **Unit tests (21, all green via `deno task test`, asserted against the REAL bundle):** the two REQUIRED handoff assertions both pass — `resolve("death of Tywin")` → `assassination-of-tywin-lannister` (exact, score 1.0, category events) and `walkChain` on it returns the **7-link** upstream chain matching `web/data/featured-tywin.json` (set-equal on source|edge_type|target). Plus: article/case/whitespace-insensitivity, character-phrase resolve, fuzzy fallback floor, full-chain⊇plain + no-ENABLES-leak, neighbors direction/count invariants, and invalid-input safety across all four tools.
- **Installed Deno 2.9.0** (`~/.deno/bin/deno`, official installer — was absent; needed for `deno test`). Added `web/deno.json` (test/check tasks + fmt/lint config); ran `deno fmt`.
- **Docs flipped (anti-drift):** `web/README.md` + `web/src/lib/README.md` (TO BUILD → BUILT, with the API + test instructions) + design §0 status table (split the retrieval row: 4 tools ✅ S172, live-search ⏳ fast-follow).
- **DEFERRED (handoff): `searchChapters` / `readPassage`** — need a build-time inverted index to stay under the Edge 50 ms CPU budget; the curated MVP grounds on graph quotes only.
**Decisions:** none new (executed the locked plan). Note: tools return a SUPERSET of the `{slug,category}` contract (added `score`/`matchType`) — structurally compatible, richer receipts.
**What's next:** **the function chunk** — `progress/continue-prompts/2026-06-29-chat-ui-function.md` (Session 173, **Sonnet 4.6** to write `chat.ts`; the live `netlify dev` proof spends real API $ → gate on Matt's go-ahead): the Claude tool-loop over these tools + Bloodraven system prompt + SSE streaming + global spend cap + cite-verification gate. Then front-end + deploy. Parked: granular dips, D&E Pass-1, SIFT.

### Session 175 — Chat-UI alpha: removed Tywin placeholder + committed local-live dev server — [Track: meta] (2026-06-30)
**Detail:** none (execution/cleanup; this entry + `web/README.md` carry specifics). **Model:** Opus 4.8 (handoff recommended Sonnet 4.6 — code cleanup; ran on session Opus). **No graph writes; no API $** — verification was DEMO-mode (no key) + the 27 deno tests. Harvest queue at 0.
**Context:** Matt: "remove all tywin placeholder context" before a fresh-context design session. Also folds in the prior UNLOGGED local-live dev-server setup (a separate harness conversation that never wrote a worklog entry) — committed here so the tree is clean for S176.
**Changes made:**
- **Placeholder removal (every layer):** `scripts/build-chat-export.py` — deleted `build_featured()` + `FEATURED_SLUG` + the `featured-tywin.json` write/manifest/print + now-dead `subprocess`/`sys`/`GRAPH_QUERY`/`CAUSAL_TYPES` (**the build was REGENERATING the fixture — root cause; deleting the artifact alone would not have stuck**). Deleted `web/data/featured-tywin.json` + `web/public/data/featured-tywin.json` (gitignored). `web/public/app.js` — removed `FEATURED_PLACEHOLDER_ANSWER` + `loadFeatured()`/`renderFeatured()`; init now renders a **clean empty landing**. `web/scripts/dev.ts` — removed synthetic `DEMO_PROSE` + the deleted-fixture replay; DEMO mode now streams an honest "not wired — run LIVE" notice (scaffold kept for a future real recording).
- **Tests/docs decoupled:** `web/src/lib/_fixtures.ts` dropped the `featured`-JSON export (kept `data` + `TYWIN_SLUG`); `graph_test.ts` rewrote the one `featured`-coupled test to assert structural props on the real node. `web/README.md` + `web/src/lib/README.md` flipped to existence-truth.
- **Local-live setup committed (prior unlogged conv):** `web/scripts/dev.ts` (LIVE+DEMO dev server), `web/package.json` (static-preview npm aliases), `web/.gitignore`, `web/netlify/edge-functions/chat.ts` (MODEL now env-overridable `Deno.env.get("WEIRWOOD_MODEL") ?? "claude-opus-4-8"` — **deploy default Opus UNCHANGED**), `web/deno.lock`.
- **Verified:** 27 deno tests green; `deno check` dev.ts + chat.ts clean; lint clean (16 files); DEMO server boots, serves placeholder-free `app.js`, streams without touching the deleted fixture (no 500).
**Decisions:** "remove all tywin placeholder context" = the fake demo content (hand-written prose + fixture + auto-load + its generation), **NOT** the real integration tests. KEPT the resolve/read-node/walk-chain/agent tests pivoting on `assassination-of-tywin-lannister` (a real, permanent graph node) — a legitimate engine fixture, not placeholder; offered to repoint to a Red Wedding node if Matt wants the word gone everywhere. No mocked AI prose remains anywhere (Matt firm).
**What's next:** the **fresh-context chat-UI DESIGN session** (Matt's design notes + enrichment ideas to come) → `progress/continue-prompts/2026-06-30-chat-ui-design.md` (`/continue chat-ui-design`, **Sonnet 4.6**): chain "see-the-edges" mockups, Band-vs-Spine default, the failed-turn-wipes-chain bug, live slug→name polish, backdrop opacity. THEN gated ship: capture a real recorded conversation (multi-turn Red Wedding) → wire DEMO replay to it → deploy private→Netlify + `ANTHROPIC_API_KEY`. NOTE (pre-existing): `deno task test` lacks `--node-modules-dir=auto` (errors on `@types/node`); add the flag or `"nodeModulesDir":"auto"` to `web/deno.json`.

---

### Session 176 — Chat-UI alpha: DESIGN evaluation against live conversations — [Track: meta] (2026-06-30)
**Detail:** `history/session-details/session-176.md` (the design narrative: advisory board, the mid-flight reversals, decisions). **Model:** Opus 4.8 (handoff recommended Sonnet 4.6). **Small API $** — live Sonnet 4.6 turns for design iteration (a few cents); no graph writes; harvest queue at 0.
**Context:** First fresh-context DESIGN session after the S171–S175 build/cleanup. Stood the live Deno server up under the preview MCP (`weirwood-live` config) and iterated against REAL conversations from Matt's notes. Heavily interactive — several reversals reshaped the plan.
**Changes made (all `web/`):**
- **Prose quality FIXED** (`agent.ts` SYSTEM_PROMPT): added an **"Evidence discipline"** block (setup-then-quote · one quote/beat · ≤~20 words · no standalone block-quotes · quote-must-do-a-job), fixed the **"Ask your questions…" opener leaking** into real answers, added a **mandatory-walk** rule for causal Qs. Verified by curl: in-context set-up quotes, no dumps, walk fires.
- **`walkChain` tamed + enriched** (`src/lib/graph.ts`+`types.ts`): `DEFAULT_MAX_DEPTH=2`, `MAX_LINKS_PER_DIRECTION=12`, causal-only default; links carry `source_name/target_name/source_type/target_type` (slug→name fix). `graph_test.ts` updated (8 pass). Verified curl: "what led to the Red Wedding" → **8 clean named links**.
- **Frontend** (`app.js`/`app.css`): re-rendered the chain to the **Mockup-A annotated spine** (node cards + type, hub highlight, inline edge evidence, `cleanQuote()` strips `(wiki:…)` markup, repeat-slim, bounded scroll); fixed the **failed-turn-wipes-chain** bug (`lastGoodChain` persistence). ⚠️ interim spine NOT visually verified end-to-end (Sonnet variance kept the browser from walking) — the S177 rebuild replaces most of it.
- **About page** (`index.html`/`app.css`/`app.js`): page now opens **straight to chat**; the "what is this" blurb → a toggled **About view** (header **About** ↔ "← Chat"). Verified.
- **Prose-first on mobile** (CSS `order`); the full-width **Band-on-top RETIRED** (it sat above the prose → chain = rail desktop / below mobile). **Backdrop opacity 0.06→0.11** (`tokens.css`). **Dev caching fixed**: `dev.ts` no-store + `index.html` asset `?v=176b`.
**Decisions:** **Bloodraven = DEFAULT persona** (advisory board overrode the earlier Loremaster-default lean — a characterful voice forces narrative cohesion → better in-context quotes; flat Loremaster is a *later* toggle that inherits the same Evidence Discipline). **Keep-the-detail-via-progressive-disclosure** (NOT hard-prune — Matt reversed): clean spine default + ENABLES web behind a "show preconditions (+N)" toggle. **Prose-first on every screen.** Mockups picked: **A** (annotated spine) + **C** (node dossier). The 4-advisor board (3 returned, 1 hung) converged on all of the above. Locked list unchanged.
**What's next:** the **chain-display REBUILD (Phase 2, green-lit)** → `progress/continue-prompts/2026-06-30-chat-ui-design.md` (`/continue chat-ui-design`, **Sonnet 4.6**): dedup spine + "show preconditions (+N)" toggle (keep detail, progressive) → hover-peek + click-dossier on every node (add `/api/node` lookup) → stretch prose↔edge highlight. All S176 work committed at this endsession.

> _Sessions 157 (A1.6 Euron), 158 (script consolidation), 159 (A2.6 Jaime), 160 (chat-UI Bloodraven demo), and 161 (A2.4 Tyrion) archived to `history/worklog-archives/archive033.md` (full at 5). Sessions 162 (harvest drain), 163 (WO5K-battles PASS 1), 164 (PASS 2 Westerlands raid), 165 (harvest drain), and 166 (PASS 3 the unravelling) archived to `history/worklog-archives/archive034.md` (full at 5). Sessions 167 (A2.8 Davos/Sam residual — CLOSES the 🅰 A-roundup), 168 (granular-dip planning + chat-UI alpha scoped), 169 (chat-UI alpha design review), 170 (chat-UI publishing settled + repo/auth), and 171 (chat-UI alpha BUILD step-0 + Foundation) archived to `history/worklog-archives/archive035.md` (full at 5). Sessions 172 (retrieval-core chunk), 173 (function chunk), 174 (chat-UI front-end chunk), and 175 (Tywin-placeholder removal + local-live dev server) archived to `history/worklog-archives/archive036.md` (4 of 5)._

---

## Principles

> Guiding principles for the project. Reference these when making design decisions.

1. **The chapter text is immutable.** Source files in `sources/chapters/` never change. Everything else layers on top.
2. **Facts before interpretations.** Mechanical extraction before analytical. Tier 1 before Tier 4.
3. **Agents propose, humans decide.** Analytical findings go to the curation queue. Matt assigns confidence.
4. **Spoiler gating is deferred, not dropped.** `first_available` is optional in v1 nodes and backfills via deterministic script post-first-release (DEFERRED decision, 2026-04-27/S24). Don't spend context reasoning out individual values; the wiki cite_ref/infobox sources for the backfill are documented in architecture.md.
5. **The index and the graph are complementary.** The index routes. The graph traverses. Both are needed.
6. **Each token should be productive.** Structure exists to reduce waste — agents should read relevant, trustworthy content, not re-derive known relationships.
7. **Start with the foundation, not the flashiest feature.** Chapter files → mechanical extraction → wiki → index → graph → analytical passes. In that order.

