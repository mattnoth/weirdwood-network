# Worklog Archive 036

> Archived Session Log entries from `worklog.md` (CLAUDE.md rule #8: worklog keeps the 5 most recent; older entries move here, 5 per archive file). Newest-first within the file.

---

### Session 173 — Chat-UI alpha BUILD: function chunk (`chat.ts` + `agent.ts`) — [Track: meta] (2026-06-29)
**Detail:** none (execution chunk; `web/netlify/edge-functions/README.md` + design §0 carry specifics + the SSE contract). **Model:** Opus 4.8 (handoff recommended Sonnet 4.6 — mechanical TS; ran on the session's Opus). **No graph writes; no API calls / no API $** — all validation was offline (stubbed model). No harvest rows (web-tooling).
**Context:** 3rd of the 4 build chunks, per the now-archived `progress/continue-prompts/2026-06-29-chat-ui-function.md`. Build the Edge function: a Claude tool-use loop over the S172 retrieval tools + Bloodraven system prompt + SSE streaming + global spend cap + cite-verification gate. Locked decisions (Edge/Deno · `claude-opus-4-8` swappable · Option A · curated-MVP) NOT reopened.
**Changes made:**
- **Split the function into a pure core + a thin shell** so the loop is testable offline (no SDK, no API spend): **`web/netlify/edge-functions/agent.ts`** (SDK-FREE) holds the `SYSTEM_PROMPT` (Bloodraven persona notes baked in verbatim — golden lines, the tidbit-don't-volunteer rule, the flagged anti-patterns — plus the tool/grounding/cite rules), the four `TOOL_DEFS`, the streaming tool-loop `runAgent`, the cite gate (`harvestResult`/`verifyCites`), and `estimateCostUsd`. The **`RunTurn` seam** is what a stub swaps for the real model. **`chat.ts`** is the Netlify handler: Anthropic client (`npm:@anthropic-ai/sdk@^0.106.0` — newest before the dep-date cap that types adaptive thinking), the SSE `Response`, request parsing, and the durable spend cap.
- **Tool loop:** manual streaming loop (not `toolRunner`) for per-token control + the separate receipts channel. Adaptive thinking on; full assistant content (incl. thinking blocks) pushed back each turn so multi-tool turns stay valid. Bounded: `max_tokens` 4096 + `MAX_TOOL_ITERATIONS` 6. Tools self-validate (S172 trust boundary) so bad input yields data, never a throw.
- **Receipts channel (design §3):** emitted from the tool RETURNS as `receipt` SSE events ({tool, input, result}) — the panel renders from the structured result, NOT by parsing prose. SSE event vocabulary documented in `web/netlify/edge-functions/README.md`: `token` / `receipt` / `cite-check` / `status` (no-grounding · loop-bound-hit · unverified-cites · api-error · cost-cap-tripped) / `error` / `done`.
- **Cite-verification gate:** `harvestResult` collects every cite the tools return this turn into an allowlist; `verifyCites` flags any `chapter:line` the model writes in prose that isn't in it (fabricated). Emitted on `cite-check`. "Quotable book lines" (node-quote `cite`s / link `ref`s) kept separate from "context" in the prompt so the model doesn't quote grounding prose as a book line (the S171 smoke slip).
- **Cost guard:** GLOBAL daily ceiling in Netlify Blobs (`spend-YYYY-MM-DD`, $5/day) pre-checked before any token; trips → `cost-cap-tripped`. Blobs-absent (local dev) degrades gracefully (cap advisory). Per-request `max_tokens` + iteration bound are the always-on backstops.
- **Auth (design §4):** `chat.ts` passes `ANTHROPIC_API_KEY` when set (deployed), else constructs bare so the SDK's own resolution (AUTH_TOKEN / OAuth profile) runs for local `netlify dev`. Key-present is the only dev/prod diff.
- **Static validation (offline, no spend):** **27 Deno tests green** (`deno task test` = 21 lib + **6 new `agent_test.ts`** driving the loop with a STUBBED model against the REAL bundle — resolve→read→walk→answer with receipts + grounding, the cite gate catching a fabricated cite while a real one passes, no-grounding status, loop-bound-hit, cost math). **`deno task check:fn`** type-checks `chat.ts` against the SDK + Netlify edge types — clean. Lint clean (excluded only the stylistic `no-import-prefix` — inline `npm:`/`https:` is the proven Netlify-edge pattern).
- **Docs flipped (anti-drift):** `web/netlify/edge-functions/README.md` (TO BUILD → BUILT + status table + the SSE contract) + `web/README.md` row + design §0 (5 function-chunk rows → ✅ S173).
**Decisions:** none new (executed the locked plan). The one live `netlify dev` "who killed Tywin?" proof is **GATED on Matt** — it calls the real API and spends $ (no-extraction-without-asking spirit); everything validatable offline was validated.
**What's next:** **the FRONT-END chunk** (the last) — `progress/continue-prompts/2026-06-29-chat-ui-frontend.md` (Session 174, **Sonnet 4.6** — static HTML/CSS/JS): the chat page + SSE client, the typed-edge receipts panel (renders from `receipt` events), the static Tywin exchange from `featured-tywin.json`, the failure-mode UX, then deploy (private repo → Netlify, `ANTHROPIC_API_KEY` env). Live `netlify dev` + deploy gated on Matt. Parked: granular dips, D&E Pass-1, SIFT.

### Session 172 — Chat-UI alpha BUILD: retrieval-core chunk (`web/src/lib/`) — [Track: meta] (2026-06-29)
**Detail:** none (execution chunk; `web/src/lib/README.md` + design §0 carry specifics). **Model:** Sonnet 4.6 (per the S171 handoff — mechanical TS porting, **no API calls, no API $**). No graph writes; no harvest rows (this is web-tooling, not a corpus pass).
**Context:** 2nd of the 4 build chunks, per `progress/continue-prompts/2026-06-29-chat-ui-retrieval-core.md`. Port the read-only retrieval tools the demo agent runs by hand (`graph-query.py` + `event_alias_resolver.py`) to Deno/TS over the S171 `web/data/` bundle. Locked decisions (Edge/Deno · `claude-opus-4-8` swappable · Option A · curated-MVP-first) NOT reopened.
**Changes made:**
- **Built `web/src/lib/` (Deno/TypeScript, pure over the in-memory bundle, no Python shell-out, no network, no LLM):** `normalize.ts` (`normalize`/`tokenize` ported verbatim from the Python resolver — the alias-map keys were produced by that same normalize, so a query must normalize identically or exact lookups silently miss), `validate.ts` (the trust boundary — slug allowlist + phrase cap; invalid input → empty result, never throw/scan), `data.ts` (`loadGraphData` reads the 3 JSON files once at cold start), `resolve.ts` / `graph.ts` / `read-node.ts` (the four tools), `types.ts` (bundle + receipts-contract shapes), `mod.ts` (public surface + `createTools(data)` for the function chunk).
- **The four tools:** `resolve(phrase)` → ranked `[{slug, category, score, matchType}]` (exact alias-map lookup → fuzzy token-overlap fallback ≥ 0.5 with a slug-token bonus, top 5); `walkChain(slug, {full?})` → `{upstream[], downstream[]}` BFS over CAUSES/TRIGGERS/MOTIVATES (+ENABLES for full-chain), links carry the receipts fields `{source, edge_type, target, evidence_quote, ref, tier, depth}`; `neighbors(slug)` → edges split by direction, grouped by type; `readNode(slug)` → `{name, type, identity, quotes}` or null.
- **Unit tests (21, all green via `deno task test`, asserted against the REAL bundle):** the two REQUIRED handoff assertions both pass — `resolve("death of Tywin")` → `assassination-of-tywin-lannister` (exact, score 1.0, category events) and `walkChain` on it returns the **7-link** upstream chain matching `web/data/featured-tywin.json` (set-equal on source|edge_type|target). Plus: article/case/whitespace-insensitivity, character-phrase resolve, fuzzy fallback floor, full-chain⊇plain + no-ENABLES-leak, neighbors direction/count invariants, and invalid-input safety across all four tools.
- **Installed Deno 2.9.0** (`~/.deno/bin/deno`, official installer — was absent; needed for `deno test`). Added `web/deno.json` (test/check tasks + fmt/lint config); ran `deno fmt`.
- **Docs flipped (anti-drift):** `web/README.md` + `web/src/lib/README.md` (TO BUILD → BUILT, with the API + test instructions) + design §0 status table (split the retrieval row: 4 tools ✅ S172, live-search ⏳ fast-follow).
- **DEFERRED (handoff): `searchChapters` / `readPassage`** — need a build-time inverted index to stay under the Edge 50 ms CPU budget; the curated MVP grounds on graph quotes only.
**Decisions:** none new (executed the locked plan). Note: tools return a SUPERSET of the `{slug,category}` contract (added `score`/`matchType`) — structurally compatible, richer receipts.
**What's next:** **the function chunk** — `progress/continue-prompts/2026-06-29-chat-ui-function.md` (Session 173, **Sonnet 4.6** to write `chat.ts`; the live `netlify dev` proof spends real API $ → gate on Matt's go-ahead): the Claude tool-loop over these tools + Bloodraven system prompt + SSE streaming + global spend cap + cite-verification gate. Then front-end + deploy. Parked: granular dips, D&E Pass-1, SIFT.
