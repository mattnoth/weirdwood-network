### Session 207 — Graph vocab/schema hygiene: event schema-drift reconciled + dup-Quotes dedup — [Track: graph] (2026-07-10)
**Detail:** `history/session-details/session-207.md`. **Model:** Sonnet 4.6 (deterministic scripts + one schema design call + read-only de-risk). **Graph mutation: YES** — node type retypes + quote-section merges; **edges untouched (25,313)**. Harvest: **2 open** (the S206 pair, below the ~30 bar — no action).
**Changes made:**
- **Part A — event schema-drift RECONCILED (Matt's hybrid disposition, AskUserQuestion).** Sanctioned **5** into architecture.md's Type Reference Table (+ Active Decision, rule #6): `event.death`(141)/`capture`(43)/`ceremony`(27)/`decree`(11)/`council`(10). `scripts/s207-event-retype.py` retyped **139 event nodes**: off-schema tail → existing leaves (`other`+singletons→incident; marriage→wedding, siege/raid/occupation→battle, assassination_attempt→assassination, banishment→exile, succession→investiture, funeral→ceremony, reform→decree, imprisonment→capture, coming-of-age→ceremony); **26 war-mistypes `event.battle`→`event.war`** (Robert's Rebellion, the Dance, Aegon's Conquest, all four Blackfyre rebellions, the Dornish wars…); 3 held → incident (great-spring-sickness/hour-of-the-wolf/lysene-spring, Matt-dispositioned). **0 off-schema subtypes remain** (all 21 live event.* types now sanctioned). Pivot: `event.death` was already referenced normatively by the `SUSPECTED_OF` row → sanction, not retype.
- **Dup-`## Quotes` dedup (S205 backlog): 84 nodes merged** (`scripts/s207-dedup-quotes-headers.py`, line-level union; 39 exact-dup + 45 union incl. the 1 `## Quotes (continued)`). **+29 previously-hidden quotes surfaced across 15 nodes, 0 regressions** — validated vs the real `parse_quotes`/`split_sections` loader (which is **last-wins**, so the 2nd section was being dropped from the bundle). `parse_quotes` only renders `>`-blockquote style (wiki-plain-line dedup = pure file hygiene).
- **Surgical index revert (S206 recipe):** `weirwood refresh` → reverted **8,285 timestamp-only** derived files, kept 140 events-index + architecture.md + 139 source nodes. pytest **1457** / deno **100** green.
- **DEPLOYED to prod (Matt's go):** `netlify deploy --prod --build` (deploy `6a5164f6a64fe8…`) regenerated the 5 web/data bundle files from live graph → retyped node types + merged quotes now LIVE at https://weirwood-network.netlify.app. Verify turn "Dance of the Dragons" → 2 tools / 6 grounding / 0 unverified cites / $0.021 (Opus).
**Decisions (Matt):** hybrid disposition (sanction 5 + retype tail); 26 war-fixes, 3 held→incident. **`event.betrothal`/`event.betrayal` MAY get their own types** (Matt leaning yes) — folded into `incident` for now; DECIDE when the review-bucket betrothals land, then **retrofit the folded nodes up**. **Edge-vocab retrofit CONFIRMED wanted** ("edges need retrofit as well") — deferred behind the review bucket, NOT dropped. **Node-type promotion sweep** queued (Part A was conservative → incident). Part B (edge-vocab retrofit) NOT done this session → todos. Endsession authorized (commit + endsession, triage as the continue prompt).
**De-risk (read-only, for the next track):** `fab-reconcile-candidates.py --redirect-map` is **slug→slug** (post-resolution, line 1087), NOT a raw name→slug override; recovery = add aliases / mint missing-event nodes → `weirwood refresh` → re-run. **The real open Q = re-injection idempotency** (confirm the `mint_enrichment` P4 dedup guard makes re-runs safe, or add a `--only-names` filter) — top of the triage prompt.
**What's next:** **F&B review-bucket triage** (S208) → `progress/continue-prompts/2026-07-10-review-bucket-triage.md` — the deferred-events vein first (`event-dedup-risk` 221 + `composite-name` 57 = real missing canon: Death of Queen Helaena, Dance births, Battle of the Stepstones, Betrothal of Aegon III & Daenaera). Then node-type promotion sweep + edge-vocab retrofit. Strip-boilerplate still Matt-gated. graph-vocab-hygiene prompt archived (Part A done; Part B → todos).

### Session 208 — F&B review-bucket RECOVERY LANDED: 995 edges + 201 event nodes from the 1,440 quarantined rows — [Track: graph] (2026-07-10)
**Detail:** `history/session-details/session-208.md`. **Model:** Fable 5 orchestrator + Sonnet (events triage ×4) + Haiku (fresh-verify ×5). **Graph mutation: YES** (continue-prompt scope; idempotency proven on a checkpointed unit before scaling per the prompt's gate).
**Changes made:**
- **Recovery mechanics built:** `--recover` mode on `fab-reconcile-candidates.py` (per-unit curated `{name: slug|CREATE}` maps; processes ONLY mapped names — quarantined rows never landed, so double-apply is impossible by construction; run_id suffix `-recovery` vs mint's re-run guard; unmapped names can never CREATE, protecting post-apply folds/renames). Idempotency proven 4 ways on a scratch copy (land / same-run_id ABORT / new-run_id all-skip via P4 same-quote dedup / 0 live-graph overlap). +4 router tests (157/157).
- **Name curation (all 4 classes):** small classes + `no-decisive-margin` (172 rows, 51 names — per-unit disambiguator-decided; ~14 rows dropped where no correct node exists) + `unresolved-status` head (147 names ≥2×; **tail DROPPED: 415 names / 406 proposal rows**, logged `tail-drop-log.json`) → `curation-decisions.json` (~160 global + ~100 per-unit entries). Traps dodged: `cheese`(food)≠Cheese(assassin)→`cheese-ratcatcher`, Septon Murmison≠his assassination event, `queen-rhaenys`/`princess-rhaenys` are SHIPS, 5 disambig-hub Targaryen bare names routed per-unit (`jaehaerys-targaryen` etc.), `Mouse`=Marilda's nickname → would self-SAME_AS → dropped.
- **Deferred-events vein (276 rows):** token-screen vs the 1,032-slug event inventory → 4× Sonnet create-or-skip triage (215C/54M/5D/2 collide) → semantic-dup scan caught 8 cross-unit collisions (3 same-event flips, 5 different-event premints) → **192-CREATE Haiku fresh-verify: 190 CONFIRM / 2 dupe-flips** (the-green-council, aegons-first-coronation). 9 hand-premints (`death-of-rhaenys-targaryen-daughter-of-aemon`, `yielding-of-the-red-keep-48-ac`, `birth-of-viserys-i/ii-targaryen`, both High-Septon deaths, both Vulture-King deaths, `maegors-trial-of-seven`) + `ronnel-arryn-son-of-sharra` (the King Who Flew — bare slug is a disambig trap).
- **LANDED 39/39 units: +995 edges** (52 disputed, all sourced; AGENT_IN 217 / PARENT_OF 140 / VICTIM_IN 128 / PARTICIPATES_IN 101…) **+201 reconciler-composed event nodes** → edges.jsonl **26,308**, event nodes **1,242**, prose merges on ~120 recovered names (Sea Snake voyages → corlys, Six-Times-to-Sea text CREATE). Marquee recoveries: Betrothal of Aegon III & Daenaera, Battle of the Stepstones (133 AC), Death of Jaehaerys I, the Blood-and-Cheese role edges, deaths of Syrax/Vhagar/Sunfyre/Dreamfyre + the whole Dance dragon-death cluster, Kingsguard/Iron-Throne/KL fixture edges.
- **`event.betrothal` SANCTIONED (Matt) / `event.betrayal` REJECTED** — see Active Decisions. `mellos` added to the `in_universe_source` enum + the wrongly-derived KILLS attribution repaired (Mellos, not Mushroom, muses the king ordered Lyonel Strong's death). S208 schema gate added to the reconciler CREATE path (S207 RETYPE_MAP applied at mint time — no new off-schema drift).
- **6 P1 harm-gate retypes** (S207 fallout: target events retyped battle→war/x→incident flipped old VICTIM_IN edges out of the harm set) → gate **4/4 PASS**; pytest **1457** / deno **100**; index churn surgical (8,416 timestamp-only reverted, 5 meaningful kept + new-node index files).
**Residue (logged, not blocking):** **76 dispute-held recovery rows** in `recovery-s208/out/*/dispute-review.jsonl` (hedge-neighborhood quarantine — needs the S201 preclassify→adjudicate→inject machinery; small follow-up); 42 quote-quarantined rows; 237 partner-endpoint drops (tail-drop cost, closed); batch-5 Haiku verify self-confused (coverage via Sonnet triage + token screen + mint line-check instead); resolver observation for a future track — exact-name top-candidate returns status `candidates`, not `hit`, which is WHY fixtures like "Kingsguard" quarantined at all.
**Harvest queue at endsession: 2 open rows** (far under the ~30 bar — no drain needed). **What's next:** the **76 dispute-held rows drain** (S209 — the last review-bucket step) → `progress/continue-prompts/2026-07-10-recovery-dispute-drain.md`; then Matt's pick — edge-vocab retrofit (Part B, confirmed wanted, now unblocked) · the 76 dispute-held rows drain · node-type promotion sweep (todos MED) · strip-boilerplate (un-park condition met, Matt-gated). Prod deploy of the new bundle is Matt-gated as usual.

### Session 209 — F&B dispute-held recovery rows DRAINED (76 rows) — the LAST review-bucket step — [Track: graph] (2026-07-11)
**Model:** Opus 4.8 orchestrator (light adjudication over deterministic S201 machinery). **Graph mutation: YES** (continue-prompt scope; all deterministic-scripted, primary-text-anchored overrides).
**Changes made:**
- **Drained the 76 dispute-held rows** the S208 recovery quarantined under the §7.2 hedge-proximity guard (`recovery-s208/out/*/dispute-review.jsonl`). `fab-dispute-preclassify` auto-resolved **49/72** (68%; every hedge_term a chronicler-name proximity false-positive) → **23 NEEDS_READ** edges adjudicated against the primary text.
- **Adjudication: 20 confirm (tier-1) / 3 drop** — `CAPTURES corlys→alicent` (badge-wearing captors tried as individuals; no Corlys agency), `RULES tyland→seven-kingdoms` (Hand "gathered power" under a regency ≠ king), `SAME_AS nettles→netty` ("Netty" is a diminutive → alias-not-cross-identity; **queued to harvest** as a `nettles` alias). None needed a tier-2 disputed tag.
- **Landed:** inject → per-unit re-mint with fresh run_ids (`…-recovery-disputes-s209`) — mint's same-quote dedup **skipped every S208-landed edge**, appended only the 44 `DISP*` edges → edges.jsonl **26,308 → 26,352** (+44 / −0). Prose: a **dispute-only** merge-plan (diff vs pre-inject snapshot, **consolidated 1-entry-per-slug** to avoid the same-run_id `skipped_marker` trap) → **24 bullets on 11 character nodes**, `section_extended` with zero duplicate bullets. **5 event-kind rows deferred** (out of dispute-inject scope, logged); **3 dropped**.
- **Excluded-units caveat handled:** `fab-heirs-of-the-dragon-15-p01/p02` (4 rows) collide *by name* with the S200-smoke `EXCLUDED_UNITS` guard baked into both scripts — a false-positive here (recovery dir ≠ standard `apply/`). Ran preclassify+inject on just those 2 units with the guard patched off (reuses 100% of the real logic; `chapter=unit` provenance preserved). All 4 flat AUTO_CLEAR.
- **Gates:** `fab-semantic-gate` **4/4 PASS** (0 new VICTIM_IN, 0 disputed edges → harm-gate + disputed-invariant trivially clean); pytest **1457** / deno **100**. **No `weirwood refresh`** — prose-append touches node bodies only, no new slugs/aliases (S205 precedent); `graph/index/` stays valid.
**Records:** `working/fire-and-blood/recovery-s208/ADJUDICATION-s209-disputes.md` + `verdicts-s209-disputes.jsonl` + `dispute-merge-plan-s209.json`.
**Harvest queue: 3 open** (added the one "Netty"→nettles alias row; far under the ~30 bar — no drain). **Residue (logged, not blocking):** 5 event-kind dispute rows deferred in `out/*/dispute-events-deferred.jsonl` (Wedding of Aegon III & Jaehaera, Death of Alicent, Death of Tyland, Taming of Sheepstealer, Green-council embassy) — a future event-mint could reconsider them.
**What's next (Matt's pick):** **edge-vocab retrofit (Part B — confirmed wanted, now the top open graph track)** · node-type promotion sweep (todos MED) · **strip-boilerplate (un-park condition MET, Matt-gated)** · prod deploy of the enlarged bundle (Matt-gated). The review-bucket track is now fully CLOSED.

### Session 210 — Prod deploy of the S205-S209 backlog → strip "from the AWOIAF wiki" boilerplate → redeploy — [Track: graph] (2026-07-11)
**Detail:** `history/session-details/session-210.md`. **Model:** Opus 4.8. **Graph mutation: YES** (6,481 nodes; Matt-directed + dry-run-approved). Harvest queue: **3 open** (unchanged; no drain). **2 prod deploys.**
**Changes made:**
- **Deploy #1 (`6a51e7cd`):** shipped the S205-S209 graph backlog live (nothing had deployed since S204) — harvest quotes, cross-era seams, schema hygiene, the +995-edge review-bucket recovery, the +44 dispute-drain. Verified S209 dispute bullets on the deployed corlys node.
- **Stripped the "from the AWOIAF wiki" boilerplate from all 6,481 nodes** (`scripts/strip-wiki-boilerplate.py --apply`; 5,312 rich + 1,169 thin, 0 remaining). **Design pivot (Matt-decided):** the live UI already hides the wiki-tail form (`app.js isWikiBoilerplate` → "never shown"), so the board's "keep a stripped `X is a <type>.` stub for rich nodes" would NEWLY SURFACE ~5,300 contentless stubs → **DROP-EVERYWHERE** instead (empty Identity → UI shows nothing; type is in the card subtitle). Also fixed the classifier: inverted `RICH_SECTIONS_RE` allowlist → "rich = any section beyond Identity/Edges" (docstring intent; the old list missed Culture/Aftermath/Heraldry/Fire&Blood).
- **5 composed-prose wiki-attributions stripped** (Matt's "more detailed thing" recall): hound-helm/house-sweet/lothar/gender-and-sexuality/porridge-gaoler ("the wiki notes,"/"the wiki lists…"/etc.) — every fact kept. **2 provenance/build notes LEFT** (red-wedding-conspiracy `evidence_kind wiki`, mutiny-at-castle-black `tier-1 wiki Origins`) — "wiki"=source-tier there, the class kept like `wiki_source`.
- **Emitters:** the "patch generators" half is **20 files / 33 lines** (retired Pass-2 ingestion; new nodes use enrichment/F&B which never emit the phrase). Matt's call: **guard-test not patch** → `tests/test_no_wiki_boilerplate.py` asserts 0 nodes carry the phrase (fails loudly on any reintroduction). 20-file list logged in todos.
- **Deploy #2 (`6a51ee9b`) + a final redeploy:** stripped bundle live — verified `arakh` ships `identity: ""`, phrase absent from deployed data, chat pipeline healthy.
- **Fixed the family.json golden** (`generationCounts` repin): NOT the strip — traced (stashed the strip, still failed) to the built bundle being stale since the S204 deploy; deploy #1's rebuild surfaced legit reshuffle from the S205-S209 F&B genealogy edges. mustInclude + memberCount=96 held; repinned + logged (gen 5:8→11, 7:5→4, 12:11→10, 13:4→3).
**Gates:** pytest **1458** (incl. new guard) / deno **100**; fab-semantic-gate untouched (no edges changed). No `weirwood refresh` (body-prose edits; deploy `--build` rebuilds the shipped search index).
**Advisory-board review (Matt-requested, 4 Sonnet advisors) + 2 fixes shipped** (`008780102c`, deploy `6a527b43`): board validated the S210 calls; surfaced (1) a LIVE mislabel — the dossier captioned EVERY non-boilerplate identity "From the wiki" incl. book-cited prose → relabelled **"Overview"** (`app.js`; verified live on red-wedding-conspiracy); (2) the `family.json` corpus goldens read the gitignored `web/data` bundle (rebuilt only on deploy) so "green" in S205–S209 validated STALE data → added a **staleness guard** (`test_spec_cases.py`: bundle-reading goldens skip loudly when `web/data/edges.json` lags `edges.jsonl`; verified stale→skip/fresh→pass). Deferred board findings + the 120-note "leave" decision logged in todos (S210 advisory-board block). **Demo-wow nuance verified:** the "chain walked" spine is causal-only (1.8% of edges); Part B's types aren't causal, so its win is richer typed receipts + better whodunit answers, not "more spine."
**What's next:** **Session 211 = present highest-value options → Matt picks one → execute** (Matt: next handoff to **Fable**) → `progress/continue-prompts/2026-07-11-highest-value-options.md`. Options: (1) edge-vocab retrofit Part B [targeted, SUSPECTED_OF headline] · (2) Stage 4 staged candidate-pool bulk typing [HIGH VOLUME — ~32k rows; Events sub-pool SHELVED at the precision gate, gated] · (3) node-type promotion sweep [cheap; board's do-first] · (4) board-deferred UI polish [search-card leak, thin-node dossiers] · (5/6) decided-leave + Small-Fixes grab-bag.

### Session 211 — Edge-vocab retrofit Part B (+30 edges) + node-type promotion sweep (12) + the relationship-routing fix — [Track: graph] (2026-07-11)
**Detail:** `history/session-details/session-211.md`. **Model:** Fable 5 orchestrator + Sonnet ×3 (proposers) + Haiku ×3 (fresh-verify). **Graph mutation: YES** (Matt's live gos). **3 prod deploys** (`6a52c19f` / `6a52c2f2` / `6a52c383`). Harvest: **17 open** (3+14 new; under the ~30 bar — no drain).
**Changes made:**
- **Menu → pick:** presented the S210 highest-value options; Matt picked **Part B (edge-vocab retrofit)**, then extended the session to the **promotion sweep** and deploys.
- **Part B DONE — +30 edges** (edges.jsonl 26,352 → **26,382**): 18 SUSPECTED_OF tier-2 (SUSPECTED_OF 23→41; marquees: the 4-suspect Maegor High-Septon whodunit, the Helaena riot rumor ×2, Rhaegar/Harrenhal-tourney [book-grounded via adwd-kingbreaker], Sandor+BwB/Saltpans, Marillion/Lysa, Euron/kingsmoot, Larys→fall-of-KL) · 10 KNIGHTED_BY tier-1 (16→26 knighting edges; Gregor←Rhaegar, Barristan←Aegon V, Dunk←Arlan…) · `corlys-velaryon AGENT_IN death-of-aegon-ii` tier-1 (confession+conviction; Larys tier-2 SUSPECTED_OF stands per S202/S203) · `tyrion COMMANDS_IN plot-to-free-jaime` tier-1 (own-POV proof; S148 pattern). Machine: deterministic packets (41 knighting + 75 suspicion) → 3 Sonnet proposers → 3 Haiku adversarial verifiers (31C/2A/1P) → orchestrator adjudication (**caught 3 history collisions the verifiers couldn't**: 2× S159-rejected Vance/Piper, 1× S148-retired Petyr) → quotecheck 30/30 → dry-run → Matt's go → mint. Records: `working/edge-retrofit-s211/ADJUDICATION-s211.md`.
- **Node-type promotion sweep DONE** (todos-294): **12 retypes** (7 battle→war [Ninepenny/Stepstones/Wolves/Across-the-Water + 3 uprisings, per the S207 Blackfyre precedent] · 2 murders→**assassination** [comparables: joffrey/tywin/renly] · 2 arrests→capture · winterfell-murders→death) + **5 documented holds** (abduction-of-lyanna stays `incident` — capture would assert the R+L abduction reading). Harm-gate interaction pre-checked; surgical index patches. Census: incident 306→301, battle 280→273, war 56→63. Record: `PROMOTION-SWEEP-s211.md`.
- **THE ROUTING-GAP FIND + FIX (sleeper of the session):** post-deploy live probe "Who knighted Gregor?" answered "text does not reveal" **while the edge sat in the deployed bundle** — blob toolTrace showed the routing table had NO row for relationship-fact questions (read_node dossier carries no edges; search_quotes can't see edge evidence). Added the "Who did X to Y" → **neighbors** routing row + stronger neighbors tool line to `agent.ts` SHARED_RULES (length pin 12489→13212, dated comment). Re-probe: 2 tool calls, new edge cited verbatim, 0 unverified. **This predated S211 and had silently nullified typed-edge facts in chat.**
- **2 stale-prose fixes** (both were nodes narrating edge-states later adjudicated away): blackfish-escapes-riverrun (S159-rejected pair narrated as fact) + murder-of-jon-arryn (Petyr now COMMANDS_IN + the new Tyrion SUSPECTED_OF noted).
**Gates:** fab-semantic-gate 4/4 · pytest **1458** (bundle goldens re-armed post-deploy) · deno **100** · check:fn clean. All live-verified on prod.
**Decisions:** disproven-but-load-bearing in-world suspicion IS valid SUSPECTED_OF (brienne/renly precedent, applied to Helaena-rumor/Saltpans/Marillion); promote-only-where-slug-AND-content-agree for retypes; wiki-only knighting facts (aerys←tywin, daemon-sand←oberyn, jorah←robert) parked NOT minted; a live-probe verify turn after graph-layer ships is mandatory practice (bundle-presence ≠ answer-reachability).
**What's next:** **roles slice** (Part B continuation: 313 events with zero role edges → ranked top-~50 round) → `progress/continue-prompts/2026-07-11-roles-slice.md`. Then: board-deferred UI polish (search-card gate + thin-node chips, todos) · small residue (death-of-queen-ceryse mismatch, Tyrek node).
