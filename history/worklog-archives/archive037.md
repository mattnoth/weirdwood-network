# Worklog Archive 037

> Archived worklog Session Log entries (oldest-first within this file). Each archive file holds up to 5 entries per CLAUDE.md rule #8. This file opened at S182 endsession when S177 rolled off the live worklog.

### Session 177 — Chat-UI alpha: chain-display REBUILD + unified quote system — [Track: meta] (2026-06-30)
**Detail:** `history/session-details/session-177.md` (the design narrative: rebuild, the `[[q|…]]` marker design, the cold-subagent bold call). **Model:** Opus 4.8 (handoff recommended Sonnet 4.6; Sonnet had botched a prior attempt on this same task — smart-quote/Deno breakage — and Matt reverted it). **Small API $** — live Sonnet 4.6 curl/preview turns for verification (a few cents); no graph writes; harvest queue at 0.
**Changes made (all `web/` + `netlify.toml`, all UNCOMMITTED before this endsession):**
- **Backend `walkChain` finalized** (`src/lib/graph.ts`+`types.ts`+`mod.ts`): returns the causal spine PLUS a separate capped `enables[]` (ENABLES edges targeting spine nodes, deduped, `MAX_ENABLES=24`) in ONE round-trip; **retired the vestigial `full` param/branch** everywhere (tool def, dispatch, prompt, `mod.ts` signature); `agent.ts` `harvestResult` now allowlists `enables` cites. `graph_test.ts`: swapped the obsolete `full` test for an `enables`-contract test. **22/22 lib tests green; all edge fns + dev.ts type-check.**
- **Frontend chain REBUILT** (`app.js`/`app.css`/`index.html`): **band layout fully retired** — one deduped vertical spine in the rail; node **degree badges** (`·N`); the **"show preconditions (+N)" toggle** (progressive disclosure — ENABLES web dimmed/indented, sticky, default-collapsed); **hover-peek** (node hover lights its edges); **click-dossier on every node** via a NEW keyless endpoint **`web/netlify/edge-functions/node.ts` `/api/node?slug=`** (wired into `dev.ts` local + `netlify.toml` deploy).
- **Unified quote system** (the big Matt-driven addition): one **`bookQuote()`** component renders quotes identically in the answer prose, chain edges, preconditions, and dossier. **`prettyCite()`** turns any cite token/path → a clean chapter label ("ASOS Arya 11"); folded into `cleanQuote` (also strips baked file-paths, wiki markup, backticks, and internal `book-cite overlay`/`harvest row` provenance tails). Bloodraven now wraps spoken quotes in a **`[[q|text|speaker|source]]` marker** (SYSTEM_PROMPT change; relaxes "no cites in voice" for quotes only) → a **streaming-safe `renderProse`** turns them into styled, sourced pull-quotes; the cite-gate validates the in-prose sources (`unverifiedCites=[]`). Graceful degrade when speaker/source absent; `stripEdgeQuotes` kills the double-wrapped `"”`; source de-duped when baked into both attribution + cite.
- **Persona polish** (`agent.ts`): forbid "chain"/"link" in prose + the counting-formula opener; **NO markdown** (bold/headers/lists) — flowing paragraphs + pull-quotes only (a **cold subagent** picked "drop the bold" over "render it": bold headers read as an AI-assistant tic clashing with the austere record-keeper voice, and the pull-quotes already carry the rhythm).
- **Two latent S176 bugs fixed during verify:** `el()` only flattened children one level → nested arrays rendered `[object HTMLDivElement]`; `.dossier-overlay{display:flex}` beat the `[hidden]` attr → empty overlay dimmed the whole page.
**Decisions:** Quotes render in ONE unified style everywhere INCLUDING the answer prose (Matt: "exactly how I want quotes displayed everywhere, in prose"). Source = clean chapter only ("ASOS Arya 11"), never a file path/line. Bloodraven emits `[[q|…]]` markers (the renderer stays markdown-STRICT on purpose — stray markdown shows as an obvious blemish in testing rather than being silently prettified). No "chain"/"link" in his voice; no markdown formatting. `full`-chain retired (UI gets the fuller ENABLES set for the toggle; model always narrates the clean spine). Lesson logged: write JS/TS with ASCII delimiters + `deno check`/`node --check` before reload (the Sonnet smart-quote trap).
**What's next:** the **chat-UI REFINEMENTS pass** → `progress/continue-prompts/2026-07-01-chat-ui-refinements.md` (`/continue chat-ui-refinements`, **Sonnet 4.6**): (1) **genealogy/family-tree queries hit the loop bound** ("Targaryen family tree from Aegon" → `loop-bound-hit`, thin answer — no tree-traversal mode; the model fans out via `neighbors` and exhausts `MAX_TOOL_ITERATIONS`); (2) **fuzzy resolve on marquee names** ("Aegon the Conqueror"/"Targaryen dynasty" → fuzzy to aegon-i/ii/iii, not an exact hit on `aegon-i-targaryen` — alias-map gaps); (3) **loop-bound UX** (empty answer above the status line — confirm a partial answer always renders); (4) **(bigger) a real family-tree view** — genealogy is a distinct shape from the causal spine. **PLUS Matt's own DESIGN refinements (TBD — he'll bring them next session).** Gated ship (deploy private→Netlify + `ANTHROPIC_API_KEY`) still pending Matt's go-ahead.

### Session 178 — Chat-UI family-tree traversal + genealogy-conflation discovery — [Track: meta/graph] (2026-06-30)
**Detail:** `history/session-details/session-178.md`. **Model:** Opus 4.8 (handoff recommended Sonnet 4.6). **Small API $** — live Sonnet 4.6 curl/preview verification (~cents); **ZERO graph writes (all read-only)**; harvest queue at 0.
**Changes made (all `web/` + docs — no graph mutation):**
- **NEW `familyTree(slug)` traversal** (`src/lib/graph.ts`+`types.ts`+`mod.ts`): BFS over `PARENT_OF` (both dirs) + `SPOUSE_OF`, spanning-tree, capped 64 + `truncated`; wired `agent.ts` `family_tree` tool + MANDATORY-lineage prompt steering; **persona DROPPED for lineage answers** (one-line caption; the chart is the answer). Fixes the `loop-bound-hit` on "Targaryen family tree from Aegon" (one tool call now, not 6 `neighbors` fan-outs).
- **Render** (`app.js`/`app.css`): rail generation-ladder card + a **left-to-right SVG descendant tree drawn into the chat**, auto-centred on root, every person clickable → existing `/api/node` dossier. (First cut top-down/7600px-wide → flipped to LR.)
- **Prominence signal** (`degree + 4·quoteCount`) on every member — Dany 402, Rhaegar 118, Viserys 102, Daemon 68 vs stubs ~1. Lets the render foreground book-present characters.
- **Static fixture** `web/dev/family-tree-fixture.html` (real-node SVG, no API) previewing prominence-tier highlighting — built, not browser-verified (frontend paused).
- **28 lib tests pass** (+6 familyTree/prominence). Audit doc `working/family-tree-genealogy-audit.md`.
**Decisions:** Genealogy = its own traversal + render shape, persona-free (Matt: "ignore the bloodraven persona on this"). Sigils PARKED (blazon TEXT in wiki infoboxes — 362/633 houses — not images). **Graph-parentage cleanup is a NEW track**: the feature EXPOSED that repeated dynastic names collapse onto bare-name slugs — **88 of 1,015 parented nodes have >2 PARENT_OF parents** (Targaryen 26, Stark 19…). Approach DECIDED with Matt: **Python deterministic proposal → agentic batch review vs LOCAL wiki+chapters** (he can't hand-review 88+; policy-gated); mostly delete-only dedup (disambiguated nodes already exist + wired). **Incident/lesson:** I wrongly claimed "Maester Aemon has no node"; Matt caught it — he has `aemon-targaryen-son-of-maekar-i` (16 quotes); I'd checked the wrong slug. Next session cold-reads/re-derives before executing. Frontend polish (pop-out, prominence-wiring, fuzzy-resolve) PARKED.
**What's next:** LIVE → `progress/continue-prompts/graph-parentage-cleanup.md` (`/continue graph-parentage-cleanup`, **Sonnet 4.6**): cold-read + validate plan → read-only `scripts/audit-parent-conflation.py` classifying the 88 → agentic batch verify vs local wiki+chapters → apply after review + rebuild bundle/indexes; also resolver prominence-ranking + epithet-alias backfill. PARKED → `archive/2026-07-01-chat-ui-frontend-remainder-PARKED.md` (frontend). Task chip open: `aerion-targaryen` two-Aerions split (folds into the cleanup track).
