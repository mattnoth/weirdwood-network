# Archived TODO Blocks — 2026-06-11

> Moved **verbatim** from `working/todos.md` during the 2026-06-11 todos cleanup
> (fable-audit session; punch-list items 8–10 at `working/audits/fable-audit-2026-06-11/doc-rot-punch-list.md`).
> Rule applied: **nothing deleted, only moved or collapsed.** Every block below was removed from
> `working/todos.md` and preserved here exactly as it stood. Items that were still open were either
> kept verbatim in `working/todos.md` (with a status tag) or collapsed there to one-liners pointing here.
> This file is a frozen historical record — do not edit; append-only via new dated archive files.


---

## Original file header (replaced by the restructured header)

# Actionable TODOs

> **Purpose:** Tasks that need doing but aren't part of the current session's work. Organized by topic. Check items off as they're completed. Move completed items to a "Done" section at the bottom periodically.


---

## From `## Model-fit Policy` — completed items

- [x] **Pass 1 model-fit smoke test (Opus → Sonnet 4.6) — OBSOLETED 2026-05-06.** Trigger was "after ASOS lands and ADWD is the only book remaining"; both books completed on Opus before the smoke-test ran. Pass 1 now done across all 5 books on Opus. The forward-looking model-fit question is now about future passes (dialogue, meals, mention-index, prose-edge-classifier, etc.) — see standing rule above. If any future Pass 1 re-run is contemplated (schema change, etc.), revisit Sonnet then.
- [x] **Audit existing agent prompts for model-fit drift (resource-conservation pass)** — Done 2026-05-06 (Session 37). Report at `working/audits/agent-model-fit-2026-05-06/execution/agent-model-fit-report.md`. 27 agents audited: 6 Opus → Sonnet (mechanical-extractor, wiki-ingester, citation-validator, orphan-edge-finder, prose-edge-classifier [smoke-gated], schema-drift-auditor); 2 → Haiku (status-reporter, duplicate-detector); 2 keep Opus (cross-identity-detector + reviewer); 3 already correct; 13 STUBs deferred. Recommendations are advisory — no agent prompts modified.


---

## `## NODE/EDGE RECOVERY — RESOLVED (Session 72)` — entire section. Resolved S72/S73; postmortem: `history/session-details/session-072.md`. The one open item (skeleton-untrack DEFERRED DECISION) is kept live in todos.md.

## NODE/EDGE RECOVERY — RESOLVED (Session 72, 2026-05-25→26)

> **Correction (CLAUDE.md #9):** the S71 "~7,251 unpromoted node backlog" was a **FALSE ALARM** — a file count without a slug check. Slug intersection: 7,039/7,047 staged skeletons already promoted; only ~8 net-new (all singular/variant dups of canonical nodes → NOT promoted). The node layer was whole. Detail: `history/session-details/session-072.md`. Memory: `project_s72_node_gap_false_alarm`.
> Remaining hygiene RESOLVED/DEFERRED S73 (continue prompt deleted): worktrees removed; scripts folderization deferred; 27 comention scripts kept (revival lever); scratch left tracked per Matt; skeleton-untrack deferred to its own decision (see new bullet below).

- [x] **Node accounting** — no backlog. 1,307 skeleton↔node "conflicts" = stale staging vs canonical (richer) nodes → no action; do not re-promote (would downgrade).
- [x] **Real "entities missing" gap = the INDEX** — `graph/index/` covered only 5/21 categories; extended `build-entity-indexes.py` + rebuilt (+1,861 `*.index.json`). Edge scripts read `graph/nodes/` (not the index), so the index is traversal/query only — decoupled from edge results.
- [x] **Edge re-validation** — type-contract fixed (COMMANDS accepts faction/house unit targets); `refine-v1-edges.py` now passes `slug_category_index` (category contracts had never fired). Edges v1→v1.2 (3,842→3,825).
- [x] **Resolver title-person pass** — ship/artifact/title nodes named after people (`lord-tywin`=Cersei's dromond) remapped to characters via a category-aware exact rung (`resolved-title-person`) + `CAPTAIN_OF`/`CREW_OF` target-not-character guard. Edges v1.2→v1.3 (3,825→3,811). 814 tests green.
- [x] **Folder reorg — RESOLVED/DEFERRED (S73):** 2 leftover worktrees REMOVED (+2 merged `claude/*` branches). `scripts/` folderization DEFERRED indefinitely (cosmetic; high import-coupling risk: `tests/_helpers.py:load_script` loads by flat filename + `stage4-*`↔`wiki-pass2-*` cross-bridges via hardcoded `_REPO/"scripts"/"<name>"`). 27 comention/wiki-prose-edge scripts KEPT (superseded-not-dead; future recall lever — **do NOT re-propose archiving**). Only stray file: `working/wiki/classify-mormont-edges.py`.
- [x] **Scratch (0.1) — Matt's call (S73):** Matt chose to LEAVE the 2 scratch files git-tracked ("ignore scratch files"). Not untracked. (`.gitignore scratch*` doesn't retroactively untrack already-tracked files; revisit only if Matt asks.)
- [ ] **DEFERRED DECISION (surfaced S73) — skeleton-untrack:** `working/wiki/pass2-buckets/` has **23,081 tracked files** incl. **7,180 stale `skeleton/*.node.md`** (~28 MB, S72-verified redundant w/ `graph/nodes/`). `git rm --cached` would de-bloat the repo but it's entangled — ~24 `wiki-pass2-*` promotion scripts read `skeleton/` dirs. Treat as its own focused decision; 28 MB isn't urgent. (CLAUDE.md #9: the prior continue prompt's "gitignored" claim was false — only `pass1-derived/` is ignored.)
- [x] **Health-check lesson** — use a SLUG INTERSECTION (staged-vs-promoted by slug), not a file count; a file count is exactly what produced the false alarm.


---

## From `## Stage 4 — Haiku Cutover Prep` — section header + intro, the Pass-1 pivot/spine record (SHIPPED S66–S67; collapsed to a pointer in todos.md Track 3), completed GRAPH exercise follow-ups (S75), and the Plate-5 parent done-record (SHIPPED S87; live sub-items kept in todos.md Track 3)

## Stage 4 — Haiku Cutover Prep (NEW 2026-05-18 — Session 55)

> **Why this section exists:** Stage 4 v1 bulk run on Sonnet projects ~$2,800 / ~340h / month-locked-up at solo-account pace (75/1,089 batches done as of 2026-05-18). Haiku 4.5 at ~$800 / ~170h is the target. Prior Haiku smoke (Session 54) failed with ~80% semantic drift, but ran against an un-locked vocab + un-flagged soft fallbacks — the smoke wasn't fair. Five-step prep below ports the bulk to a runtime where Haiku has a fair shot.
> → continue: `progress/continue-prompts/2026-05-23-stage4-pass1-finishing.md` (Stage 4 spine + LLM tail DONE S67 = **5,219 book-pass1 edges**; finishing work = resolver levers + tail cleanup/dedup/merge)
> Recommended model: prep STEPs 1–5 complete (Sessions 56–59); Session 60 built the normalizer + no-silent-drop pipeline; the run phase is an **Opus 4.7 conductor** — see the continue prompt.

- [~] **STAGE 4 — PIVOT: Pass-1-derived deterministic edge pipeline (ACTIVE — supersedes the wiki-comention / Haiku-bulk tasks below)** — NEW 2026-05-22 (S65); **deterministic SPINE BUILT + committed S66 (`047e49b3b`)**. Use Pass 1 `## Relationships Observed` tables as the candidate source; DEPRECATE the 29,259-candidate wiki chapter-summary comention pass. Spine = `scripts/stage4-pass1-edge-candidates.py` + `stage4-pass1-evidence-locator.py` + `stage4_name_resolver.py` → **2,818 typed, ~99%-cited `book-pass1` edges at zero LLM cost** (278 tests green; validator clean; conform 0 drift). Honest recalibration: resolution (not typing) was the wall — 38% of rows → edges, not the design's ~50%; first-name enrichment 2.7×'d the naive yield (1,035→2,818); two misresolution bugs (generic role-words; title-first-token→ser-pounce) caught by spot-audit, fixed. **S67 DONE:** (A) conservative alias recovery applied (+16 edges via 13 vetted single-referent aliases in `pass1-derived-supplementary-aliases.json`); (B) **LLM tail RAN** (Sonnet via `claude -p`, `scripts/stage4-tail-classifier.py`): **2,385 typed edges (78%, $20.88, 0 failed)**, `typed_by: sonnet`, in `_tail-typed/`; (C) **133** comention files deprecate-stamped in-data (`scripts/stage4-deprecate-comention-stamp.py`). **Total book-pass1 = 2,834 deterministic + 2,385 tail = 5,219 edges.** **NEXT (finishing — see continue prompt):** (1) 2 resolver levers (full-surname rung ~72; index-pollution filter ~417 — **Matt's "how aggressive" call**); (2) tail-violation cleanup (21/2,385, 0.88% — 6× HOLDS_TITLE→place, 4× ENCOUNTERS verb-gate, 1× qualifier, ~6 node-type bugs); (3) tail dedup; (4) merge `_tail-typed/` into the canonical edge set; (5) optional book-pass1 validator schema. → continue: `progress/continue-prompts/2026-05-23-stage4-pass1-finishing.md` (**Sonnet 4.6** deterministic; **Opus 4.7** lever-decision only). Carry-overs: throwaway `classify_*` cleanup ON HOLD (S66); **decide whether to commit S67 (all uncommitted).**
- [x] **GRAPH — exercise follow-ups (S74 → DONE S75, 2026-05-26)** — Built both $0 deterministic tools: (1) **conflict-pair audit** `scripts/graph-conflict-pairs.py` (+tests) → `working/wiki/data/graph-conflict-pairs.{md,jsonl}`, **32 flagged pairs** (mostly temporal arcs, not errors); (2) **`scripts/graph-query.py`** EXTENDED (S39 node-inspection preserved) with `--neighbors`/`--path`/`--health` over `edges.jsonl`. 920 tests green; 0 orphans confirmed. **Finding:** conflicts are dominated by legitimate temporal arcs → real fix is **edge temporal-scoping** (deterministic; edges carry `evidence_book`+`evidence_chapter`). Folded into the Events enrichment track below.
- [x] **EDGE/EVENT MODELING — ALL PLATES SHIPPED INCLUDING PLATE 5 (S82-S87, 2026-06-09).** Plate 5 (S87, Opus 4.7, autonomous after Matt's explicit "promote them" go-ahead) folded all staged work into canonical `graph/edges/edges.jsonl`: **3,811 → 4,757 (+946)**; `graph/nodes/events/` **371 → 583 (+212 mints)**; `_conflicts/` **+6** (1 aerys + 5 collision losers). Validator: **4,725 kept / 32 dropped** (all SUB_BEAT_OF empty-evidence — Plate 4 Pass-B/Pass-C inference-only emissions; rows remain in JSONL as read-only audit). Red Wedding smoke test PASSES (`--neighbors red-wedding` shows 8 SUB_BEAT_OF; 2-hop traversal surfaces AGENT_IN/VICTIM_IN/COMMANDS_IN/LOCATED_AT with verbatim quotes). Field-name fix applied in-place during merge (Plate 4 staging used `quoted_evidence` → renamed to canonical `evidence_quote` across 51 rows; 19 retained text, 32 got explanatory `plate5_evidence_note`). Phases merged: Plate 0 (10 normalizer flips + 3 aerys repoints), Plate 2.5 (27 wiki schema retypes + 12 drift retypes + 4 high-conf collision merges), Plate 4 cluster (51 SUB_BEAT_OF + 2/3 DUPLICATE_OF), Plate 3 (217 mints with `title:`→`name:` rewrite + 897/914 role edges + 55 supersede stamps), S77 carryover (2 LOVES drops + 21 ASSAULTS→ATTACKS retypes). Backup: `graph/edges/_regrounding/edges-pre-reification-2026-06-09.jsonl`. Script: `scripts/plate5-merge.py`. Diff doc: `working/edge-modeling/plate5-merge-diff.md`. Commit: `435330d69`.


---

## Completed post-Plate-5 followups #7 and #8 (S89 overnight)

  - [x] **POST-PLATE-5 followup #7 — `graph-query.py --event-participants <hub>` primitive (S89 overnight, COMPLETE).** Unions SUB_BEAT_OF children's role edges. `cmd_event_participants()` added to `scripts/graph-query.py` (~110 lines). All 4 smoke tests pass: red-wedding (8 beats, 29 role edges, 13 distinct participants — Walder Frey COMMANDS_IN 7 beats, Roose+Frey AGENT_IN, Catelyn+Robb VICTIM_IN); tourney-at-harrenhal (clean "no beats" message); nonexistent slug (clean "hub not found"); --json (valid). Results: `working/session-results/2026-06-10-overnight-event-participants.md`.
  - [x] **POST-PLATE-5 followup #8 — Event-alias-resolver (S89 overnight, COMPLETE).** Built per S86 design (deterministic lookup-only, no LLM): `scripts/event_alias_resolver.py` + `working/wiki/data/event-alias-lookup.json`. 876 unique phrases indexed, 1 (correct) collision (`conquest-of-dorne` ambiguity). 7/9 smoke tests HIT (Red/Purple Wedding, Blackwater, Robert's Rebellion, Sack of KL, Harrenhal tourney, Doom of Valyria); 2 MISS are correct ("Ned's execution" miss auto-resolves when #10 rename lands and resolver is rebuilt; "the Trident" needs editorial `aliases:` entry if shorthand should resolve). Results: `working/session-results/2026-06-10-overnight-alias-resolver.md`.


---

## Completed post-Plate-5 followup #10 (S91 rename batch)

  - [x] **POST-PLATE-5 followup #10 — Action-slug rename batch (S89→S91). PRIMARY + 9 SECONDARY APPLIED 2026-06-10 → 2026-06-11. Two structural restructures DEFERRED to S92.** Script `scripts/rename-event-node.py`. **S91 (2026-06-11) executed by Opus 4.7:** 9 renames applied = 5 clean (Sand Snakes, Slynt, Dontos, Symon, Kerwin) + 2 subagent-rec flagged (cersei-orders-executions → execution-of-the-blackwater-deserters; qhorin-orders-ygritte-s-execution → jon-spares-ygritte) + 2 ambiguous-flagged subagent-rec (cersei-orders-osney → cersei-s-plot-to-assassinate-jon-snow; lord-wyman-orders-arrest → wyman-publicly-arrests-davos-at-white-harbor). 1 aliases-only treatment (ned-orders-janos-slynt — subagent rec KEEP slug). Plus 3 curator pilot edges (1 BETRAYS + 2 DECEIVES per deception-edges feasibility study confirming DECEIVES already in locked vocab at 11 live instances). `edges.jsonl` 4,757 → 4,760 (+3 curator pilot edges). 10/10 alias-chain probes HIT; all 9 renamed slugs retain full role-edge sets via `--neighbors`; `--health` clean. Bug 1 + Bug 3 workarounds applied per rename (3 plate5_superseded_note free-text patches: Slynt, Symon, Qhorin). Full session details: `working/session-results/2026-06-11-rename-execution.md`. **Deferred to S92 (structural — bigger than rename):** lord-wyman-orders-execution arc (new parent + SUB_BEAT_OF + DECEIVES + SECRETLY_ALLIED_WITH) + jaime-sheathes-his-sword arc (consolidate with sibling jaime-lannister-ambushes-ned-s-party under new parent attack-on-ned-stark-in-the-streets-of-kings-landing). Subagent decision packets reproduced verbatim in `progress/continue-prompts/2026-06-12-deferred-structural-restructures.md`. **Side-asks satisfied inline:** Tyrion-Symon verified (ASOS Tyrion IV — Bronn as agent), Kerwin source backed up (ADWD Iron Suitor + wiki canonical), WIELDS longclaw → execution-of-janos-slynt edge already existed (rename script auto-updated target_slug). **Script-hardening candidates (optional upgrades):** (1) `slug_to_name()` doesn't handle `-s-` infix → `'s` conversion (Cersei-Osney "Cersei S Plot" auto-derived name had to be manually fixed); (2) Bug 3 script doesn't rewrite plate5_superseded_note free-text (3 hits manually patched); (3) Bug 1 alias parser only handles inline form; (4) status-field flipper (renamed nodes still say "Staging only minted-plate3" though Plate 5 merged).


---

## Stale/duplicate continue pointers from the followup block — the Mode 3 dip pointer's "Unblocked / parallel-safe" claim predates the 2026-06-11 resequencing (dip now runs AFTER the infobox merge; live pointer kept in todos.md Track 2); the Plate-4-disposition pointer is a duplicate of the one kept under the Track 4 backfill entry

  → continue: `progress/continue-prompts/2026-06-11-phase2-mode3-dip.md` (**Opus 4.7**) — Phase 2 Mode 3 grounded-agent dip. Unblocked. Parallel-safe with the structural restructures.
  → continue: `progress/continue-prompts/2026-06-05-edge-modeling-plate-4-haiku-disposition.md` (**Opus 4.7** bucketing-review; **Sonnet** filter pass) — Track B sub-step: re-bucket the 1,617 Haiku bulk under the reify lens before promotion; absorbs the S81 NO-GO. Still queued post-Plate-5.


---

## Stale continue pointer under the backfill entry — Plate 5 merge prompt (Plate 5 shipped S87)

  → continue: `progress/continue-prompts/2026-06-05-edge-modeling-plate-5-merge.md` (**Opus 4.7** deterministic merge — upgraded from Sonnet S86) — first plate to write to canonical `edges.jsonl`; backs up to `_regrounding/`; folds in the 3 S77 core-cleanups (cersei↔tyrion LOVES drop, ASSAULTS→ATTACKS retypes, OWNS→BONDED_TO for direwolves/dragons).


---

## Events-Haiku bulk escalation entry (S80–S81 NO-GO borderline; absorbed into backfill Track B re-bucketing / Plate-4 disposition — collapsed to a pointer in todos.md Track 4)

- [~] **GRAPH — edge enrichment with EVENTS (BULK COMPLETE S80; STEP-1 DRIFT AUDIT NO-GO S81 — borderline; chain halted, awaiting Matt's escalation pick) — NOTE S82: partially superseded by cleanroom track above.** Bulk: 1,617 typed + 14,884 rejected over 16,502 candidates at `_events-haiku-bulk/`, single prompt_sha `d31ca56c4768`. Drift audit (S81, $0.93): triple 48 % / pair 56 % vs 70 % / 85 % gates. Fresh-eyes pressure-test: ~11/22 REJECTs are clear Haiku drifts (Rule 4a hint-as-evidence / V5-R2 / Rule 12 co-presence); ~3-4 clear Sonnet over-rejections (judge_idx 2/6/14/16); adjusted ≈ 56-70 %, **No-Go stands but borderline**. The S69 smoke-comparison in the audit was mis-cited (actual session is S77; it measured *hand-read precision on fresh candidates*, NOT *Sonnet-judges-Haiku-emit* — different metric on different sample). Matt's 3 up-front decisions still hold IF promotion happens: full re-merge / Events-only v2.0 now / reject_reason deferred. Audit script `scripts/events-drift-audit.py` (sha `576cc815649c`) is throwaway-single-purpose. Reports: `working/audits/events-haiku-bulk-2026-05-29/{analysis.md (S80), cross-model-audit.md (S81)}`.
  → continue: `progress/continue-prompts/2026-06-01-events-bulk-escalation-pick.md` (**Opus 4.7** for the pick) — **NOTE: cleanroom reframes this; the free A-E choice is no longer accurate. Treat the cleanroom-execution prompt as the primary; this one is reference for the 5 path descriptions.** Matt picks one of 5 paths in cross-model-audit.md §6: (A) re-run Sonnet ~$340, (B) long-tail-only, (C) Sonnet-filter named-types ~$2-5 (audit recommends), (D) v6 Haiku, (E) abandon for v2.0. The 7-step chain at `progress/continue-prompts/2026-05-31-events-v2-promotion-chain/` is halted.


---

## From `## Stage 4 — Haiku Cutover Prep` — remaining block: S76 core cleanup (items 1–2 confirmed folded into Plate 5; item 3 OWNS→BONDED_TO unverified — a VERIFY item is kept live in todos.md Track 3), S77 ops wrapper (done), S68–S74 recall expansion (RESOLVED S74 — enrichment NO-GO, core shipped; Dialogue/Events tails stay shelved pending the Mode 3 dip), Haiku-cutover STEPs 1–5 (all done), and the comention-bulk run items (SUPERSEDED — wiki-comention bulk deprecated S65, do NOT relaunch): Opus-conductor run item, lever fixes, candidate-enrichment pipeline, bulk relaunch, F5 deferral, prompt-hardening, test-suite bootstrap

- [ ] **CORE CLEANUP — 3 gated edge fixes (S76; await Matt before/after sign-off; do NOT touch `edges.jsonl` without it):** (1) drop 2 `cersei↔tyrion` LOVES mis-types (3,811→3,809; sarcasm/accusation); (2) **retype ~22 physical `ASSAULTS`→`ATTACKS`** (`ASSAULTS`=sexual only per architecture.md:233; ~22/32 core ASSAULTS are physical violence — jaime→bran shove, cersei→eddard slap, etc.; root cause: spine phrase→type map predates the split → ALSO fix the map; build a deterministic flagger for ASSAULTS-quotes-lacking-sexual-indicators); (3) merge-time `OWNS→BONDED_TO` for direwolf (ghost/grey-wind/lady/nymeria/summer/shaggydog) + dragon (drogon/rhaegal/viserion) targets — swords/horses stay OWNS. v5 Events prompt already obeys the ASSAULTS rule (0 emitted).
- [x] **OPS — sleep-until-reset wrapper — DONE (S77).** `scripts/stage4-events-bulk-run.sh` (paced auto-resume; resumes walls via `--skip-existing`, exit-42→1h sleep) + classifier `--sleep-between` (chunked, stop-file-aware pacing) + `--validate-every`/`--reject-rate-floor` (mechanical drift check → hard-halt **exit 43**). Hardened vs the historical "sleeps-but-never-restarts" bug via `sleep_with_stop_check` (pattern from `stage4-haiku-loop.sh`); Ctrl-C(130) terminal; `MAX_ITER=200`. Runbook `working/runbooks/stage4-events-haiku-bulk.md`. 1072 tests green.
- [x] **STAGE 4 — RECALL EXPANSION (Sessions 68-69) — RESOLVED S74 (2026-05-26): enrichment NO-GO, core SHIPPED.** The 5,219-edge spine mines ONLY `## Relationships Observed`. **S70: graph/edges/ v1 LANDED** (commit `c3880e160`). **S72: edges v1.2→v1.3** (3,811). **S74: locator hint-grounding + `:11` line-number fix; out-of-sample smokes 74.5%/62.5% (<75% gate) → enrichment SHELVED (v5 precision rules kept-not-run); core citations re-grounded (`scripts/stage4-reground-core-citations.py`), shipped `63b8b461a`.** Gate result: `working/wiki/data/pass1-derived-enrichment-gate-result.md`. Historical prior status:
  - **(S69 SMOKES — DONE, HELD AT $270 GATE)** Built Events/Info/Food candidate generators + locator-anchoring + `--output-dir` safety (273 tests); wrote 32,194 untyped candidate rows; smoked Dialogue + Events/Info/Food (~$3.60) → both **SYSTEMATIC** (~60-66% strict precision; reject ~90%; Events fan-out ~18% / direction ~7% / bare-slug ~15%). Full run re-baselined **~$270-290 + ~3-4 days** → NOT launched. **3 $0 fixes BEFORE re-smoke:** (1) prompt vocab-restriction + anti-patterns (INFORMS ~100% wrong, ADVISES/MANIPULATES/SUPPORTS/ALIAS_OF over-typed, uniform Tier-1); (2) generator direction-validation + slug-quality gate (= the endpoint filter — also cleans the 529 Hospitality edges); (3) `candidate_kind` provenance (`stage4-tail-classifier.py:502`). Then re-smoke (~$4, ≥80% target) → scoped full run (Events+Dialogue; defer Info; Food separate) via parallel wrapper → **FORMALIZE into `graph/edges/`** (the milestone; absorbs S66/S67 merge/dedup/resolver-lever finishing work). Reports: `working/wiki/data/pass1-derived-smoke-report.md`.
  - **(1a) Mine all relational Pass 1 tables — DONE.** `scripts/stage4-pass1-extra-tables.py` (`--extra-tables`, staging `_extra-tables/`, canonical spine untouched) + 81 tests (**431 green**) + report `working/wiki/data/pass1-derived-extra-tables-report.md`. **529 deterministic $0 edges** from Hospitality (460 GUEST_OF + 69 VIOLATES_GUEST_RIGHT; Red Wedding verified). Dialogue → 4,422 tail rows (~$30 to type). Food/Events/Info counted-only (1,263 / 8,384 / 5,654). **NOT merged — inherit `all-for-joffrey`-class endpoint noise; need endpoint filter first.**
  - **(recall-sample) — DONE.** `working/wiki/data/pass1-derived-recall-sample.md` (7 chapters / 196 rels): **A 64% / B 28% / C 9%**. **KEY TENSION:** bucket-B productivity ranks Events & Actions > Information Revealed > Hospitality > Dialogue, but the miner emits cheap deterministic edges only from Hospitality (#3); the #1/#2 tables are prose-shaped (need ~$95+ LLM pass); Dialogue ($30) is lowest-yield. So 1a-as-built = A + Hospitality (~free), NOT the implied ~92%.
  - **(R1 NEXT) merge the 529** after the endpoint filter (= Track-1 resolver levers — do once for spine + these edges); promote per `graph/edges/` convention; spot-audit.
  - **(R2) Dialogue tail:** smoke ~200 rows before the $30 — needs Matt's OK + ENCOUNTERS Rule-6 gate in prompt.
  - **(R3 — Matt's call) bounded Events/Info LLM pass** (~14k rows, ~$95+) — where the real missing recall is; or defer / fold high-value subset into Pass 3. Bucket-C (~3% high-value: Gregor/Aegon, Cersei/Maggy, Dany/Viserys) → targeted narrative-aside audit, NOT a full prose pass.
  - **(b) after 1a:** quantify combined **book ∪ wiki** coverage on a sample (32%-corroborate / 68%-new split shows different territory).
  - **(#2 — fast narrow wiki layer):** wiki STRUCTURAL edges already deterministic via `scripts/wiki-infobox-parser.py` (only prose comention was slow, now DEPRECATED). "Fewer things" = extend the infobox-field→edge map to unmapped fields (`dynasty`, `vassal`, `cadet branch`, `fathers`, `hatched`) — Python, not LLM. Candidate task (Track W).
- [x] **HAIKU-CUTOVER STEP 1 — Finish the vocab lock** — DONE 2026-05-18 (Session 56). Architecture.md applied: 17 new edge types + 2 description mods + vocab callout bumped 132 → 149 + Session-55-second-wave historical note added + gap-filing default flipped to FINAL. Classifier prompt updated: vocab-FINAL default + new types in category list + reverse-direction edges section updated (KNIGHTED_BY/BESTOWS_KNIGHTHOOD_ON and NURSED_BY/WET_NURSE_OF added to both-sided; CHILD_OF/HOST_OF/RESURRECTED_BY/SERVED_BY/DEFEATED_BY/GUARDIAN_OF added to one-sided). 68 gap rows in `working/wiki/pass2-buckets/questions-for-matt.jsonl` closed via `scripts/stage4-close-vocab-gaps.py`. Three additional uncovered types resolved during apply: COMPETES_IN (rejected, duplicate of post-mod FIGHTS_IN), SOLD_TO (rejected, reverse of PURCHASED_FROM), TRANSACTS_WITH (rejected, too-generic); SLAIN_BY_WEAPON/KILLED_WIELDING resolved-pre-adopted under Session-54 KILLED_WITH. Session-results: `working/session-results/2026-05-18-stage4-vocab-applied-and-smoke-prepped.md`.
- [x] **HAIKU-CUTOVER STEP 1.5 — Qualifier vocab lock-down** — DONE 2026-05-18 (Session 57). 17 edge types verdicted with enums (8 Tier-1 REQUIRED + 9 Tier-2 OPTIONAL); 132 Tier-3 (no qualifier). Data sources: 4,152 emit_edges across 21 Sonnet batches (notes empty in 99.7% — drift problem preventive not observed), 7,398 structured Pass 1 `Relationships Observed` rows from 344 extractions, 2,287 wiki-infobox qualifier-bearing rows. **Notes field DELETED ENTIRELY** from schema across all tiers (Matt's "zero freeform" call). 21 already-emitted Sonnet batches preserved as freeform control arm for later Haiku enum-locked comparison — no normalizer. Encoding strategy: **Option C** (new file `reference/edge-qualifier-vocab.md`; architecture.md tables untouched) chosen by fresh-context agent. Decisions: `working/qualifier-vocab/decisions.md`.
- [x] **HAIKU-CUTOVER STEP 1.6 — Encode qualifier vocab + delete notes field** — DONE 2026-05-18 (Session 58). Wrote `reference/edge-qualifier-vocab.md` (8 Tier-1 + 9 Tier-2 edge types with enums, rationale, data source); added one cross-reference line to `reference/architecture.md` `## Edge Types` intro; updated `.claude/agents/prose-edge-classifier.md` — qualifier field in all three emit_edge schemas updated with tier-dependent behavior, qualifier-lookup step added as step 4, Pattern 4 failure-mode callout added (notes = schema violation, Tier-3 qualifier = schema violation). `notes` does NOT appear in the classifier prompt. Session-results: `working/session-results/2026-05-18-stage4-qualifier-vocab-encoded.md`.
  → continue: `progress/continue-prompts/2026-05-18-stage4-qualifier-vocab-encode.md`
- [x] **HAIKU-CUTOVER STEP 2 — Burn `[LINK]` substitution into the candidate data** — DONE 2026-05-18 (Session 58). Investigation: 122,442 queued candidate rows, 100% with `[LINK]` in snippet; 351 completed-batch edges also carry `[LINK]` (freeform control arm, preserved untouched). Architecture chosen: post-processing script `scripts/stage4-resolve-link-placeholders.py` (plan/apply modes, idempotent, bucket-scoped) + inline substitution added to `scripts/wiki-pass2-build-edge-candidates.py` for future runs. Substitution marker: `[LINK]` → `«anchor_text»` (angle quotes avoid collision with other `[text](wiki:...)` links in the same snippet). Applied to all 4,744 queued candidate files (121,310 substitutions). Completed batches (21 Sonnet control arm) left untouched. Session-results: `working/session-results/2026-05-18-stage4-link-substitution-built.md`.
- [x] **HAIKU-CUTOVER STEP 3 — Extend the validator with the type-contract table + qualifier-enum enforcement** — DONE 2026-05-18 (Session 58 / script-builder agent). Three new check classes added to `scripts/wiki-pass2-validate-edge-jsonl.py`: (1) **Type contracts** — 22 edge-type → target-type constraints transcribed from prose-edge-classifier.md § "Type contracts"; target slug resolved via 8,050-slug `build_node_type_index()` from `graph/nodes/`; unresolvable slugs skipped with warning; violation kind `type-contract-violation`. (2) **Qualifier enums** — loads `reference/edge-qualifier-vocab.md`; Tier-1 rejects missing/out-of-enum; Tier-2 rejects out-of-enum only; Tier-3 rejects any non-empty qualifier; violation kinds `qualifier-required-missing`, `qualifier-not-in-enum`, `qualifier-tier3-not-allowed`. (3) **Notes-rejection** — rejects any row (any decision type) with `notes` field; violation kind `notes-field-present`. New CLI flags: `--qualifier-vocab`, `--graph-nodes`, `--skip-type-contracts`, `--skip-qualifier-enums`. Self-test on 21 Sonnet control-arm batches (14,799 rows, 898 files): 193 `notes-field-present`, 149 `qualifier-required-missing`, 2,137 qualifier enum violations (380 not-in-enum + 1,757 tier3-not-allowed), 49 `type-contract-violation`. Old 9-violation costayne subset still caught unchanged. Session-results: `working/session-results/2026-05-18-stage4-validator-extended.md`.
- [x] **HAIKU-CUTOVER STEP 4 — Build the suspicious-edges worklist generator** — DONE 2026-05-18. Script: `scripts/wiki-pass2-flag-suspicious-edges.py`. Implements 6 pattern classes: (1) `knows_as_fallback` — KNOWS without knowing-verb in snippet; (2) `attends_non_event` — ATTENDS targeting non-event.*; (3) `fights_in_non_event` — FIGHTS_IN targeting non-event.*; (4) `killed_by_non_person` — KILLED_BY with non-char/creature/weapon source; (5) `tier3_weak_evidence` — confidence_tier=3 AND snippet < 20 chars; (6) `contemporary_with_char_pair` — CONTEMPORARY_WITH on character-pairs. Output: `working/wiki/data/stage4-suspicious-edges.jsonl`. Full run (72 done batches, 4,075 emits): **288 flagged (7.1%)**. Top patterns: knows_as_fallback 163, contemporary_with_char_pair 73, attends_non_event 30, fights_in_non_event 19. Dominant finding: 82.3% of all KNOWS emits are fallbacks; batch-0020 accounts for 140 of 163 (concentrated Frey-cluster pass). Session-results: `working/session-results/2026-05-18-stage4-suspicious-edges-flagger.md`.
- [x] **HAIKU-CUTOVER STEP 1.7 — Vocab Round 2 (10 new edge types)** — DONE 2026-05-19 (Session 58). 10 new edge types adopted from vocab-completeness audit: SPIES_ON, INFORMS, NAMED_AFTER, STEP_PARENT_OF, STEP_CHILD_OF, IN_LAW_OF (Tier-2), RESCUES, BANISHES, TORTURES, CONSPIRES_WITH. Vocab 149 → 159. Tier-2 count 9 → 10. Sub-qualifiers: 0 adopt (audit confirmed lockdown). Files modified: reference/architecture.md, reference/edge-qualifier-vocab.md, .claude/agents/prose-edge-classifier.md. Decisions: working/qualifier-vocab/decisions.md Round 2 section.
- [x] **HAIKU-CUTOVER STEP 5 — Haiku smoke + separate Haiku worker built** — DONE 2026-05-19 (Session 59). batch-0020 Opus audit re-run (verdict "needs prompt change first" → R1 KNOWS STOP rule applied to the classifier prompt). Haiku could not drive the Sonnet worker harness's batch-bookkeeping (early-exit, wrong-batch claim, asked-human-mid-task) → built a SEPARATE Haiku worker: `scripts/stage4-haiku-run.py` (Python orchestrator — chunking, `--concurrency`, rate-limit detection, provenance) + `.claude/commands/stage4-haiku-classify.md` (thin classify-only prompt, hardened with a CRITICAL RULES section). batch-0020 chunk-10 ×3 parallel = $1.86 / 5.7 min (under Sonnet's $3.42); ~17.5% validator violations (hardening fixed `qualifier-required-missing` 38→0, cut `KNOWS` 60→16; exposed invented type-name variants + type-contract). Decision: Haiku IS the bulk worker, Sonnet off the table (memory `project_stage4_haiku_not_sonnet`).
- [ ] **STAGE 4 RUN — Opus conductor: prevention → residual-resolution → targeted Opus review → scale** — NEW 2026-05-19 (Session 59); reshaped Session 60. Session 60 built the deterministic edge-type normalizer (`scripts/stage4-haiku-normalize-edge-types.py`) + the persistent `unresolved-edges-log.jsonl` + the printed `locked-edge-vocab-159.md` reference, fixed the validator vocab parser (161→159), and locked the 6-stage no-silent-drop pipeline. Remaining: STEP 1 inline the 159-vocab into the classify prompt (prevention), STEP 2 build the 2nd-Haiku residual-resolution pass, STEP 3 targeted self-contained Opus review, STEP 4 validator-appends-to-log, then run→compare-vs-Sonnet→harden→scale to the ~1017-queue. → continue: `progress/continue-prompts/2026-05-19-stage4-haiku-normalize-and-residual.md` (**Opus 4.7** conductor).
- [x] **STAGE 4 RUN — LEVER 2 loop fix: rate-limit reset detection** — DONE 2026-05-21 (Session 62). Bash loop parses `rate_limit_events_count` from each batch's result JSON; if > 0, looks up latest `resets_at_ts` for that batch_id in `rate-limit-events.jsonl` and sleeps until reset+60s. Re-architected `for` → `while idx` loop so re-runs don't advance. Orchestrator: new `--skip-existing` CLI flag → `plan_batch_chunks(..., skip_if_output_exists=True)` filters files with existing outputs. **Re-run on resume** (Matt's call): batch-0013 retries the 18 failed stragglers, skips the 12 done. End-to-end smoke verified. Commit `ecd948f0c`.
- [x] **STAGE 4 RUN — Tier 1 prep: Heavy ENCOUNTERS + KNOWS deprecation + Option C scope** — DONE 2026-05-21 (Session 63). Heavy Rule 6 ENCOUNTERS block (8 bad-pattern categories from overnight verb-gate-failure log + decision flow); KNOWS deprecated (vocab 164 → 163; 82.3% fallback rate; defer to future Pass-1 pass); Option C list built (222 high-value batches). All committed (`bd2d05903`).
- [x] **STAGE 4 RUN — Candidate enrichment pipeline (F1+F2+F3+F5+F7)** — DONE 2026-05-21 (Session 63). `scripts/wiki-pass2-enrich-candidates.py` enriches every candidate row with `target_type` + `evidence_paragraph` (clean prose) + `valid_edge_types` (pre-filtered) + `staging_verbs_present` (verb-gate hint). 141,067 rows enriched in 13.5s; 100% target resolution; 99.9% evidence located. Haiku no longer reads source/target node files. Smoke (batch-0019): 4.6 min vs 9 min baseline (-49%), 2.80% violations vs 3.96% (-29%), $2.73 vs $3.36 (-19%). **~5.5× faster than Sonnet original.** Committed `caf8dcc79`.
- [~] **STAGE 4 RUN — Bulk relaunch — SUPERSEDED 2026-05-22 (Session 65)** by the Pass-1-derived pivot (top of section). The wiki-comention bulk is deprecated; do NOT relaunch it. Continue prompt deleted. (Historical: was Tier 1 = 222 Option-C batches, SLEEP=60/CHUNK=5/CONC=4.)
- [ ] **F5 (locked-vocab compression in classify prompt) — DEFERRED** — Would save ~$5-6 across full bulk run by trimming the inlined 509-line locked vocab to ~150 lines (name + 1-line summary). Not worth 2-4 hrs of delay. The per-row `valid_edge_types` already does most of F5's filtering. Revisit only if mid-bulk evidence shows vocab bloat is hurting throughput or quality. (Session 63 design decision.)
- [ ] **Haiku rule-violation: prompt-harden against unauthorized script creation** — Session 63 discovery: Haiku wrote `scripts/stage4-classify-batch.py` (273-line deterministic classifier) during smoke despite prompt saying "No writes except to the output paths listed below." Add explicit `## ABSOLUTE PROHIBITIONS` block to classify prompt: "DO NOT create, write, or edit any .py files. DO NOT write to scripts/, working/, or anywhere outside the FILE PAIRS output paths. If you feel you need to write a helper script, that is a signal you misunderstand the task — stop and emit your decisions directly to the listed output paths." Low priority — script was deleted; output was still valid; but the pattern is the same Haiku-reinvents-its-own-tooling failure mode that built the Python orchestrator originally.
- [x] **STAGE 4 — Python test suite bootstrap** — DONE 2026-05-21 (Session 62). 71 tests across 4 modules (orchestrator, validator, normalizer, flagger). Stdlib `unittest`, hermetic, no external deps. Three regression-locked historical bugs: vocab parser miscount, normalizer ATTACKED_BY over-reach, notes-field deletion. Run: `python3 -m unittest discover tests`. Commit `e1da3c5db`.


---

## `## Mission Protocol & Orchestration` — entire section (DORMANT since S45–S49; one-line versions kept in todos.md § Dormant)

## Mission Protocol & Orchestration (NEW 2026-05-12 — Session 45)

> Mission = bounded orchestration unit between session-task and full fleet daemon. DRAFT protocol at `working/agent-fleet-specs/mission-protocol.md` (NOT locked). Distinct from Agent Improvements: this is about HOW agents are coordinated, not which agents exist.

- [x] **DONE — First mission: case-collision top-10 reconstruction (2026-05-12)** — `working/agent-fleet-specs/missions/done/2026-05-12-case-collision-top-10.md`. 10/10 workers returned (9 pass / 1 partial / 0 fail); avg conf ~0.89; wall-clock ~6 min via 2-wave subagent orchestration. Outputs land in `working/missions/case-collision-top-10/worker-<slug>/output.md` awaiting promotion to `graph/nodes/`. Postmortem in archived mission file flags 3 protocol lessons: workers should be wave-sized not slug-sized; reverse-lookup must be default; schema validation step required (every worker drifted on timestamps/field names). Watcher was opened then closed mid-mission as redundant for bounded subagent-orchestrated work. → next: protocol refinement (`mission-protocol.md` v0 → v1) before launching follow-up batch for the 115 remaining case-collision pages.
- [ ] **Refine watcher.md v1 → v2** — After first mission lands. Update Matt's-model watcher based on observed friction.
- [ ] **FUTURE — Dispatcher-watcher mode (separate agent file)** — Surfaced 2026-05-12 (Session 45). My original v0 watcher (overwritten by Matt's-model v1 same session) imagined a different pattern: watcher as PROCESS SUPERVISOR that uses the Agent tool to dispatch worker subagents in waves, applies escalation rules proactively, polls signals, manages the dispatch loop. That model is more daemon-adjacent — closer to `fleet-orchestration-plan.md`'s vision. Not appropriate for v1 (Matt orchestrates parallel sessions himself; watcher is a briefing assistant). Future use case: when Matt wants automated orchestration of many parallel workers without manual session-launch — e.g., 50+ worker workflows where opening 50 windows isn't viable. At that point, file a new agent at `.claude/agents/dispatcher-watcher.md` (separate from `watcher.md`) with the original v0 shape: tools include Agent, lifecycle is dispatch+poll+escalate, applies mission escalation rules autonomously. Distinct enough from the briefing-assistant watcher to warrant a separate agent file rather than a mode flag.
- [ ] **Mission protocol DRAFT v0 redline** — After first mission lands. Resolve the 5 open questions in `mission-protocol.md` based on observed friction. Update schema if any field was unused or any field was missing.
- [ ] **Decide mission file git tracking** — Open question 4. Lean: commit (project story benefits). Decide when first mission lands.
- [ ] **Reconcile Track-A/B mapping in next.md** — Session 45 marked Tracks 2/3/5 as DONE based on Matt's mid-session "Track A and B done"; verify which tracks were actually meant.
- [ ] **Add timers to Python scripts (measure, don't estimate)** — Surfaced 2026-05-12 (Session 45 Matt aside). Most scripts (`build-character-indexes.py`, `build-entity-indexes.py`, `wiki-pass2-extract-prose.py`, `wiki-pass2-attach-prose.py`, `wiki-pass2-bucket-a-backfill.py`, etc.) print no wall-clock timing today. Pattern to standardize: import `time` + record `t0 = time.perf_counter()` at script start + print `elapsed = time.perf_counter() - t0` at end formatted as `"Completed in {elapsed:.2f}s"` or `"Completed in {elapsed/60:.1f}m"` for >60s runs. Bonus: per-phase timing for multi-stage scripts (e.g., `extract-prose.py` Stage 3b: load page-index, walk buckets, write outputs — each phase its own timer). Could be a small `scripts/_timer.py` helper. Low priority, pure curiosity / replaces estimates in worklog/todos/next.md with measurements over time.
- [ ] **MED — Spot-check Track A's 60 promoted nodes** — Surfaced 2026-05-12 (Session 49b watcher). Today's Track B spot-check (10 nodes) surfaced 2 orphan-edge defects (20% rate): 2× `ramsay-bolton` alias-not-canonical, 1× `MEMBER_OF: bastards-boys` to non-existent faction. Track A's 60 nodes from sessions 46/47 (`working/missions/case-collision-{top-10,batch-2}/worker-*/output.md` → promoted) share the same worker protocol, so similar defect class is likely. Trigger: run `scripts/orphan-edges-audit.py` (already done 2026-05-12) and use the cat1 TSV to filter Track A's slug list; manual audit covers only the residual. Not HIGH because: batch-2 outputs passed schema-validation per Session 46, Track A nodes went through worker dispatch (Track B 10 didn't), and orphan-edge-finder will catch most residual. **Note:** `bastards-boys` node now exists (Session 50, 2026-05-12) — that specific defect class is resolved.
- [ ] **NEW — Bake session-results write into every future continue prompt** — Surfaced 2026-05-12 (Session 49b-watcher). Convention landed: every continue prompt's final-step list must include "write `working/session-results/<date>-<session-name>.md` with status, headline numbers, what's next" so watchers don't depend on Matt copy-pasting chat-window summaries. README at `working/session-results/README.md` describes the format. General-watcher runbook (`working/runbooks/general-watcher.md`) already updated to check that dir first. Apply this rule to: (a) the Stage 4 continue prompt at `progress/continue-prompts/2026-05-02-stage4-v1-prose-edge-classifier.md` before next run, (b) `/endsession` step 3 when generating future continue prompts, (c) `working/agent-fleet-specs/worker-snippets/case-collision-template.md` — the mission orchestrator's wrap-up (not the per-worker output) should write the session-results file. Could also retrofit existing in-flight prompts but not worth doing as backlog work; just apply going forward.
- [ ] **FUTURE — Richer watcher hookup (session-state.jsonl)** — Surfaced 2026-05-12 (Session 49b-watcher). If session-results files prove insufficient (e.g., watcher needs to know "session is mid-flight" not just "session is done"), upgrade to: each session writes a JSONL line to `working/session-state.jsonl` at start ("running", with continue-prompt path) and at end ("done", with result-file path). Watcher tails it. Could be a `PostToolUse` hook on `worklog.md` writes that auto-emits the JSONL line. Not urgent — the file-convention approach is the minimal unlock.


---

## From `## Agent Improvements` — completed mechanical-extractor and script-builder items

- [x] **mechanical-extractor: test variations** — Done 2026-04-22. Smoke test on 3 AGOT chapters (prologue, bran-01, catelyn-01) surfaced three issues: cross-chapter dramatic-irony leak, no confidence tier convention, missing `first_available` field in metadata. All addressed via prompt patches (see below).
- [x] **mechanical-extractor: chapter isolation enforcement** — Done 2026-04-22. Added dedicated "Chapter Isolation — Critical" section to agent prompt; flipped Rule #5 from "Note dramatic irony" (which encouraged leakage) to "Dramatic irony is NOT your concern"; tightened the `Known To (Reader Only?)` column definition to within-chapter scope only.
- [x] **mechanical-extractor: confidence tier convention** — Done 2026-04-22. All Pass 1 output now treated as Tier 1 by default, with only two explicit exception markers: `(inferred)` and `(uncertain — verify)`. Eliminates per-row tier-tag noise.
- [x] **mechanical-extractor: first_available field** — Done 2026-04-22. Added `first_available` field to Chapter Metadata section of output schema so downstream passes can grep spoiler-gating anchor directly.
- [x] **mechanical-extractor: no meta-commentary in tables** — Done 2026-04-22. Added Rule #9 forbidding the extractor from using table cells to explain its own extraction choices (observed in agot-bran-01 smoke output — "symbolic weight is implicit…" in the Information Revealed table).
- [x] **mechanical-extractor: tighten Raw Entity List categories** — Done 2026-04-24. Prompt now v3: 12 categories (10 named + Other catch-all), strict formatting rules (all headers required, no merging/renaming, "None" for empty). Prior extractions archived (agot-v1, agot-v2, acok-v2). v3 run in progress (30/73 AGOT).

- [x] **script-builder: verify POV table completeness** — Done. Found 6 missing headings (THE REAVER, THE BLIND GIRL, A GHOST IN WINTERFELL, THE IRON SUITOR, THE KINGBREAKER, THE QUEEN'S HAND). All added to reference file and splitter.


---

## `## Direwolves & Dragons` — done (rule encoded in extractor prompt 2026-04-22)

## Direwolves & Dragons

- [x] **Direwolves and dragons are characters, not creatures** — Ghost, Grey Wind, Lady, Nymeria, Summer, Shaggydog, Drogon, Rhaegal, and Viserion must be treated as characters in all extractions and graph nodes. They have agency, POV-adjacent perspectives, and narrative arcs. Added to mechanical-extractor prompt (2026-04-22). Verify AGOT re-run captures them in Characters Present tables, not as fauna. Ensure node schema and future agent prompts also enforce this.


---

## From `## Agent Prompts to Write` — section header + the Pass 2 wiki-ingester item (stale as written: the prompt was written S19 and Pass 2 ran 2026-04-26 → 05-01)

## Agent Prompts to Write

- [ ] **Pass 2: wiki-ingester** — Full prompt. Needs: node file schema, wiki infobox parser output, entity target list from Pass 1, **wiki-category → confidence-tier mapping** (wiki mixes canon, fan inference, and theory on the same pages — the category/tag system helps route to correct tiers, but needs explicit rules).


---

## From `## Wiki / Pass 2 Prep` — section header + completed build/run records (Sessions 16–29), SUPERSEDED comention Stage-4 items (ops polish; prose-derived bulk at 21/201 batches — deprecated S65, do NOT relaunch), and the suspicious-edges flagger item (built as HAIKU-CUTOVER STEP 4)

## Wiki / Pass 2 Prep

- [x] **Track B orchestration planning (PLAN-ONLY session)** — Done 2026-04-25. Output: `working/runbooks/wiki-pass2-orchestration.md` covering 7 design items (work decomposition, concurrency, drift prevention, process metadata, resumption, failure modes, storage). Key calls: triage stage precedes Pass 2; bucket = chapter-equivalent; `wiki-pass2.sh` mirrors `extract.sh`; markdown+JSONL+CSV stays (no DB, with revisit triggers). 7 open questions surfaced for implementation session.
- [x] **Fresh-agent review of wiki Pass 2 orchestration plan** — Done 2026-04-25 (Session 16). 21 issues in `working/runbooks/wiki-pass2-orchestration-review.md`, all accepted. Verdict: needs targeted patches before implementation. Bias note logged: writing+prosecuting agent was the same; future reviews should rotate reviewer.
- [x] **Apply review patches to wiki Pass 2 orchestration** — Done 2026-04-25 (Session 17). 21 decisions applied to runbook + Track B prompt + implement prompt. Self-review caught 4 additional coherence issues (5.1.1 vs 5.4 fingerprint-mismatch contradiction, missing status codes, stale Implementation Sequence, stale `weirwood-wiki` references) and fixed them. Patch + review continue prompts deleted.
- [x] **Build wiki Pass 2 orchestration scripts** — Done across an unwritten-up build session (2026-04-25/26) + Session 18 cleanup (2026-04-26). Final scripts: `wiki-pass2-triage.py` (page categorization + bucket grouping + `--accept` manifest writer), `wiki-pass2-coherence.py`, `wiki-pass2.sh` (10 subcommands), `weirwood.zsh` extended with `wiki` subcommand. Fixed pipefail/SIGPIPE bug in `wiki-pass2.sh` status checks. 507 per-bucket manifests written (35 core, 472 secondary).
- [x] **Wiki Pass 2 launch prep — script review + wiki-ingester prompt** — Done 2026-04-26 (Session 19). Cold script review at `working/runbooks/wiki-pass2-orchestration-build-self-review.md` (3 blockers + 11 correctness issues). Arya bug fixed. Wiki-ingester prompt written (180 lines, mechanical-extractor as model). B1 path-mismatch fixed. Validator built (`scripts/wiki-pass2-validator.py`). `first_available` softened to optional in v1 (spoiler gating deferred to backfill pass).
- [x] **Smoke-test direwolves** — Done 2026-04-26 (Session 20). Silent-exit bug fixed (`scripts/wiki-pass2.sh:286-290` — `&&`-chain → `if`-block; root cause: `set -e` + arithmetic-test-as-final-line returns 1 silently). Smoke ran clean: 6 nodes, 4m 47s, $1.15, validator + coherence green. Hand-inspection surfaced T1 (triage direwolf override hard-codes "Nymeria" → routes warrior-queen page) and P1 (smoke summary doesn't surface questions-for-matt entries) — see follow-up todos.
- [x] **T1: Triage direwolf override needs disambiguator** — Done 2026-04-26 (Session 21). `DIREWOLF_PAGE_OVERRIDES = {"Nymeria": "Nymeria (direwolf)"}` in `scripts/wiki-pass2-triage.py`. Pattern extensible to other collisions (Aegon I/II/V, etc.) when surfaced.
- [x] **P1: Launcher should surface questions/conflicts/contradictions in summary** — Done 2026-04-26 (Session 21). `cmd_run` snapshots JSONL line counts per bucket and computes deltas; surfaced in per-bucket OK line, wave summary footer, and 3 new stats CSV columns.
- [x] **P2: Launcher should write `validation_report` path into manifest** — Done 2026-04-26 (Session 21). Surfaced when Matt eyeballed direwolves manifest line 28. Two-line fix in `cmd_run` (both pass and fail branches). Existing 508 not-yet-run manifests will heal on first run; direwolves backfilled by hand.
- [x] **Scale Wiki Pass 2 core (Stage 1)** — Done 2026-04-27 (Session 23). All 37/37 core buckets complete. 855 nodes promoted (591 chars + 261 houses + 3 factions; 3 video-game-only houses excluded post-emit). 74/74 questions resolved. Validator 37/37 pass. Total Stage-1 cost $95.33 ($2.58/bucket; per-node $0.111 healthy per Stage-2 cold review).
- [x] **Stage 2: Cold review of wiki Pass 2 core output** — Done 2026-04-27 (Session 24). Decision: **remediate** (`first_available` schema drift + silent parser-bug propagation). **Decision overturned same session by Matt:** spoiler gating deferred to post-first-release; remediation prompt replaced. See `2026-04-27-wiki-pass2-stage3-prep.md`.
- [x] **Stage 3: Wiki Pass 2 secondary tier (Python-first pipeline)** — Done 2026-04-28 (Session 26). Full pipeline rebuilt as Python-only after design review surfaced that LLM agent for Stage 3b was inertia-driven, not analysis-driven. Three-script chain delivered: `wiki-pass2-prioritize.py` (Tier A/B/C labels on 472 manifests), `wiki-pass2-emit-deterministic.py` (Stage 3a — 3,315 skeletons emitted), `wiki-pass2-extract-prose.py` (Stage 3b — 2,988 prose files emitted, deterministic HTML→markdown extraction with h2→schema-heading mapping table, h3 book-boundary preservation, cite_ref translation), `wiki-pass2-promote.py` (skeleton + prose concatenation, atomic-rename to `graph/nodes/<type>/`). **Result:** 3,314 new nodes promoted (graph total 855→4,169), $0 cost, ~30 seconds total wall-clock. 0 conflicts. 1 unclassified (`battle-of-the-blackwater-song` type=unknown). Mid-stage agent review ran clean (0 HIGH, 4 MED upstream issues already on this list). Pipeline runbook rewritten at `working/runbooks/wiki-pass2-pipeline.md` (v3). Single-writer-per-file invariant documented at `reference/architecture.md` § "Artifact Formats by Consumer".
- [x] **Orphan edges Cat 1 batch — top-N recovery (2026-05-12, Session 50)** — 7 ALIAS-FIX (added slugs to existing nodes: `crossroads-inn`→inn-at-the-crossroads, `dragons`→dragon, `joffrey-i-baratheon`→joffrey-baratheon, `tommen-i-baratheon`→tommen-baratheon, `vale`→vale-of-arryn, `the-wall`→wall, `giant`→giants). 8 CREATE: factions/blacks, factions/greens, events/age-of-heroes, locations/crypt-of-winterfell, factions/two-betrayers, events/andal-invasion, factions/winter-wolves, factions/bastards-boys. Rebuilt alias-resolver + mention-index. **Delta: Cat 1 1896→1673 edges (−223), 19055 edges now resolve cleanly (+224 vs pre-batch).**
- [ ] **Stage 4 ops polish (Session 53 surface)** — Three items, none blocking the bulk run:
  - **(a) Rate-limit text-pattern detection.** `scripts/stage4.sh` now catches API-level `rate_limit_event` stream-json (ported from `extract.sh`). But Max-plan **session walls** appear as plain-text `"You've hit your org's monthly usage limit"` in the CLI output — NOT structured. Extend grep patterns (`"You've hit"`, `"usage limit"`, `"five_hour"`, etc.) before any launchd auto-resume is safe.
  - **(b) Active-worker detection in `status`.** Workers in the 90-min throttle sleep don't update log files → `status` reports "Active workers: 0" mid-cycle. Cosmetic. Fix via process-list check OR heartbeat sentinel.
  - **(c) Confirm cache-read reduction is real.** Batch-0012 at 1.3M cache_read vs ~21M on multi-tab batches (16x lower). Hypothesis: 1-tab serial keeps Anthropic's prompt cache warm; multi-tab fragments it. Confirm with 3-5 more 1-tab batches before treating $3.42/batch as the new steady-state cost.
- [ ] **Stage 4: Prose-derived edge discovery** — sequential to Stage 3, NEVER parallel. Discovers prose-derived single-page edges, cross-page edges (Reek=Theon SAME_AS, etc.), and Pass-1-derived edges (perception, emotional, POV-internal) that infobox emission can't see. **Pass 1 dependency met 2026-05-06** (344/344 across 5 books). Hard rule: never modify Python-emitted infobox edges (immutable); prose-derived edges go under separate subheading or sibling artifact `prose-edges/<slug>.edges.jsonl` per architecture.md's artifact-format pattern. `prose-edge-classifier` agent prompt is full at `.claude/agents/prose-edge-classifier.md`. **Current state (2026-05-16):** 21/201 batches done under `2026-05-14-stage4-v1-bulk-sonnet` mission. Schema pinned + validator built (Session 54). Quality holding at validator-CLEAN with 0-7% type-contract baseline. Soft-fallback whack-a-mole pattern accepted — durable answer is suspicious-edges worklist for later Opus review (see new TODO below).
  → continue (resume bulk in one terminal): `progress/continue-prompts/2026-05-16-stage4-bulk-resume.md`

- [ ] **NEW — Build suspicious-edges flagger (Matt's idea, Session 54)** — Extend `scripts/wiki-pass2-validate-edge-jsonl.py` (or write parallel `scripts/wiki-pass2-flag-suspicious-edges.py`) to flag schema-clean-but-semantically-suspicious patterns to `working/wiki/data/stage4-suspicious-edges.jsonl` for later Opus review. Patterns to flag: `KNOWS` without explicit "knew/known/learned of/informed/told" in snippet, `ATTENDS` with non-event target, `FIGHTS_IN` with non-event target, `KILLED_BY` with non-person/non-org target, `confidence_tier: 3` rows, `CONTEMPORARY_WITH` with character-pair targets, `TRAVELS_TO`/`LOCATED_AT`/`BORN_AT`/`DIED_AT` with non-place targets, `SPOUSE_OF` with non-character targets, `WIELDS` with non-artifact targets. Produces a worklist Opus reviews in chunks at any future time. **This is the durable answer to "edge schema lockdown"** — we don't lock the prompt, we lock the audit. Spec in continue prompt Step 4. Implement BEFORE resuming bulk if possible.


---

## From `## Wiki / Pass 2 Prep` — obsoleted items (wiki-ingester v2 prose role; byte-equality validator)

- [x] **Wiki-ingester prompt v2 (prose-only role)** — OBSOLETED 2026-04-28 (Session 26). The Stage 3b agent role itself was eliminated when Stage 3b became Python (`wiki-pass2-extract-prose.py`). The wiki-ingester v1 prompt is preserved for Stage 1 re-runs only with a header-note explaining the redesign. Future Stage 4 agent will need its own prompt — different role (cross-page edge discovery, not prose synthesis).
- [x] **Validator edge byte-equality enforcement** — OBSOLETED 2026-04-28 (Session 26). The byte-equality check was needed when an agent paraphrasing the skeleton in `tmp/` was the threat model. With Stage 3b as Python writing to a sibling `prose/` directory (single-writer-per-file invariant), no agent has a write path to the skeleton bytes. The check is structurally unnecessary — concatenation at promotion is the only path that produces final-node bytes.


---

## From `## Wiki / Pass 2 Prep` — resolved items: alias-aware slug resolver (BUILT — `scripts/wiki-pass2-build-alias-resolver.py`, S38–S49), major-houses gap (RESOLVED by the Tier 1/Tier 2 recovery items below it), cross-refs index, Tier 1 + Tier 2 entity recovery, religion-type drift (obsoleted)

- [ ] **Alias-aware slug resolver for cross-references (Stage 4 prep)** — Surfaced 2026-04-28 (Session 26) when `wiki-pass2-build-cross-refs.py` reported 34,782 broken links (42.9% of 81,090). Spot-check showed many "broken" refs are actually slug-mismatch: Stage 1 agent promoted Brienne as `brienne-tarth.node.md` (her aliases include `Brienne of Tarth`), but other pages link to `wiki:Brienne_of_Tarth` → kebab-cased to `brienne-of-tarth`, which doesn't match any node filename. Fix: build a Python pass that walks every promoted node's `aliases` field and emits a `wiki_form → canonical_slug` resolver dict. Update `wiki-pass2-build-cross-refs.py` to consult the resolver before declaring a target broken. Should drop broken-link rate from 42.9% to a realistic baseline (probably 10-15% — true gaps in the graph, not slug-mismatch noise). **Stage 4 candidate generation depends on this** — without alias resolution, we'd skip thousands of legitimate edges.
- [ ] **Major houses missing from graph (Stage 1 triage gap)** — Surfaced 2026-04-28 (Session 26) when cross-refs leaderboard showed `house-lannister` at rank 19 (387 references) but no `graph/nodes/houses/house-lannister.node.md` exists. Investigation: Stage 1's "characters-house-lannister-*" buckets processed *characters belonging to House Lannister* (members), not the *house entity* itself. The wiki page "House Lannister" was never in any bucket. Same gap likely applies to: House Tully, House Tyrell, House Greyjoy, House Martell, House Arryn, House Baratheon (the parent house — the cadet branches `house-baratheon-of-kings-landing` and `house-baratheon-of-dragonstone` ARE in graph). House Stark and House Targaryen ARE in graph (they made it into a houses-* bucket, not a characters-* one). Fix: either (a) re-run Stage 1 triage with house-entity pages explicitly routed to a `houses-major-*` bucket, then run Stage 3 pipeline against those buckets, OR (b) add the missing house pages to a one-off Stage 3 bucket and run the existing skeleton+prose+promote pipeline. Option (b) is cheaper. Estimated: 7-10 missing major houses, ~30 minutes to fix end-to-end. **High priority for graph completeness** — these are the most-referenced entities in the corpus.
- [x] **Cross-references index built — Stage 4 prep complete (informational)** — Done 2026-04-28 (Session 26). `scripts/wiki-pass2-build-cross-refs.py` walks all 2,988 prose files, extracts 81,090 markdown links, emits `working/wiki/data/{cross-references.jsonl, backlink-counts.json, cross-refs-summary.md}`. Top-50 leaderboard validated (all 6 expected major characters in top 20). 42.9% broken-link rate (see alias-resolver TODO above for the planned reduction). Wall-clock: 1.4s. This is Stage 4 Step 1 from the hybrid plan in `progress/continue-prompts/2026-04-27-wiki-pass2-stage4-edge-discovery.md`.
- [x] **Tier 1 entity recovery (major houses, regions, locations, pre-narrative Targs)** — Done 2026-04-28 (Session 26). One-off bucket `houses-major-recovery` with 46 pages: 7 missing major houses (Lannister, Greyjoy, Baratheon, Frey, Bolton, Mormont, Karstark), 27 regions+locations (Westeros, North, Reach, Riverlands, Vale, Westerlands, Stormlands, Iron Islands, Dorne, Beyond the Wall, Crownlands, Free Cities, Slaver's Bay, Essos, Iron Throne, Wall, Red Keep, Castle Black, etc.), 10 pre-narrative Targaryens (Aegon I-V, Aerys II, Aenys I, Viserys I, Daemon, Corlys), Gendry + Gilly. Type-patched no-infobox pages (regions/locations defaulting to `unknown`). Broken links 34,782 → 31,933 (-2,849). Same Stage 3 pipeline.
- [x] **Tier 2 entity recovery (religions, titles, cultures, organizations)** — Done 2026-04-28 (Session 26). One-off bucket `tier2-recovery` with 24 pages: 4 religions (Faith of the Seven, R'hllor, Old Gods of the Forest, Drowned God), 12 titles (Knight, Squire, Septon, Lord, Bastard, Maester, Grand Maester, Regent, Sellsword, King in the North, Prince of Dorne, Hedge Knight), 6 cultures (Ironborn, Free Folk, Wildlings, Dornishmen, Dothraki, First Men) typed as `organization.faction` v1 placeholder, 2 organizations (Brotherhood Without Banners, Brave Companions). Broken links 31,933 → 29,291 (-2,642). Cumulative recovery this session: -5,491 broken links.
- [x] **Religion node type drift — normalize to `religion`** — OBSOLETED 2026-05-11. Originally surfaced 2026-04-28 (Session 26) when architecture.md declared bare `religion` as canonical. Architecture.md has since been updated (likely Session 28 alongside `organization.faction` formalization) to list `organization.religion` as the canonical type (line 83, Type Reference Table). All 63 religion nodes already match the current spec. No drift remains. Closing as a stale-todo.


---

## From `## Wiki / Pass 2 Prep` — agent pipeline plan (written S26; checkbox was never flipped)

- [ ] **Agent pipeline plan written** — Done 2026-04-28 (Session 26). `working/agent-fleet-specs/agent-pipeline-plan.md` walks through all open work, groups by Group A (deterministic Python, no agent), Group B (hybrid Python + agent), Group C (pure agent — Pass 3-6), Group D (Pass 1 books pending). Suggests dependency-driven ordering; estimates total remaining-work cost ~$1,200-1,800. Prominent multi-type entity policy and the four new agent roles needed (prose-edge-classifier, entity-merge-resolver, disambiguation-resolver, chronology-extractor) are documented there.


---

## From `## Wiki / Pass 2 Prep` — completed agent prompts (prose-edge-classifier, cross-identity-detector)

- [x] **NEW AGENT — `prose-edge-classifier` (Stage 4)** — Done. Full prompt at `.claude/agents/prose-edge-classifier.md` (135 lines). Reads candidate-edge JSONL + source/target node prose, decides edge_type from locked vocabulary OR rejects as just-a-mention OR escalates. Writes one prose-edges JSONL per source slug. Awaits the candidate-generator script + run.
- [x] **NEW AGENT — `cross-identity-detector` (Stage 4 sub-task)** — Done. Full prompt at `.claude/agents/cross-identity-detector.md` (114 lines). Replaces the old "entity-merge-resolver" framing — reads candidate same-as pairs from Python preprocessing + wiki redirects + alias-overlap, emits SAME_AS decisions. Companion `cross-identity-reviewer` also exists for full-pass review.


---

## From `## Wiki / Pass 2 Prep` — per-entity index tables (BUILT — `scripts/build-entity-indexes.py` S43, extended to all 21 categories S72; checkbox was never flipped)

- [ ] **Per-entity index tables (post-Stage-4 cleanup)** — Surfaced 2026-04-28 (Session 26). Already in `reference/architecture.md` § "Artifact Formats by Consumer" as `entity-index/<slug>.index.json` (post-promotion script output). Build a Python script `scripts/build-entity-indexes.py` after Stage 4 prose-edges promote, to derive per-entity views: outbound edges (from node), inbound edges (from cross-references.jsonl), chapter appearances (from Pass 1 mentions), node-mention list (from cross-refs prose mentions), temporal timeline (when temporal-edge fields land), in_count/out_count (from backlink-counts.json), book appearances (from cite_ref books). Output: `graph/index/<type>/<slug>.index.json`. Lives separately from the node — derived artifact, rebuilt when underlying data changes. Enables "give me everything about Eddard" queries that node-file alone can't.


---

## From `## Wiki / Pass 2 Prep` — GitHub state note (informational, 2026-04-28)

- [ ] **GitHub state confirmed** — 2026-04-28 (Session 26). Repo is `mattnoth/weirdwood-network` (note spelling: weiRdwood, with extra `d`). Visibility: PRIVATE. Remote URL `https://github.com/mattnoth/weirdwood-network.git`. .gitignore correctly excludes `sources/raw/` and `sources/chapters/`. For mattnoth-dev (Netlify-deployed) to include this as a submodule, authorize Netlify's GitHub app on this repo (one-time OAuth flow in Netlify repo connection settings). For backend deployment, books reach the backend host via separate path (recommended: local build + private container registry like GitHub Container Registry's free tier).


---

## `## Path B Promotion Campaign (Session 28, complete)` — header + completed items. The open temporal-edges-design and Stage-1-prose-drift items are kept live in todos.md § Dormant.

## Path B Promotion Campaign (Session 28, complete)

> Pre-session: `unknown` 12,434 (70.4%). Post-session: `unknown` 2,118 (12.0%). Graph 5,008 → 7,248 (+2,240). Cat 1 orphan edges 2,955 → 1,973 (−982).

- [x] **Path B categorizer extension + categories backfill** — Done 2026-04-30 (Session 28 Act 1). `scripts/wiki-fetch-categories.py` (bounded `cloudscraper` exception fetch); CATEGORY_TYPE_MAP added to parser. `unknown` 12,434 → 2,487. CLAUDE.md narrow re-fetch exception clause + audit log entry.
- [x] **Path B promotion sub-tasks 1a/1b/2/3/4/5/6** — Done 2026-05-01 (Session 28 Act 2). 7 promotion scripts at `scripts/wiki-pass2-tier3-pathb-{texts,artifacts,locations,events,orgs,characters,longtail}.py`. Per-type routing via TYPE_TO_DIR map for orgs/longtail. 14 commits (1 revert). Bootstrapped `texts/` (150), `theories/` (45), `concepts/` (31), `species/` (38), `foods/` (69).
- [x] **Real-world publications skip rule** — Done. Parser-level: pages categorized only as `Books` (without `Books and scrolls`) classify as skip. 31 affected pages: A Game of Thrones, A Clash of Kings, A Knight of the Seven Kingdoms, Fire & Blood, etc.
- [x] **Chapter-summary pages skip rule** — Done. Parser-level: `A Song of Ice And Fire chapters` category → SKIP_CATEGORY. Caught a 338-node false-positive disaster in events Sub-task 3 (reverted commit `e72aa039` via `fbbf764c`, then re-emitted clean).
- [x] **`event.tournament` new entity type** — Done. CATEGORY_TYPE_MAP `Tourneys|Tournaments → event.tournament`; PAGE_NAME_TYPE_PATTERNS `\btourney\b → event.tournament` for empty-category pages like Ashford Tourney. Routes to `events/` via TYPE_DIR_MAP.
- [x] **`object.food` new entity type + `foods/` dir** — Done. Schema add to architecture.md. Food + Drinks combined per Matt's design value. 69 nodes promoted (Bowl of brown, mead, dreamwine, lemon cakes, Arbor wine, etc.). Trees route to `species/` (broadens dir scope to in-world flora).
- [x] **Knight of the Laughing Tree promoted** — Done. ENTITY_TYPE_OVERRIDES: `Knight of the Laughing Tree → character.human`. Lyanna's secret identity at Harrenhal — R+L=J pillar. Promoted as separate node from Lyanna; SAME_AS edge is Stage 4 cross-identity-detector territory.
- [x] **Promotion completion + schema-drift audit** — Done 2026-05-01 (Session 29). 4 new entity types (object.material, concept.language, concept.medical, concept.custom). +315 net graph nodes. 130 stale-dir mismatches cleaned. Schema-drift audit clean (0 HIGH, 4 MED → 2 fixed + 1 decided + 1 deferred-to-Stage-4, 4 LOW deferred). unknown 2,098 → 1,257. Continue prompt deleted (work complete).


---

## From the Path B section — stale-dir cleanup script reusability note (reference info; script exists at `scripts/wiki-pass2-stale-dir-cleanup.py`)

- [ ] **Stale-dir cleanup script reusability** — Built 2026-05-01 (Session 29) at `scripts/wiki-pass2-stale-dir-cleanup.py`. Stage-1 carve-out wired in (skips `prompt_version: v1` nodes). Reusable for future parser changes that reclassify already-promoted nodes. Pattern to remember: parser change → re-run parser → run cleanup --plan → run cleanup --apply → re-run dedicated promotion scripts.


---

## `## Audit Findings` — all three Session 27/28 audit sections (houses+locations sample, characters sample, citation-validator full-corpus), verbatim. Open items collapsed to one-liners in todos.md § Dormant → Wiki / Pass 2 long-tail (War_for_the_Dawn type question, blackwater-song type, YAML-escape aliases bug, Stage-1 cite-hygiene cluster, citation-coverage-scan idea).

## Audit Findings (Session 27, sample audit on houses + locations)

> Source: `working/audits/schema-drift-2026-04-30/execution/schema-drift-sample-2026-04-30.md` (sample of 500 nodes; full corpus 4,239)
>
> **Stage framing (added Session 27):** the audit-driven cleanup work below is **Stage 3c** (corrective — fixes emission bugs in already-promoted nodes), not Stage 4 (additive — prose-derived edge discovery). See `working/runbooks/wiki-pass2-pipeline.md` § "Stage 3a/3b/3c vs Stage 4 — corrective vs additive boundary" for the canonical definition. Stage 3c is the natural boundary for audit + parser-fix + targeted re-promotion cycles; Stage 4 only begins once Stage 3c stabilizes.

### HIGH — fix at parser level

- [x] **Type-classifier fallback bug — wars/factions/artifacts mistyped as `place.location`** (Finding 1.1) — PARSER FIXED + 14 NODES RE-PROMOTED 2026-04-30. Added `PAGE_NAME_TYPE_PATTERNS` (regex for `\bwar\b`, `\brebellion\b`, `\bconquest\b`, `\binvasion\b` → `event.war`) and extended `ENTITY_TYPE_OVERRIDES` with Triarchy + Second Sons (`organization.faction`), Iron Throne (`object.artifact`). 7 wars moved `locations/`→`events/`, 2 factions moved `locations/`→`factions/`, Iron Throne moved `locations/`→`artifacts/`. New script: `scripts/wiki-pass2-repromote-targeted.py` (preserved for reuse). Full-corpus re-run still pending; was scoped down to targeted re-promotion only.
- [x] **Religion-field parser bleed bug** (Finding 5.2) — PARSER FIXED + 4 NODES RE-PROMOTED 2026-04-30. `InlineTextExtractorHTML` now tracks `<small>` depth; links inside `<small>(...)` emitted as `qualifier_for` metadata rather than standalone targets. Lowercase-first-character targets skipped. `seven-kingdoms.node.md`, `pentos.node.md`, `lys.node.md`, `tyrosh.node.md` re-promoted with clean edges. `triarchy` retains `WORSHIPS: Mixed` (uppercase passes filter — pre-existing wiki imperfection, not a parser bug).
- [ ] **OPEN QUESTION: `War_for_the_Dawn` type — `event.war` or `concept.prophecy`?** — Currently fixed to `event.war` via the `\bwar\b` regex. But the War for the Dawn is arguably a prophesied future event / legendary myth, which might fit `concept.prophecy` better. Both are valid; `event.war` is the conservative choice (it's a named war, like others on this wiki). Decide before full-corpus re-run. If `concept.prophecy`, add explicit override in `ENTITY_TYPE_OVERRIDES`.
- [ ] **OPEN QUESTION: `battle-of-the-blackwater-song` type — needs `object.text` subtype** — Currently sits in `graph/nodes/_unclassified/` because the parser emits `unknown`. This page is an in-world song named after a battle. Correct type is likely `object.text`. Add to `ENTITY_TYPE_OVERRIDES` in `scripts/wiki-infobox-parser.py` once architecture.md confirms `object.text` is the right subtype (it is — see architecture.md Type Reference Table). Also note: the regex `\bwar\b` now matches "Battle of the Blackwater" _not_ (no "war" in the name), so this page still falls through to infobox classification. The `object.text` fix remains a separate override entry.

### LOW — policy decisions

- [x] **Deleted `*-telltale` and `*-theories` sub-page nodes 2026-04-30** — Per `project_video_game_entities_excluded.md`: `house-tuttle-telltale.node.md` + `house-whitehill-telltale.node.md` deleted. `house-cassel-theories.node.md` deleted (wiki sub-page collecting fan theories doesn't fit the future Pass-5 theory-node-shape; will be re-ingested properly when theory-seeds.md drives Pass 5). Confirmed corpus-wide grep — no other slug-suffixed `*-telltale` or `*-theories` files remain.
- [x] **`*-guards` sub-page policy DECIDED + FIXED 2026-04-30 (Session 27)** — Matt's call: retype as `organization.faction`. Parser fix: added `\bguards$` pattern to `PAGE_NAME_TYPE_PATTERNS` in `wiki-infobox-parser.py`; moved pattern evaluation BEFORE the "House " prefix check so "House Bolton guards" isn't intercepted as a house. Re-promoted all 6 nodes out of `houses/` into `factions/`: `house-bolton-guards`, `house-stark-guards`, `house-tully-guards`, `house-tyrell-guards`, `house-arryn-guards`, `house-targaryen-guards`. Old `houses/` files deleted.

## Audit Findings (Session 27, sample audit on characters)

> Source: `working/audits/schema-drift-2026-04-30/execution/schema-drift-sample-characters-2026-04-30.md` (first 500 of 3,373 character nodes; sister run to houses+locations sample)

### HIGH — fix at parser level

- [x] **BORN_AT / DIED_AT date-bleed in target field** (Finding 5.1c) — PARSER FIXED + NODES RE-PROMOTED 2026-04-30 (Session 27). `wiki-infobox-parser.py` Born/Died field handling now separates date links from place links using `_is_date_link()`: date strings ("263 AC", "Age of Heroes", "In 94 AC") become `qualifier` metadata; place links become the edge target. Also handles "Place, Year" plain-text pattern for Stage-1 nodes. Re-promotion: 1,137 Stage-3 nodes fully re-emitted; 44 Stage-1 nodes had `## Edges` surgically replaced (prose preserved). 0 remaining date-bleed nodes confirmed. `scripts/wiki-pass2-repromote-targeted-2.py` handles all three bugs.
- [x] **Dragon mistyping — all dragons typed `character.human` instead of `character.dragon`** (Finding 1.1c) — PARSER FIXED + 19 NODES RE-PROMOTED 2026-04-30 (Session 27). Added `classify_by_species()` to `wiki-infobox-parser.py`: reads the "Species" infobox field → `character.dragon` (28 pages confirmed). Re-promoted 19 existing nodes (all Stage-3 deterministic). 9 dragon pages not yet in graph (Drogon, Meleys, Meraxes, Seasmoke, Sheepstealer, Shrykos, Silverwing, Terrax, Cannibal) — will be promoted when their buckets are run. Direwolves confirmed correct (regression pass clean). Parser entity distribution now shows `character.dragon: 28`, `character.direwolf: 6`.
- [x] **2 nodes with malformed YAML aliases** (Finding 3.1c) — DATA FIXED 2026-04-30 via direct edit (single-quote outer wrapper so inner double-quotes are literal). Note: this fix gets reverted by any re-emission via `wiki-pass2-emit-deterministic.py` because the underlying parser doesn't YAML-escape inner double-quotes when emitting the `aliases:` array. The cargyll fix is data-only; the upstream parser bug remains.
- [ ] **UPSTREAM — YAML-escape inner quotes in `aliases:` field emission** — Parser bug in `scripts/wiki-pass2-emit-deterministic.py` (and possibly `wiki-infobox-parser.py` aliases extraction). When an alias contains inner double-quotes (e.g., `One of "the celebrated Cargyll twins"`), it's emitted as `aliases: ["One of "the celebrated Cargyll twins""]` — invalid YAML. Fix: either escape inner quotes (`\"`) in double-quoted form, OR switch to single-quoted YAML scalars when alias contains `"`. Affects: at least 2 known cargyll nodes; may affect other characters with quoted-text aliases. Without this fix, any future re-emission re-introduces the bug.

- [x] **Slug-regression bug introduced and fixed in same session 2026-04-30** — `scripts/wiki-pass2-repromote-targeted-2.py` had two `slug = node_path.stem` lines (422 + 492). `Path.stem` on `arryk-cargyll.node.md` returns `arryk-cargyll.node` (only strips `.md`), so 1,120 re-emitted nodes ended up with `slug: <slug>.node` (the `.node` filename suffix bled into the slug field). Script fix: `slug = node_path.stem.removesuffix(".node")` on both lines. Data fix: `sed -i ... -E 's/^(slug: [a-z0-9-]+)\.node$/\1/' ` across all 1,120 affected files. Verified: 0 bad slugs remaining. Lesson: if a future targeted-re-promotion script is built, audit `Path.stem` usage on `*.node.md` paths.

### MED / LOW (already known or out-of-scope)

- `first_available` on 121/614 v1 character nodes — same as houses+locations Finding 3.1, already deferred-policy.
- Edge directionality variants — same as houses+locations Finding 5.1, already documented.
- Cross-identity duplication (Theon/Reek, Sansa/Alayne coexist as separate nodes with no `SAME_AS`/`ALIAS_OF`/`DISGUISED_AS` edges between them) — for `cross-identity-detector` agent in Stage 4; not a schema violation.

### Confirmed clean (same as sister audit)

- Edge-vocabulary drift: 0 violations in ~3,000+ character edges. Lock holds.
- Slug format: 0 violations across 614 character slugs.
- Missing structural sections: 0/614.

### Confirmed clean (skip full audit on these)

- **Edge vocabulary**: 0 violations across 1,562 edges. The 22-type lock holds.
- **Slug format**: 0 violations across 500 slugs.
- **Required frontmatter fields**: 100% present.

### Confirmed deferred (no action — handled later phases)

- `first_available` present in 250 of 322 v1-houses (78%) — already deferred per 2026-04-27 decision; backfill script overwrites all of these post-first-release. Do not invest agent context fixing per-node.
- Edge directionality variants (`(reverse)`, `→`, `->`) — three formats coexist across emitters. Normalize at graph-build time, not at promotion.

## Audit Findings (Session 27, citation-validator full-corpus audit)

> Source: `working/audits/citation-issues-2026-04-30/execution/citation-issues.md` (4,236 in-scope nodes, ~50,000+ citation tokens)

### HIGH — concrete malformed cite_refs (Stage-1 emission limitations)

- [ ] **11 occurrences of bare `(wiki:R{book}{N})` cite_ref-as-page** — Stage-1 agent abbreviated wiki cite_ref anchors as if they were wiki pages. Confirmed in: `house-darke.node.md` `(wiki:Raffc9)`, `house-bar-emmon` `(wiki:Rasos75)`, `house-darklyn` `(wiki:Raffc9)`, `house-blackberry` `(wiki:Rasos10, Rasos63)`, `house-darkwood` `(wiki:Raffc9)`, `house-cave` `(wiki:Raffc20)`, `house-chyttering` `(wiki:Rasos10)`, `house-boggs` `(wiki:Raffc20)`, `house-dargood` `(wiki:Raffc9)`, `house-cargyll` `(wiki:Ragot8)`, `house-byrch` `(wiki:Raffc7, Raffc32, Raffc37)`, `house-chelsted` `(wiki:Rasos11)`, `factions/dragonkeepers` `(wiki:Ragot20, Rasos61)`. Pattern: `R + {book code} + {N}` = "{book} reference #{N}". These are partial pointers — they identify the book but not the chapter. **Fix path:** either (a) deterministic Python pass that parses the wiki source for the parent page, finds the actual cite_ref-N anchor, expands to full citation; OR (b) accept as known Stage-1 limitation and replace with `(source: AFFC)` / `(source: AGOT)` / `(source: ASOS)` to preserve book signal but drop the broken cite_ref pointer; OR (c) leave as-is and address during Stage 1 re-emission. Lean (b) — small one-time data fix, no info loss beyond what's already lost.

- [ ] **2 chapter-numberless `(agot-{pov})` quote citations in `pycelle.node.md`** — lines 47, 49: `(agot-eddard)`, `(agot-sansa)` — should be `(agot-eddard-NN)` / `(agot-sansa-NN)`. Without source extraction lookup, can't recover the chapter number. Same Stage-1 limitation pattern. Defer or hand-patch by reading the chapters where Pycelle delivers those exact quotes.

### MED — Stage-1 vs Stage-3 cite-format divergence

- [ ] **978 occurrences of verbose `cite: track_b_row.relationships.X` form across 223 nodes** — Stage-1 agent emitted relationship-edge citations using a different format than Stage-3 deterministic. Both lower- and upper-case variants coexist. Same edge content as Stage-3's `(track_b: <Field>)` — just verbose. Resolves at graph-build time when the consumer normalizes both formats; do NOT data-fix per-node.

- [ ] **Comma-joined multi-source cites `(wiki:Page, agot-pov-NN)`** — sampled in `cayn`, `donal-noye`, `godwyn`. Probably Stage-1 agent intent (multi-source citation), but not the canonical format. Audit didn't exhaustively count. Defer to graph-build normalizer or Stage 1 re-emission.

- [ ] **Multi-cite_ref chained `(wiki:Page.cite_ref-A, cite_ref-B)`** — sampled in `lemore.node.md`, `tyrion-lannister.node.md`. Same shape as above; format inconsistency rather than missing data.

- [ ] **Stage-1 paragraph-bracketed missing-cite pattern** — Stage-1 nodes (855) cite at paragraph boundaries; mid-paragraph claims rely on the bracketing. `house-mallister.node.md:33` confirmed as a representative case (3 mid-paragraph claims between cited bookends). Not a per-node fix; this is the systemic Stage-1 emission style. Address via:  (a) Stage-1 re-emission via Stage-3 pipeline (converts editorial agent prose to wiki-literal), OR (b) accept and document as Stage-1 cite-style.

### LOW — validated as not-a-bug

- **3,400 cite_refs with URL-encoded gibberish** (`cite_ref-Raffc4.7B.7B.7B3.7D.7D.7D.7B.7B.7B4.7D.7D.7D_12-5`) — verified to be real wiki anchor IDs preserved verbatim from `sources/wiki/_raw/*.json`. They reflect MediaWiki template-substitution leakage in the wiki HTML, NOT parser noise. Functional but ugly. Cosmetic cleanup (regex-strip the `.7B.7B...` suffix) is possible but not blocking.

### VERIFY (Pass 1 complete 2026-05-06)

- [x] **Re-run citation-validator audit on the full 5-book corpus.** Done 2026-05-06 (Session 37). Report at `working/audits/citation-corpus-rerun-2026-05-06/execution/citation-issues.md`. PENDING-PASS-1 bucket from 2026-04-30 fully closed: 8 of 9 previously-deferred chapter cites were re-emitted away in interim node-rebuild work; the 1 remaining (`house-hayford.node.md:39 → (affc-jaime-03)`) resolves cleanly. Zero broken chapter-file references corpus-wide; zero new HIGH findings.

### Confirmed clean

- **AGOT chapter cites all resolve** — every `(agot-pov-NN)` cite has a corresponding `extractions/mechanical/agot/agot-pov-NN.extraction.md`. Zero broken file references for the only book with completed Pass 1.
- **Stage-3 nodes have rigorous claim-level citations** — sampled 30 nodes, zero missing-cite findings in narrative prose. Deterministic emitter preserves every wiki `<sup>` cite.

### Recommendation (post-audit)

Run a narrower scripted scan (`scripts/citation-coverage-scan.py` — TODO) that flags sentences ending in a period without a following parenthetical citation in the same paragraph, restricted to sentences with a tensed verb plus proper noun. Without that, Stage-1 missing-cite finding is undersampled.
- Stub Identity prose (`X is a place.location from the AWOIAF wiki.`) on all Stage 3 v1-python nodes — defer to prose-fill phase.
- Empty `## Edges` sections in 62 of 500 — Stage 4 prose-edge-classifier backfills from prose; many are also Finding-1.1 misclassifications that will resolve when bucketing is fixed.


---

## `### Wiki Pass 2 — Session 22 Followups` — full subsection, verbatim. Open items collapsed to one-liners in todos.md § Dormant. Notes: the "BLOCKER: HTML categories empty" item was RESOLVED by the 2026-04-30 categories backfill (`scripts/wiki-fetch-categories.py`; CLAUDE.md exception-fetch audit log); the "Wiki infobox field → edge type extraction" item is resolved in practice (the parser's FIELD_EDGE_MAP exists and produced `infobox-data.jsonl`); the "Edge taxonomy gaps surfaced by Track B" item moved LIVE into todos.md Track 1 (infobox merge).

### Wiki Pass 2 — Session 22 Followups (post-Stage-1 fixes)

- [ ] **Book-chapter wiki pages (`<Book>-Chapter <N>`) silently dropped by triage** — Surfaced 2026-04-27 (Session 25) during Stage 3 prep. 349 chapter/prologue/epilogue/appendix wiki pages exist in `page-index.jsonl`; triage routed 344 to the `battles` mega-bucket and 5 to `singletons-unknown`, but the alphabetical 30-cap split kept only 14 ASOS chapter pages (in `battles-a`) — the other 335 are not in any wiki-pass2 bucket. `scripts/wiki-pass2-triage.py` mis-classifies these as `event.battle` from an infobox signal, then drops them during split. Plan: (a) remove the 14 stranded pages from `battles-a` so Stage 3a doesn't emit empty skeletons for them, (b) route all 349 into a deferred bucket (`book-chapter-pages-defer` or similar) that Pass 2 ignores, (c) revisit later — these pages may feed Track A (per-chapter mechanical extraction) as a wiki-side cross-reference. Don't delete; defer.
- [ ] **Multi-letter character bucket overlap (triage script bug)** — Confirmed in Session 22: `stark-h-p ∩ stark-h-q = 26/27 pages`. Same pattern likely for stark-{a-b, b-h, q-w, r-w}, greyjoy-{g-r, s-w}, martell-{a-m, m-t}, tyrell-{a-l}. Single-letter buckets (lannister-b/j/r, targaryen-a/b/d/j/o/t) TBD. Fix the alphabetical-split logic in `scripts/wiki-pass2-triage.py` to produce disjoint letter ranges. Existing duplicates in `graph/nodes/_conflicts/` need cleanup pass. Defer to post-Stage-1 (Matt accepted the waste this round).
- [ ] **Launcher wave-math gotcha** — `cmd_launch` in `scripts/wiki-pass2.sh` silently rejects wave numbers > `total_waves`, leaving Terminals 2-N with empty assignments. `weirwood wiki core 4 9` opens only 1 tab when there are 9 waves. Fix: clamp `waves_per` to `total_waves / terminals` before assignment, or warn when terminals get empty waves. Workaround used: `3 3` for 9 waves.
- [x] **Launcher should auto-wipe stale `tmp/`** — Done 2026-04-27 (Session 25). `cmd_run` now reads `prior_status` via `manifest_get` before flipping to in-progress; when prior status is `fail` or `validation-failed`, the bucket's `tmp/` directory is `rm -rf`'d before the new run so the agent starts with a blank canvas. Logs the wipe (`Wiping stale tmp/ from prior <status> run...`).
- [ ] **Question ID collisions (parallel-safe IDs)** — All Session 22 questions filed as `q-2026-04-26-001`. Parallel agents generate IDs without coordination. Fix in agent prompt or post-process: include bucket_id in ID, e.g., `q-2026-04-27-arryn-001`, or have launcher renumber on append.
- [ ] **CSV stats race (parallel JSONL writes)** — `cmd_run` snapshots `q_before`/`c_before`/`p_before` at bucket start, computes delta at end. With parallel tabs writing to the same JSONL files, the snapshot delta picks up sibling-tab writes. CSV `questions_filed`/`conflicts_filed` columns are unreliable. Fix: filter delta to current bucket via `bucket_id` field in JSONL, not raw line count.
- [ ] **Rate-limit detection misclassification** — Session 22 logs show JSON response had `"status":"needs-auth"` AND `"rateLimitType":"seven_day"`, but rate-limit grep at `wiki-pass2.sh:684-688` requires `"status":"rejected"`. The detection still flagged skip-rate-limit (likely a second status entry elsewhere in JSON), but the grep is fragile. Audit + tighten.
- [ ] **Spoiler-gating backfill pass (POST-FIRST-RELEASE)** — `first_available` is fully deferred per 2026-04-27 decision. Build a deterministic Python backfill that walks every node in `graph/nodes/`, derives `first_available` from (a) `track_b_row.books` field as primary signal and (b) cite_ref anchors as refinement, with a cross-check against Pass 1 raw entity lists to catch parser-bug class (e.g., Tyrion/Varys → ADWD). **Pass 1 prerequisite met 2026-05-06** (344/344 across 5 books); the cross-check has full coverage when this work eventually runs. Output: in-place frontmatter rewrite across the whole node corpus in one pass. CLAUDE.md and architecture.md already updated to reflect deferral. When backfill ships, update both back to "active".
- [ ] **Bucket fingerprint missing track_b_row hashes (C1 from cold review)** — `bucket_fingerprint` covers `sorted(input_pages) + prompt_version + chunk_strategy` but not the Track B JSONL row content. Wiki re-crawl that updates an infobox doesn't trigger re-queue. Cheap fix: SHA-256 of sorted JSON of `track_b_row` per page, folded into existing payload.
- [ ] **`tier_default` for direwolves should be tier-1 (C8 from cold review)** — Currently falls to fallback `tier-2`; named characters with verified canon facts. Either add `direwolves|dragons` to tier-1 regex in `TIER_DEFAULT_RULES`, or extend rule to match `character` substring.
- [ ] **Oversized bucket-of-one suffix regex (C10 from cold review)** — `_SPLIT_SUFFIX` allows at most two single-letter segments. A bucket-of-one named `eddard-stark` has multi-char hyphenated suffix and lands in secondary tier instead of core. Fix: match `^direwolves(-.+)?$` (or generalize for any bucket-of-one).
- [ ] **`wiki-pass2-triage.py --limit N` is destructive** — opens output in `"w"` mode and clobbers full output. Either guard or write to a separate path when `--limit` is set. Non-blocking but real footgun.
- [ ] **Tripwire threshold mismatch** — `wiki-pass2-triage.py` defaults to 0.8 but only ~30% of pages are non-singleton (correct data shape: 70% are stubs without infoboxes). Either lower default to ~0.25 or measure tripwire against `pages-with-infoboxes` (~5,279) instead of total.
- [x] **`direwolves` bucket includes Arya Stark via alias match** — Fixed 2026-04-26 (Session 19). `scripts/wiki-pass2-triage.py:419-427` direwolf override now matches on page name only (alias clause removed). Verified manifest: 6 canonical direwolves.
- [x] **Wiki infobox parser for `first_available`** — Done 2026-04-25 (Session 18). `scripts/wiki-infobox-parser.py` produces 3 outputs in `working/wiki/data/`: `infobox-data.jsonl` (5,279 lines), `page-index.jsonl` (17,657 lines, L1 mandatory deliverable), `parse-stats.md`. `first_available` populated for 2,888 of 5,279 infobox-rich pages (54.7%, 99.9% from cite_ref). Bug fixed during build (falsy-zero on chapter 0 cite_refs). **Three open issues surfaced — see follow-up todos below.**
- [ ] **BLOCKER: HTML categories empty in page-index** — `categories[]` is `[]` for all 17,657 entries because the wiki-scraper used MediaWiki's parse API, which strips the `mw-normal-catlinks` footer. Runbook §1.2.1 explicitly requires HTML categories for triage bucketing. **Decide before Pass 2 build session:** (a) re-fetch with `&prop=categories` (~17k re-requests), (b) hit `action=query&prop=categories` separately and merge, or (c) amend runbook to drop categories requirement and rely on `entity_type_guess` (already populated from infobox-field signatures). Option (c) is cheapest but underexamined — was assumed away during runbook review.
- [ ] **Investigate parser `books` field discrepancy** — `parse-stats.md` reports 1,953 raw `books` infobox field occurrences across the corpus, but only 37 entries in `infobox-data.jsonl` have a populated `books` array. Either parser drops most of them or the field exists empty/malformed on most pages. Affects book-level `first_available` fallback (currently fires only 3 times). Likely a small parser fix.
- [ ] **Edge taxonomy gaps surfaced by Track B** — Unmapped infobox fields worth adding: `dynasty` (222 pages, no `MEMBER_OF_DYNASTY`-style edge), `written by` (168, no `WRITTEN_BY` edge for in-world texts), `hatched` (8, dragon-classification opportunity). Parser-level mapping gaps: `fathers` plural (21), `vassal` (8), `cadet branch` singular (11) — easy additions to `FIELD_EDGE_MAP`. Roll into v3 schema review or address standalone. **Vocabulary lock:** any new edge type must land in `reference/architecture.md` § "Wiki Infobox Fields → Edge Type Mapping" FIRST, then in `scripts/wiki-infobox-parser.py`'s `FIELD_EDGE_MAP`. Re-run parser → re-run prioritize → re-run emit. No script or agent invents edge types out-of-band.
- [ ] **Edge polish phase (FUTURE — post-ingestion)** — After all wiki ingestion completes (Stage 3a + 3b across the full corpus), an agent-reasoning phase reviews the emitted edges to merge semantically-equivalent variants and resolve near-duplicates that the locked parser vocabulary couldn't disambiguate up front. The 22 edge types currently in the corpus may consolidate further. The point of locking the parser vocabulary is so this polish step is bounded and reviewable; if Stage 3a/3b had been allowed to invent edge types, this phase would be unbounded. Triggered after Pass 2 finishes; not before.
- [ ] **Non-ASCII edge qualifier normalization (graph layer)** — Edge qualifiers carried through Stage 3a contain non-breaking spaces (`\u00a0`) and en-dashes (`–`) from wiki source — visually identical to space/hyphen but break string equality. Stage 3a passes them through verbatim by design (pure data passthrough; not its job to normalize). Normalization happens at graph-build time when nodes are linked into edges: lowercase, NFKC-normalize unicode, replace `\u00a0` → space and `–` → `-`, strip whitespace before any string-equality match. Document this in the graph-build script when written.
- [ ] **Wiki infobox field → edge type extraction** — Infobox fields (Father, Mother, Spouse, Allegiance, Overlord, etc.) map directly to edge types. See mapping table in `reference/architecture.md`. Script should parse these for Pass 2 input.
- [x] **AGOT supplementary entity index** — Obsoleted 2026-04-25. Original purpose: backfill the 7 entity types missing from v2's 4-category Raw Entity List. v3 prompt now captures all 12 categories directly, so this is no longer needed for AGOT (already v3 complete) and not needed for the remaining books (will run v3 from the start). If gaps surface in v3 schema review, address as a v3 prompt issue rather than a backfill index.
- [ ] **Wiki uncategorized page triage** — 17,305 of 17,945 wiki pages are in `_uncategorized/`. Before designing Pass 2, scan `_category-reports/` and sample uncategorized pages to determine what entity types are actually needed. Will validate or reject the 7 candidate types.
- [ ] **`wiki-pass2-resolver.py`** — Deterministic resolver: given a Pass 1 raw entity name (free-text, e.g., "the children", "those who sing the song of earth"), return canonical wiki page (or null / list if ambiguous). Uses Track B's alias data (infobox `Alias, Aliases` field) plus fuzzy match. Sits between Pass 1 and Pass 2; not a new agentic pass. Surfaces from Pass 1/Pass 2 asymmetry decision (memory: `project_pass1_pass2_asymmetry.md`).
- [x] **Alias-resolver backfill from mention-index unresolved set (Session 38)** — DONE 2026-05-11 (Session 40, commit `ace00ee0`). Added 6 aliases to canonical node frontmatters (durable path): `eddard-stark` ← "Ned Stark", `tormund` ← "Tormund Giantsbane", `eastwatch-by-the-sea` ← "Eastwatch", `blackwater-rush` ← "The Blackwater" (river — battle has own node), `brienne-tarth` ← "Brienne", `thoros` ← "Thoros of Myr". Re-ran `scripts/wiki-pass2-build-alias-resolver.py --apply` + `scripts/build-mention-index.py --all`. Resolution climbed 70.0% → 70.6% (+209 newly-resolved across 344 chapter files). Smaller bump than the projected 75% — remaining top-20 unresolved are ambiguous (`joffrey`, `aegon`, `godswood`) or genuine missing nodes (`the-narrow-sea`, `flea-bottom`), not alias misses. A second round of alias-backfill could close `the-vale` → `vale-of-arryn`, `maester-aemon` → `aemon-targaryen-maester`, `prince-aemon-the-dragonknight` → existing node, etc. — but those need a separate session.
- [x] **Per-character index roll-up (`graph/index/characters/<slug>.index.json`)** — DONE 2026-05-11 (Session 41). `scripts/build-character-indexes.py` emits 3,910 character-index files in <1 sec. Schema: stats (appearances_total, chapters_pov, chapters_mentioned_in, out_edge_count from `## Edges`, in_edge_count from backlink-counts.json) + chapters.pov + chapters.mentioned_in (each mention record carries chapter_id, book, pov_character_slug, mention_count, sections, resolved_via). 18 POVs detected; descriptive-title chapters (The Prophet, The Captain of Guards, etc. = 36 chapters) unresolved as expected (Stage 4 disambiguation problem). Alayne/Sansa + Reek/Theon kept separate (Stage 4 SAME_AS will merge). Smoke checks: Eddard POV=15 ✓, Tyrion POV=47 ✓, Jon POV=42 ✓, Dany POV=31 ✓. Continue prompt: `progress/continue-prompts/2026-05-11-per-character-index-rollup.md`.
- [ ] **Write `graph/index/chapters/README.md`** — Surfaced 2026-05-07 (Session 39). Each file lists every named entity surfaced in the chapter — characters, locations, artifacts, food. The "mention" term is technically standard but reads person-ish in casual usage; cheap clarity fix is a README explaining "any named entity, not just people" + how the file is consumed (reverse-lookup so an agent answering 'where does Robb appear?' can scan files instead of re-reading chapters). No rename needed.
- [x] **Commit the working/wiki/ subtree reorg** — DONE in commit `6f37b940` "session 39". Worklog Session 39 entry said "no commit" but the reorg actually landed under that commit ID; this todo went stale.
- [ ] **`pass1-coverage-audit.py`** — Coverage audit: given a chapter's Pass 1 raw entity list and the same chapter's wiki cite_refs (from Track B), flag wiki entities the chapter mentions that Pass 1 missed. This is the real Pass 1 quality gate — higher signal than category-correctness checking. Run as part of v3 schema review.


---

## From `## Infrastructure` — section header + completed items (chapter splitter, wiki scraper, batch runner, cSpell)

## Infrastructure

- [x] **Chapter splitter script** — Done. `scripts/chapter-splitter.py`, all 5 books split (344 chapters total).
- [x] **Wiki scraper script** — Done. `scripts/wiki-scraper.py` (1213 lines, stdlib-only). Full crawl complete (17,945 pages, 377 MB).
- [x] **Batch extraction runner** — Done. `scripts/extract.sh` + `scripts/weirwood.zsh`. Wave-based parallel extraction via iTerm tabs, stats tracking, rate-limit detection, auto-worklog updates.

- [x] **cSpell dictionary expansion** — Done 2026-04-26 on branch `chore/cspell-dictionary-expansion` (commit 26650f3, merged to main). +~30 technical neologisms + ~120 ASOIAF proper nouns. ignorePaths unchanged.


---

## From `## Infrastructure` — auto-relaunch Option A/C plan (SUPERSEDED in practice by the run-forever wrappers and `scripts/longrun.sh` — see todos.md Track 6)

- [ ] **Auto-relaunch on rate-limit reset (Option A) + scheduled launches (Option C)** — Decision 2026-04-25. Two-part rate-limit-prevention infra, applies to both `extract.sh` and `wiki-pass2.sh`.
  - **Part A (cheapest):** When rate-limit hit, the launcher already extracts `resetsAt` from the rejected JSON (extract.sh:502-520). Add: write `working/extraction-stats/next-eligible.txt` and use `ScheduleWakeup` / launchd to auto-relaunch the same wave at reset+5min buffer. Removes "remember to relaunch in 4h" burden.
  - **Part C (multi-day work):** `weirwood schedule <book>` (and `weirwood wiki schedule <tier>`) — cron / launchd / CronCreate fires launches at off-peak intervals automatically. Useful when walking away for days.
  - **Multi-pass prompting trigger:** If a wiki parse run is projected to span several rate-limit windows (e.g., >24h of cumulative wall time), the launcher prompts the user upfront: "this run will require N reset cycles — schedule unattended (Y/n)?" Avoids a silent multi-day commitment and gives the user a chance to bail or split into manual sessions.
  - Defer Option B (predictive throttling) — Anthropic's per-org caps aren't cleanly published, so any predictive model is approximate. Reactive halt + auto-relaunch is more robust.
  - **Future directions** once v1 (A + C) is in place: multi-day unattended runs, cross-pass orchestration (completion sentinels triggering next stage), periodic wiki re-crawl + diff, proactive monitoring (weekly status post), state snapshotting (daily commit of derived artifacts), notification sinks (Slack/email). Common primitives across all six: a scheduling layer (cron/launchd/CronCreate), sentinel files for inter-stage signaling, a status-emitter, an optional notification adapter. Sequence: v1 (A + C) → v2 (sentinels + 1-step pipeline) → v3 (full event-driven orchestration + monitoring + snapshotting).


---

## From `## Tiny Follow-ups (Session 40 surface)` — section header + completed items

## Tiny Follow-ups (Session 40 surface)

- [x] **architecture.md typo: "cred sites" → "sacred sites" — FIXED Session 49**
- [x] **Second round of alias-backfill (the-vale, maester-aemon, etc.) — DONE 2026-05-12 (Session 49)** — Added 3 aliases: `"The Vale"` → `vale-of-arryn`, `"Maester Aemon"` → `aemon-targaryen-son-of-maekar-i`, `"Prince Aemon the Dragonknight"` → `aemon-targaryen-son-of-viserys-ii`. Note: continue prompt used slugs `aemon-targaryen-maester` / `aemon-targaryen-dragonknight` (neither exists); correct slugs are the `son-of-*` convention. Resolution rate: 70.6% → 72.9% (+2.3 pp, ~849 newly-resolved mentions). Better than the 72-74% projected ceiling.

- [x] **DONE — Case-collision tail (65 remaining slugs, Track B) — DONE 2026-05-12 (Session 49)** — `scripts/filter-case-collision-tail.py` classified all 65: 28 DROP (real-world books, disambig, list articles, meta-wiki, hound names, zero-source), 23 ALREADY_EXISTS (nodes existed or alias covered), 10 CANONICAL created as new nodes + 4 alias updates to existing nodes. New nodes: handsome-man, stern-face, starved-man (Faceless Men identifiers), damon-dance-for-me, red-raven-free-folk, henly-maester, grazdan-mo-ullhor (characters), ice-dragon, ghost-grass (species), wine-of-courage (food). Alias updates: arryk-cargyll ← "Arryk (guard)", erryk-cargyll ← "Erryk (guard)", dragonbinder ← "dragon horn", jeyne-fowler ← "Fowler twins". DROP manifest at `working/missions/case-collision-tail/drop-manifest.md`. Graph: 7,949 → 7,959. Case-collision gap fully closed (125/125 processed).
- [x] **HIGH — Missing-node backfill: 138 Bucket A unpromoted wiki pages** — DONE 2026-05-11 (Session 43). `scripts/wiki-pass2-bucket-a-backfill.py` ran against all 138 slugs: 68 new nodes with prose, 17 stub nodes, 53 alias additions to existing nodes (total 138 changes). Bucket A dropped 138 → 53 (the 53 remaining are alias-resolved — audit doesn't check alias fields). New graph total: 7,915 nodes. Top hits resolved: `godswood` (place.location, 36 mentions), `flea-bottom` (place.location, 31), `seastone-chair` (artifact, 14), `unsullied` (faction, 9), `valyrian-steel-dagger` (alias on `littlefingers-blade`, 8), `old-gods` (alias on `old-gods-of-the-forest`, 22). 17 stubs for circular-redirect cases (queens-men, valar-morghulis, crossroads-inn, etc.) — no wiki content available. Categorizer-fix (`CATEGORY_TYPE_MAP` update to catch 138 slugs) filed as follow-up todo. Continue prompt deleted.

- [x] **DONE — Wiki prose extraction backfill (Session 42, 2026-05-12)** — Original premise (5,975 stub-only) was an over-count because the original stub detector didn't recognize `## Origins`/`## Narrative Arc` as prose-present indicators. Real gap turned out to be 2,030 stub-only `pass2-wiki-deterministic` nodes after a stricter detector. Of those, 1,011 had non-empty prose files in `working/wiki/pass2-buckets/*/prose/` that were never concatenated into the graph node (probably lost during a Session 27 Stage 3c re-emission of skeletons that didn't reattach prose). New tools: `scripts/audit-prose-coverage.py` (read-only audit; emits `working/audits/wiki-prose-coverage-2026-05-12/execution/coverage.jsonl`), `scripts/wiki-pass2-attach-prose.py` (idempotent in-place attacher; appends `\n + prose_bytes` to existing node body). Attached **990 nodes** (945 character.human + 45 across artifact/title/location/species/event/concept). Walgrave smoke-test: 519 bytes → 4,688 bytes with Origins / Appearances / Narrative Arc / Quotes sections. Remaining 1,019 stub-only nodes are genuinely empty-prose wiki pages (short titles/disambiguations/lists; 206 of them are case-collision redirect-only caches, already a separate todo). 67 false positives in the audit detector were caught by the live attacher's secondary check (these had `## Culture` or `## Quotes` sections that the audit regex didn't recognize as prose-present — non-blocking, audit regex updated post-fact).
- [x] **DONE — Per-LOCATION + per-ARTIFACT index roll-ups (Session 43, 2026-05-11)** — `scripts/build-entity-indexes.py --type [locations|artifacts|houses]` built. Emits `graph/index/locations/` (1,056 files), `graph/index/artifacts/` (265), `graph/index/houses/` (556). Each index: stats (appearances_total, chapters_referenced_in, chapters_in_primary_section, chapters_in_raw_list, out_edge_count, in_edge_count) + section-aware chapter lists with pov_character_slug. Smoke-test: winterfell 191 chapters ✓, kings-landing 217 ✓, iron-throne 59 chapters ✓, house-lannister 70 ✓, house-frey 25 ✓. Runs in <0.5s per type. Stage 4 component (b) prerequisite now met for all primary node types.


---

## Replaced organizational lines (section headers/intros whose sections were renamed or regrouped; their open items survive in todos.md)

## Reframed Chat / Query UI (NEW item, not a salvage)

## Reference Files to Create

## Project Story / Auxiliary
> Non-blocking. Not part of the actual build pipeline. These exist for telling the story of how the project came together — useful retrospectively, not load-bearing for any active work.
