# Contradictions report

## manfryd-mooton
- **SWORN_TO**: F&B proposes `manfryd-mooton` -> `rhaenyra-targaryen` (tier tier-1); existing wiki-infobox has `manfryd-mooton` -> `house-mooton`.
- **SWORN_TO**: F&B proposes `manfryd-mooton` -> `rhaenyra-targaryen` (tier tier-1); existing wiki-infobox has `manfryd-mooton` -> `blacks`.
- **SWORN_TO**: F&B proposes `manfryd-mooton` -> `rhaenyra-targaryen` (tier tier-1); existing wiki-infobox has `manfryd-mooton` -> `greens`.
