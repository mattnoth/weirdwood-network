# Contradictions report

## aegon-targaryen-son-of-gaemon
- **PARENT_OF**: F&B proposes `aegon-targaryen-son-of-gaemon` -> `maegon-targaryen` (tier tier-1); existing wiki-infobox has `aegon-targaryen-son-of-gaemon` -> `aerys-targaryen-son-of-aegon`.

## aenar-targaryen
- **PARENT_OF**: F&B proposes `aenar-targaryen` -> `daenys` (tier tier-1); existing wiki-infobox has `aenar-targaryen` -> `daenys-targaryen`.
- **PARENT_OF**: F&B proposes `aenar-targaryen` -> `daenys` (tier tier-1); existing wiki-infobox has `aenar-targaryen` -> `gaemon-targaryen-son-of-aenar`.

## edmyn-tully
- **SWORN_TO**: F&B proposes `edmyn-tully` -> `harren-hoare` (tier tier-1); existing wiki-infobox has `edmyn-tully` -> `house-tully`.

## mern-ix-gardener
- **PARENT_OF**: F&B proposes `mern-ix-gardener` -> `edmund-gardener` (tier tier-1); existing wiki-infobox has `mern-ix-gardener` -> `gawen-gardener`.
- **PARENT_OF**: F&B proposes `mern-ix-gardener` -> `gawen-gardener` (tier tier-1); existing wiki-infobox has `mern-ix-gardener` -> `edmund-gardener`.

## valaena-velaryon
- **PARENT_OF**: F&B proposes `valaena-velaryon` -> `aegon-i-targaryen` (tier tier-1); existing wiki-infobox has `valaena-velaryon` -> `rhaenys-targaryen`.
- **PARENT_OF**: F&B proposes `valaena-velaryon` -> `aegon-i-targaryen` (tier tier-1); existing wiki-infobox has `valaena-velaryon` -> `visenya-targaryen`.
