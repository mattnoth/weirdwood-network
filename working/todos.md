# Actionable TODOs

> **Purpose:** Tasks that need doing but aren't part of the current session's work. Organized by topic. Check items off as they're completed. Move completed items to a "Done" section at the bottom periodically.

---

## Model-fit Policy (standing rule, 2026-05-04)

> **Default to the cheapest model that can do the job.** Opus only when reasoning depth genuinely requires it.

- **Bash/Python script edits, simple agent loops:** Sonnet 4.6 or Haiku 4.5.
- **Mechanical extraction (Pass 1):** Currently Opus (AGOT/AFFC/ACOK precedent). **Open question:** would Sonnet 4.6 produce equivalent v3 output? Smoke-test before committing Opus to ASOS (82 ch) + ADWD (73 ch). See todo below.
- **Schema audits, full-corpus reviews:** Opus is justified (Session 29 paid $50 for an audit that mattered).
- **Orchestration / coordination:** Sonnet.
- **Wiki Pass 2 stages 3+:** mostly Python now; agent prompts (review, etc.) → Sonnet default.
- **When writing new agent prompts:** include a `model:` field in the prompt frontmatter; pick the cheapest tier that meets the task. Document the rationale in the prompt header so future sessions don't blindly upgrade.

Apply this lens when reviewing existing prompts (mechanical-extractor, wiki-ingester, voice-analyzer, etc.) — if Opus was chosen by default rather than necessity, downgrade with a smoke-test gate.

- [ ] **Pass 1 model-fit smoke test (Opus → Sonnet 4.6) — DEFERRED until one book remains** — Matt wants at least one full Opus pass on every book before considering Sonnet for Pass 1. ASOS currently being run on Opus by Matt's friend (shared Max account). After ASOS lands and ADWD is the only book remaining, revisit: pick 2 ADWD chapters (1 dialogue-heavy, 1 action-heavy), run with Sonnet 4.6, side-by-side compare against an Opus baseline for 12 Raw Entity List headers, Food & Drink / Hospitality / Physical Environment sections, character appearances, confidence-tier markers. If ≥90% parity, switch default for ADWD. **Do not include in any current continue prompt.**
- [ ] **Audit existing agent prompts for model-fit drift** — Sweep `.claude/agents/*.md` for hardcoded Opus usage. Where the task is mechanical (parsing, classification, structured emission), drop to Sonnet/Haiku with a smoke-test gate. Where the task requires deep reasoning (cross-corpus contradiction surfacing, schema-drift audit), keep Opus. Document each decision in a comment block at the top of the prompt.

---

## Agent Improvements

- [x] **mechanical-extractor: test variations** — Done 2026-04-22. Smoke test on 3 AGOT chapters (prologue, bran-01, catelyn-01) surfaced three issues: cross-chapter dramatic-irony leak, no confidence tier convention, missing `first_available` field in metadata. All addressed via prompt patches (see below).
- [x] **mechanical-extractor: chapter isolation enforcement** — Done 2026-04-22. Added dedicated "Chapter Isolation — Critical" section to agent prompt; flipped Rule #5 from "Note dramatic irony" (which encouraged leakage) to "Dramatic irony is NOT your concern"; tightened the `Known To (Reader Only?)` column definition to within-chapter scope only.
- [x] **mechanical-extractor: confidence tier convention** — Done 2026-04-22. All Pass 1 output now treated as Tier 1 by default, with only two explicit exception markers: `(inferred)` and `(uncertain — verify)`. Eliminates per-row tier-tag noise.
- [x] **mechanical-extractor: first_available field** — Done 2026-04-22. Added `first_available` field to Chapter Metadata section of output schema so downstream passes can grep spoiler-gating anchor directly.
- [x] **mechanical-extractor: no meta-commentary in tables** — Done 2026-04-22. Added Rule #9 forbidding the extractor from using table cells to explain its own extraction choices (observed in agot-bran-01 smoke output — "symbolic weight is implicit…" in the Information Revealed table).
- [x] **mechanical-extractor: tighten Raw Entity List categories** — Done 2026-04-24. Prompt now v3: 12 categories (10 named + Other catch-all), strict formatting rules (all headers required, no merging/renaming, "None" for empty). Prior extractions archived (agot-v1, agot-v2, acok-v2). v3 run in progress (30/73 AGOT).
- [ ] **mechanical-extractor: add worked example** — Still open. The original spec had an AFFC Prologue example extraction showing ideal output. Consider adding as a reference file the agent loads. Deferred until we've seen the patched prompt run a full book — if output quality is high without the example, may not be needed.
- [ ] **mechanical-extractor: verbose extraction rules** — Still open. The original spec had richer fact-vs-interpretation explanations (e.g., "Ned thinks about a promise" = fact vs. "Ned is thinking about Lyanna's deathbed promise about Jon" = interpretation). Deferred pending full-book output review.
- [x] **script-builder: verify POV table completeness** — Done. Found 6 missing headings (THE REAVER, THE BLIND GIRL, A GHOST IN WINTERFELL, THE IRON SUITOR, THE KINGBREAKER, THE QUEEN'S HAND). All added to reference file and splitter.

## Extraction Infrastructure

- [ ] **Hands-off pacing v2 — single-coordinator design** — `--chain`/`--delay` were removed in the Session 33 fix because every-terminal-relaunches is combinatorially unsafe. If hands-off pacing is wanted later, design ONE coordinator process that owns the schedule and dispatches waves at intervals — never spawning new terminals that themselves re-launch. Coordinator reads the stats CSV (already a single source of truth post-fix), claims the next batch of waves, sleeps, dispatches, repeats. Single point of control = no chain explosion possible. Deferred — not currently blocking any work.

## Timeline & Chronology

- [ ] **Timeline reconstruction pass or script** — Once all books have Pass 1 extractions with `time_markers` fields, build a dedicated pass (or Python script) that attempts fan-community-style relative timeline reconstruction from the captured markers across all chapters. Cross-reference with known travel times, moon-turn references, and seasonal indicators.

## Direwolves & Dragons

- [x] **Direwolves and dragons are characters, not creatures** — Ghost, Grey Wind, Lady, Nymeria, Summer, Shaggydog, Drogon, Rhaegal, and Viserion must be treated as characters in all extractions and graph nodes. They have agency, POV-adjacent perspectives, and narrative arcs. Added to mechanical-extractor prompt (2026-04-22). Verify AGOT re-run captures them in Characters Present tables, not as fauna. Ensure node schema and future agent prompts also enforce this.

## Agent Prompts to Write

- [ ] **Pass 2: wiki-ingester** — Full prompt. Needs: node file schema, wiki infobox parser output, entity target list from Pass 1, **wiki-category → confidence-tier mapping** (wiki mixes canon, fan inference, and theory on the same pages — the category/tag system helps route to correct tiers, but needs explicit rules).
- [ ] **Pass 3: voice-analyzer** — Full prompt. Needs: voice profile schema, perception mapping format, identity-split handling.
- [ ] **Pass 4: foreshadowing-scanner** — Full prompt. Needs: mapping output schema, confidence criteria, scanning strategy (per-chapter vs per-event).
- [ ] **Pass 5: theory-extractor** — Full prompt. Needs: theory seeds file first (`reference/theory-seeds.md`), evidence mapping schema.
- [ ] **Pass 6: discovery-agent** — Full prompt. Needs: scoping strategy, candidate finding format, chunking approach.

## Reference Files to Create

- [ ] **theory-seeds.md** — Top 20-30 ASOIAF theories with descriptions and confidence tiers. Input for Pass 5.
- [ ] **taxonomy.md** — Standalone reference for entity types, edge types, confidence tiers (currently embedded in architecture.md).
- [ ] **node-schema.md** — What does a single entity node file look like? Frontmatter fields, required sections.
- [ ] **Expand `reference/foreshadowing-events.md` before Pass 4** — currently 26 events + 15 Chekhov's guns. Audit/expand both: more event anchors (deaths, identity reveals, plot reveals, magic returnings, prophecy fulfillments) AND a richer Chekhov's gun *pattern library* so the scanner can flag candidate unknown foreshadowing, not just match known events. See `progress/scratch-notes.md` "Foreshadowing Pass Prep" entry for detail. Long-lead — Pass 4 is many sessions away, but file this now so it's not forgotten.

## Collaboration

- [ ] **Schema lock-in before collaborator handoff** — A collaborator may join to share extraction load. They have less ASOIAF depth than Matt, so the Pass 1 schema must produce correct output without needing lore knowledge to second-guess. Sequence: finish Track B → schema review across AGOT v3 → lock schema → onboard collaborator. See `progress/scratch-notes.md` "Collaborator Onboarding" entry.
- [ ] **Revisit `/install-github-app`** — Useful once a collaborator is producing extractions: tagging Claude in PRs/issues becomes the natural review surface. Skip until handoff is near.
- [ ] **Collaborator quick-reference doc** — README onboarding works for first-run, but a collaborator extracting on another machine needs a focused "running extractions" reference that doesn't require reading CLAUDE.md end-to-end. Write before handoff.

## Wiki / Pass 2 Prep

- [x] **Track B orchestration planning (PLAN-ONLY session)** — Done 2026-04-25. Output: `working/runbooks/wiki-pass2-orchestration.md` covering 7 design items (work decomposition, concurrency, drift prevention, process metadata, resumption, failure modes, storage). Key calls: triage stage precedes Pass 2; bucket = chapter-equivalent; `wiki-pass2.sh` mirrors `extract.sh`; markdown+JSONL+CSV stays (no DB, with revisit triggers). 7 open questions surfaced for implementation session.
- [x] **Fresh-agent review of wiki Pass 2 orchestration plan** — Done 2026-04-25 (Session 16). 21 issues in `working/runbooks/wiki-pass2-orchestration-review.md`, all accepted. Verdict: needs targeted patches before implementation. Bias note logged: writing+prosecuting agent was the same; future reviews should rotate reviewer.
- [x] **Apply review patches to wiki Pass 2 orchestration** — Done 2026-04-25 (Session 17). 21 decisions applied to runbook + Track B prompt + implement prompt. Self-review caught 4 additional coherence issues (5.1.1 vs 5.4 fingerprint-mismatch contradiction, missing status codes, stale Implementation Sequence, stale `weirwood-wiki` references) and fixed them. Patch + review continue prompts deleted.
- [x] **Build wiki Pass 2 orchestration scripts** — Done across an unwritten-up build session (2026-04-25/26) + Session 18 cleanup (2026-04-26). Final scripts: `wiki-pass2-triage.py` (page categorization + bucket grouping + `--accept` manifest writer), `wiki-pass2-coherence.py`, `wiki-pass2.sh` (10 subcommands), `weirwood.zsh` extended with `wiki` subcommand. Fixed pipefail/SIGPIPE bug in `wiki-pass2.sh` status checks. 507 per-bucket manifests written (35 core, 472 secondary).
- [x] **Wiki Pass 2 launch prep — script review + wiki-ingester prompt** — Done 2026-04-26 (Session 19). Cold script review at `working/runbooks/wiki-pass2-orchestration-build-self-review.md` (3 blockers + 11 correctness issues). Arya bug fixed. Wiki-ingester prompt written (180 lines, mechanical-extractor as model). B1 path-mismatch fixed. Validator built (`scripts/wiki-pass2-validator.py`). `first_available` softened to optional in v1 (spoiler gating deferred to backfill pass).
- [x] **Smoke-test direwolves** — Done 2026-04-26 (Session 20). Silent-exit bug fixed (`scripts/wiki-pass2.sh:286-290` — `&&`-chain → `if`-block; root cause: `set -e` + arithmetic-test-as-final-line returns 1 silently). Smoke ran clean: 6 nodes, 4m 47s, $1.15, validator + coherence green. Hand-inspection surfaced T1 (triage direwolf override hard-codes "Nymeria" → routes warrior-queen page) and P1 (smoke summary doesn't surface questions-for-matt entries) — see follow-up todos.
- [x] **T1: Triage direwolf override needs disambiguator** — Done 2026-04-26 (Session 21). `DIREWOLF_PAGE_OVERRIDES = {"Nymeria": "Nymeria (direwolf)"}` in `scripts/wiki-pass2-triage.py`. Pattern extensible to other collisions (Aegon I/II/V, etc.) when surfaced.
- [x] **P1: Launcher should surface questions/conflicts/contradictions in summary** — Done 2026-04-26 (Session 21). `cmd_run` snapshots JSONL line counts per bucket and computes deltas; surfaced in per-bucket OK line, wave summary footer, and 3 new stats CSV columns.
- [x] **P2: Launcher should write `validation_report` path into manifest** — Done 2026-04-26 (Session 21). Surfaced when Matt eyeballed direwolves manifest line 28. Two-line fix in `cmd_run` (both pass and fail branches). Existing 508 not-yet-run manifests will heal on first run; direwolves backfilled by hand.
- [x] **Scale Wiki Pass 2 core (Stage 1)** — Done 2026-04-27 (Session 23). All 37/37 core buckets complete. 855 nodes promoted (591 chars + 261 houses + 3 factions; 3 video-game-only houses excluded post-emit). 74/74 questions resolved. Validator 37/37 pass. Total Stage-1 cost $95.33 ($2.58/bucket; per-node $0.111 healthy per Stage-2 cold review).
- [x] **Stage 2: Cold review of wiki Pass 2 core output** — Done 2026-04-27 (Session 24). Decision: **remediate** (`first_available` schema drift + silent parser-bug propagation). **Decision overturned same session by Matt:** spoiler gating deferred to post-first-release; remediation prompt replaced. See `2026-04-27-wiki-pass2-stage3-prep.md`.
- [x] **Stage 3: Wiki Pass 2 secondary tier (Python-first pipeline)** — Done 2026-04-28 (Session 26). Full pipeline rebuilt as Python-only after design review surfaced that LLM agent for Stage 3b was inertia-driven, not analysis-driven. Three-script chain delivered: `wiki-pass2-prioritize.py` (Tier A/B/C labels on 472 manifests), `wiki-pass2-emit-deterministic.py` (Stage 3a — 3,315 skeletons emitted), `wiki-pass2-extract-prose.py` (Stage 3b — 2,988 prose files emitted, deterministic HTML→markdown extraction with h2→schema-heading mapping table, h3 book-boundary preservation, cite_ref translation), `wiki-pass2-promote.py` (skeleton + prose concatenation, atomic-rename to `graph/nodes/<type>/`). **Result:** 3,314 new nodes promoted (graph total 855→4,169), $0 cost, ~30 seconds total wall-clock. 0 conflicts. 1 unclassified (`battle-of-the-blackwater-song` type=unknown). Mid-stage agent review ran clean (0 HIGH, 4 MED upstream issues already on this list). Pipeline runbook rewritten at `working/runbooks/wiki-pass2-pipeline.md` (v3). Single-writer-per-file invariant documented at `reference/architecture.md` § "Artifact Formats by Consumer".
- [ ] **Stage 4: Prose-derived edge discovery** — sequential to Stage 3, NEVER parallel. Discovers prose-derived single-page edges, cross-page edges (Reek=Theon SAME_AS, etc.), and Pass-1-derived edges (perception, emotional, POV-internal) that infobox emission can't see. Pipeline shape TBD; flesh out when Pass 1 finishes the remaining 4 books (Stage 4 wants Pass-1 cross-checks). Hard rule: never modify Python-emitted infobox edges (immutable); prose-derived edges go under separate subheading or sibling artifact `prose-edges/<slug>.edges.jsonl` per architecture.md's artifact-format pattern. Skeleton drafted at `2026-04-27-wiki-pass2-stage4-edge-discovery.md`.
  → continue: `2026-05-02-stage4-v1-prose-edge-classifier.md`
- [x] **Wiki-ingester prompt v2 (prose-only role)** — OBSOLETED 2026-04-28 (Session 26). The Stage 3b agent role itself was eliminated when Stage 3b became Python (`wiki-pass2-extract-prose.py`). The wiki-ingester v1 prompt is preserved for Stage 1 re-runs only with a header-note explaining the redesign. Future Stage 4 agent will need its own prompt — different role (cross-page edge discovery, not prose synthesis).
- [x] **Validator edge byte-equality enforcement** — OBSOLETED 2026-04-28 (Session 26). The byte-equality check was needed when an agent paraphrasing the skeleton in `tmp/` was the threat model. With Stage 3b as Python writing to a sibling `prose/` directory (single-writer-per-file invariant), no agent has a write path to the skeleton bytes. The check is structurally unnecessary — concatenation at promotion is the only path that produces final-node bytes.
- [ ] **Style-mismatch between Stage 1 (agent prose) and Stage 3 (Python prose) nodes (FUTURE)** — Stage 1's 855 promoted nodes have agent-synthesized terse paraphrased prose; Stage 3's 3,314 promoted nodes have deterministic-extracted verbose-but-cited wiki prose. Visible cosmetic mismatch in `graph/nodes/`. Mitigation when relevant: re-run `wiki-pass2-extract-prose.py` against Stage 1 buckets (after temporarily emitting their skeletons via Stage 3a) and re-promote with `--force` overwrite. Not blocking; v1 graph traversal queries care about edges, not prose style. Defer until cosmetic issue surfaces in real query workflow.
- [ ] **`type='unknown'` page (`battle-of-the-blackwater-song`)** — Routed to `graph/nodes/_unclassified/` during Stage 3 promotion. Likely a parser bug — the wiki page is a song named after a battle and the infobox parser couldn't determine entity type. Add to `ENTITY_TYPE_OVERRIDES` in `scripts/wiki-infobox-parser.py` (probably `entity.song` or `text.song`) once that subtype is added to `reference/architecture.md`. Until then, the node sits in `_unclassified/` — readable but not in the type taxonomy.
- [ ] **Alias-aware slug resolver for cross-references (Stage 4 prep)** — Surfaced 2026-04-28 (Session 26) when `wiki-pass2-build-cross-refs.py` reported 34,782 broken links (42.9% of 81,090). Spot-check showed many "broken" refs are actually slug-mismatch: Stage 1 agent promoted Brienne as `brienne-tarth.node.md` (her aliases include `Brienne of Tarth`), but other pages link to `wiki:Brienne_of_Tarth` → kebab-cased to `brienne-of-tarth`, which doesn't match any node filename. Fix: build a Python pass that walks every promoted node's `aliases` field and emits a `wiki_form → canonical_slug` resolver dict. Update `wiki-pass2-build-cross-refs.py` to consult the resolver before declaring a target broken. Should drop broken-link rate from 42.9% to a realistic baseline (probably 10-15% — true gaps in the graph, not slug-mismatch noise). **Stage 4 candidate generation depends on this** — without alias resolution, we'd skip thousands of legitimate edges.
- [ ] **Major houses missing from graph (Stage 1 triage gap)** — Surfaced 2026-04-28 (Session 26) when cross-refs leaderboard showed `house-lannister` at rank 19 (387 references) but no `graph/nodes/houses/house-lannister.node.md` exists. Investigation: Stage 1's "characters-house-lannister-*" buckets processed *characters belonging to House Lannister* (members), not the *house entity* itself. The wiki page "House Lannister" was never in any bucket. Same gap likely applies to: House Tully, House Tyrell, House Greyjoy, House Martell, House Arryn, House Baratheon (the parent house — the cadet branches `house-baratheon-of-kings-landing` and `house-baratheon-of-dragonstone` ARE in graph). House Stark and House Targaryen ARE in graph (they made it into a houses-* bucket, not a characters-* one). Fix: either (a) re-run Stage 1 triage with house-entity pages explicitly routed to a `houses-major-*` bucket, then run Stage 3 pipeline against those buckets, OR (b) add the missing house pages to a one-off Stage 3 bucket and run the existing skeleton+prose+promote pipeline. Option (b) is cheaper. Estimated: 7-10 missing major houses, ~30 minutes to fix end-to-end. **High priority for graph completeness** — these are the most-referenced entities in the corpus.
- [x] **Cross-references index built — Stage 4 prep complete (informational)** — Done 2026-04-28 (Session 26). `scripts/wiki-pass2-build-cross-refs.py` walks all 2,988 prose files, extracts 81,090 markdown links, emits `working/wiki-parsed/{cross-references.jsonl, backlink-counts.json, cross-refs-summary.md}`. Top-50 leaderboard validated (all 6 expected major characters in top 20). 42.9% broken-link rate (see alias-resolver TODO above for the planned reduction). Wall-clock: 1.4s. This is Stage 4 Step 1 from the hybrid plan in `progress/continue-prompts/2026-04-27-wiki-pass2-stage4-edge-discovery.md`.
- [x] **Tier 1 entity recovery (major houses, regions, locations, pre-narrative Targs)** — Done 2026-04-28 (Session 26). One-off bucket `houses-major-recovery` with 46 pages: 7 missing major houses (Lannister, Greyjoy, Baratheon, Frey, Bolton, Mormont, Karstark), 27 regions+locations (Westeros, North, Reach, Riverlands, Vale, Westerlands, Stormlands, Iron Islands, Dorne, Beyond the Wall, Crownlands, Free Cities, Slaver's Bay, Essos, Iron Throne, Wall, Red Keep, Castle Black, etc.), 10 pre-narrative Targaryens (Aegon I-V, Aerys II, Aenys I, Viserys I, Daemon, Corlys), Gendry + Gilly. Type-patched no-infobox pages (regions/locations defaulting to `unknown`). Broken links 34,782 → 31,933 (-2,849). Same Stage 3 pipeline.
- [x] **Tier 2 entity recovery (religions, titles, cultures, organizations)** — Done 2026-04-28 (Session 26). One-off bucket `tier2-recovery` with 24 pages: 4 religions (Faith of the Seven, R'hllor, Old Gods of the Forest, Drowned God), 12 titles (Knight, Squire, Septon, Lord, Bastard, Maester, Grand Maester, Regent, Sellsword, King in the North, Prince of Dorne, Hedge Knight), 6 cultures (Ironborn, Free Folk, Wildlings, Dornishmen, Dothraki, First Men) typed as `organization.faction` v1 placeholder, 2 organizations (Brotherhood Without Banners, Brave Companions). Broken links 31,933 → 29,291 (-2,642). Cumulative recovery this session: -5,491 broken links.
- [ ] **Religion node type drift — normalize to `religion`** — Surfaced 2026-04-28 (Session 26). Tier 2 recovery typed religion nodes as `type: organization.religion` (which the script-builder added to TYPE_DIR_MAP), but `reference/architecture.md` declares `religion` as the canonical type. Files landed in `graph/nodes/religions/` correctly but the type string drifted. Fix: one-shot `sed` over the 4 religion node files to change `type: organization.religion` → `type: religion`. Or extend architecture.md to allow both. Lean: normalize to `religion` per architecture.md.
- [ ] **PROMINENT — Multi-type entity policy (v1 = single node)** — Some wiki pages encode multiple entity types simultaneously: Citadel (organization + place), Faith of the Seven (religion + organization), Night's Watch (organization + sworn brotherhood — already handled via `organization.faction` v1 placeholder). **v1 policy:** emit each as ONE node with the dominant type from the wiki infobox. Don't split. Don't add `secondary_types`. The "X is also a place" relationship goes in `## Edges` (e.g., `HEADQUARTERED_AT: Oldtown`), not a second node. **Multi-type tension belongs to Stage 4 entity-merge-resolver**, not node creation. **Disambiguation pages (Aegon disambiguation, Brandon disambiguation, etc.) NOT promoted as graph nodes** — each named individual has their own unique-slugged node; "which Aegon does this prose mean?" is a Stage 4 disambiguation-resolver concern. See `working/agent-pipeline-plan.md` for the full policy.
- [ ] **Agent pipeline plan written** — Done 2026-04-28 (Session 26). `working/agent-pipeline-plan.md` walks through all open work, groups by Group A (deterministic Python, no agent), Group B (hybrid Python + agent), Group C (pure agent — Pass 3-6), Group D (Pass 1 books pending). Suggests dependency-driven ordering; estimates total remaining-work cost ~$1,200-1,800. Prominent multi-type entity policy and the four new agent roles needed (prose-edge-classifier, entity-merge-resolver, disambiguation-resolver, chronology-extractor) are documented there.
- [ ] **Tier 3 — chronology extraction (year pages, e.g. 130-ac, 133-ac, 209-ac)** — Deferred 2026-04-28 (Session 26). Year pages are dense in cite_refs and reference structure. Plan: build `chronology-extractor` agent that ingests year pages and emits `OCCURRED_IN_YEAR(<event>, <year>)` and `PRECEDES`/`FOLLOWS` edges. Hybrid pattern (Python prep + agent classification) per `working/agent-pipeline-plan.md` Group B3. Skeleton continue-prompt to be drafted. NOT a node-creation task — year pages may not become graph nodes themselves; they're sources of temporal edges between existing event nodes.
- [ ] **Tier 3 — concept pages decision (Tourney, Marriage, Sept, Trial by Combat, Kingsmoot, Fool, Maiden)** — Deferred 2026-04-28 (Session 26). Glossary-style pages. Three options: (i) new `concept` type, (ii) selectively promote role-like ones as `title` (Maiden, Fool, Septon), (iii) defer until Stage 4 traversal patterns reveal whether anything edges to them. **Recommendation: (iii) defer** — wait for empirical signal from Stage 4 candidate generation; adding 20-30 concept nodes nothing edges to is graph noise.
- [ ] **NEW AGENT — `prose-edge-classifier` (Stage 4)** — Need to write the prompt at `.claude/agents/prose-edge-classifier.md`. Role: read candidate-edge JSONL rows + source prose snippet + target node frontmatter → decide edge_type from locked vocabulary OR reject as just-a-mention OR escalate cross-identity OR escalate to questions-for-matt. Forbidden from: emitting edge types not in architecture.md, modifying any existing nodes, modifying skeleton/prose artifacts. Write to `working/wiki-pass2/<bucket>/prose-edges/<slug>.edges.jsonl` only. See `working/agent-pipeline-plan.md` Group B1.
- [ ] **NEW AGENT — `entity-merge-resolver` (Stage 4 sub-task)** — Need to write the prompt. Role: resolve multi-type entity tensions (Citadel place+org), wiki-redirect-to-canonical-node mappings, and obvious cross-identity merges that don't need full reasoning (e.g., wiki redirect explicitly says "Reek redirects to Theon"). Output: `working/wiki-parsed/entity-merge-decisions.jsonl` with one row per resolved/escalated case. Probably consolidates with `prose-edge-classifier` if candidate volume is low.
- [ ] **NEW AGENT — `disambiguation-resolver` (Stage 4 sub-task)** — Need to write the prompt. Role: when prose says "King Aegon", disambiguate which (I, II, III, IV, V) by reading surrounding context + chronology hints. May consolidate with `prose-edge-classifier`.
- [ ] **NEW AGENT — `chronology-extractor` (Tier 3 deferred)** — Need to write the prompt. Role: ingest year pages, emit chronology edges. See Tier 3 todo above.
- [ ] **Temporal edges (v2 schema)** — Surfaced 2026-04-28 (Session 26). Current schema handles temporal/state-changing edges via bracketed qualifiers (`SPOUSE_OF: Lysa Tully [third wife]`). Adequate for human reading, BUT can't support temporal range queries ("who ruled Winterfell between 298 and 305 AC?"). v2 plan: keep the bracket for human readability AND add OPTIONAL structured temporal fields per edge — `start_year` (int, AC), `end_year` (int, AC), `precision` (exact / approximate / range / unknown), `scope` (free-text qualifier), and `start_event` / `relation` for event-anchored cases when AC year unknown. Goes through vocabulary-lock procedure: architecture.md edit → parser update → re-run. The fields apply to ALL edge types (any edge can carry temporal scope), not just RULES/HOLDS_TITLE. Dovetails with `chronology-extractor` (Tier 3 deferred) — that agent's output supplies the AC-year anchors that other edges can reference. Don't build until query-pattern signal demonstrates it's needed; the bracket-qualifier-only approach is fine for v1 read-only graph traversal.
- [ ] **Per-entity index tables (post-Stage-4 cleanup)** — Surfaced 2026-04-28 (Session 26). Already in `reference/architecture.md` § "Artifact Formats by Consumer" as `entity-index/<slug>.index.json` (post-promotion script output). Build a Python script `scripts/build-entity-indexes.py` after Stage 4 prose-edges promote, to derive per-entity views: outbound edges (from node), inbound edges (from cross-references.jsonl), chapter appearances (from Pass 1 mentions), node-mention list (from cross-refs prose mentions), temporal timeline (when temporal-edge fields land), in_count/out_count (from backlink-counts.json), book appearances (from cite_ref books). Output: `graph/index/<type>/<slug>.index.json`. Lives separately from the node — derived artifact, rebuilt when underlying data changes. Enables "give me everything about Eddard" queries that node-file alone can't.
- [ ] **PROMINENT — Spoiler gate defaults FULLY OPEN for v1** — Confirmed 2026-04-28 (Session 26). For v1 chat UI / retrieval layer, every chunk + every node + every edge is fair game regardless of user read-progress. No `first_available` filtering at query time. No user read-progress UI in v1. The `first_available` backfill (deferred per `project_first_available_deferred.md`) stays scheduled but is NOT a blocker for chat UI shipping. Saves complexity: chunker doesn't compute `first_available`, embedding pipeline stays simpler, query layer skips the gate stage. When v2 spoiler-gating ships, the gate becomes an ADDITIVE filter — `first_available` joins the existing structured fields without changing the corpus shape. Don't accidentally re-introduce gating earlier; this is an explicit v1 simplification.
- [ ] **PROMINENT — Chat UI scope shifted to deployable D&D-group preview** — Confirmed 2026-04-28 (Session 26). v1 chat UI is NOT personal-local — it's a shareable web UI behind minimal auth (shared password) for Matt's D&D friend group, with full prose retrieval including book chunks. Architecture lives at `working/chat-ui-architecture.md` (deployment + auth + cost envelope + book-copyright posture all documented there). Cost envelope ~$10-50/month for D&D-group scale. Book-copyright posture: friend-group only behind auth, synthesis-not-quotation enforced in LLM system prompt, snippet-limited source rendering, ready-to-remove-if-asked. Builds on top of all other pipeline work (Stage 4 + Pass 1 catch-up + per-entity indexes + chunkers + embedding + FastAPI backend + pure-TS+CSS chat component matching mattnoth-dev's stack). NOT shipping until Pass 1 has multi-book coverage (chunks need mention-tagging from Pass 1's per-chapter character lists). Phase 5 of the build order in chat-ui-architecture.md is "the chat UI exists and is shareable"; Phases 2-3 are the bottleneck.
- [ ] **Two-repo split before chat-UI deployment** — Surfaced 2026-04-28 (Session 26). Current pattern (`.gitignore` protecting copyrighted content within a single private `weirdwood-network` repo) is brittle — one accidental `git add -f` could leak. Cleaner architecture: split into two repos along the copyright boundary. **`weirdwood-network`** (current, could become PUBLIC eventually) keeps scripts/docs/schema/graph-nodes/wiki-cache/design-docs/chat-UI-component-code — all wiki-derived or original, public-safe. **`weirwood-corpus`** (NEW, permanently PRIVATE) holds `sources/raw/`, `sources/chapters/`, `extractions/mechanical/`, future `working/book-chunks/` — all book-derived. Repo split makes copyright protection STRUCTURAL not gitignore-dependent. Frontend deploy (Netlify) imports `weirdwood-network` (zero copyright risk). Backend deploy bundles both at build time OR mounts corpus separately. `weirdwood-network` can be open-sourced if desired. Execute before chat UI ships, not before. Not blocking pipeline construction work.
- [ ] **GitHub state confirmed** — 2026-04-28 (Session 26). Repo is `mattnoth/weirdwood-network` (note spelling: weiRdwood, with extra `d`). Visibility: PRIVATE. Remote URL `https://github.com/mattnoth/weirdwood-network.git`. .gitignore correctly excludes `sources/raw/` and `sources/chapters/`. For mattnoth-dev (Netlify-deployed) to include this as a submodule, authorize Netlify's GitHub app on this repo (one-time OAuth flow in Netlify repo connection settings). For backend deployment, books reach the backend host via separate path (recommended: local build + private container registry like GitHub Container Registry's free tier).

## Path B Promotion Campaign (Session 28, complete)

> Pre-session: `unknown` 12,434 (70.4%). Post-session: `unknown` 2,118 (12.0%). Graph 5,008 → 7,248 (+2,240). Cat 1 orphan edges 2,955 → 1,973 (−982).

- [x] **Path B categorizer extension + categories backfill** — Done 2026-04-30 (Session 28 Act 1). `scripts/wiki-fetch-categories.py` (bounded `cloudscraper` exception fetch); CATEGORY_TYPE_MAP added to parser. `unknown` 12,434 → 2,487. CLAUDE.md narrow re-fetch exception clause + audit log entry.
- [x] **Path B promotion sub-tasks 1a/1b/2/3/4/5/6** — Done 2026-05-01 (Session 28 Act 2). 7 promotion scripts at `scripts/wiki-pass2-tier3-pathb-{texts,artifacts,locations,events,orgs,characters,longtail}.py`. Per-type routing via TYPE_TO_DIR map for orgs/longtail. 14 commits (1 revert). Bootstrapped `texts/` (150), `theories/` (45), `concepts/` (31), `species/` (38), `foods/` (69).
- [x] **Real-world publications skip rule** — Done. Parser-level: pages categorized only as `Books` (without `Books and scrolls`) classify as skip. 31 affected pages: A Game of Thrones, A Clash of Kings, A Knight of the Seven Kingdoms, Fire & Blood, etc.
- [x] **Chapter-summary pages skip rule** — Done. Parser-level: `A Song of Ice And Fire chapters` category → SKIP_CATEGORY. Caught a 338-node false-positive disaster in events Sub-task 3 (reverted commit `e72aa039` via `fbbf764c`, then re-emitted clean).
- [x] **`event.tournament` new entity type** — Done. CATEGORY_TYPE_MAP `Tourneys|Tournaments → event.tournament`; PAGE_NAME_TYPE_PATTERNS `\btourney\b → event.tournament` for empty-category pages like Ashford Tourney. Routes to `events/` via TYPE_DIR_MAP.
- [x] **`object.food` new entity type + `foods/` dir** — Done. Schema add to architecture.md. Food + Drinks combined per Matt's design value. 69 nodes promoted (Bowl of brown, mead, dreamwine, lemon cakes, Arbor wine, etc.). Trees route to `species/` (broadens dir scope to in-world flora).
- [x] **Knight of the Laughing Tree promoted** — Done. ENTITY_TYPE_OVERRIDES: `Knight of the Laughing Tree → character.human`. Lyanna's secret identity at Harrenhal — R+L=J pillar. Promoted as separate node from Lyanna; SAME_AS edge is Stage 4 cross-identity-detector territory.
- [x] **Promotion completion + schema-drift audit** — Done 2026-05-01 (Session 29). 4 new entity types (object.material, concept.language, concept.medical, concept.custom). +315 net graph nodes. 130 stale-dir mismatches cleaned. Schema-drift audit clean (0 HIGH, 4 MED → 2 fixed + 1 decided + 1 deferred-to-Stage-4, 4 LOW deferred). unknown 2,098 → 1,257. Continue prompt deleted (work complete).
- [ ] **v2 temporal-edges schema design** — Surfaced 2026-05-01 (Session 29). `working/wiki-parsed/chronology-events.jsonl` (2,245 rows) extracted from 74 year pages. Locked 22-type vocabulary doesn't include `OCCURRED_IN_YEAR`. v2 plan (Session 26 TODO) adds OPTIONAL structured per-edge fields (`start_year`, `end_year`, `precision`, `scope`) rather than a dedicated edge type. Decide architecture before promoting chronology rows as edges. Until then, the JSONL stays as input data for future backfill.
- [ ] **Stage-1 v1 prose drift cleanup (LOW)** — Schema-drift audit (2026-05-02) flagged 4 LOW findings, all in Stage-1 v1 prose carve-out: ~544 v1 nodes use `## Origins` instead of `## Identity` heading; 360 v1 nodes have backtick-wrapped edge labels in prose body; 11 v1 prose lines retain malformed bare `(wiki:Rasos11)` cite refs; empty `prophecies/` dir (placeholder, harmless). Auditor's recommendation: bundle into Stage-4 prose-edge-classifier work, do NOT fix piecemeal.
- [ ] **Stale-dir cleanup script reusability** — Built 2026-05-01 (Session 29) at `scripts/wiki-pass2-stale-dir-cleanup.py`. Stage-1 carve-out wired in (skips `prompt_version: v1` nodes). Reusable for future parser changes that reclassify already-promoted nodes. Pattern to remember: parser change → re-run parser → run cleanup --plan → run cleanup --apply → re-run dedicated promotion scripts.

## Audit Findings (Session 27, sample audit on houses + locations)

> Source: `working/audits/schema-drift-sample-2026-04-30.md` (sample of 500 nodes; full corpus 4,239)
>
> **Stage framing (added Session 27):** the audit-driven cleanup work below is **Stage 3c** (corrective — fixes emission bugs in already-promoted nodes), not Stage 4 (additive — prose-derived edge discovery). See `working/runbooks/wiki-pass2-pipeline.md` § "Stage 3a/3b/3c vs Stage 4 — corrective vs additive boundary" for the canonical definition. Stage 3c is the natural boundary for audit + parser-fix + targeted re-promotion cycles; Stage 4 only begins once Stage 3c stabilizes.

### HIGH — fix at parser level

- [x] **Type-classifier fallback bug — wars/factions/artifacts mistyped as `place.location`** (Finding 1.1) — PARSER FIXED + 14 NODES RE-PROMOTED 2026-04-30. Added `PAGE_NAME_TYPE_PATTERNS` (regex for `\bwar\b`, `\brebellion\b`, `\bconquest\b`, `\binvasion\b` → `event.war`) and extended `ENTITY_TYPE_OVERRIDES` with Triarchy + Second Sons (`organization.faction`), Iron Throne (`object.artifact`). 7 wars moved `locations/`→`events/`, 2 factions moved `locations/`→`factions/`, Iron Throne moved `locations/`→`artifacts/`. New script: `scripts/wiki-pass2-repromote-targeted.py` (preserved for reuse). Full-corpus re-run still pending; was scoped down to targeted re-promotion only.
- [x] **Religion-field parser bleed bug** (Finding 5.2) — PARSER FIXED + 4 NODES RE-PROMOTED 2026-04-30. `InlineTextExtractorHTML` now tracks `<small>` depth; links inside `<small>(...)` emitted as `qualifier_for` metadata rather than standalone targets. Lowercase-first-character targets skipped. `seven-kingdoms.node.md`, `pentos.node.md`, `lys.node.md`, `tyrosh.node.md` re-promoted with clean edges. `triarchy` retains `WORSHIPS: Mixed` (uppercase passes filter — pre-existing wiki imperfection, not a parser bug).
- [ ] **OPEN QUESTION: `War_for_the_Dawn` type — `event.war` or `concept.prophecy`?** — Currently fixed to `event.war` via the `\bwar\b` regex. But the War for the Dawn is arguably a prophesied future event / legendary myth, which might fit `concept.prophecy` better. Both are valid; `event.war` is the conservative choice (it's a named war, like others on this wiki). Decide before full-corpus re-run. If `concept.prophecy`, add explicit override in `ENTITY_TYPE_OVERRIDES`.
- [ ] **OPEN QUESTION: `battle-of-the-blackwater-song` type — needs `object.text` subtype** — Currently sits in `graph/nodes/_unclassified/` because the parser emits `unknown`. This page is an in-world song named after a battle. Correct type is likely `object.text`. Add to `ENTITY_TYPE_OVERRIDES` in `scripts/wiki-infobox-parser.py` once architecture.md confirms `object.text` is the right subtype (it is — see architecture.md Type Reference Table). Also note: the regex `\bwar\b` now matches "Battle of the Blackwater" _not_ (no "war" in the name), so this page still falls through to infobox classification. The `object.text` fix remains a separate override entry.

### LOW — policy decisions

- [x] **Deleted `*-telltale` and `*-theories` sub-page nodes 2026-04-30** — Per `project_video_game_entities_excluded.md`: `house-tuttle-telltale.node.md` + `house-whitehill-telltale.node.md` deleted. `house-cassel-theories.node.md` deleted (wiki sub-page collecting fan theories doesn't fit the future Pass-5 theory-node-shape; will be re-ingested properly when theory-seeds.md drives Pass 5). Confirmed corpus-wide grep — no other slug-suffixed `*-telltale` or `*-theories` files remain.
- [x] **`*-guards` sub-page policy DECIDED + FIXED 2026-04-30 (Session 27)** — Matt's call: retype as `organization.faction`. Parser fix: added `\bguards$` pattern to `PAGE_NAME_TYPE_PATTERNS` in `wiki-infobox-parser.py`; moved pattern evaluation BEFORE the "House " prefix check so "House Bolton guards" isn't intercepted as a house. Re-promoted all 6 nodes out of `houses/` into `factions/`: `house-bolton-guards`, `house-stark-guards`, `house-tully-guards`, `house-tyrell-guards`, `house-arryn-guards`, `house-targaryen-guards`. Old `houses/` files deleted.

## Audit Findings (Session 27, sample audit on characters)

> Source: `working/audits/schema-drift-sample-characters-2026-04-30.md` (first 500 of 3,373 character nodes; sister run to houses+locations sample)

### HIGH — fix at parser level

- [x] **BORN_AT / DIED_AT date-bleed in target field** (Finding 5.1c) — PARSER FIXED + NODES RE-PROMOTED 2026-04-30 (Session 27). `wiki-infobox-parser.py` Born/Died field handling now separates date links from place links using `_is_date_link()`: date strings ("263 AC", "Age of Heroes", "In 94 AC") become `qualifier` metadata; place links become the edge target. Also handles "Place, Year" plain-text pattern for Stage-1 nodes. Re-promotion: 1,137 Stage-3 nodes fully re-emitted; 44 Stage-1 nodes had `## Edges` surgically replaced (prose preserved). 0 remaining date-bleed nodes confirmed. `scripts/wiki-pass2-repromote-targeted-2.py` handles all three bugs.
- [x] **Dragon mistyping — all dragons typed `character.human` instead of `character.dragon`** (Finding 1.1c) — PARSER FIXED + 19 NODES RE-PROMOTED 2026-04-30 (Session 27). Added `classify_by_species()` to `wiki-infobox-parser.py`: reads the "Species" infobox field → `character.dragon` (28 pages confirmed). Re-promoted 19 existing nodes (all Stage-3 deterministic). 9 dragon pages not yet in graph (Drogon, Meleys, Meraxes, Seasmoke, Sheepstealer, Shrykos, Silverwing, Terrax, Cannibal) — will be promoted when their buckets are run. Direwolves confirmed correct (regression pass clean). Parser entity distribution now shows `character.dragon: 28`, `character.direwolf: 6`.
- [x] **2 nodes with malformed YAML aliases** (Finding 3.1c) — DATA FIXED 2026-04-30 via direct edit (single-quote outer wrapper so inner double-quotes are literal). Note: this fix gets reverted by any re-emission via `wiki-pass2-emit-deterministic.py` because the underlying parser doesn't YAML-escape inner double-quotes when emitting the `aliases:` array. The cargyll fix is data-only; the upstream parser bug remains.
- [ ] **UPSTREAM — YAML-escape inner quotes in `aliases:` field emission** — Parser bug in `scripts/wiki-pass2-emit-deterministic.py` (and possibly `wiki-infobox-parser.py` aliases extraction). When an alias contains inner double-quotes (e.g., `One of "the celebrated Cargyll twins"`), it's emitted as `aliases: ["One of "the celebrated Cargyll twins""]` — invalid YAML. Fix: either escape inner quotes (`\"`) in double-quoted form, OR switch to single-quoted YAML scalars when alias contains `"`. Affects: at least 2 known cargyll nodes; may affect other characters with quoted-text aliases. Without this fix, any future re-emission re-introduces the bug.

- [x] **Slug-regression bug introduced and fixed in same session 2026-04-30** — `scripts/wiki-pass2-repromote-targeted-2.py` had two `slug = node_path.stem` lines (422 + 492). `Path.stem` on `arryk-cargyll.node.md` returns `arryk-cargyll.node` (only strips `.md`), so 1,120 re-emitted nodes ended up with `slug: <slug>.node` (the `.node` filename suffix bled into the slug field). Script fix: `slug = node_path.stem.removesuffix(".node")` on both lines. Data fix: `sed -i ... -E 's/^(slug: [a-z0-9-]+)\.node$/\1/' ` across all 1,120 affected files. Verified: 0 bad slugs remaining. Lesson: if a future targeted-re-promotion script is built, audit `Path.stem` usage on `*.node.md` paths.

### MED / LOW (already known or out-of-scope)

- `first_available` on 121/614 v1 character nodes — same as houses+locations Finding 3.1, already deferred-policy.
- Edge directionality variants — same as houses+locations Finding 5.1, already documented.
- Cross-identity duplication (Theon/Reek, Sansa/Alayne coexist as separate nodes with no `SAME_AS`/`ALIAS_OF`/`DISGUISED_AS` edges between them) — for `cross-identity-detector` agent in Stage 4; not a schema violation.

### Confirmed clean (same as sister audit)

- Edge-vocabulary drift: 0 violations in ~3,000+ character edges. Lock holds.
- Slug format: 0 violations across 614 character slugs.
- Missing structural sections: 0/614.

### Confirmed clean (skip full audit on these)

- **Edge vocabulary**: 0 violations across 1,562 edges. The 22-type lock holds.
- **Slug format**: 0 violations across 500 slugs.
- **Required frontmatter fields**: 100% present.

### Confirmed deferred (no action — handled later phases)

- `first_available` present in 250 of 322 v1-houses (78%) — already deferred per 2026-04-27 decision; backfill script overwrites all of these post-first-release. Do not invest agent context fixing per-node.
- Edge directionality variants (`(reverse)`, `→`, `->`) — three formats coexist across emitters. Normalize at graph-build time, not at promotion.

## Audit Findings (Session 27, citation-validator full-corpus audit)

> Source: `working/audits/citation-issues-2026-04-30.md` (4,236 in-scope nodes, ~50,000+ citation tokens)

### HIGH — concrete malformed cite_refs (Stage-1 emission limitations)

- [ ] **11 occurrences of bare `(wiki:R{book}{N})` cite_ref-as-page** — Stage-1 agent abbreviated wiki cite_ref anchors as if they were wiki pages. Confirmed in: `house-darke.node.md` `(wiki:Raffc9)`, `house-bar-emmon` `(wiki:Rasos75)`, `house-darklyn` `(wiki:Raffc9)`, `house-blackberry` `(wiki:Rasos10, Rasos63)`, `house-darkwood` `(wiki:Raffc9)`, `house-cave` `(wiki:Raffc20)`, `house-chyttering` `(wiki:Rasos10)`, `house-boggs` `(wiki:Raffc20)`, `house-dargood` `(wiki:Raffc9)`, `house-cargyll` `(wiki:Ragot8)`, `house-byrch` `(wiki:Raffc7, Raffc32, Raffc37)`, `house-chelsted` `(wiki:Rasos11)`, `factions/dragonkeepers` `(wiki:Ragot20, Rasos61)`. Pattern: `R + {book code} + {N}` = "{book} reference #{N}". These are partial pointers — they identify the book but not the chapter. **Fix path:** either (a) deterministic Python pass that parses the wiki source for the parent page, finds the actual cite_ref-N anchor, expands to full citation; OR (b) accept as known Stage-1 limitation and replace with `(source: AFFC)` / `(source: AGOT)` / `(source: ASOS)` to preserve book signal but drop the broken cite_ref pointer; OR (c) leave as-is and address during Stage 1 re-emission. Lean (b) — small one-time data fix, no info loss beyond what's already lost.

- [ ] **2 chapter-numberless `(agot-{pov})` quote citations in `pycelle.node.md`** — lines 47, 49: `(agot-eddard)`, `(agot-sansa)` — should be `(agot-eddard-NN)` / `(agot-sansa-NN)`. Without source extraction lookup, can't recover the chapter number. Same Stage-1 limitation pattern. Defer or hand-patch by reading the chapters where Pycelle delivers those exact quotes.

### MED — Stage-1 vs Stage-3 cite-format divergence

- [ ] **978 occurrences of verbose `cite: track_b_row.relationships.X` form across 223 nodes** — Stage-1 agent emitted relationship-edge citations using a different format than Stage-3 deterministic. Both lower- and upper-case variants coexist. Same edge content as Stage-3's `(track_b: <Field>)` — just verbose. Resolves at graph-build time when the consumer normalizes both formats; do NOT data-fix per-node.

- [ ] **Comma-joined multi-source cites `(wiki:Page, agot-pov-NN)`** — sampled in `cayn`, `donal-noye`, `godwyn`. Probably Stage-1 agent intent (multi-source citation), but not the canonical format. Audit didn't exhaustively count. Defer to graph-build normalizer or Stage 1 re-emission.

- [ ] **Multi-cite_ref chained `(wiki:Page.cite_ref-A, cite_ref-B)`** — sampled in `lemore.node.md`, `tyrion-lannister.node.md`. Same shape as above; format inconsistency rather than missing data.

- [ ] **Stage-1 paragraph-bracketed missing-cite pattern** — Stage-1 nodes (855) cite at paragraph boundaries; mid-paragraph claims rely on the bracketing. `house-mallister.node.md:33` confirmed as a representative case (3 mid-paragraph claims between cited bookends). Not a per-node fix; this is the systemic Stage-1 emission style. Address via:  (a) Stage-1 re-emission via Stage-3 pipeline (converts editorial agent prose to wiki-literal), OR (b) accept and document as Stage-1 cite-style.

### LOW — validated as not-a-bug

- **3,400 cite_refs with URL-encoded gibberish** (`cite_ref-Raffc4.7B.7B.7B3.7D.7D.7D.7B.7B.7B4.7D.7D.7D_12-5`) — verified to be real wiki anchor IDs preserved verbatim from `sources/wiki/_raw/*.json`. They reflect MediaWiki template-substitution leakage in the wiki HTML, NOT parser noise. Functional but ugly. Cosmetic cleanup (regex-strip the `.7B.7B...` suffix) is possible but not blocking.

### PENDING-PASS-1

- **9 chapter-cite occurrences across 6 nodes pointing to AFFC/ACOK/ADWD chapters** — citations like `(affc-brienne-NN)` will resolve once Pass 1 finishes those books (chapters not yet extracted, so the cite target file doesn't exist yet).

### Confirmed clean

- **AGOT chapter cites all resolve** — every `(agot-pov-NN)` cite has a corresponding `extractions/mechanical/agot/agot-pov-NN.extraction.md`. Zero broken file references for the only book with completed Pass 1.
- **Stage-3 nodes have rigorous claim-level citations** — sampled 30 nodes, zero missing-cite findings in narrative prose. Deterministic emitter preserves every wiki `<sup>` cite.

### Recommendation (post-audit)

Run a narrower scripted scan (`scripts/citation-coverage-scan.py` — TODO) that flags sentences ending in a period without a following parenthetical citation in the same paragraph, restricted to sentences with a tensed verb plus proper noun. Without that, Stage-1 missing-cite finding is undersampled.
- Stub Identity prose (`X is a place.location from the AWOIAF wiki.`) on all Stage 3 v1-python nodes — defer to prose-fill phase.
- Empty `## Edges` sections in 62 of 500 — Stage 4 prose-edge-classifier backfills from prose; many are also Finding-1.1 misclassifications that will resolve when bucketing is fixed.

---

### Wiki Pass 2 — Session 22 Followups (post-Stage-1 fixes)

- [ ] **Book-chapter wiki pages (`<Book>-Chapter <N>`) silently dropped by triage** — Surfaced 2026-04-27 (Session 25) during Stage 3 prep. 349 chapter/prologue/epilogue/appendix wiki pages exist in `page-index.jsonl`; triage routed 344 to the `battles` mega-bucket and 5 to `singletons-unknown`, but the alphabetical 30-cap split kept only 14 ASOS chapter pages (in `battles-a`) — the other 335 are not in any wiki-pass2 bucket. `scripts/wiki-pass2-triage.py` mis-classifies these as `event.battle` from an infobox signal, then drops them during split. Plan: (a) remove the 14 stranded pages from `battles-a` so Stage 3a doesn't emit empty skeletons for them, (b) route all 349 into a deferred bucket (`book-chapter-pages-defer` or similar) that Pass 2 ignores, (c) revisit later — these pages may feed Track A (per-chapter mechanical extraction) as a wiki-side cross-reference. Don't delete; defer.
- [ ] **Multi-letter character bucket overlap (triage script bug)** — Confirmed in Session 22: `stark-h-p ∩ stark-h-q = 26/27 pages`. Same pattern likely for stark-{a-b, b-h, q-w, r-w}, greyjoy-{g-r, s-w}, martell-{a-m, m-t}, tyrell-{a-l}. Single-letter buckets (lannister-b/j/r, targaryen-a/b/d/j/o/t) TBD. Fix the alphabetical-split logic in `scripts/wiki-pass2-triage.py` to produce disjoint letter ranges. Existing duplicates in `graph/nodes/_conflicts/` need cleanup pass. Defer to post-Stage-1 (Matt accepted the waste this round).
- [ ] **Launcher wave-math gotcha** — `cmd_launch` in `scripts/wiki-pass2.sh` silently rejects wave numbers > `total_waves`, leaving Terminals 2-N with empty assignments. `weirwood wiki core 4 9` opens only 1 tab when there are 9 waves. Fix: clamp `waves_per` to `total_waves / terminals` before assignment, or warn when terminals get empty waves. Workaround used: `3 3` for 9 waves.
- [x] **Launcher should auto-wipe stale `tmp/`** — Done 2026-04-27 (Session 25). `cmd_run` now reads `prior_status` via `manifest_get` before flipping to in-progress; when prior status is `fail` or `validation-failed`, the bucket's `tmp/` directory is `rm -rf`'d before the new run so the agent starts with a blank canvas. Logs the wipe (`Wiping stale tmp/ from prior <status> run...`).
- [ ] **Question ID collisions (parallel-safe IDs)** — All Session 22 questions filed as `q-2026-04-26-001`. Parallel agents generate IDs without coordination. Fix in agent prompt or post-process: include bucket_id in ID, e.g., `q-2026-04-27-arryn-001`, or have launcher renumber on append.
- [ ] **CSV stats race (parallel JSONL writes)** — `cmd_run` snapshots `q_before`/`c_before`/`p_before` at bucket start, computes delta at end. With parallel tabs writing to the same JSONL files, the snapshot delta picks up sibling-tab writes. CSV `questions_filed`/`conflicts_filed` columns are unreliable. Fix: filter delta to current bucket via `bucket_id` field in JSONL, not raw line count.
- [ ] **Rate-limit detection misclassification** — Session 22 logs show JSON response had `"status":"needs-auth"` AND `"rateLimitType":"seven_day"`, but rate-limit grep at `wiki-pass2.sh:684-688` requires `"status":"rejected"`. The detection still flagged skip-rate-limit (likely a second status entry elsewhere in JSON), but the grep is fragile. Audit + tighten.
- [ ] **Spoiler-gating backfill pass (POST-FIRST-RELEASE)** — `first_available` is fully deferred per 2026-04-27 decision. Build a deterministic Python backfill that walks every node in `graph/nodes/`, derives `first_available` from (a) `track_b_row.books` field as primary signal and (b) cite_ref anchors as refinement, with a cross-check against Pass 1 raw entity lists to catch parser-bug class (e.g., Tyrion/Varys → ADWD). Pass 1 must finish all 5 books first (cross-check needs full coverage). Output: in-place frontmatter rewrite across the whole node corpus in one pass. CLAUDE.md and architecture.md already updated to reflect deferral. When backfill ships, update both back to "active".
- [ ] **Bucket fingerprint missing track_b_row hashes (C1 from cold review)** — `bucket_fingerprint` covers `sorted(input_pages) + prompt_version + chunk_strategy` but not the Track B JSONL row content. Wiki re-crawl that updates an infobox doesn't trigger re-queue. Cheap fix: SHA-256 of sorted JSON of `track_b_row` per page, folded into existing payload.
- [ ] **`tier_default` for direwolves should be tier-1 (C8 from cold review)** — Currently falls to fallback `tier-2`; named characters with verified canon facts. Either add `direwolves|dragons` to tier-1 regex in `TIER_DEFAULT_RULES`, or extend rule to match `character` substring.
- [ ] **Oversized bucket-of-one suffix regex (C10 from cold review)** — `_SPLIT_SUFFIX` allows at most two single-letter segments. A bucket-of-one named `eddard-stark` has multi-char hyphenated suffix and lands in secondary tier instead of core. Fix: match `^direwolves(-.+)?$` (or generalize for any bucket-of-one).
- [ ] **`wiki-pass2-triage.py --limit N` is destructive** — opens output in `"w"` mode and clobbers full output. Either guard or write to a separate path when `--limit` is set. Non-blocking but real footgun.
- [ ] **Tripwire threshold mismatch** — `wiki-pass2-triage.py` defaults to 0.8 but only ~30% of pages are non-singleton (correct data shape: 70% are stubs without infoboxes). Either lower default to ~0.25 or measure tripwire against `pages-with-infoboxes` (~5,279) instead of total.
- [x] **`direwolves` bucket includes Arya Stark via alias match** — Fixed 2026-04-26 (Session 19). `scripts/wiki-pass2-triage.py:419-427` direwolf override now matches on page name only (alias clause removed). Verified manifest: 6 canonical direwolves.
- [x] **Wiki infobox parser for `first_available`** — Done 2026-04-25 (Session 18). `scripts/wiki-infobox-parser.py` produces 3 outputs in `working/wiki-parsed/`: `infobox-data.jsonl` (5,279 lines), `page-index.jsonl` (17,657 lines, L1 mandatory deliverable), `parse-stats.md`. `first_available` populated for 2,888 of 5,279 infobox-rich pages (54.7%, 99.9% from cite_ref). Bug fixed during build (falsy-zero on chapter 0 cite_refs). **Three open issues surfaced — see follow-up todos below.**
- [ ] **BLOCKER: HTML categories empty in page-index** — `categories[]` is `[]` for all 17,657 entries because the wiki-scraper used MediaWiki's parse API, which strips the `mw-normal-catlinks` footer. Runbook §1.2.1 explicitly requires HTML categories for triage bucketing. **Decide before Pass 2 build session:** (a) re-fetch with `&prop=categories` (~17k re-requests), (b) hit `action=query&prop=categories` separately and merge, or (c) amend runbook to drop categories requirement and rely on `entity_type_guess` (already populated from infobox-field signatures). Option (c) is cheapest but underexamined — was assumed away during runbook review.
- [ ] **Investigate parser `books` field discrepancy** — `parse-stats.md` reports 1,953 raw `books` infobox field occurrences across the corpus, but only 37 entries in `infobox-data.jsonl` have a populated `books` array. Either parser drops most of them or the field exists empty/malformed on most pages. Affects book-level `first_available` fallback (currently fires only 3 times). Likely a small parser fix.
- [ ] **Edge taxonomy gaps surfaced by Track B** — Unmapped infobox fields worth adding: `dynasty` (222 pages, no `MEMBER_OF_DYNASTY`-style edge), `written by` (168, no `WRITTEN_BY` edge for in-world texts), `hatched` (8, dragon-classification opportunity). Parser-level mapping gaps: `fathers` plural (21), `vassal` (8), `cadet branch` singular (11) — easy additions to `FIELD_EDGE_MAP`. Roll into v3 schema review or address standalone. **Vocabulary lock:** any new edge type must land in `reference/architecture.md` § "Wiki Infobox Fields → Edge Type Mapping" FIRST, then in `scripts/wiki-infobox-parser.py`'s `FIELD_EDGE_MAP`. Re-run parser → re-run prioritize → re-run emit. No script or agent invents edge types out-of-band.
- [ ] **Edge polish phase (FUTURE — post-ingestion)** — After all wiki ingestion completes (Stage 3a + 3b across the full corpus), an agent-reasoning phase reviews the emitted edges to merge semantically-equivalent variants and resolve near-duplicates that the locked parser vocabulary couldn't disambiguate up front. The 22 edge types currently in the corpus may consolidate further. The point of locking the parser vocabulary is so this polish step is bounded and reviewable; if Stage 3a/3b had been allowed to invent edge types, this phase would be unbounded. Triggered after Pass 2 finishes; not before.
- [ ] **Non-ASCII edge qualifier normalization (graph layer)** — Edge qualifiers carried through Stage 3a contain non-breaking spaces (`\u00a0`) and en-dashes (`–`) from wiki source — visually identical to space/hyphen but break string equality. Stage 3a passes them through verbatim by design (pure data passthrough; not its job to normalize). Normalization happens at graph-build time when nodes are linked into edges: lowercase, NFKC-normalize unicode, replace `\u00a0` → space and `–` → `-`, strip whitespace before any string-equality match. Document this in the graph-build script when written.
- [ ] **Wiki infobox field → edge type extraction** — Infobox fields (Father, Mother, Spouse, Allegiance, Overlord, etc.) map directly to edge types. See mapping table in `reference/architecture.md`. Script should parse these for Pass 2 input.
- [x] **AGOT supplementary entity index** — Obsoleted 2026-04-25. Original purpose: backfill the 7 entity types missing from v2's 4-category Raw Entity List. v3 prompt now captures all 12 categories directly, so this is no longer needed for AGOT (already v3 complete) and not needed for the remaining books (will run v3 from the start). If gaps surface in v3 schema review, address as a v3 prompt issue rather than a backfill index.
- [ ] **Wiki uncategorized page triage** — 17,305 of 17,945 wiki pages are in `_uncategorized/`. Before designing Pass 2, scan `_category-reports/` and sample uncategorized pages to determine what entity types are actually needed. Will validate or reject the 7 candidate types.
- [ ] **`wiki-pass2-resolver.py`** — Deterministic resolver: given a Pass 1 raw entity name (free-text, e.g., "the children", "those who sing the song of earth"), return canonical wiki page (or null / list if ambiguous). Uses Track B's alias data (infobox `Alias, Aliases` field) plus fuzzy match. Sits between Pass 1 and Pass 2; not a new agentic pass. Surfaces from Pass 1/Pass 2 asymmetry decision (memory: `project_pass1_pass2_asymmetry.md`).
- [ ] **`pass1-coverage-audit.py`** — Coverage audit: given a chapter's Pass 1 raw entity list and the same chapter's wiki cite_refs (from Track B), flag wiki entities the chapter mentions that Pass 1 missed. This is the real Pass 1 quality gate — higher signal than category-correctness checking. Run as part of v3 schema review.

## Infrastructure

- [x] **Chapter splitter script** — Done. `scripts/chapter-splitter.py`, all 5 books split (344 chapters total).
- [x] **Wiki scraper script** — Done. `scripts/wiki-scraper.py` (1213 lines, stdlib-only). Full crawl complete (17,945 pages, 377 MB).
- [x] **Batch extraction runner** — Done. `scripts/extract.sh` + `scripts/weirwood.zsh`. Wave-based parallel extraction via iTerm tabs, stats tracking, rate-limit detection, auto-worklog updates.
- [ ] **AGOT v3 schema review** — Review extraction output quality across AGOT v3 (73 chapters). Check for: section completeness, entity coverage gaps, Raw Entity List consistency, Food & Drink / Hospitality coverage. One known gap: agot-eddard-01 missing `### Other` header. **Sequenced after Track B** — see worklog "DECIDED: Track B Before v3 Schema Review" (2026-04-25).
- [x] **cSpell dictionary expansion** — Done 2026-04-26 on branch `chore/cspell-dictionary-expansion` (commit 26650f3, merged to main). +~30 technical neologisms + ~120 ASOIAF proper nouns. ignorePaths unchanged.
- [ ] **Auto-relaunch on rate-limit reset (Option A) + scheduled launches (Option C)** — Decision 2026-04-25. Two-part rate-limit-prevention infra, applies to both `extract.sh` and `wiki-pass2.sh`.
  - **Part A (cheapest):** When rate-limit hit, the launcher already extracts `resetsAt` from the rejected JSON (extract.sh:502-520). Add: write `working/extraction-stats/next-eligible.txt` and use `ScheduleWakeup` / launchd to auto-relaunch the same wave at reset+5min buffer. Removes "remember to relaunch in 4h" burden.
  - **Part C (multi-day work):** `weirwood schedule <book>` (and `weirwood wiki schedule <tier>`) — cron / launchd / CronCreate fires launches at off-peak intervals automatically. Useful when walking away for days.
  - **Multi-pass prompting trigger:** If a wiki parse run is projected to span several rate-limit windows (e.g., >24h of cumulative wall time), the launcher prompts the user upfront: "this run will require N reset cycles — schedule unattended (Y/n)?" Avoids a silent multi-day commitment and gives the user a chance to bail or split into manual sessions.
  - Defer Option B (predictive throttling) — Anthropic's per-org caps aren't cleanly published, so any predictive model is approximate. Reactive halt + auto-relaunch is more robust.
  - **Future directions surfaced:** see `progress/scratch-notes.md` "Scheduled Launches — Direction Bank" for 6 downstream possibilities (multi-day unattended runs, cross-pass orchestration, periodic re-crawl + diff, proactive monitoring, state snapshotting, notification sinks) once v1 (A + C) is in place. Track B's parser surfaces schema signals (entity boundaries, edge vocabulary, redundancy) that an isolated v3 review cannot.


