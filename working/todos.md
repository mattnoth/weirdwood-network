# Actionable TODOs

> **Purpose:** Tasks that need doing but aren't part of the current session's work. Organized by live track; everything parked sits under § Dormant / Long-lead. Check items off as they're completed.
> **2026-06-11 cleanup:** resolved/historical blocks moved **verbatim** to `history/todo-archives/2026-06-11-resolved-blocks.md` (see § Archived at the bottom). Nothing was deleted — only moved or collapsed.
> **Status tags:** **[READY]** greenlit, do next · **[OPEN]** actionable now · **[IN-PROGRESS]** underway · **[GATED(x)]** blocked on x · **[VERIFY]** confirm before acting · **[LOW]** open, low priority · **[DORMANT]** parked, not dead · **[POLICY]** standing decision, no action · **[DEFERRED]** explicit deferral decision

---

## Live Tracks (at a glance)

1. **Infobox-structural merge** — **[DONE S94 2026-06-13]** shipped; edges 4,764 → 21,770; connectivity 14.7% → 71.0%. → Track 1
2. **Mode 3 grounded-agent dip** — **[DONE S96 2026-06-14]** ran on merged graph (10 grounded Qs, 4 correct / 2 partial / 4 failed). Dominant failure = **slug-discoverability** (data present, resolver MISS). Routing: **primary = alias-resolver fix** (NEW Track 7), **secondary = historical structural-attachment** (followup #9, ungated). Arc reification de-prioritized. Results: `working/session-results/2026-06-14-mode3-dip-results.md`. → Track 2
3. **Graph cleanup (FIX-22 + S95 resolutions + plate5 followups)** — **[QUEUED — 2nd in sequence (after Mode 3 dip)]** Was parallel-safe; S95 flipped to sequential. Ships 27 S95 edges + 7 new nodes incl. Trident-incident arc parent. → Track 3
4. **Narrative-arc reification — wave 1** — **[NEW S95 — 3rd in sequence]** dip-driven priority list; mint 3-5 small arcs using S95's parallel-research-subagent pattern. First arc (Trident incident) lands with cleanup. Memo: `working/narrative-arcs-design-memo-2026-06-13.md`. → Worklog Ideas&Backlog HIGH (top).
5. **Post-Plate-5 followups** — **[OPEN]** #1–#5 + #13 now; #12 structural restructures **[DONE S93]**; #6 + #9 gated on the dip. NEW graph defects (S92) also here. → Track 5
6. **Edge backfill Tracks A/B/C** — **[GATED(Mode 3 dip)]** ~$25–75 for 300–850 edges. → Track 6
5. **Doc hygiene: repo audit + project-story** — **[MOSTLY DONE S92]** remaining: memory consolidation, design-doc build (Matt picks), repo-reorg. **Nomenclature scheme DECIDED S103** (3 terms; `reference/glossary.md`); 2 narrow follow-ups queued (Tier-rename + agent pointers). → Track 5
6. **Extraction infra: longrun.sh migration** — **[OPEN]** supervisor built same-day; migrate legacy wrappers as their tracks finish. → Track 6
7. **Hooks & mechanical enforcement** — **[OPEN]** two PreToolUse hooks to spec. → § Hooks
8. **Small fixes** — **[OPEN]** classify_failed retry, categorizer map, blackwater-song type. **DONE S102:** year-page mistypes (10 deleted) + vocab-count test (reconciled 163→166). Remaining test fail: only `cwd-is-tmp` (environmental). → § Small Fixes
9. **Event dating + narrative_first** — **[SHIPPED S101; Track-3 leftovers DONE S102]** 118 `event.*` nodes dated (`occurred.ac_year`, +spans via `ac_year_end`) + 29 `narrative_first`. S102: 5 spans + `long-night` dated, 10 mistyped year nodes deleted. **3 follow-up decisions remain, ALL Matt's** (#1 PRECEDES/FOLLOWS · #2 causal TRIGGERS · #4 Fable) → `progress/continue-prompts/2026-06-16-next-move-decisions.md`. → § Timeline & Chronology

Standing rule: **Model-fit policy** (next section). Everything else is in **§ Dormant / Long-lead** (parked, tagged) or archived.

---

## Model-fit Policy (standing rule, 2026-05-04)

> **Default to the cheapest model that can do the job.** Opus only when reasoning depth genuinely requires it.

- **Bash/Python script edits, simple agent loops:** Sonnet 4.6 or Haiku 4.5.
- **Mechanical extraction (Pass 1):** All 5 books extracted with Opus v3 (AGOT/ACOK/ASOS/AFFC/ADWD = 344/344 complete 2026-05-06). The Sonnet smoke-test never ran — Pass 1 finished on Opus. The model-fit question now applies forward: any future re-run / Pass 1.5 (dialogue, meals) should default to Sonnet/Haiku unless evidence shows Opus is needed.
- **Schema audits, full-corpus reviews:** Opus is justified (Session 29 paid $50 for an audit that mattered).
- **Orchestration / coordination:** Sonnet.
- **Wiki Pass 2 stages 3+:** mostly Python now; agent prompts (review, etc.) → Sonnet default.
- **When writing new agent prompts:** include a `model:` field in the prompt frontmatter; pick the cheapest tier that meets the task. Document the rationale in the prompt header so future sessions don't blindly upgrade.

Apply this lens when reviewing existing prompts (mechanical-extractor, wiki-ingester, voice-analyzer, etc.) — if Opus was chosen by default rather than necessity, downgrade with a smoke-test gate.

> 2026-06-11 note on the two items below: both assumed the agent-fleet plan would execute; the fleet was never run (doc-rot punch-list item 10) and most audited agents are retired with their pipelines. Parked, not dead — revive only if the fleet or those agents come back into use.

- [ ] **[DORMANT]** **READY TO DO — Apply model-fit recommendations to agent prompt frontmatter** — Pick recommendations from the Session 37 audit. Apply directly for clear cheap-tier fits (status-reporter → Haiku, schema-drift-auditor → Sonnet, etc.). Run the suggested smoke test first for the gated cases (prose-edge-classifier Sonnet smoke must pass `prose-edge-reviewer` CLEAN/CONCERNS-low across 3 representative buckets before downgrade; mechanical-extractor Sonnet on 3 ACOK Bran chapters vs Opus baseline within ~5% completeness). Update each prompt's `model:` frontmatter field one by one as smoke tests pass.
- [ ] **[DORMANT]** **READY TO DO — Review fleet plan against model-fit recommendations** — Read `working/agent-fleet-specs/fleet-orchestration-plan.md` and `working/agent-fleet-specs/fleet-runtime-architecture.md` alongside the Session-37 audit's "Connection to fleet plan" section (estimates near-term Stage 1-3 spend dropping from $100-200 to $25-65 if classifier smoke passes). Reconcile per-stage budgets with new per-agent recommendations; surface plan-level decisions that change.

---

## Track 1 — Infobox-Structural Merge (NEW 2026-06-11, greenlit)

> Accepted by Matt 2026-06-11 — `working/audits/fable-audit-2026-06-11/synthesis.md`. The wiki infobox layer (20,614 typed relationship rows, already parsed to 23 vocab edge types in `working/wiki/data/infobox-data.jsonl`, 92% target-resolvable, 98.4% additive to book edges) merges as Tier-2 edges. One Python script, $0. This is the one true next step; the dialog work (Track 2) runs on the merged graph.

- [x] **[DONE S92] Spec v2.** `working/infobox-merge/spec.md` — adversarial critic confirmed direction-inversion on 10 fields; fact-key quarantine closes the Joffrey-mirror leak; all findings applied.
- [x] **[DONE S92] Script + tests.** `scripts/infobox-merge.py` + 75 tests green.
- [x] **[DONE S92] Dry-run.** Reproduces spec v2 EXACTLY: 20,614 → 17,006 merged / 1,128 filtered / 1,037 quarantined / 1,356 deduped / 87 corroborations; edges.jsonl would go 4,760 → 21,766; connectivity 14.7% → 71.0%. Report: `working/infobox-merge/dry-run-report-2026-06-12.md` (11 YOUR-DECISIONS items for Matt).
- [x] **[DONE S94 2026-06-13] Ship.** Matt reviewed (all 11 decisions accept-default; both semantic remaps accepted, S93 closeout banner in report); apply ran successfully. `edges.jsonl` **4,764 → 21,770 (+17,006 wiki-infobox)**; connectivity **14.7% → 71.0%**; hygiene A 52 slug remaps + hygiene B 944 typed_by stamps applied. Backup at `graph/edges/_regrounding/edges-pre-infobox-merge-2026-06-13.jsonl`. Verification: `--health` reports the 63 documented orphans only (spec §5), 123 edge types, 0 UNKNOWN. Tests 1144 pass + the 3 pre-existing failures only.
- [x] **[DONE S92] Hygiene fixes folded into the merge:** (a) 115 orphan endpoint slugs; (b) 948 reified role edges missing `typed_by` + file:line `evidence_ref`.
- [ ] **[OPEN]** **Edge taxonomy gaps surfaced by Track B** — Unmapped infobox fields worth adding: `dynasty` (222 pages, no `MEMBER_OF_DYNASTY`-style edge), `written by` (168, no `WRITTEN_BY` edge for in-world texts), `hatched` (8, dragon-classification opportunity). Parser-level mapping gaps: `fathers` plural (21), `vassal` (8), `cadet branch` singular (11) — easy additions to `FIELD_EDGE_MAP`. Roll into v3 schema review or address standalone. **Vocabulary lock:** any new edge type must land in `reference/architecture.md` § "Wiki Infobox Fields → Edge Type Mapping" FIRST, then in `scripts/wiki-infobox-parser.py`'s `FIELD_EDGE_MAP`. Re-run parser → re-run prioritize → re-run emit. No script or agent invents edge types out-of-band.

## Track 2 — Mode 3 Grounded-Agent Dip

- [x] **[DONE S96 2026-06-14] Phase 2 Mode 3 grounded-agent dip — ran on the MERGED graph.** Fresh evaluator subagent answered 10 grounded ASOIAF Qs using `graph-query.py` + alias-resolver, graded vs wiki/chapter ground truth: **4 correct / 2 partial / 4 failed.** Results + corrections: `working/session-results/2026-06-14-mode3-dip-results.md`.
  - **What works (today):** dyadic `--path` queries (Tywin↔Gregor: best-in-class); post-infobox kinship/allegiance/title/culture (dense, reliable); beat-reified on-page events via `--event-participants`. The graph is genuinely useful to a consumer agent for relationship + on-page-event questions.
  - **Dominant failure = slug-discoverability (3-4×):** in Q1/Q3/Q7 the graph held the perfect quote-cited answer; the ONLY failure was the alias resolver MISSing obvious phrasings ("Robb Stark's death", "Ned Stark's execution"). A deployed agent can't fs-grep its way around that. → drives **Track 7 (primary)**.
  - **Secondary = dark-historical structural-attachment:** Q5 (tourney-at-harrenhal, 0 edges) and Q10 (battle-of-the-trident, only PART_OF) are isolated even though underlying facts exist as dyads. Q5 confirmed: the Rhaegar→Lyanna CROWNS edge exists with a Harrenhal-referencing quote, just not attached to the hub. → **followup #9, ungated.**
  - **Corrections (Matt 2026-06-14):** Q4 re-graded `failed`→not-applicable (unnamed longsword ≠ object.artifact, so WIELDED_IN correctly absent — no defect, no backfill). Q9 Trident roadside hub fail = EXPECTED (mints next session). Arc reification (Track 4) **de-prioritized** — only 2 Qs arc-shaped, one already scheduled; dominant problem is a lookup bug on correct data, not missing arcs.

## Track 7 — Query-Layer Tooling (NEW S96 2026-06-14, dip-driven PRIMARY)

- [x] **[DONE S96 (commit c58770549); S101 dip re-run CONFIRMED it works — natural phrasings now HIT] Fix the alias resolver / slug-discoverability gap.** The Mode 3 dip's #1 finding: the graph repeatedly holds the correct answer but `event_alias_resolver.py --lookup` returns MISS on natural phrasings, so a consumer agent never reaches the node. Cheapest, highest-leverage fix; converts ~3 fails/partials to correct (~4/10 → ~7/10) with ZERO new graph data. Scope: (a) fuzzy/substring fallback against the node-slug index when exact lookup misses; (b) index `event.death`/`event.execution` hubs by their victim so "X's death" / "X's execution" resolve (e.g. "Robb Stark's death" → `robb-is-killed`, "Ned Stark's execution" → `execution-of-eddard-stark`); (c) when a phrase names a character, return that character node as a candidate so node-attached edges (e.g. QoLB) are reachable. Deterministic Python, $0. Smoke-test against the dip's 10 questions before/after. NEEDS a continue prompt when picked up. (**Sonnet 4.6** — deterministic script work.)

## Track 3 — Post-Plate-5 Followups (edge/event modeling)

> Shipped context (full done-records archived 2026-06-11): Stage 4 pivot → Pass-1-derived spine + Sonnet tail = **5,219 book-pass1 edges** (S66–S67); edge/event modeling **Plates 0–5 ALL SHIPPED** (S82–S87) — `edges.jsonl` 3,811 → 4,757, events 371 → 583, Red Wedding smoke passes; S91 rename batch + 3 curator deception edges → **4,760**. Diff doc: `working/edge-modeling/plate5-merge-diff.md`. Done followups #7 (`--event-participants`), #8 (event-alias-resolver), #10 (S91 renames) archived.

- [ ] **[OPEN]** **POST-PLATE-5 followup #1 — display-bullet regeneration.** Node files' `## Edges` sections show pre-Plate-5 state; canonical authority is `edges.jsonl` (graph-query.py reads from there). No canonical `build-node-display-edges.py` exists; needs one (or accept that node-file bullets are human-only stale state).
- [x] **[DONE S96 2026-06-14]** **POST-PLATE-5 followup #2 — 32 SUB_BEAT_OF empty-quote disposition.** Contract-6 exemption added for SUB_BEAT_OF in `scripts/stage4-type-contract-validator.py` (C-1). `robb-is-killed` mis-sourced quote fixed (C-2). Backfill of remaining 32 rows deferred (C-3) per design.
- [x] **[DONE S96 2026-06-14]** **POST-PLATE-5 followup #3 — 109 hub-review-queue triage.** FIX-22 applied (F1–F6); QUARANTINE 5 resolved + 2 SKIPped (S95 research) + 3 open Track-B items remain. KEEP-81 untouched.
- [x] **[DONE S96 2026-06-14]** **POST-PLATE-5 followup #4 — 2 deferred collision merges.** A1: `the-conquest-of-dorne` reclassified event.war → object.text, moved to `graph/nodes/texts/`. A2: `tourney-of-maidenpool` merged into canonical `tourney-at-maidenpool` (alias added, loser moved to `_conflicts/`).
- [x] **[DONE S96 2026-06-14]** **POST-PLATE-5 followup #5 — donal-noye↔mag mutual-kill reverse direction.** B-1: `mag-mar-tun-doh-weg KILLS donal-noye` reverse edge added. B-2: forward edge quote fixed to mutual-kill passage (asos-jon-08.md:171).
- [ ] **[GATED(Mode 3 dip)]** **POST-PLATE-5 followup #6 — backfill tracks A/B/C (~$25-75, ~300-850 edges).** Vocab drift retype + reification of existing edges + head-rule retroactive cleanup. Design: `working/edge-modeling/post-plate5-backfill-design.md`. Sequence after validation track lands.
- [~] **[WAVES 1+2 DONE — S97/S100 2026-06-15; only optional wave 3 remains]** **POST-PLATE-5 followup #9 — Historical-anchor STRUCTURAL backfill.** Pre-narrative events sat isolated even though their facts exist (in Pass-1 POV-reminiscence dyads AND the hub's own wiki body). Fix = attach participants/location/parent to the hub. **Wave 1 shipped: 8 Robert's-Rebellion / R+L=J cluster hubs, +118 edges, +3 Trident commander follow-up (`edges.jsonl` 21,829 → 21,950).** Hubs: `tourney-at-harrenhal` (0→25), `the-hands-tourney` (0→33), `battle-of-the-trident` (2→16: +3 COMMANDS_IN robert/rhaegar/lewyn-martell, wiki T2, added 2026-06-15), `sack-of-kings-landing`, `combat-at-the-tower-of-joy`, `greyjoy-rebellion`, `defiance-of-duskendale`, `tragedy-at-summerhall`. Method: per-hub Sonnet subagents read node body + wiki cache + chapters → JSON attach edges (FIGHTS_IN/ATTENDS/AGENT_IN/VICTIM_IN/COMMANDS_IN/LOCATED_AT); 2-stage validation (verbatim-quote check vs chapter AND wiki body) + curator drop/fix (6 dropped, 4 quote-fixed). Provenance discipline: 99 book-pass1 (tier-1/2) / 19 wiki-historical-anchor (tier-2 max — NEW evidence_kind). Tooling: `scripts/historical-anchor-{candidates,validate,mint}.py`; candidates+notes in `working/historical-anchor/`; backup `graph/edges/_regrounding/edges-pre-historical-anchor-2026-06-15T17-42-43.jsonl`. Confirmed: dip Q5 ("who fought at Harrenhal?") + Q10 (Trident participants) now answerable; `--health` 62 orphans unchanged (0 new); pytest 1185 pass + 3 documented pre-existing fails. **WAVE 2 DONE — S100 2026-06-15:** +43 edges (`edges.jsonl` 21,950 → 21,993), 4 WO5K hubs POV chars witnessed/recall attached: `siege-of-riverrun` (2→13, AFFC Jaime POV, tier-1), `battle-of-the-camps` (1→12, AGOT Catelyn), `battle-of-oxcross` (1→8, ACOK recalled tier-2), `melee-at-bitterbridge` (0→14, ACOK Catelyn). Curator: 2 quote-fixes + downgrades on camps, 1 melee semantic-drift edge dropped. `--health` 62 orphans unchanged; pytest 1231 pass + 3 documented fails. Backup `edges-pre-historical-anchor-2026-06-15T23-03-00.jsonl`. **Deferred `siege-of-storms-end`** (duplicate cluster -299/-300/-recalled — needs dedup first). Wave-2 candidate/notes in `working/historical-anchor/`; wave-1 artifacts moved to `working/historical-anchor/wave1-archive/`; wave-2 continue prompt archived. **WAVE 3 (optional, low):** deep-lore wiki-only set is approach (b) — defer until a dip shows demand. Approach (c) retrospective-mention extraction still deferred. (**Sonnet 4.6**.)
  → wave-2 continue prompt archived S100 → `progress/continue-prompts/archive/2026-06-15-historical-anchor-wave2.md`
- [ ] **[LOW]** **POST-PLATE-5 followup #11 — Role-edge citation harmonization (S89, cosmetic).** Pass-1-direct edges cite at line-scope (`evidence_ref`); Plate 3 role edges (`evidence_kind=book-pass1-reified`) cite at chapter-scope (`evidence_book`+`evidence_chapter`+`evidence_source_file`). Either synthesize a unified ref string, or update `graph-query.py` display to render both shapes consistently, or backfill line refs by string-matching the quote against source files. Low-priority, presentational.
- [x] **[DONE S93 2026-06-12]** **POST-PLATE-5 followup #12 — Deferred structural restructures from S91 rename pass.** Both arcs landed: Wyman fake-execution as 4-beat `event.deception` parent (new vocab type added to architecture.md:118) + 2 new sub-beats (lookalike beheading, Frey small-council) + 1 rename + 6 edges (4 SUB_BEAT_OF + DECEIVES wyman→cersei + CONSPIRES_WITH wyman↔stannis). Jaime arc collapsed to single hub `attack-on-ned-stark-in-the-streets-of-kings-landing` (event.battle) — sibling stub deleted, 4 dup edges deduped, 1 COMMANDS_IN repointed, 2 new edges (DECEIVES + TRIGGERS). Counts: edges 4,760 → 4,764; events 583 → 585. See Session 93 worklog entry + `working/session-results/2026-06-12-deferred-restructures.md` + `history/session-details/session-093.md`.
- [ ] **[OPEN]** **POST-PLATE-5 followup #13 — Deception-edges pilot scaling (NEW S91, 2026-06-11).** S91 minted 3 curator pilot edges (1 BETRAYS + 2 DECEIVES) from the 9-rename batch. Subagent C rec: continue manual minting up to ~10 high-confidence deception edges (4-5 already identified from the S90 audit + the cersei-claims-ned-s-men-attacked-first follow-on flagged by 2 independent subagents), then build `scripts/surface-deception-candidates.py` to mine the broader 30-50 medium-confidence retypes from existing `hint_raw` markers (`feigns|deceives|pretends|secretly|claims|false`). Zero schema cost — `DECEIVES` (now 13 live) + `BETRAYS` (now 39 live) already in locked vocab.
- [x] **[DONE S96 2026-06-14]** **NEW S92 — Graph defect: F1c dangling edge.** `siege-of-storm-s-end-recalled` LOCATED_AT `storms-end` repointed to `siege-of-storms-end-299` + 5 staged role edges added (mace-tyrell/stannis/gawen-wylde/cressen/davos-seaworth).
- [x] **[DONE S96 2026-06-14]** **NEW S92 — Graph defect: 3 Red Wedding beats missing SUB_BEAT_OF.** Added `ser-ryman-kills-dacey-mormont`, `the-camp-becomes-a-battlefield`, `red-wedding-revealed` SUB_BEAT_OF red-wedding. Red Wedding now has 12 beats (up from 8).
- [x] **[DONE S96 2026-06-14]** **NEW S92 — Graph defect: `robb-is-killed SUB_BEAT_OF red-wedding` mis-sourced quote.** Fixed: now uses Jaime Lannister sends his regards passage from asos-catelyn-07.md:137.
- [x] **[DONE S96 2026-06-14]** **NEW S92 — Graph defect: `donal-noye KILLS mag` quote mismatch.** Fixed: forward edge quote updated to mutual-kill passage (asos-jon-08.md:171).
- [x] **[DONE S96 2026-06-14]** **NEW S92 — Doc/data reconcile: LOCATED_AT direction.** Correction note added to `working/edge-modeling/edge-modeling-reification-design.md` line 153 (design doc described `location → event`; live data is correctly `event → location`).
- [x] **[DONE S96 2026-06-14]** **NEW S92 — Hub-triage FIX-22 list is ready to execute.** ALL FIX-22 items applied. See `curation/hub-review-triage-2026-06-12.md` [EXECUTED 2026-06-14] markers. +59 net edges, +10 nodes minted, indexes rebuilt.
- [ ] **[VERIFY] OWNS→BONDED_TO for direwolves/dragons — confirm it actually merged.** The S76 core-cleanup record (archived) listed 3 gated fixes; Plate 5's done-record explicitly confirms (1) the 2 cersei↔tyrion LOVES drops and (2) the 21 ASSAULTS→ATTACKS retypes, but does NOT mention (3) OWNS→BONDED_TO for direwolf/dragon targets. Check `edges.jsonl` for OWNS rows targeting ghost/grey-wind/lady/nymeria/summer/shaggydog/drogon/rhaegal/viserion before assuming it shipped.

## Track 4 — Edge Backfill (existing-graph Tracks A/B/C)

> Collapsed context: the Events-Haiku bulk (1,617 typed rows at `_events-haiku-bulk/`, S81 cross-model drift audit NO-GO borderline) is **absorbed into Track B's re-bucketing** below — the free A–E escalation pick is no longer a live decision. Full record archived 2026-06-11.

- [ ] **[GATED(infobox merge shipped)] NEW (S93 2026-06-12) — Infobox-merge v2.1: per-fact citation backfill.** Q11 of the dry-run report shipped with `evidence_book`/`evidence_chapter = null` on all 17,006 merged wiki-infobox edges (deferred-not-abandoned). This pass harvests cite_ref anchors embedded inside infobox values (e.g. `Spouse: Joanna Lannister[12]`), resolves each anchor to a chapter slug, and retroactively populates `evidence_chapter` where the infobox field carried a per-fact citation. Steps: (1) audit `scripts/wiki-infobox-parser.py` — does it preserve cite_ref anchors inside field values, or strip them at parse? (2) if stripped, add anchor preservation + re-parse to a side file (NOT a full re-merge — patch the existing rows). (3) build resolver: anchor → cite_ref → chapter slug via the existing AWOIAF cite_ref scheme. (4) patch `graph/edges/edges.jsonl` in place for rows where resolution succeeds; leave null where no per-fact anchor exists. Expected coverage: structural facts with explicit footnotes (kinship + allegiance fields typically cite the chapter where the relationship is established). Cost: ~1 session of script work + dry-run + apply, deterministic (no LLM). Sequence after Mode 3 dip so failure modes inform whether the citations move the needle.

- [ ] **[GATED(Mode 3 dip)]** **HIGH (POST-PLATE-5) — Edge backfill on the existing 3,811-edge graph — 3 tracks.** Surfaced S86 (2026-06-08) when Matt pushed back on my over-narrow "no backfill" framing AND corrected my misreading of S74. **Standing rule:** S74 was a precision-gate failure on a specific Events+Dialogue Haiku run, NOT a permanent ban; S75 amended to "enrichment wanted, gated on precision." All LLM-touching backfill phases below pass the same precision gate. The edge-modeling Plates 0-5 focused on EVENT modeling and applied ~50 retroactive cleanups; the existing graph still carries 100-200+ vocab-drift candidates (currently mis-typed under older vocab) and 500-1,500 reification candidates (existing person↔person edges anchored to named events that should be hub'd). Three tracks, sequenced after Plate 5 lands: (A) deterministic vocab-drift dictionary + Haiku fallback (~$0-15, 100-200 retypes); (B) reification of existing edges into event hubs — cleanroom's main carry-over (~$20-50, 200-600 reifications); (C) head-rule retroactive cleanup beyond Plate 0's 10 flips (~$0-10, 5-30 flips). Total cost ~$25-75 for 300-850 edges touched (6-17× Plate 5's retroactive scope). Each track has its own gate. Pass 1 chapter re-extraction stays OFF the table; backfill operates on the graph, not chapter prose. Full design: `working/edge-modeling/post-plate5-backfill-design.md`. Sequence: Plate 5 → Track A Phase 1 → A Phase 2 → C Phase 1 → B Phase 1 → audit checkpoint → B Phase 2 → optional C Phase 2 → Dialogue v2.1 escalation (informed by edge-modeling lessons).
  → continue (PARKED in archive/ S99 — restore when this becomes next): `progress/continue-prompts/archive/2026-06-05-edge-modeling-plate-4-haiku-disposition.md` (**Opus 4.7** bucketing-review; **Sonnet** filter pass) — re-bucket the 1,617 Haiku bulk under the reify lens; absorbs the S81 NO-GO.

## Track 5 — Doc Hygiene & Project Story

- [x] **[DONE S92] Repo audit + strategy reconciliation.** Full 91-session history audit + graph deep-dive → `working/audits/fable-audit-2026-06-11/` (synthesis, history-audit, graph-deep-dive, doc-rot-punch-list). Doc truth-pass: CLAUDE.md, worklog STATUS block, todos 420→232 (`history/todo-archives/`), continue-prompts manifest, `reference/schema-legend.md`, `reference/roadmap.md`.
- [x] **[DONE S92] Project-story narrative.** `history/project-story/` — 8 chapters incl. the reification explainer with a live Red Wedding walkthrough. Portfolio/article source material.
- [ ] **[OPEN — GATED(Matt's Option pick)] Design-doc consolidation build (~3-4 sessions).** Option A (reference/design/ 3-doc set, recommended) vs B (single DESIGN.md) vs C (minimal). Matt picks the structure; agent sessions execute the consolidation. → `working/audits/fable-audit-2026-06-11/design-doc-proposal.md`
- [x] **[DONE S103] Nomenclature scheme DECIDED.** Matt rejected the 6-term proposal as overkill → 3 canonical terms (Pass/Track/Tier) + lowercase `step`; canon in `reference/glossary.md`; CLAUDE.md `## Vocabulary` stub; memory `feedback_vocabulary_canon`. **Full retroactive doc-sweep DECLINED** as churn-for-tidiness (history glossary decodes old docs). Two narrow follow-ups remain ↓.
- [ ] **[OPEN — narrow, ~20-30 edits] Reserve "Tier" for confidence-only.** Rename the live *non-confidence* "Tier" uses → class/level: `reference/architecture.md` qualifier requirement-levels ("Tier-1/Tier-2 qualifier") + the wiki promotion-class ("tier-secondary", "Tier-A/B pages"). The ONLY rename that prevents a data error (Tier is stamped on edges). Living docs only; skip history/archives.
- [ ] **[OPEN — pull channel] Add glossary pointer to live agents.** One line in the ~8 live `.claude/agents/*` defs: "Before naming a Track / numbering steps / labeling a sequence, read `reference/glossary.md` and use only those terms." Closes the subagent-doesn't-load-CLAUDE.md gap (the orchestrator push channel already covers it via the CLAUDE.md stub instruction). Grep-linter for drift = deferred until a retired term actually slips into a living doc.
- [ ] **[OPEN] Repo-reorg P1/P2 (~1-1.5 sessions).** Plan at `working/repo-reorg-plan-2026-06-12.md` — working/ triage, pass2-buckets skeleton-untrack options, progress/ archive substructure, root strays + replies convention. Risk-ordered.
- [ ] **[OPEN] Memory consolidation** — run `anthropic-skills:consolidate-memory` to merge point-in-time entries, fix stale facts, prune the index. Low urgency (doc truth-pass covered the most critical stale entries).
- [ ] **[GATED(graph cleanup landed)] NEW (S93 2026-06-12) — Harden /endsession against doc-drift + directory bloat.** Matt's concern: as the project grows, working/ + progress/continue-prompts/ + history/ accumulate stale artifacts faster than the endsession checklist catches them. S92's Fable audit had to manually trim 420 → 232 TODOs + surface a doc-rot punch list because the standing endsession steps don't include periodic drift/bloat sweeps. Sequence after the cleanup-prompt artifacts (`curation/hub-review-triage-2026-06-12.md`, `plate5-small-followups`) consume themselves so the post-cleanup state is the new baseline. Scope: (1) audit `.claude/commands/endsession.md` (the slash-command implementation) — add a "drift check" step (every 5 sessions? every endsession?) that flags: numeric claims in arch.md/CLAUDE.md without `(verified YYYY-MM-DD)`, working/ subdirs with no touches in 30+ days, continue-prompts referencing slugs/files that no longer exist, todos.md items with broken `→ continue:` pointers. (2) Decide what runs every-endsession vs every-Nth (cost balance). (3) Possibly add an auto-archive rule for working/ subdirs the same way archive019 handles worklog (5-session window before moving to history/). (4) Update CLAUDE.md rule 8 (worklog archive) with a sibling rule for the other directories. ~1 session of skill-edit work, deterministic. Low-stakes, high-leverage.
  → continue: `progress/continue-prompts/2026-06-07-repo-audit-strategy-reconciliation.md` (**Opus 4.7**) *(stale pointer — kept for the archive018 S86-append judgment item only)*
- [ ] **[LOW]** **LOW — Anchor numeric claims in architecture.md to a script or session decision.** Session 52 (2026-05-13) surfaced that "the 22 edge types currently in the corpus" had rotted into being wrong (real count: ~96 master, ~26 wiki-infobox subset, ~62 actually emitted in graph). The claim was made at Session 26 when it was true, but propagated to 6 places (architecture.md, design-philosophy.md, schema-drift-auditor.md, prose-edge-classifier.md ×3) without anyone updating as the corpus grew. Pattern to apply going forward: bare-count assertions in architecture.md should either (a) be a derived count regenerable from a script (e.g., `grep -c '^| \`[A-Z_]\`' reference/architecture.md`), or (b) be hedged with "as of Session N" + a pointer to the session entry. Not urgent; preventive.
- [ ] **[DORMANT]** **Audit session-details/ coverage and consider backfills** — The two-tier session-doc rule was originally "every session gets a `session-details/session-NNN.md`"; in practice it was applied as-needed. The CLAUDE.md rule is now explicitly as-needed (write only when the session contains design discussion, incidents, or novel decisions). Result: existing `history/session-details/` coverage is inconsistent — some sessions have rich files, others have nothing despite warranting one. For project-story purposes (a future writeup, retrospective, or portfolio piece), audit which past sessions deserve a backfilled detail file based on the worklog Session Log + archives, and write them retroactively. **Not blocking any project work.** Lower priority than anything in the build pipeline.

## Track 6 — Extraction Infrastructure

- [x] **[DONE S98+S99 2026-06-15]** **Central long-run supervisor script (consolidate the run-forever wrappers)** — Surfaced 2026-06-11 (Matt request). Six wrappers (~1,209 lines total: `stage4-run-forever.sh` 120, `edge-reify-run-forever.sh` 240, `stage4-haiku-run-forever.sh` 87, `stage4-tail-bulk-forever.sh` 240, `stage4-haiku-loop.sh` 223, `stage4-events-bulk-run.sh` 299) each reimplement sleep/rate-limit-wall/crash-retry/resume with **different env var names** (`STAGE4_SLEEP_BETWEEN` vs `EDGE_REIFY_WALL_SLEEP` vs `STAGE4_HAIKU_RELAUNCH_SLEEP`), **different defaults** (1200/1800/3600/600s), and **different exit-code conventions**. Replace with ONE generic supervisor, e.g. `scripts/longrun.sh <cmd...>`: standard env vars (`LONGRUN_SLEEP_BETWEEN`, `LONGRUN_WALL_SLEEP`, `LONGRUN_MAX_CRASHES`), standard exit-code contract (0=done, 2=rate-limit wall → wall-sleep + resume, other=crash → bounded retry), uniform timestamped logging. **DONE 2026-06-11 (same day): `scripts/longrun.sh` built + tested by script-builder** (150 lines, shellcheck-clean, 4-scenario test matrix passed; exit contract 0=done / 2=wall / 10=more-work / other=crash). REMAINING: migrate each legacy wrapper to a thin `longrun.sh` call-through as its track finishes — do NOT touch a wrapper while its run is live; underlying runners must adopt the exit-code contract (notably exit 10 for more-work) before migrating. Related: the "Hands-off pacing v2 single-coordinator" item below — same single-point-of-control principle; build longrun.sh first, coordinator can layer on it later.
  → continue prompt ARCHIVED S99 → `progress/continue-prompts/archive/2026-06-15-script-consolidation.md`
  → design spec: working/orchestration-pacer-design-2026-06-15.md (§0 status table = ALL BUILT as of S99)
  - **[SESSION 1 DONE — S98 2026-06-15]** Orchestration/pacer BUILT: `scripts/pace.py` v1 (backfill normalizer for 6 stat schemas → per-track `working/telemetry/<track>.jsonl`; report-only baselines + conservative sleep + honest M3 wall note; `emit_telemetry_row` helper), `scripts/worker-template.py` (M1/M2/M4 contract), 40 tests (`tests/test_pace.py`). `longrun.sh` supervisor done+tested (S97).
  - **[SESSION 2 DONE — S99 2026-06-15]** Cleanup shipped: 30 one-offs/wrappers `git mv`→`scripts/archive/` (24 + smoke-prep + `migrate-stats-csv.py` de-referenced from extract.sh + 4 shelved wrappers; KEPT `stage4-run-forever.sh` + `edge-reify-run-forever.sh` + 27 comention); 9 frozen CSVs→`working/extraction-stats/_archive/` (read-fallbacks added to 3 status commands); `weirwood graph/resolve/refresh` aliased (NEW `scripts/weirwood-refresh.sh`); `scripts/README.md` rewritten as universal index (Class A/B/C/D + provenance, all 124 live + 32 archived covered); design §0 fully BUILT. pytest 1231 pass / 3 documented fails. **Loose end:** wire `weirwood refresh --check` into a git pre-commit hook (design §13 S8) — see Track 6 item below.
  - **[READY — own session; continue prompt `progress/continue-prompts/2026-06-15-script-consolidation.md` (S97 2026-06-15)]** Matt confirmed this is the NEXT dedicated session. Most of the target architecture already exists (`scripts/README.md` inventory, `scripts/longrun.sh` supervisor, `scripts/weirwood-run.sh`+`weirwood.zsh` CLI) — the work is EXECUTION + README refresh + session-provenance column, not rebuild. Matt's vision: few central scripts + sub-scripts; reusable one-offs (e.g. today's `historical-anchor-*`) fold into the `weirwood` CLI; README lists every script (use/session) so divergent sleep/rate-limit wrappers are findable + collapsed onto `longrun.sh`.
  - **[MATT DIRECTIVE 2026-06-14 — sharper scope, own session, sequence AFTER alias-resolver fix (A) + narrative-arc wave 1 (C)]:** Full script-consolidation/hygiene pass. (1) Identify the run-forever wrapper KNOWN to have worked reliably for long runs (proven in practice, not just tested) — make it the reference. (2) Standardize ALL long-run scripts onto `longrun.sh` + the `weirwood run` CLI (adopt exit-code contract incl. exit-10). (3) For everything else in the flat `scripts/` dir (148 files): either ARCHIVE (one-off dated mutation scripts that have served their purpose → `scripts/archive/` or a `scripts/mutations/` subdir) or rework to run under longrun/weirwood. Net goal: scripts/ is organized, long-runs go through ONE proven path, dead one-offs archived. Mind the import-coupling risk (~24 cross-referencing scripts) — move conservatively, test after each move.
- [ ] **[OPEN]** **Wire `weirwood refresh --check` into a git pre-commit hook (design §13 S8).** Surfaced S99. `scripts/weirwood-refresh.sh --check` exists + runs (compares `graph/nodes/` mtime vs the resolver artifact, WARNs if stale), but the pre-commit hook that would auto-flag a node change committed without a refresh is NOT installed — it touches Matt's commit workflow, so needs buy-in before wiring. Soft enforcement only until then (auto-call in mutation scripts + the `rebuild-derived-artifacts-after-node-mutation` memory).
- [ ] **[OPEN]** **Edge-reify runner: adopt exit-10 contract to flip `weirwood run` registry to READY.** The `edge-reify-run-forever.sh` wrapper and its underlying runner need to emit exit 10 ("more work available") before `weirwood run start edge-reify` can be safely registered as a READY track. Until then, `weirwood run` subcommand exists (tested, shellcheck-clean) but has no READY tracks — custom-only entry per `scripts/weirwood-run.sh`. Do when the edge-reify track next runs.
- [ ] **[MOSTLY DONE S102]** **Pre-existing test failures.** `test_vocab_count_is_163` ×2 → **reconciled to 166 S102** (renamed `*_is_166`/`*_at_166`, both green; +3 = reification AGENT_IN/VICTIM_IN/SUB_BEAT_OF). **Remaining: only `test_cwd_is_tmp`** (environmental — requires running pytest from `/tmp` to avoid loading repo CLAUDE.md; not a code bug). Net: 3 documented fails → 1.
- [ ] **[DORMANT]** **Hands-off pacing v2 — single-coordinator design** — `--chain`/`--delay` were removed in the Session 33 fix because every-terminal-relaunches is combinatorially unsafe. If hands-off pacing is wanted later, design ONE coordinator process that owns the schedule and dispatches waves at intervals — never spawning new terminals that themselves re-launch. Coordinator reads the stats CSV (already a single source of truth post-fix), claims the next batch of waves, sleeps, dispatches, repeats. Single point of control = no chain explosion possible. Deferred — not currently blocking any work.
- [ ] **[DORMANT]** **Stage 4 worker: deterministic batch-claim helper** — Surfaced 2026-05-17 watching live worker output. Each batch claim re-scans `batch-manifest.jsonl` (~1089 rows) via Read/Glob/Grep/Bash to find the first `status:queued` row at the highest unmet priority tier — costs ~10+ pre-flight tool calls per batch. Replace with `scripts/stage4-claim.py` that does it deterministically: read manifest → find first queued+priority match → atomically claim lock file → append claim event to state.jsonl → output JSON `{batch_id, files, shape, priority_tier}` to stdout. Worker prompt then replaces the "Batch claim (atomic)" section with a single `python3 scripts/stage4-claim.py` call. Expected ~20-30% per-batch pre-flight token reduction. **Risk:** changes worker behavior mid-corpus — per drift-detection rule, smoke-test 1-2 batches and verify output schema unchanged before swapping. Not tonight (active burst run); do in next planning session.
  *(2026-06-11 note on the item above: written for the deprecated wiki-comention bulk; pattern reusable if a manifest-driven bulk returns.)*
- *Note:* the old "Auto-relaunch on rate-limit reset (Option A) + scheduled launches (Option C)" plan was archived 2026-06-11 — superseded in practice by the run-forever wrappers and `scripts/longrun.sh`.

## Hooks & Mechanical Enforcement

> **Why this section exists:** Session 37 (2026-05-06) surfaced two soft-rule cases where Matt explicitly said the rule should be a hook, not just prose: "we cannot depend on a rule, apparently, for this type of behavior." These convert prose-only protections into mechanical ones. Spec each properly in a follow-up session — do NOT freelance the hook config.

- [ ] **[OPEN]** **PreToolUse hook — block edits to historical archives** — Add a `PreToolUse` hook in `.claude/settings.json` that blocks `Edit|Write|MultiEdit` on paths matching `history/worklog-archives/`, `history/session-details/session-*.md`, or `progress/continue-prompts/archive/`. These are records-of-thought-at-the-time and the no-edit-historical-archives rule has been violated by drift before. Surfaced 2026-05-06 (Session 37 handoff § 0).
- [ ] **[OPEN]** **PreToolUse hook — block writes under `sources/`** — Add a `PreToolUse` hook that blocks `Edit|Write|MultiEdit` on any path under `sources/`. The "Anything in the source folder, period, cannot be written to or overwritten" rule is currently prose-only; the gitignore + permission denials don't catch in-place writes that would corrupt source data. Surfaced 2026-05-06 (Session 37 handoff § 0).
- [ ] **[LOW, optional — S101 advisor idea]** **Worklog rotation pytest-guard.** A ~20-line `tests/test_worklog_rotation.py` that fails when `worklog.md` has > 5 `^### Session` headers UNLESS a `ROTATION_PAUSED_UNTIL: <trigger>` marker is present in the Session Log header. Makes a *paused* rotation self-healing/visible instead of silently lapsing (the documented failure mode). NOT needed while we never pause (S101 chose archive-normally); build only if Matt ever wants to pause rotation for connected-session runs.

## Small Fixes (open)

- [ ] **[OPEN]** **CLASSIFIER — `classify_failed` parse-fail block never self-resolves (S79, one-line fix, non-urgent):** `stage4-tail-classifier.py` fails a batch all-or-nothing on a JSON parse error (`:1185`) → 40 rows to `*.classify_failed.jsonl`. But `count_remaining` (in `stage4-events-bulk-run.sh`) + `load_existing_keys` (`:1090`, drives `--skip-existing`) count ONLY `edges`+`rejected` as done — so `classify_failed` rows are re-fed every run and, if Haiku again returns non-JSON, fail forever (observed: same ~40 acok rows, joffrey↔mandon-moore, fail as batch 1 each run). Rare (2 parse errors in ~700 batches) and non-fatal, but they never clear. **Fix (pick one):** (a) add `*.classify_failed.jsonl` to the skip-key set so they're not retried (accepts the loss), OR (b) add a one-shot per-batch retry in `classify_batch` before writing classify_failed (recovers transient non-JSON responses). (b) is better — it actually recovers the rows. Smoke 1-2 batches per drift-detection rule before relying on it.
- [x] **[DONE S102 2026-06-16]** **Year-page type bug — 10 wiki year pages mis-typed `character.human`** — Resolved via **option (c): deleted** (Matt's call, advisory-board Track 3). The 10 slugs (`129/130/131/134/143/157/209/283/286/298-ac`) were `character.human`, 0 edges, boilerplate "X is a character from the AWOIAF wiki" — auto-mis-promoted (Stage 3 default-to-`character.human`, Path B categorizer missed). Deleted the 10 `.node.md` + their 10 index files; `all-node-alias-lookup.json` resynced (dropped 10 slugs); `characters/_summary.json` `year_pages_emitted_as_characters: 10 → 0`. Rationale for delete-not-retype: aligns with the `chronology-extractor` design ("year pages may not become graph nodes — they're sources of temporal edges"); in-world year-lookup now served by `occurred.ac_year` on event nodes (S101–S102); source JSON in `sources/wiki/_raw/` untouched. If a future chronology pass wants year anchors, mint them properly as `event.year` then. Detail: `history/session-details/session-102.md`.
- [ ] **[LOW]** **LOW — Categorizer fix: `CATEGORY_TYPE_MAP` gaps that sent Bucket A to `unknown`** — Surfaced 2026-05-11 (Session 43) as deferred follow-up to Bucket A backfill. The backfill script `wiki-pass2-bucket-a-backfill.py` used a hand-built `CATEGORY_TYPE_RULES` list to classify 85 new nodes. These same classification rules should be applied upstream to `scripts/wiki-infobox-parser.py`'s `CATEGORY_TYPE_MAP` so future categorizer runs catch them automatically. Key missing rules: Godswoods→place.location, Brothels→place.location, Septries→place.location, Religious buildings→place.location, Slave soldiers→organization.faction, Khalasars→organization.faction, Thrones→object.artifact, Trebuchets→object.artifact, Swan ships / Ironborn longships→object.artifact. Low priority — the 138 Bucket A slugs are now in the graph; this is a future-proofing task. Estimate: 30 min Python edit + re-run categorizer smoke test.
- [ ] **[LOW]** **Storm's End siege duplicate-node cluster (surfaced S100).** `siege-of-storms-end` (Robert's Rebellion, deg 2), `siege-of-storms-end-299`, `siege-of-storms-end-300`, and `siege-of-storm-s-end-recalled` all coexist as event nodes for what is partly the same / partly distinct sieges (283 AC Robert's Rebellion vs 299 AC WO5K). S100 deferred historical-anchor wave-2 attach for these — attaching participants to one slug of a duplicate set risks worsening fragmentation. Decide canonical-vs-alias mapping (which are genuinely distinct events vs surface dups) BEFORE any structural attach. Then a wave-3 historical-anchor pass can attach Davos/Stannis/Mace-Tyrell/Gawen-Wylde to the correct hub(s).
- [ ] **[LOW]** **`type='unknown'` page (`battle-of-the-blackwater-song`)** — Routed to `graph/nodes/_unclassified/` during Stage 3 promotion. Likely a parser bug — the wiki page is a song named after a battle and the infobox parser couldn't determine entity type. Add to `ENTITY_TYPE_OVERRIDES` in `scripts/wiki-infobox-parser.py` (probably `entity.song` or `text.song`) once that subtype is added to `reference/architecture.md`. Until then, the node sits in `_unclassified/` — readable but not in the type taxonomy.
- [ ] **[LOW, S102 lesson]** **`weirwood refresh` re-stamps `generated_at` on every index file → 7.9k timestamp-only diffs.** When S102 deleted 10 edgeless nodes and ran a full `scripts/weirwood-refresh.sh`, it produced 7,921 timestamp-only index churns that buried 16 real changes (had to revert them by hand before commit). Two fixes (either): (a) make the index builders skip rewriting a file when its content (minus `generated_at`) is byte-identical to what's on disk; (b) document/standardize "prefer targeted index-file deletion over a full refresh when mutated nodes are edgeless" (edgeless node deletion only needs its own per-node index file removed — no other index content changes). Preventive; not urgent.

---

## Dormant / Long-lead

> Parked, not dead. Nothing here blocks the live tracks. Where an item was collapsed to a one-liner, the full original text lives in `history/todo-archives/2026-06-11-resolved-blocks.md`.

### Mission Protocol & Orchestration — [DORMANT] (untouched since S45–S49; full section text archived)

- [ ] **[DORMANT]** Refine watcher.md v1 → v2 after the next mission runs.
- [ ] **[DORMANT]** Dispatcher-watcher mode (separate agent file) — for 50+ worker automated orchestration.
- [ ] **[DORMANT]** Mission protocol DRAFT v0 redline — resolve the 5 open questions in `mission-protocol.md`.
- [ ] **[DORMANT]** Decide mission-file git tracking (lean: commit).
- [ ] **[DORMANT]** Reconcile Track-A/B mapping in next.md (`next.md` unmentioned since S52 — verify it's still a live file before acting).
- [ ] **[DORMANT]** Add wall-clock timers to Python scripts (measure, don't estimate; possible `scripts/_timer.py` helper).
- [ ] **[DORMANT]** Spot-check Track A's 60 promoted case-collision nodes for the orphan-edge defect class.
- [ ] **[DORMANT]** Bake a session-results write into every future continue prompt (convention exists at `working/session-results/README.md`; apply going forward).
- [ ] **[DORMANT]** Richer watcher hookup via `working/session-state.jsonl` if session-results files prove insufficient.

### Agent Improvements

- [ ] **[DORMANT]** **FUTURE — `vocab-decision-reviewer` agent** — Surfaced 2026-05-18 (Session 55). Reusable second-opinion agent for vocab-lock decisions. Pre-loaded reads of `reference/architecture.md` § "Edge Types" + `.claude/agents/prose-edge-classifier.md` + `working/wiki/pass2-buckets/questions-for-matt.jsonl` + the in-progress rollup. Tools: Read/Glob/Grep (advisory only — never writes architecture.md). Sonnet model. Per-invocation prompt passes the specific items needing verdicts. Pattern proven by ad-hoc dispatch in Session 55 — agent overrode 3 of my leans (PURCHASED_FROM, BUILT, REPUTED_AS) and surfaced 1 I'd missed (CAPTAIN_OF). Write when next vocab-lock round surfaces (Stage 4 v2, or cross-book book-tier evidence). Full design sketch in `history/session-details/session-055.md` Phase 10.
- [ ] **[DORMANT]** **mechanical-extractor: add worked example** — Still open. The original spec had an AFFC Prologue example extraction showing ideal output. Consider adding as a reference file the agent loads. Deferred until we've seen the patched prompt run a full book — if output quality is high without the example, may not be needed.
- [ ] **[DORMANT]** **mechanical-extractor: verbose extraction rules** — Still open. The original spec had richer fact-vs-interpretation explanations (e.g., "Ned thinks about a promise" = fact vs. "Ned is thinking about Lyanna's deathbed promise about Jon" = interpretation). Deferred pending full-book output review.

### Future Passes 3–6 (long-lead)

**Agent prompts to write:**

- [ ] **[DORMANT]** **Pass 3: voice-analyzer** — Full prompt. Needs: voice profile schema, perception mapping format, identity-split handling.
- [ ] **[DORMANT]** **Pass 4: foreshadowing-scanner** — Full prompt. Needs: mapping output schema, confidence criteria, scanning strategy (per-chapter vs per-event).
- [ ] **[DORMANT]** **Pass 5: theory-extractor** — Full prompt. Needs: theory seeds file first (`reference/theory-seeds.md`), evidence mapping schema.
- [ ] **[DORMANT]** **Pass 6: discovery-agent** — Full prompt. Needs: scoping strategy, candidate finding format, chunking approach.

**Reference files to create:**

- [ ] **[DORMANT]** **theory-seeds.md** — Top 20-30 ASOIAF theories with descriptions and confidence tiers. Input for Pass 5.
- [ ] **[DORMANT]** **taxonomy.md** — Standalone reference for entity types, edge types, confidence tiers (currently embedded in architecture.md).
- [ ] **[DORMANT]** **node-schema.md** — What does a single entity node file look like? Frontmatter fields, required sections.
- [ ] **[DORMANT]** **Expand `reference/foreshadowing-events.md` before Pass 4** — currently 26 events + 15 Chekhov's guns. Audit/expand both: more event anchors (deaths, identity reveals, plot reveals, magic returnings, prophecy fulfillments) AND a richer Chekhov's gun *pattern library* so the scanner can flag candidate unknown foreshadowing, not just match known events. Each gun entry should describe **setup** (where introduced), **payoff shape** (or "unfired" if open), and **textual pattern** that signals "this matters later" (named in dialogue, dwelt on by POV, contrasted with normal description). The pattern library — not the named-events list — is the real scanner input. Long-lead — Pass 4 is many sessions away.
- [ ] **[DORMANT] Theories/prophecies connectivity (NEW 2026-06-11, from audit synthesis).** `theories/` (45 nodes, effectively 0 real edges) and `prophecies/` (2 nodes, 0 edges) stay 100% dark even after the infobox merge (their wiki pages have no infoboxes). Options when this surfaces: (a) deterministic Tier 3–4 ABOUT/MENTIONS edges minted from theory-page prose links (cheap, scriptable); (b) the designed-but-unbuilt Pass 4/5 surface (FORESHADOWS / evidence edges via `reference/foreshadowing-events.md` + theory-seeds, findings to `curation/`); (c) prophecy node minting — 2 nodes is an undercount (Azor Ahai, valonqar, dragon-has-three-heads exist as wiki pages). See synthesis.md § "Matt's question".

### Collaboration — [DORMANT] (team is solo; revisit only if a collaborator actually materializes)

- [ ] **[DORMANT]** **Schema lock-in before collaborator handoff** — A collaborator may join to share extraction load. They have less ASOIAF depth than Matt, so the Pass 1 schema must produce correct output without needing lore knowledge to second-guess. Sequence: finish Track B → schema review across AGOT v3 → lock schema → onboard collaborator. (Note: ASOS Pass 1 was effectively a collaborator run via Okey on Opus — schema held up, supports the case that v3 is collaborator-ready as-is.)
- [ ] **[DORMANT]** **Revisit `/install-github-app`** — Useful once a collaborator is producing extractions: tagging Claude in PRs/issues becomes the natural review surface. Skip until handoff is near.
- [ ] **[DORMANT]** **Collaborator quick-reference doc** — README onboarding works for first-run, but a collaborator extracting on another machine needs a focused "running extractions" reference that doesn't require reading CLAUDE.md end-to-end. Write before handoff.

### Timeline & Chronology

- [x] **[SHIPPED S101 2026-06-16] Event in-world dating + narrative reading-position.** 112 `event.*` nodes carry `occurred.ac_year` (signed int; deterministic from `chronology-events.jsonl`; tier-3/wiki-year-page) + 29 carry `occurred.narrative_first`. Schema: `working/design-opinions/2026-06-16-era-field-analysis.md` (signed ac_year, no `era:AC|BC`; 9-invariant validator). Scripts `date-event-nodes.py` + `backfill-narrative-first.py`. Architecture documented.
- [ ] **[DECISION — Matt, gated] `PRECEDES`/`FOLLOWS` ordering edges.** NOT mechanical: (a) the edge types are **NOT in the locked vocab** (verified absent S102) — a deferred schema decision (roadmap D3); adding them must bump the vocab-count test (now 166); (b) the 118 dated events share NO `PART_OF` parent → no cluster to order within. Needs Matt's vocab add + grouping basis. → continue: `progress/continue-prompts/2026-06-16-next-move-decisions.md`
- [ ] **[DECISION — Matt, sign-off] Causal `TRIGGERS`/consequence edges** — the Mode-3 dip's measured gap (Trident has participants+date, 0 consequence edges). **`TRIGGERS` already in vocab (no add needed, verified S102).** Interpretive/pollution-sensitive. Pilot: Robert's Rebellion chain. → same continue prompt.
- [x] **[DONE S102 2026-06-16] Dating leftovers** — All resolved (advisory-board Track 3): 4 spans dated via `ac_year_end` (`dance` 129–132, `wo5k` 298–300, `greyjoy` 289–290, `regency` 131–136); `first-blackfyre` = single-year 196 (212 dropped as wiki error); `long-night` = `ac_year:null`/`precision:relative-only` (297 mention-index error excluded; architecture.md:476); `conquest-of-dorne` verified (date on event, book is separate `texts/` node); 10 mistyped year-page nodes deleted (see Small Fixes). Detail: `history/session-details/session-102.md`.
- [ ] **[DORMANT]** **Timeline reconstruction pass or script** — Once all books have Pass 1 extractions with `time_markers` fields, build a dedicated pass (or Python script) that attempts fan-community-style relative timeline reconstruction from the captured markers across all chapters. Cross-reference with known travel times, moon-turn references, and seasonal indicators.

### Chat / Query UI (placeholder — downstream of the graph)

- [ ] **[DORMANT]** **"Ask-questions interface on top of the graph"** — Surfaced 2026-05-06 (Session 37). The D&D-group / shared-password / friend-group-only chat-UI framing was retired this session; the *concept* of a chat UI is preserved as a way to demonstrate the graph's value outward-facing once the knowledge graph is solid enough. Constraints: no auth, unpublished/unlinked, downstream of the graph (don't grow the graph to chase chat-UI requirements). Not designed yet; this is a placeholder so the concept doesn't get lost. The retired bullets are NOT being salvaged — when this gets designed, start fresh.
- [ ] **[DORMANT]** **LOW — Quotes-as-evidence chat-UI surfacing (idea)** — Surfaced 2026-05-12 (Session 42 Matt aside). The `## Quotes` sections on prose-attached nodes carry chapter cite_refs in the form `(wiki:NodeName.cite_ref-Rasos27)`. These could feed a future query-UI evidence pane ("here's what characters say about Walgrave, with chapter pointers"). Not actionable until there's a query UI to surface them in. Filed for future surfacing.

### Wiki / Pass 2 long-tail — [DORMANT] unless tagged otherwise (pipeline complete; these matter on re-run or future polish)

- [ ] **[DEFERRED]** **DEFERRED DECISION (surfaced S73) — skeleton-untrack:** `working/wiki/pass2-buckets/` has **23,081 tracked files** incl. **7,180 stale `skeleton/*.node.md`** (~28 MB, S72-verified redundant w/ `graph/nodes/`). `git rm --cached` would de-bloat the repo but it's entangled — ~24 `wiki-pass2-*` promotion scripts read `skeleton/` dirs. Treat as its own focused decision; 28 MB isn't urgent. (CLAUDE.md #9: the prior continue prompt's "gitignored" claim was false — only `pass1-derived/` is ignored.)
- [ ] **[DORMANT]** **Stage 4 in its richest form — 3 expanded components** — Surfaced 2026-05-11 during catch-up synthesis. Original Stage 4 scope = edge classifier only. Accumulated raw material (Pass 1 across 5 books, mention-index 70% resolution, cross-references.jsonl 91k rows, Python prose on 6,968 nodes) makes a richer scope economical. Three independently-shippable components:
  - **(a) Prose-inferred edge backfill** — the original Stage 4 scope above. Smoke-gated on Sonnet via `prose-edge-reviewer`.
  - **(b) Chapter-evidence backfill** — for each node, add a "see also: agot-bran-01, acok-tyrion-12" pointer to specific chapter mentions. Combines the per-character index roll-up todo (pure Python, $0) with prose-derived enrichment. Closes the gap between graph nodes and chapter prose; an agent answering "what does the graph say about Eddard?" wouldn't have to scan 344 chapter files.
  - **(c) Rich Identity rewrites for top-N high-traffic nodes** — the canned "X is a Y from the AWOIAF wiki." stub Identity on the 6,968 Stage-3 Python nodes can be replaced by a real 1-2 sentence Identity. With Python prose + Pass 1 evidence in-context, this is Haiku-cheap. Pick top-N by backlink count from `working/wiki/data/backlink-counts.json`. The 855 Stage-1 agent nodes already have rich Identities and stay untouched.
  Sequence recommendation: (a) first (already specced + smoke-gated), (b) second (cheapest, pure Python first then agent for ambiguous mentions), (c) last (optional polish). Memory: `project_stage4_richest_form.md`.
- [ ] **[DORMANT]** **Style-mismatch between Stage 1 (agent prose) and Stage 3 (Python prose) nodes (FUTURE)** — Stage 1's 855 promoted nodes have agent-synthesized terse paraphrased prose; Stage 3's 3,314 promoted nodes have deterministic-extracted verbose-but-cited wiki prose. Visible cosmetic mismatch in `graph/nodes/`. Mitigation when relevant: re-run `wiki-pass2-extract-prose.py` against Stage 1 buckets (after temporarily emitting their skeletons via Stage 3a) and re-promote with `--force` overwrite. Not blocking; v1 graph traversal queries care about edges, not prose style. Defer until cosmetic issue surfaces in real query workflow.
- [ ] **[POLICY]** **PROMINENT — Multi-type entity policy (v1 = single node)** — Some wiki pages encode multiple entity types simultaneously: Citadel (organization + place), Faith of the Seven (religion + organization), Night's Watch (organization + sworn brotherhood — already handled via `organization.faction` v1 placeholder). **v1 policy:** emit each as ONE node with the dominant type from the wiki infobox. Don't split. Don't add `secondary_types`. The "X is also a place" relationship goes in `## Edges` (e.g., `HEADQUARTERED_AT: Oldtown`), not a second node. **Multi-type tension belongs to Stage 4 entity-merge-resolver**, not node creation. **Disambiguation pages (Aegon disambiguation, Brandon disambiguation, etc.) NOT promoted as graph nodes** — each named individual has their own unique-slugged node; "which Aegon does this prose mean?" is a Stage 4 disambiguation-resolver concern. See `working/agent-fleet-specs/agent-pipeline-plan.md` for the full policy.
- [ ] **[DORMANT]** **Tier 3 — chronology extraction (year pages, e.g. 130-ac, 133-ac, 209-ac)** — Deferred 2026-04-28 (Session 26). Year pages are dense in cite_refs and reference structure. Plan: build `chronology-extractor` agent that ingests year pages and emits `OCCURRED_IN_YEAR(<event>, <year>)` and `PRECEDES`/`FOLLOWS` edges. Hybrid pattern (Python prep + agent classification) per `working/agent-fleet-specs/agent-pipeline-plan.md` Group B3. Skeleton continue-prompt to be drafted. NOT a node-creation task — year pages may not become graph nodes themselves; they're sources of temporal edges between existing event nodes.
- [ ] **[DORMANT]** **Tier 3 — concept pages decision (Tourney, Marriage, Sept, Trial by Combat, Kingsmoot, Fool, Maiden)** — Deferred 2026-04-28 (Session 26). Glossary-style pages. Three options: (i) new `concept` type, (ii) selectively promote role-like ones as `title` (Maiden, Fool, Septon), (iii) defer until Stage 4 traversal patterns reveal whether anything edges to them. **Recommendation: (iii) defer** — wait for empirical signal from Stage 4 candidate generation; adding 20-30 concept nodes nothing edges to is graph noise.
- [ ] **[DORMANT]** **NEW AGENT — `disambiguation-resolver` (Stage 4 sub-task)** — Stub at `.claude/agents/disambiguation-resolver.md` (36 lines). Full prompt deferred until `prose-edge-classifier` accumulates enough `escalate_disambiguation` decisions to justify the volume. Role: when prose says "King Aegon", disambiguate which (I, II, III, IV, V) by reading surrounding context + chronology hints.
- [ ] **[DORMANT]** **NEW AGENT — `chronology-extractor` (Tier 3 deferred)** — Stub at `.claude/agents/chronology-extractor.md` (51 lines). Full prompt deferred per Session 26. Role: ingest year pages, emit chronology edges. See Tier 3 todo above.
- [ ] **[DORMANT]** **Temporal edges (v2 schema)** — Surfaced 2026-04-28 (Session 26). Current schema handles temporal/state-changing edges via bracketed qualifiers (`SPOUSE_OF: Lysa Tully [third wife]`). Adequate for human reading, BUT can't support temporal range queries ("who ruled Winterfell between 298 and 305 AC?"). v2 plan: keep the bracket for human readability AND add OPTIONAL structured temporal fields per edge — `start_year` (int, AC), `end_year` (int, AC), `precision` (exact / approximate / range / unknown), `scope` (free-text qualifier), and `start_event` / `relation` for event-anchored cases when AC year unknown. Goes through vocabulary-lock procedure: architecture.md edit → parser update → re-run. The fields apply to ALL edge types (any edge can carry temporal scope), not just RULES/HOLDS_TITLE. Dovetails with `chronology-extractor` (Tier 3 deferred) — that agent's output supplies the AC-year anchors that other edges can reference. Don't build until query-pattern signal demonstrates it's needed; the bracket-qualifier-only approach is fine for v1 read-only graph traversal.
- [ ] **[DORMANT]** **v2 temporal-edges schema design** — Surfaced 2026-05-01 (Session 29). `working/wiki/data/chronology-events.jsonl` (2,245 rows) extracted from 74 year pages. Locked 22-type vocabulary doesn't include `OCCURRED_IN_YEAR`. v2 plan (Session 26 TODO) adds OPTIONAL structured per-edge fields (`start_year`, `end_year`, `precision`, `scope`) rather than a dedicated edge type. Decide architecture before promoting chronology rows as edges. Until then, the JSONL stays as input data for future backfill.
- [ ] **[OPEN]** **Pass-1-vs-wiki-nodes coverage audit (Matt's idea, Session 61, 2026-05-19)** — With Pass 1 complete (344/344 chapters, 5 books) and wiki promotion complete (~7,563 nodes across all entity types), a deterministic Python audit can cross-check coverage in both directions: (a) every Pass 1 mention (characters, places, artifacts, events, etc. from the 12-category Raw Entity List) resolves to a graph node — surfaces wiki coverage gaps (entity exists in books but missing from cache or missed by promotion); (b) every graph node has at least one Pass 1 mention — surfaces wiki-only entities (background lore, mis-classified pages, or candidates for demotion). Builds on the alias-resolver + mention-index already in `working/wiki/data/` (~70% resolution per Session 53). Output: a coverage report ranking gaps by frequency; high-frequency Pass 1 names with no node = priority recovery candidates; nodes with zero Pass 1 mentions = candidates for "lore-only" tagging. Pure Python, $0, can run anytime. Distinct from the spoiler-gating backfill cross-check (which uses Pass 1 to refine `first_available` values); this one tests presence/absence at the entity-graph level. Matt's framing: *"we got all of the nodes from the wiki. could have written some shit to check those nodes against the mechanical extractions. they have good shit for artifacts and objects, basically all of the entity types."*
- [ ] **[POLICY]** **PROMINENT — Spoiler gate defaults FULLY OPEN for v1** — Confirmed 2026-04-28 (Session 26). For v1 chat UI / retrieval layer, every chunk + every node + every edge is fair game regardless of user read-progress. No `first_available` filtering at query time. No user read-progress UI in v1. The `first_available` backfill (deferred per `project_first_available_deferred.md`) stays scheduled but is NOT a blocker for chat UI shipping. Saves complexity: chunker doesn't compute `first_available`, embedding pipeline stays simpler, query layer skips the gate stage. When v2 spoiler-gating ships, the gate becomes an ADDITIVE filter — `first_available` joins the existing structured fields without changing the corpus shape. Don't accidentally re-introduce gating earlier; this is an explicit v1 simplification.
- [ ] **[LOW]** **Stage-1 v1 prose drift cleanup (LOW)** — Schema-drift audit (2026-05-02) flagged 4 LOW findings, all in Stage-1 v1 prose carve-out: ~544 v1 nodes use `## Origins` instead of `## Identity` heading; 360 v1 nodes have backtick-wrapped edge labels in prose body; 11 v1 prose lines retain malformed bare `(wiki:Rasos11)` cite refs; empty `prophecies/` dir (placeholder, harmless). Auditor's recommendation: bundle into Stage-4 prose-edge-classifier work, do NOT fix piecemeal.
- [ ] **[LOW]** **Write `graph/index/chapters/README.md`** — Surfaced 2026-05-07 (Session 39). Each file lists every named entity surfaced in the chapter — characters, locations, artifacts, food. The "mention" term is technically standard but reads person-ish in casual usage; cheap clarity fix is a README explaining "any named entity, not just people" + how the file is consumed (reverse-lookup so an agent answering 'where does Robb appear?' can scan files instead of re-reading chapters). No rename needed.
- [ ] **[DORMANT]** **`pass1-coverage-audit.py`** — Coverage audit: given a chapter's Pass 1 raw entity list and the same chapter's wiki cite_refs (from Track B), flag wiki entities the chapter mentions that Pass 1 missed. This is the real Pass 1 quality gate — higher signal than category-correctness checking. Run as part of v3 schema review.
- [ ] **[DORMANT]** **AGOT v3 schema review** — Review extraction output quality across AGOT v3 (73 chapters). Check for: section completeness, entity coverage gaps, Raw Entity List consistency, Food & Drink / Hospitality coverage. One known gap: agot-eddard-01 missing `### Other` header. **Sequenced after Track B** — see worklog "DECIDED: Track B Before v3 Schema Review" (2026-04-25).
- [ ] **[DORMANT]** **Remaining 247 `_conflicts/` files cleanup** — Already covered by existing line 101 (style-mismatch entry), but worth flagging: Session 40 cleaned 3 surgical merges; the other 245 follow the same pattern (agent-rich content in `_conflicts/`, Python-deterministic in canonical dir). Bundling into Stage 4 work as planned.

**Collapsed one-liners** (full text in the 2026-06-11 archive — Session 22 followups + Session 27 audit findings):

- [ ] **[DORMANT]** Book-chapter wiki pages (349) silently dropped by triage — route to a defer bucket; 14 stranded in `battles-a`.
- [ ] **[DORMANT]** Multi-letter character bucket overlap in the triage alphabetical split (+ resulting `_conflicts/` duplicates).
- [ ] **[DORMANT]** Launcher wave-math gotcha — `cmd_launch` silently rejects wave numbers > total_waves.
- [ ] **[DORMANT]** Question ID collisions — parallel agents need bucket-scoped IDs.
- [ ] **[DORMANT]** CSV stats race — parallel JSONL writes corrupt per-bucket questions/conflicts deltas.
- [ ] **[DORMANT]** Rate-limit detection grep fragility in `wiki-pass2.sh`.
- [ ] **[GATED(post-first-release)]** Spoiler-gating `first_available` deterministic backfill pass (Pass 1 prerequisite met 2026-05-06).
- [ ] **[DORMANT]** Bucket fingerprint missing track_b_row hashes (C1 from cold review).
- [ ] **[DORMANT]** `tier_default` for direwolves should be tier-1 (C8).
- [ ] **[DORMANT]** Oversized bucket-of-one suffix regex (C10).
- [ ] **[DORMANT]** `wiki-pass2-triage.py --limit N` clobbers full output (footgun).
- [ ] **[DORMANT]** Tripwire threshold mismatch in triage (0.8 default vs ~30% non-singleton reality).
- [ ] **[DORMANT]** Parser `books` field discrepancy (1,953 raw occurrences vs 37 populated arrays).
- [ ] **[DORMANT — likely stale]** Wiki uncategorized-page triage (17,305 in `_uncategorized/`) — predates the Path B categorization; verify the gap still exists before acting.
- [ ] **[DORMANT — partially superseded]** `wiki-pass2-resolver.py` free-text → canonical-page resolver (alias-resolver + event-alias-resolver now cover much of this).
- [ ] **[DORMANT]** Edge polish phase (post-ingestion merge of semantically-equivalent edge variants).
- [ ] **[DORMANT]** Non-ASCII edge qualifier normalization at graph-build time (`\u00a0`, en-dashes).
- [ ] **[DORMANT]** OPEN QUESTION: `War_for_the_Dawn` type — `event.war` vs `concept.prophecy`.
- [ ] **[DORMANT]** UPSTREAM parser bug: YAML-escape inner double-quotes in `aliases:` emission (any re-emission re-introduces the cargyll bug).
- [ ] **[LOW]** Stage-1 cite-hygiene cluster: 11 bare `(wiki:R{book}{N})` cite_refs (lean: replace with `(source: BOOK)`); 2 chapter-numberless pycelle cites; verbose `track_b_row` / comma-joined / chained multi-cite_ref formats (normalize at graph-build time, not per-node); paragraph-bracketed missing-cite style.
- [ ] **[DORMANT]** `citation-coverage-scan.py` — flag uncited claim-sentences (post-audit recommendation).

---

## Archived

Everything below moved **verbatim** to `history/todo-archives/2026-06-11-resolved-blocks.md` (2026-06-11 cleanup; nothing deleted):

- **NODE/EDGE RECOVERY** — RESOLVED S72/S73 (false-alarm postmortem: `history/session-details/session-072.md`); the skeleton-untrack deferred decision is kept live above.
- **Stage 4 — Haiku Cutover Prep** (entire section minus the live followups): comention-bulk prep STEPs 1–5 (done) + run items (**SUPERSEDED S65 — comention bulk deprecated, do NOT relaunch**); the Pass-1 pivot/spine record (SHIPPED S66–S67); recall expansion (RESOLVED S74); the Plate-5 parent done-record + done followups #7/#8/#10; the Events-Haiku escalation entry (absorbed into Track 4); S76 core cleanup (folded into Plate 5; OWNS→BONDED_TO VERIFY kept above).
- **Mission Protocol & Orchestration** — full section (one-liners kept in § Dormant).
- **Agent Improvements / Direwolves / Agent Prompts** — completed items + the stale "write the wiki-ingester prompt" item (written S19; Pass 2 complete).
- **Wiki / Pass 2 Prep** — completed build/run records (S16–S29), SUPERSEDED comention Stage-4 items, resolved-in-practice items (alias resolver, major-house recovery, suspicious-edges flagger, per-entity indexes, cross-refs index), GitHub state note.
- **Path B Promotion Campaign** — complete (S28–S29).
- **Audit Findings (Session 27 ×3 sections)** + **Session 22 Followups** — full text; open items collapsed to one-liners in § Dormant. Note: the "BLOCKER: HTML categories" item was resolved by the 2026-04-30 categories backfill.
- **Infrastructure** — done items + the auto-relaunch Option A/C plan (superseded by the run-forever wrappers + `scripts/longrun.sh`).
- **Tiny Follow-ups (S40)** — completed items.
- Model-fit completed items; the original file header; replaced section headers/intros.

